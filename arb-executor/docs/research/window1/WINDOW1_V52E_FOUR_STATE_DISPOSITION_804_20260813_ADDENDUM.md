# V52e full-804 disposition exam — four-state score

The operator ratified `COMPLETE_AT_LOSS` as the fourth exhaustive game state:
a pair with both expressions credited and combined entry greater than or equal
to 100. It is a first-class defect class and is never folded into
`COMPLETE_AT_DELTA`, `PARTIAL_FOR_REASON`, or `NEITHER_FOR_REASON`.

This ruling resolves only the pre-score specification block frozen at
`d9f83b30c44c3910f40d84b4b4f32daccabf3604`. No policy replay occurred. A
score-only adapter consumed the already-run 101 receipt-grain decision-diary
shards, the same hash-bound development print source, the frozen span ledger,
and the `22441e058f9efa7ea8c3065334a238ec8786416f` offer denominator. The adapter
imports no policy module and reports zero policy invocations. Its reconstructed
credits, entries, and fill timestamps match all 60 legs in the frozen V52e
30-game observation set with zero differences.

The exhaustive market census is 267 `COMPLETE_AT_DELTA`, 4
`COMPLETE_AT_LOSS`, 232 `PARTIAL_FOR_REASON`, and 301 `NEITHER_FOR_REASON`,
conserving exactly 804 games. The four loss games are VANDRO, ZHEBOU, BERSAI,
and BARYUA; each completed at exactly 100. Every loss has the same receipt-level
mechanism: the first credit repriced the other standing rest to the lawful pair
cap, then a later V52e machine-read license repriced that rest one cent above
the active cap. The missing check is precisely
`target_cents <= 99 - credited_sibling_entry_cents` inside V52e machine-read
level licensing. This package diagnoses that absence and makes no repair.

The CANON market ruler is 271 completed, 267 under par, with frontier
13/24/51/267/271 at <=93/<=95/<=97/<100/any. The strict print-cross
build-verification reclassification is 124 completed, 122 under par, with
frontier 5/8/17/122/124. Offered-post-onset capture is 266/612; the margin
ladder is 61/90 at >=10c, 128/236 at >=5c, 198/384 at >=3c, and 68/228 in the
thin 1–2c class. Formation-only and not-offered populations remain separate.

`REFLEX_POST` is zero. All 14,166,494 retained decision rows consume N2, N4,
and N5 continuously from the CLEAN store; priors gate zero rows. Two clean
score builds produce byte-identical artifacts across 26 compared files. The
focused package suite passes 35 assertions. No sealed, holdout, deployment,
live, network, order, position, exit, settlement, or DCA access occurred.
