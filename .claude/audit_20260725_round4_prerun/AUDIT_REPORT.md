# INDEPENDENT WINDOW-1 ROUND-4 PRE-RUN AUDIT @ 4f653446

**FINAL RULING: BLOCKED — DO NOT RUN ROUND 4.**
One hard violation confirmed (five floor-passing D events terminally censored by the executable instrument, D=799≠804), plus two stale contradictory 100¢ specification fields proven inert but requiring removal/deprecation. Everything else passes, including full byte-identical deterministic regeneration.

Date: 2026-07-25 · Branch: `audit/window1-independent` · Auditor: independent CC session
Method: detached read-only worktree at `4f65344672430adc51fe0a5a7e8c9279b2b354ed`; no benchmark/scorer run; no tuning; no ranking; no holdout or live access; target commit untouched; all reproduction in temp directories.

## 0. Prior art (C45)

`25735d9c` (Round-3 results audit — dominant failure = unfilled lawful maker orders; smallest correction named as cutoff-aware laddering), `f09c9ae7`, `b415a98e`, `807e2c86`. Delta: first audit of the Round-4 PRE-RUN implementing the headroom ladder; adjudicates the two flagged hard issues.

## Item-by-item results

### 1. Identity and freeze — PASS
`4f653446` ("research: freeze Window-1 Round-4 pre-run") has parent exactly `754415bb` (all-refs children scan: sole child; remote tip verified). Diff = **23 additions, zero modifications/deletions** (16 PRE-RUN artifacts, 4 analysis sources, 1 candidates contract, 2 test suites). All 13 artifact receipts, 7 code receipts, and 12 source receipts verify (9 source receipts record CRLF disk-basis sha256 over the exact committed blobs — verified equivalent; same benign pattern as Rounds 2–3). Private-input receipts: market-cache aggregate `aad8d055…` (804 files, holdout rows 0), 1,608 frozen leg identities (`c7bc10c3…`), external inputs via the audited Round-3 package; no raw private data committed. D=804 declared; **two candidates**; **1,608 streams** (2×804) with all 1,608 stream receipts matching embedded stream hashes; **C/PC/S/IC all null** (`metrics: null`, `scored: false`, `C_PC_S_IC_populated: false`); no scorer, `benchmark_execution_authorized: false`, holdout unopened, no live/production access.

### 2. Tests and deterministic regeneration — PASS
- `python -B -m pytest -q arb-executor/tests/test_window1_round4_instrument.py arb-executor/tests/test_window1_round4_diagnostics.py arb-executor/tests/test_window1_round3_instrument.py` → **52 passed** (32 Round-4 + 20 inherited Round-3), matching the claim.
- Full regeneration from frozen inputs: `python -B arb-executor/analysis/window1_round4_prerun_builder.py --repo . --events <private>/joined/events.jsonl --market-cache <private>/fit-local/guarded-cache-v3 --output-dir <temp> --workers 6` → **all four regenerable artifacts byte-identical to the committed git blobs**: FROZEN_CANDIDATE_EVENT_STREAMS.jsonl.gz (1,608 streams, decompressed content), ROUND4_HEADROOM_DECISION_RECEIPTS.jsonl.gz (167,501 receipts), ROUND4_CANDIDATE_ORDER_DIFFERENCES.jsonl (707 rows), ROUND4_REAL_CAPABILITY.json. **Zero nondeterminism.** (Two initial file-level mismatches were my worktree's CRLF checkout; git-blob bytes are identical.)

### 3. Primary fill path — PASS
`Round4Instrument._fill_from_print` (window1_round4_instrument.py:783–859): qualifying volume = chronological, receipt-identified (`trade_id`/`receipt_id` recorded; duplicates excluded upstream by the inherited seen-receipt set), positive-size (`size <= 0` rejected), non-self (taker-side entry flow; own-order subtraction upstream), **at the limit or better** (`price > order.price` rejected — correct direction), accumulating across prints into `order["remaining"]`/`state["quantity"]` and **capped at exactly five** (fill = `min(size, remaining)`, LOT=5). Partial quantity **persists across reprices** (`_replace_at_price` sets `remaining = LOT − state["quantity"]`). Cutoff enforced by the guarded stream horizon with `post_cutoff_fills_excluded_from_PC: true` in the frozen census contract. Queue: tracked only inside `queue_sensitivity_diagnostic_only{…, "alters_primary_fill": false}`; unknown queue is flagged (`queue_unknown`) and **never defaults to nonfill**; no five-displayed / top-five-depth / one-print / trade-through / depth-conjunction requirement anywhere (explicit negative flags recorded on every fill action; census confirms `estimated_queue_required_for_primary_completion: false`).

### 4. Causal headroom — PASS
From the 167,501 committed headroom receipts plus code: partial fill arms without budget (1,421 `headroom_partial_arm`, reason `positive_partial_fill_arms_sibling_without_budget`); the first exact-five arms the budget (1,373 `headroom_exact5_arm`, freezing VWAP and contemporaneous own-subtracted external R1 with source receipt identity); `b1 = VWAP − R1`; sibling `b2` uses contemporaneous receipt-identified R2; `fee = 0`; **`b2_max = floor(−b1 − fee − 1)`** (the frozen `headroom_b2_max`) — recomputed on **all 167,501 receipts with zero mismatches** (a stricter integer-cent reading than "−b1−1": conservative for fractional b1, always satisfying strictness); **all 21,838 accepted one-cent improvements satisfy `b1+b2+fee < 0` and `b2 ≤ b2_max` — zero violations**; 134,698 guard refusals; 5,721 budget-reducing corrections only at lawful triggers; movement never exceeds **+1¢ per strictly-later receipt-identified sibling print**. No future close or evaluation-real-start access (`FORBIDDEN_ORACLE_FIELDS = {window1_close_reference_cents, window1_close, evaluation_reference, evaluation_real_start_ts, strict_positive_cutoff_ts}` validated at event intake); neither IC nor S gates any order (S proven inert in item 6; IC appears nowhere in policy code).

### 5. HARD D=804 CENSOR AUDIT — **BLOCK (violation confirmed)**
Independently reproduced from the frozen streams: exactly these five events are terminally censored in **both** candidates —
`KXATPCHALLENGERMATCH-26JUL19KRUCAS`, `KXATPCHALLENGERMATCH-26JUL20CREMAT`, `KXWTAMATCH-26JUL13TAUTOM`, `KXWTAMATCH-26JUL14PUTJEA`, `KXWTAMATCH-26JUL20KUDKOR` —
each with `feature_censor / required_feature_absent / missing_features=["causal_role"]` on **both legs** and **zero order placements**. Code location: `window1_round3_instrument.py:87` appends `causal_role` to *required* features whenever the `leg_specific_posture` family is present — and **both** Round-4 profiles include `leg_specific_posture` — so missing favorite/underdog role terminally censors the whole event. The actionable candidate stream population is 799, not 804.
**Ruling:** this violates the immutable D law and the stated principle that unavailable macro/steering features produce named NO_CALL while independent pair presence continues. Missing role may lawfully disable role-*specific* park/join steering; it must not suppress both-leg presence. The codebase already contains the correct pattern — `Round4Instrument._initialize_birth` converts the inherited `dynamic_recut_cell` censor into a named NO_CALL and restores the leg — it simply was not applied to `causal_role`.
**Smallest correction (not implemented by me):** extend the same censor→NO_CALL conversion to `causal_role`: emit a named NO_CALL (e.g. `causal_role_unavailable_neutral_posture_fallback`), disable role-specific posture steering only, and continue with a role-agnostic neutral maker-presence posture so both legs place; D returns to 804 in both denominator and actionable population.
**Required invariants/tests for the corrected PRE-RUN:** (i) zero `censored_feature` terminals attributable to `causal_role`; (ii) the five named events show both-leg placements and named NO_CALL rows in both candidates; (iii) every one of the 804 events reaches `place` on both legs in both candidates (absent a different lawful terminal); (iv) all 799 previously-eligible event streams remain **byte-identical** (the fallback must not perturb events that have `causal_role`) — proven by regeneration diff; (v) the existing 52 tests still pass plus a new test pinning (i)–(iv).

### 6. HARD 100¢ CONTRACT AUDIT — inert, deprecation REQUIRED
- `Round4Instrument._pair_cost_passes` (window1_round4_instrument.py:198–210) returns `True, combined` **unconditionally** — the Round-3 gate is overridden out of existence; the `pair_cost_no_call` annotation paths that would echo `maximum_pair_order_cost_cents` fire only on refusal and are therefore unreachable.
- The only other consumer of `first_fill_sibling_max_combined_cost_cents` is the Round-2 one-shot reaim guard (window1_round2_instrument.py:858) — unreachable in Round 4: **zero** `sibling_reaim_*` or `pair_cost_no_call` actions exist in all 1,608 frozen streams (action census).
- **Perturbation proof (audit-only temp reproduction; target untouched):** running the frozen instrument on 16 real candidate-events (including ladder-active, walk-active, divot-active, and completion events) with the two fields set to 57/31 → **all stream hashes identical**; with both fields **deleted** → runs cleanly, **all stream hashes identical**. Harness fidelity proven: the unperturbed harness reproduces the committed frozen stream hashes exactly.
**Classification: stale contradictory specification fields, completely inert.** Per the audit law they must be **removed or explicitly marked non-operative** in the corrected PRE-RUN (with a pinning test) so S cannot be reintroduced as a gate downstream. This joins the item-5 correction as a required change; it is not an active violation.

### 7. Candidate distinctness and mechanism attribution — PASS
Using the builder's frozen decision signature (place/reprice/cancel over leg/ts/action/reason/price/quantity/remaining/queue): **707 differing events reproduced exactly, set-identical to the committed ledger** (a wider signature including the posture label yields 738; the extra 31 differ in labels only — bookkeeping, correctly excluded). Attribution by first differing real decision: **669 presence/posture-depth price differences** (the drift stack's steering changes parked prices — this is where drift/orientation/atlas/dynamic-recut actually bite), 21 recut-driven, 17 walk-driven, 32 headroom-ladder ordinal differences. Full-drift-only real-decision mechanisms: `verified_nonself_chain_exact_one_cent` walk (213), divot recuts 258 vs 151, and a larger ladder (14,556 vs 13,003 actions). **Bound but inert or universally NO_CALL:** cohort steering — the reported 1,598 cohort NO_CALLs reconcile exactly as **2 legs × 799 eligible events = one consultation per leg, every one NO_CALL at n<30, zero decisions**; atlas reach unavailable on 23 legs (named NO_CALL); top-five pressure real but small (256 order effects). Drift/orientation observations (1,596/1,608 rows) are annotations whose decision effect is realized through posture depth — proven real by the 669 price differences, not accepted from the "full drift stack" label.

### 8. Presence under optional-feature absence — PASS
All 3,196 eligible candidate-legs (2 candidates × 2 × 799) place at least once. Lifecycle: every leg whose last real action is a cancel (781) carries reason `declared_policy_horizon` — lawful end-of-window cleanup, not terminal absence; cancel/replace gaps mid-window always end in a replacement. Optional-feature absence produces named NO_CALL with presence continuing: `dynamic_recut_cell_unavailable_pair_presence_continues` (254), `atlas_reach_unavailable_underlying_presence_continues` (23). (The five causal_role events are the item-5 exception — the blocker.)

### 9. Oracle firewall — PASS
The instrument imports only stdlib + round2/round3 instruments — never the diagnostics module or any diagnostic artifact; `FORBIDDEN_ORACLE_FIELDS` intake validation makes close values and post-cutoff evidence unreachable from policy; the calibration (1,826 samples) and false-negative census live only in diagnostics artifacts with `calibration_threshold_tuned: false` and `free_numeric_parameters: []` (no threshold derived); the census pins `source_round3_audit = 25735d9c` (my audit) and `source_round3_result = 754415bb`, and its Round-3 counts **match my independently audited numbers exactly** (naked singles 281/278/165/165/264/263/38/38); all performance fields remain null everywhere (`supplied_selected_candidate_counts_reproduced: true`; queue never alters primary completion).

## Discrepancy classification

- **Measurement failure:** none — the fill law is executable and correct; regeneration is byte-deterministic.
- **Evidence censoring (unlawful):** the five causal_role events — the confirmed blocker (item 5).
- **Evidence censoring (lawful/declared):** cohort n<30 all-NO_CALL; atlas 23 legs; the declared-unavailable surfaces (sealed dual-divot object, Pinnacle, full depth, shape, schedule revisions) — named, unproxied.
- **Candidate-family limitation:** two frozen candidates only; steering machinery's decision effect flows almost entirely through posture depth.
- **Genuine market behavior:** fill scarcity itself (per the Round-3 results) — untested here by design (no scoring).
- **Housekeeping (non-blocking):** CRLF disk-basis sha256 in 9 source receipts (blob-equivalent, verified); the stale 100¢ fields (item 6, removal required).

## FINAL RULING

**BLOCKED — DO NOT RUN ROUND 4.**

Required before any execution authorization, and nothing more:
1. **Censor repair (item 5):** causal_role → named NO_CALL + role-agnostic neutral maker-presence fallback; D=804 actionable; the five named events place on both legs; all 799 other streams byte-identical; invariants/tests (i)–(v) above.
2. **Spec hygiene (item 6):** delete `first_fill_sibling_max_combined_cost_cents` and `maximum_pair_order_cost_cents` (or mark them explicitly non-operative) with a pinning test that no consumer reads them.

The corrected PRE-RUN must arrive as a new additions-only commit on the same lineage and pass a fresh independent audit before any execution package is considered. No Round-4 execution package may be built against `4f653446`.
