# LAJSVA case-study spine v1–v12

Versions v1–v11 remain committed in their own directories. V12 repairs only F-VS-114(a/b/c): remaining dip is total minus arrived, placement uses the conditioned floor-side point rather than a deep-edge default, and a migrated envelope re-derives the rest on the same receipt. The mind-only bed outcome is INCOMPLETE. The gate self-stops on any tripwire break; lineage is untouched.
