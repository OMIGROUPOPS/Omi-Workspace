# PROOF — C-SYSTEM-PAGE v1 (the viewer's third page: /system)

**Candidate SHA: 3062d639** (code: live_viewer.py /system + knob_census_check.py --emit + deploy_live_v4.sh census regen + knob_citations.json seed).
**Class cited: UNTRACED LIVE KNOB** — this page is the class's standing exhibit: the NAKED population is now permanently visible, per-knob, with its citation column reading "UNCITED — lands NAKED until a person cites a source".

## Prior art (C45)

- `.claude/render/SYSTEM_PAGE_SPEC.md` — **reserved-verbatim slot; Plex's spec dropped in paste.** v1 is built from the dispatch's binding scope. THE SPEC WINS ON LANDING; every delta gets named in the vault. Same for `system_map.html` (placeholder) — node-for-node Plex-diff equivalence is DEFERRED to the verbatim landing; the committed HTML artifact is the diff target.
- `analysis/knob_census_check.py` (C-KNOB-CANON, drift hash pinned 1f0f2c16) — the census walk this page regenerates per deploy. Extended, not replaced.
- `analysis/live_viewer.py` (C-LIVE-VIEWER, /command + /trade/:id + /api/results) — the page lands as the third route; existing pages untouched (both re-verified 200 post-deploy).
- `docs/CLASS_LEDGER.md` UNTRACED LIVE KNOB; `docs/LIFECYCLE.md` ORGAN LEDGER (the organ strip's source); RULING_PAIR_ECONOMICS (the pin overlay's four knobs).

## Lane 1 — render proof (the replay lane for a read-only page)

Local render, 12/12 checks PASS: title · classification strip · computed NAKED count ·
anatomy spine · "procedural, no knobs" caption · dual staleness stamps · pin toggle ·
pin panel default `display:none` · all four pair-97 knobs present · knob-vs-decision
line · organ ledger · full census table. Pin lines = exactly 4.

**Computed-field property PROVEN, not asserted:** flipped `reality_divergence_cents`
DECREED→FITTED in `knob_citations.json` alone, re-emitted, re-rendered → the row
recolored to FITTED #10b981 with ZERO frontend change; reverted. A real migration
recolors without redesign, as specced.

**Red lines honored structurally:** classification comes ONLY from
`knob_citations.json` (a person's citation) — the emitter has no text-similarity
path; today's honest split is **FITTED 3 / DECREED 18 / NAKED 101 of 122 mechanical
knobs** (my citations cover 22; the gap to Plex's 124-row canon reconciles when the
verbatim census lands — uncited means NAKED, full stop). The L5 crowding renders
as-is, never merged.

VPS live verify (viewer restarted in tmux `live_viewer`, loopback 8787):
`/system` 35,719 bytes · pins=4 (PIN_OK × combined_goal, completion_combined_ceiling,
completion_all_cells, completion_reprice) · CAPTION_OK · PIN_DEFAULT_OFF ·
`/api/census` + `/api/config` 200 · `/command` 200 · `/api/results` 200.
Rendered artifact committed for Plex to diff: `.claude/render/proof/proof_system.html`.
The page's DRIFT line is live truth: census pin 1f0f2c16 vs running blob 05b03dce —
Plex's canon predates the composer deploy; the page says so instead of hiding it.

## Lane 2 — economics ($0)

Read-only end to end: renders decreed exposure, consults nothing, no order-path
import, no bot restart (tmux `live_viewer` only; live_v4 PID 3164946 untouched).
The deploy-script census-regen line is additive (`--emit` after the gate, WARN-only
on failure — it can never block a deploy).
