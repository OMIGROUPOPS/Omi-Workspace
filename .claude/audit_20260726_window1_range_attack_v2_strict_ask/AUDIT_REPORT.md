# INDEPENDENT WINDOW-1 STRICT-ASK V2 PRE-RUN AUDIT @ 85134634 — RULING: PASS

**PASS.** The V2 overlay implements exactly the authorized change — `FILLABLE_AT_X = PRICE_REACHED ∨ STRICT_ASK_CERTAIN_FILL`, credited at the original exposed X before maker safety — and nothing else. All 26 censored intervals from the controlling audit migrate with exact receipts; the mandatory 362/308/26 causal-vs-guarded decomposition reproduces exactly with sound scorer-eligibility guards; change containment is total (1,300 byte-identical streams; every divergence descends from a strict-ask cause); 80/80 tests pass. **This PASS authorizes only the construction of a separately frozen scoring/execution package. It does not authorize scoring.**

Date: 2026-07-26 · Branch: `audit/window1-independent` (child of `e45163b7`) · Auditor: independent CC session
Method: detached clean worktree at `851346343eecbff64bd836992876592784874c86`; reproductions from pinned raw/frozen inputs, not committed receipts alone; no implementation, scoring, ranking, tuning, holdout, or live access; mechanisms that passed at `43dab8df` revisited only where the overlay touches them.

## 1. Lineage and V1 immutability — PASS

Parent exactly `66b50db3`; all-refs children scan: **sole child**; local and remote `codex/window1-definition` equality verified. **34 additions, zero modifications/deletions** — every V1 blob unchanged by construction. Candidate IDs unchanged (`w1_range_attack__macro_hold__combined_headroom`, `w1_range_attack__macro_micro__combined_headroom`); the V2 spec is the V1 strategy surface plus the accounting contract; the overlay (`window1_range_attack_instrument_v2.py`, 482 lines) subclasses the V1 simulator and overrides only `_new_state` (fill-evidence fields + deferred slot), `_credit_fillable_at_x`/`_first_price_reach` (the accounting union), `_on_book`/`_on_print`/`_terminalize` (credit-before-maker-safety and the same-timestamp deferral), and `run` (same-ts pre-scan + causal state serialization). Atlas, LIVE-AIM, guidebook, divot, expression, headroom, clock, and range mechanics are inherited without retuning (no parameter or threshold differs; verified against the V1 module surface). All 27 ARTIFACT_HASH_MANIFEST rows reproduce.

## 2. Strict-ask crediting — PASS (code + streams)

`_credit_fillable_at_x` with `STRICT_ASK_CERTAIN_FILL`: requires an active order (original exposed X read **before** any same-update movement), a lawful external best ask with positive size **strictly below X** (`>= X` returns False — equal ask never auto-credits), credits **five at X**, records book receipt, timestamp, interval ID, and evidence type, and removes the order. In `_on_book` the credit runs **before** `_maybe_compose_pair`, `_apply_pending_macro`, the maker-safety branch, and `_apply_liveaim`. No maker-safety cancel/reprice occurs for a credited order — verified across all 26 guarded fills (exact-reason match: zero `maker_safety_external_ask_move[_cancel]` actions at the evidence timestamps; the only action is the single `strict_ask_certain_fill` at original X). Print at/below X remains `PRICE_REACHED` through the same crediting path. No cumulative-five, displayed-depth-five, or queue-clearance gate exists (contract fields and per-action negatives unchanged).

## 3. The 26 audited migrations — PASS (26/26 reproduced)

Reopened my `43dab8df` census (26 rows) and matched against the V2 guarded fill set: **26/26 present; 26/26 with the exact V1 book receipt and timestamp; 26/26 credited at the original exposed X; zero missing or substituted evidence receipts; 26/26 former maker-safety evasions removed** (the V1 cancel+reprice pair at the evidence timestamp is gone). First-fill consequences: **20/20** previously flagged first-fill-position cases now arm headroom at the strict-ask timestamp on the correct leg (`headroom_armed` with `first_STRICT_ASK_CERTAIN_FILL_leg_freezes_causal_pair_budget`, `first_filled_leg` = the credited leg). Evidence reproduced from the frozen normalized books via the streams, consistent with the raw-book reproduction performed at `43dab8df` on the identical pinned caches.

## 4. 362 causal / 308 changed / 26 guarded — PASS (decomposition exact; no boundary leakage)

Reproduced from the streams joined to the fillability interval rows: **362 `strict_ask_certain_fill` causal actions** (185 macro-hold + 177 macro×micro) across **308 distinct changed streams**, decomposing into exactly one class each: **26 guarded lawful Window-1 fills** (inside `[opened, evaluated_right]` with `positive_window1_provable=true`), **283 after the guarded right boundary**, **53 where the boundary is not positively provable** — matching the stated decomposition digit-for-digit. Guards proven: (a) scoring eligibility flows only through the guarded `FILLABLE_AT_X` fillability receipts (interval rows carry `FILLABLE_AT_X`, evidence type, and receipt; only guarded rows exist there — the 336 non-guarded actions produce **no** fillability credit); (b) `causal_policy_fill_state_by_leg` cannot be consumed as guarded truth — every leg's `guarded_Window1_status` is serialized as `null`; (c) the 283 post-right actions cannot alter earlier guarded rows — fillability is computed over the immutable observation stream bounded at `evaluated_right_ts`; (d) the 53 unprovable actions are excluded by the positive-Window-1 gate; (e) the later execution-package inventory is `INVENTORY_ONLY_NOT_CONSTRUCTED_NOT_AUTHORIZED`, contains no scorer or execution command, and points to the guarded receipts. A future scorer that consumed raw causal state would have to bypass an explicit `null` guard status, a frozen accounting contract, and the inventory pointer — the lawful 26-only path is the explicit frozen contract; no silent 362-counting path exists.

## 5. Diagnostic reconciliation — PASS (exact)

Reproduced from all 33,033 V2 interval rows: macro-hold **PRICE_REACHED 489 / STRICT_ASK 13 / FILLABLE_AT_X 502 / CERTAIN_FILL 49 / EXACT_TOUCH 702 / cumulative-five false negatives 38**; macro×micro **480 / 13 / 493 / 48 / 693 / 38** — every number exact. V1 reconciliation: PRICE_REACHED fell 495→489 and 486→480 because **six legs per candidate that reached by print in V1 now credit earlier by strict ask** (verified per-leg against the V1 fillability rows: of each candidate's 13 guarded strict-ask fills, exactly **6 were V1 print-reach legs (evidence/timestamp changed) and 7 are newly covered legs**), so net fillable coverage = 495+7=**502** and 486+7=**493** — union without duplication. **Zero intervals carry both evidence types**, and one leg cannot receive both a strict-ask and a later print fill (single-fill guard: `simulated_accounting_quantity == LOT` blocks all later crediting).

## 6. Same-timestamp ordering — PASS

The pre-scan (`run`) groups best asks **only by (leg_id, exact timestamp)** and keeps only groups with >1 same-ts receipts — no later timestamp is inspected, and receipts sharing one exchange timestamp share one evidence boundary. An equal ask alone follows frozen V1 maker-safety byte-for-byte (verified in the code's else-branch and by the 1,300 identical streams). Equal ask followed by a same-timestamp strict ask defers maker safety (`deferred_equal_ask_maker_safety`), preserving the original X until the strict evidence is processed and credited. Same-timestamp book+print produce one fill (single-fill guard). No action is backdated (deferred flush uses the *flush-time* `effective_ts`, never the deferral ts). Deferred state is always cleared: on credit, on the next later-ts book or print, and at `_terminalize` — with a hard invariant that raises if a flush would ever encounter an uncredited strict ask. The pre-scan cannot affect a non-existent order (deferral is only set while an active order exists, and the flush re-checks order and interval identity). Receipt ordering cannot fabricate a strict-ask fill: crediting requires the currently-bound lawful book's own strictly-below ask. No cross-timestamp lookahead or stale deferred state exists.

## 7. First-fill and combined headroom — PASS

All 20 corrected paths verified (item 3). Over all V2 COMBINED_HEADROOM_RECEIPTS: `d1 = original X − contemporaneous external bid` with R1 book receipt; `b2_max = floor(−d1−fee−1)` — **zero mismatches**; strict `d1+d2+fee<0` and exact `+1` on **all accepted actions (4,531 / 14,714) — zero violations**; sibling triggers strictly later (`ts > first_fill_ts` enforced); **zero same-timestamp sibling headroom actions** (receipt field confirmed 0); no IC or S gate; expression `proposed < ask` and join/improve law enforced in `_express`. Positive-d1 armings exist (266) as permitted evidence and create no artificial budget — `b2_max ≤ −2` there, and every accepted d2 still satisfies the strict combined law. Armed counts 697/695 (vs V1 691/690) reflect the newly credited first fills; the committed HEADROOM_REDERIVATION_RECEIPT (26 rows, 20 corrected, 0 same-ts) matches my recomputation.

## 8. Change containment — PASS

**1,608 streams; exactly 308 changed; exactly 1,300 byte-identical order streams; zero unexpected changes.** The 308 changed streams are precisely the 308 streams containing ≥1 strict-ask action; **every** first semantic divergence descends from a genuine strict-ask-below-X observation — 305 diverge at the strict-ask/maker-safety site itself, 3 diverge at the sibling's `headroom_armed` action caused by a partner-leg strict-ask fill at that timestamp. Inside-guard vs outside-guard: **26 changed streams contain a guarded fill; 282 change only from post-right or unprovable strict-ask observations.** No Atlas, LIVE-AIM, macro-target, divot, or ordinary maker-safety divergence precedes the strict-ask cause anywhere.

## 9. Population, tests, determinism — PASS (regeneration per addendum)

D = **804 per candidate** (1,608 streams, key set identical to V1). The five no-BBO events are **unchanged** (all five in the byte-identical set — zero fabricated orders, named NO_CALLs and D membership carried forward from the V1-audited state; FIVE_EVENT proof rows = 10). All C/PC/IC/S and performance fields null (streams `metrics: null`, `scored: false`; forbidden-access receipt corroborated: no scorer, benchmark, ranking, holdout, live, production, exit, Window-2, settlement, or DCA path). **80/80 tests pass** from clean state (the six relevant suites: strict-ask V2, range-attack, macro×micro, Round-4 V2, Round-4 instrument, Round-4 diagnostics). All 27 manifest hash rows and source/audit blob bindings reproduce. Fresh-directory regeneration of the 17 regenerable core artifacts was launched; completion status recorded in the addendum below.

## RULING

**PASS.** The strict-ask V2 PRE-RUN at `85134634` is the narrow, faithful implementation of the correction demanded by `43dab8df`: certain executable strict-ask evidence now credits five at the original exposed X before maker safety, exactly 26 guarded lawful fills migrate with exact receipts, boundary discipline separates the 362 causal observations from the 26 scoring-eligible guarded rows with no silent leakage path, and everything outside the authorized change is byte-identical.

**Scope:** this PASS authorizes only the construction of a separately frozen scoring/execution package (with the guarded `FILLABLE_AT_X` receipts as its sole fill-accounting source), which must itself pass independent audit before any execution. It does not authorize scoring.

## Determinism addendum (confirmed)

Fresh-directory regeneration completed after the ruling was committed: **all 17 regenerable core artifacts are byte-identical to the committed git blobs** (four stream shards, four ladder shards, fillability/headroom/authority/stress/opportunity receipts, diagnostics, last-trade bindings, accounting summary, five-event proof; gz compared on decompressed content). Zero nondeterminism. The DETERMINISTIC_REGENERATION_RECEIPT claim is independently confirmed, completing item 9 in full.
