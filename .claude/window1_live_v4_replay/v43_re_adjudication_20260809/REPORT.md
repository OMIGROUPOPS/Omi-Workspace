# V43 re-adjudication — operative replay baseline

The frozen V43 machine and its executable scores are unchanged. The prior block arose from an acceptance-law defect: named completion checks required equality rather than at-or-better, and the PUTJEA mechanism check bound a particular sibling-ask tick rather than the guard mechanism.

Under the corrected law, KIRSEK 24 <= 47, ARNROM 89 <= 90, BOSCOP 80 <= 94, and KRUFER 96 <= 96 all pass. PUTJEA emits `V42_DEEP_GAP_FEASIBILITY_WITHHOLD`; BORDIM|DIM remains unwithheld with a `MARKET_REACH_PRINT_CROSS` fill. Zero named mechanism regressions remain.

The executable V43 row is controlling: 395 completed and under-par pairs, 1,910 cents locked, -162 cents naked, and +1,748 cents true-book value. The naked-book -162 cents is a standing watch item.

The analytical dial receipts at `9ddfe8c6`, `645e035b`, and `52275c9d` are stamped `ANALYTICAL_ESTIMATE`. Their projected aggregate rows are superseded by the executable attribution scorecard; they are not acceptance targets.

V43 is the operative replay baseline. This receipt grants no deployment or live-cutover authority.
