# PROOF — AUTO-CUTOVER (C-INCUMBENT-SUNSET Part 2, DEFAULT-GO executed by silence)

**Candidate SHA: __SHA__** (the flag flip: `trendpath_live=true`, config-only — the acting code shipped dark and gate-audited at 08e1c8ed/its successors; this deploy changes ONE flag).
**Decree cited:** C-INCUMBENT-SUNSET Part 2 (operator, verbatim): "the moment the consolidated packet crosses n≥300… the cutover is default-GO: the notification arrives with the numbers and a single word — your 'stop' halts it, silence for 12 hours after the packet means the flags flip on the next boot with full audit. The burden of proof reverses: the proven design doesn't wait for ceremony; the convicted one needs a reason to survive another night."
**The outcome proof IS the packet** (RULING_PACKET.md, auto-generated at the n≥300 crossing; numbers at fire time):

```
__STATUS__
```

**What activates:** path-mode in NO-CALL posture — both legs of every non-dropped event rest at their pages' fitted path aims (p50 bottom), pair-capped ≤97 (the operator's window-1 ruling embedded). Orientation stays SHADOW until its own bar clears (`orientation_live` remains false). Sizing stays dark (`sizing_live` false — the operator's sizing word precedes arming). The convicted incumbent's static aims retire at this boot; every prior night's cost stands attributed under INCUMBENT-COST.
**Lane 2:** the packet's three letters are the dollars; the flip itself adds no unproven behavior — the acting branch replays exactly what the path grade measured.

## Deploy gate
[1/2] lint (gate-run) · [2/3] smoke (gate-run) · [3/3] this file, OUTCOME_PROOF_SHA=__SHA__ · [4/4] C50 (the sunset close-out already in the push history; this stamp is doc-only after the candidate) · [5/5] constraints surface. Executed by deploy/auto_cutover.sh under the 30-min cron; STOP file absent at execution time.
