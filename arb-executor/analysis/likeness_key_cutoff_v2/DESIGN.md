# Native-cutoff challenge — SIGN before re-fitting

2026-09-14. Bench only. V1 stays intact; engine and held rebind untouched.

V1 could select unified FIRST, not native FIRST: its finite era family omitted
the operator's fixed 2026-04-18 boundary. A containing hypothesis family cannot
lose its best in-sample objective on identical observations; a learned setting
can still lose out of sample. V1 did not establish the containing-family premise.

## Identity before inference

Audit all common tick-only/unified atlas fields, then every scored receipt.
Score both FIRST_FORMATION_2026_04_18 and FIRST_EXACT_TICK_MEMBERS on unified
rows. The latter must equal minute tick-only B, with the existing A-to-B grain
difference reported separately. The literal date boundary is not assumed to
equal source membership. Preserve and list extra/missing events. Halt fitting
if shared row fields or the exact-membership forecasts differ from B.

## Reachable controls and learning

Keep the original ERA_SELECTOR, ERA_WEIGHT, COMBINED_WEIGHT and
COMBINED_SELECTORS experts unchanged, with their original unified-FIRST option.
Add both audited FIRST controls to every family. Also make a pure FIRST
formation-date selector available at each observed library formation-month
start, plus the operator's April 18 date. These calendar boundaries are read
from the library, not chosen by inspecting outcomes; the same date options
are present in each family so the original FIRST weights are not confounded
with an era restriction. All settings are fixed experts, selected earlier-only.

Monthly cutoffs remain V1's earliest native query formation in each month.
Only earlier-month questions resolved before that freeze may validate settings.
Validation is matched on the exact tick control's callable receipts; before
that control has ten effective members there is no comparable validation,
not an invented loss or a unified-FIRST fallback. Score equal games, with
side-specific loss and coverage alongside the equal-game CRPS selection loss.
Disqualify settings that lose calls on these validation receipts. Exact tick
membership wins ties. With no comparable validation, retain exact tick FIRST.

The latest user-authorized cutoff is a reachable control, not an oracle that
can guarantee every future test loss. Report the selected key, its validation
score, both cutoff-control scores, best older-admitting key, and its validation
and untouched-for-selection test results each month. A key is older-admitting
only where it actually puts positive weight on a pre-cutoff member; no label
alone establishes that. Player diagnostics are not introduced into these keys.

## Test and uncertainty

Use the same 928 MAIN / 2,287 CHALL native queries, sides, clocks, targets,
atlas gates, role masks, Q/X emission and ten-effective-member fence as V1.
Keep all native denominators. Compare both controls and each earlier-selected
family to exact minute tick FIRST and native A. Report best-older candidates
as diagnostics, not post-test winners. V1 contract-v1 uncertainty and its
104-comparison adjustment remain conservative for these selected contrasts:
equal-game date/game bootstrap, 50,000 draws, >=100 matched games, >=0.10c
floor benefit, adjusted interval below zero, timing improvement, no CRPS or
coverage deterioration. No new clipping, conduct, engine run or push.

No fresh out-of-sample claim: these historical test months were previously
inspected. Native subset availability before the cutoff is explicitly missing.
