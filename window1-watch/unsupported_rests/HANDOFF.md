# Unsupported rests + corrected bell — continuation

## CURRENT PACKAGING WORK — timeline chunks APPROVED 2026-09-15

LATEST 22:03UTC: ALL100 local static and hosted browser checks PASS (grade/oracle/source hashes/fault labels; zero JS errors); ALT hosted lazy cards PASS with zero startup-detail fetches, one verified fetch on open, gradeC. Preview `https://omi-window1-watch-42rym9gn6-omi-groups-projects.vercel.app`, deployment dpl_2nsMygRzFXurErdtdwJE39wmk2CT, verified1152-file bundle. Proofs and publication receipt in data/unsupported-rests/proof. Ready for scoped commit/push and promotion of this exact preview. No raw/stage/renewal or unrelated worktree edits staged. After production URL verification and final delivery, remove the quiet completion automation; no more replay or regrading needed.

LATEST 21:59UTC: replay/export/finalize/fault/report ALL PASS; POSTPROCESS_STATE READY_FOR_REVIEW. Report:27→15 completed,58→60 one-sided,10→20 unfilled,5 unchanged unknown;32→20 of361¢, class-A65→82. ALT remainsC60/38; GAN remainsF ZIN72 only. Fault audit rerun with one-game-at-a-time storage and gzip readers gives byte-identical indexSHA7a6ecdc5b103dd30897099c563ad53ecda81ccb1e4ad375d8bd1ab34d85f95e6. Archive receipt has1667 explicit artifacts,6 oversized gzip archives42,946,909B; raw local copies intact/ignored. Scoped staging1750 files, unrelated dirty stages/root lock/bench files UNSTAGED. No commit/push/deploy yet. Static build/package PASS:1152files175,429,795B at `shell/.vercel/demo-upload-83abeb1645784a6e`;104picker entries,4,359,760,941B full details excluded. Browser all100 static proof session89274 is running against8082; inspect result before commit/deploy. AVDFEI development proof already PASS. Existing Vercel planPro, sourceFilesOutsideRootDirectory=true, productionBranch face/window1-watch-20260903 verified. Next: finish browser proof, deploy prebuilt preview and verify; scoped commit/push, promote/deploy production, final report, retire follow-up. Preserve sample-only acceptance label. Update/stage this handoff before commit.

LATEST 21:43UTC: all100 export outcomes complete (95 graded +5 unavailable spans), plus separate ALT. Coordinator5172 / finalizer15104 is verifying full traces after completing packaging-only migrations; check fresh POSTPROCESS_STATE, not these stale PIDs. AVDFEI browser proof PASS on local8083:55,434 receipts,55,381 markers,109 chunks; startup loads512 receipts; Home/End scrub preserves counts and fetches only3 chunks; no JS errors. Saved in runroot/timeline-proof. Largest main face2,090,611B gzip (<2MiB). Build/typecheck and exact codecs/storage tests pass. No replay needed. After automatic FINALIZE→FAULTS→REPORT reaches READY_FOR_REVIEW, manually run `archive_publication.mjs` (not part of the already-running coordinator); inspect its explicit files allowlist and archive hashes. Then build:demo/package-demo, static and hosted proof, scoped commit/push/deploy. Never stage oversized raw JSON; gzip archive is the approved published representation, raw stays local. No commit/push/deploy yet; quiet follow-up remains active.

LATEST 21:30UTC: **gzip storage APPROVED**, no user decision outstanding. `json_storage.mjs` exact gzip readers/writers added; oversized grades/pressure/history keep raw local files but archive .json.gz, hashes remain uncompressed JSON. Production assets now gzip-only in `.vercel/tune100-public-gzip`; prepare/package scripts preserve normal JSON URLs. `demo-baseline/` contains verified gzip copies of the25 prior published assets (manifest00bd62ea…), so clean Git builds no longer require untracked .demo-public. Vercel rewrites/headers added; still needs production build + preview proof before push/deploy.

AVDFEI then failed only size guard at2,363,141B; saved FACE_OVERSIZE includes all chunk bindings. Lossless transposition of receipt and marker matrices reduces prototype to2,090,595B (<2MiB), all values exact. Code/client support receipt_storage/marker_storage columns. `resume_timeline_face.mjs` verifies source pins/chunk round trip, repacks saved export, builds pressure/grade without replay or re-reading full trace. New artifact coordinator **PID5172**, logs postprocess-columnar.*. Check fresh POSTPROCESS_STATE/child; no duplicates. Three heavy processes must not overlap; browser proof waits for RAM. Finalize will refresh HER overview (all Qs), then faults/report. No commit/push/deploy yet.

LATEST 21:17UTC: old coordinator22284/face16676 exited at21:14 on AVDFEI `RangeError: Invalid string length` in the preflight's `packFace` whole-os count. Fixed storage only: `packFace` no longer serializes the unused outer array; timelines >512 receipts enter bounded chunks directly, skipping whole-face preflight. Tests pass. Restarted artifact coordinator **PID9956**, logs `postprocess-bounded.*`; check fresh state/child. It reuses all completed faces/replays; AVDFEI alone rebuilds. Node artifact children have8192MB heap ceiling, sequential only. Do not duplicate. HERALM's exact overview-Q refresh prototype1,138,864B/24,484receipts/24,494markers,48chunks; all stored Q values identical. Client tests3+codec tests2+bid round-trip tests5 pass, typecheck passes. Real large-browser proof still pending (did not launch alongside 3.4GB worker with only2GB physical RAM free). Gzip-storage approval for oversized grade/pressure/history files remains pending; **publication held**, no commit/push/deploy. Existing quiet follow-up remains useful and active.

21:10UTC: HERALM complete, main1,139,834B; AVDFEI now building under coordinator22284/face16676. Fix detected during UI review: overview must retain each stored per-side Q for the historical call line; codec/client now include dotted receipt columns and migration refreshes the earlier HER preview losslessly before finalization. Tests cover Q retention. This does not change forecasts. New pending publication question: HER grade152MB and pressure113MB exceed ordinary GitHub file size; operator asked to approve lossless .json.gz storage for oversized derived files/history, preserving normal JSON URLs. Do not push those oversized raw blobs before resolved. Original timelines/full grades local and intact.

Operator answered YES to lazy hash-verified receipt/chart-marker chunks. No outstanding decision. `timeline_chunks.mjs` now losslessly externalizes OS receipt bodies in512-receipt chunks, column-encodes lightweight marker previews with indexed aliases, and transposes full chart tick columns (no sample reduction). HERALM prototype round-trip passes, main1,069,926B gzip vs6.57MB. Client loads/verifies initial and scrub/play/inspected receipt chunks and holds playback at a missing chunk. Detailed actions above512 use bid-card-details-v2 chunks of256 actions, avoiding a >512MB JSON string on AVDFEI. Original record IDs stay in chunks; aliases are storage addresses only. Client supports v1/v2. Still verifying these newest changes; do not publish untested output.

Unblocked-only face coordinator PID20152 continues with --skip-event=HERALM and --skip-event=AVDFEI, logs faces-unblocked.*. Full postprocess coordinator PID22284 is already queued with --wait-face-pid=20152; it resumes the two large faces then finalize/faults/report. Check fresh POSTPROCESS_STATE and FACE_STATE/process. Never launch overlapping face workers or another coordinator. Completed face hashes checked before skips; no replay. New small/current faces may be v1, v2 or timeline chunked, all backward compatible and hash-bound. Existing quiet30-minute follow-up remains active. No commit/push/deploy yet.

### Superseded blocker — 2026-09-15 20:31 UTC

Bid-card lazy loading is implemented and verified (lossless GANZIN7,047 actions, main1.93MiB gzip; browser zero detail startup requests, one cached fetch on open, no JS errors). OS unchanged. Resumed artifact coordinator19320 finished through26 selected games (including unavailable-span notices), then exited at20:30:19UTC: HERALM still6,574,907 gzip bytes AFTER the bid-detail split. OS projection2.90MB + dictionary1.70MB + render2.18MB gzip sections; guard unchanged. Failed evidence: HERALM/FACE_OVERSIZE.json.gz; its full bid-detail sidecar exists separately. All original data preserved. AVDFEI has55,425 stages vs HERALM24,474, so has the same pending timeline-scale issue.

Asked operator now: authorize additional hash-verified receipt/chart-marker chunks loaded as playback/scrubbing reaches them, or pause. This is a NEW decision beyond already-approved bid cards. Do not repeat the old approval question. Do not raise2MiB guard, discard/downsample any records, or publish partial results. While awaiting the new answer, build_faces may continue the unaffected games using explicit --skip-event flags; COMPLETE_PARTIAL is NOT readiness for finalize/push/deploy. Check fresh FACE_STATE and actual process before launching anything. All replay100+fixed10 verification remains complete. No rerun required.

New `compact_completed_faces.mjs` runs at start of finalize: converts earlier small inline faces plus ALT to the approved bid-detail scheme, rebuilds unchanged grades, asserts every grade/display section identical, updates completed hashes. It has not run yet. Finalize, faults, report, scoped commit and deployment remain pending. Preserve all completed FACE_COMPLETE files.

## CURRENT ORDER — Sep15 tune-speed amendment overrides earlier all-game two-pass instructions

The operator now requires one full replay artifact for all frozen100, followed by a fixed seeded10-game determinism sample, including ALT/GAS and GANZIN. Retain all already verified extra comparisons. Label scoreboards exactly **pass 1 + 10-game determinism sample**. A sample is sufficient for this tune iteration only; full two-pass/all-game determinism remains required to finalize the organ for the record. Do not automatically run that deferred full proof or label the sample full acceptance. OS, builder, functionable, pool/pricing, grader and selection hashes are unchanged by this scheduling amendment.

Policy: `C:/tmp/unsupported_rests_20260914/TUNE_DETERMINISM_POLICY.json`; fixed seed `589f41bec8e34da939ed6664a7db9e65a25c9b8a6ac86c7119caaceddab5656c`. ALT is external to the100, so the10 comprise ALT plus9 selected (GAN mandatory,8 SHA-ranked). Seven comparisons were already complete at freeze; remaining sample second passes are RAMNOH Jul13, PODSTU Jul20, KOPPRI Jul12. Outcomes/checkpoint availability were not used in selection. `tune_policy.mjs` validates the sample against the immutable original selection every time; `test_tune_policy.mjs` passes.

New detached coordinator PID20944 (`schedule.mjs --workers 2 --wait-pids 13304,19644`), launched17:36UTC, stages PASS1 then SAMPLE then REPLAYS_COMPLETE. It automatically starts each replacement on its same shard only after that old worker exits. Old shard0 PID13304 checkpointed/exited after TROHOR at17:34; replacement PID5664 is running first-pass-only. Old shard1 PID19644 was finishing DOMSKA (pass2 started17:38) and will checkpoint, then its replacement starts automatically. DO NOT launch a duplicate coordinator or worker. Earlier idle coordinator10980 was intentionally stopped with no children. `STOP_AT_GAME_BOUNDARY` remains for old shard1; the replacements alone use `--ignore-legacy-stop`; the coordinator archives that marker after phase1. Check SCHEDULE_STATE/actual processes, not these possibly stale PIDs.

Measured old-worker peaks3.173/3.280GB working set,3.263/3.351GB peak private commit; only3.069GB free with two live workers. A third at the observed peak has no headroom: keep TWO. New runner records elapsed time and peak RSS in each completed PASS receipt; continue monitoring, never assume first small-game peak is maximum. Single-pass traces serialize immediately, before result disposal, so pass1 checkpoints now retain complete usable artifacts. Per-game `determinism_x2=false`, `passes=1`, `all_byte_identical=null` for untested games; no invented pass. Existing two-pass traces stay unchanged. Sample phase starts only after all100 complete artifacts and verifies full digests, not partial traces.

Exports/finalize/report now accept single-pass non-sample games and enforce the fixed10 comparisons before publication. `VERIFICATION.json`, unsupported-rests receipt and served scoreboard distinguish tune-sample verification from full-record acceptance. Full comparisons retained are counted explicitly; do not report200 passes when not run. Current estimated remaining replay+sample duration around2–3hours (rough, remaining tour mix differs), then sequential exports/grades/audit/deployment. This is not an end-to-end deadline. At17:39UTC,52/100 full artifacts (50 prior double-pass +2 new single-pass) and DOMSKA pass1 saved; sample7/10 complete.

The existing30-minute automation prompt has been UPDATED to this policy and remains ACTIVE, quiet except completion/failure/needed decision. OpenAI Docs guidance used for this scope update. Resume from SCHEDULE_STATE, policy and source pins; after coordinator completion, follow export/fault/report/deploy steps below but honor the sample-only contract. Snapshot-only CODE scheduling edits must not trigger new engine pins.

Pipeline clarification: finalize.mjs now defers served scoreboard rebuilding until report.mjs, after the new fault audit has been rebuilt and hash-bound. Do not disable stale-fault checks or temporarily use old credits. Sequence remains build_faces → finalize → build_fault_taxonomy → report; report builds both served scoreboards with the sample label before writing its PASS receipt. That avoids falsely treating the previous trace's fault audit as current.

### Artifact pipeline queued — 2026-09-15 19:16 UTC

97/100 replay artifacts at19:14UTC; PUTLIU Jul14, PODSTU Jul20 and VALSAS Jul16 remained. Shard1 PID21356 finished normally at18:47UTC; shard0 PID5664 still running. Coordinator20944 will start the three missing sample second passes only after all100 artifacts. Measured peak now3.706GB on shard0; do not add a third replay worker.

New lightweight detached artifact coordinator PID13980, `postprocess.mjs`, is WAITING_FOR_REPLAYS_AND_SAMPLE. It waits for SCHEDULE_STATE REPLAYS_COMPLETE, rechecks the fixed sample and source pins, then automatically runs build_faces → finalize → build_fault_taxonomy → report sequentially. It never commits, pushes or deploys. Check POSTPROCESS_STATE.json and postprocess-{faces,finalize,faults,report}.{stdout,stderr}.log; do NOT launch duplicate face/report jobs while it is alive. If a stage fails, inspect and fix orchestration only, then resume safely; saved FACE_COMPLETE outputs are retained. Final state READY_FOR_REVIEW still requires review and scoped commit/deploy. All detailed stages are gzip files already; no uncompressed-stage expansion or raw upload is authorized.

### Approved lazy bid details — 2026-09-15 (supersedes the prior packaging stop)

Operator explicitly APPROVED per-game on-demand bid-card details, no data dropped, main transfer under2MiB, then grade/report/push/deploy100. No decision remains outstanding. `bid_card_details.mjs` losslessly projects chart/card previews and stores every original action field in `<event>.bid-details.json.gz`, SHA-bound to the face and OS/trace. Grader, pressure and fault/report readers hydrate and verify it. Client fetches only on opening a card's details. Full per-tick accountability/stages remain local. GANZIN prototype:2,025,619B main gzip,2,605,761B bid-detail gzip,7,047 exact original action objects. Both guard and unchanged OS pins remain. Run tests, resume postprocess from saved completed faces; do not replay again.

### Prior failure — 2026-09-15 19:53 UTC

All100 replay artifacts and all10 fixed-sample full comparisons PASSED; scheduler completed19:21:43Z and exited. Postprocess then exited at19:24:21Z on GANZIN face export: gzip4,290,624bytes exceeds the existing2MiB transfer guard. No replay needs repeating. Preserve completed faces/grades; no push/deploy occurred. The export guard was NOT raised and no fields were removed.

Diagnosed by adding failure-only custody output in build_face_data.mjs: `runs/KXATPCHALLENGERMATCH-26JUL14GANZIN/FACE_OVERSIZE.json.gz`, plus per-section gzip sizes in FACE_0.stderr.log. OS section747,756B, dictionary691,414B, render2,826,025B, of which bid_actions2,667,761B. Read-only test extending existing dictionary packing to render round-tripped exactly but still3,977,250B (level9:3,869,001B), so this does not solve the2MiB bound. No codec/client changes were implemented from that experiment.

The operator has since approved the lossless lazy-loading solution above. Keep the automation active and quiet during healthy progress. Restart postprocess.mjs after the codec tests; existing FACE_COMPLETE entries are reused.

User explicitly authorized the conduct/bell change, same frozen100 twice, grading, before/after, ALT/GAS and GANZIN cards, push and static LAB deployment. On Sep 14 also authorized the quiet 30-minute completion follow-up. Automation ID: `finish-unsupported-rests-and-bell-alignment`. Report completion/failure/needed decision only, delete after completion.

## Verified state at handoff

- Worktree `C:/Users/omigr/omi-w1-face`, branch `face/window1-watch-20260903`, starting HEAD `e3f1df1f`. Large pre-existing dirty tree: do not stage indiscriminately.
- Run root `C:/tmp/unsupported_rests_20260914`. Immutable selection SHA `c8d0710d1aab2b43e9cb9d9e2813fc9c38f233e8e4941e8647d347aa0a6e4c7b`; copied original SELECTION with original input pins unchanged. New execution pins are in RUN_INPUTS.json.
- New OS `30b40a1a22894a5296094f4458e2be5ee036766f31f1615ffebf5ad7079cebc9`; builder `337a3fcbbddbb4dfc28df10213a1ce092cc7ed05885285754f96a1ebd62572f5`; functionable unchanged `83f44682de821f754e99ecfdc0b56fd4ac8a03d65b0ee986f39c47f7df4e6c74`.
- Eighteen synthetic support/bell boundary tests pass (`test_support.cjs`). New helper support only; pricing/pool functions unchanged. Engine/builder diff 78 insertions / 7 deletions.
- ALT/GAS two complete replays identical, digest `18eb860c346fbf7e70420d49293de0408a22fcf46d0ea0e35e29efe022b12805`; trace `40921fb26e25e6875478910980b366781198676e7620a5c7bc97938d294707e9`. Still 60/38 = 98, grade C, 2/4. Two GAS unsupported pulls. ALT/GAS is NOT in the selected100 and must remain outside denominators.
- Known completed selected games at this writing: ONCAJD Jul20, WINSHE Jul19, BARREI Jul12, VISAHT Jul19 (4/100). Check actual REPLAY_RECEIPTs for fresh status.
- Before audit: 27 complete, 58 one-sided, 10 unfilled, 5 unknown; 32/361 cents, 83 offered games; A 65 failed sides/58 games. Original raw-grade scoreboard credits 30 pairs/35 cents because three same-second fills were not yet deducted. Compare safety-audited counts to safety-audited counts.

## Running / paused jobs (verify exact process identity, not PID alone)

- PID14908: separate GANZIN two-pass pilot (`run.mjs --event KXATPCHALLENGERMATCH-26JUL14GANZIN`). Still pass1 at handoff; old loaded harness writes WORKER_0.json intermittently, so can collide with shard0 progress. Its per-game PASS files are authoritative.
- PID24384: shard0, `run.mjs --worker 0 --workers 2 --skip-event KXATPCHALLENGERMATCH-26JUL14GANZIN`; GREVAN pass1 at handoff.
- PID17744 shard1 was stopped to restore RAM headroom (had only ~1GB free). VISAHT Jul19 had finished both passes before the stop; its complete trace/receipt is preserved. The stopped incomplete game was LANYIB Jul14 pass1. Updated harness now also reuses saved PASS_1 and reproduces pass2 when available, comparing complete digests. Resume shard1 only when at most one other replay is live; limit to TWO total replay processes and no concurrent heavy face builder. User's PC is16GB; after stopping shard1, free RAM recovered to3.54GB.
- Face coordinator PID6148 stopped; its child was allowed to finish. Existing completed FACE_COMPLETE files preserved; BARREI may have an ungraded face and no completion marker. Restart builder later; it will reprocess that game. No full replay outputs were deleted.
- Node unified-exec session IDs if still accessible: GAN28892, shard0 44420, shard1 28448 (stopped), faces80229 (stopped). ALT97443 and ALT face11830 completed. Do not assume session liveness; check processes/checkpoints.

Late-bell extensions are expensive: GAN rows went from 6,187 to31,716; existing atomic rearm now evaluates for more of the game. Do not change pool/price/conduct definitions to accelerate. No runner may touch the other704. Preserve completed passes; do not blindly restart all100.

## Resume and build commands

Working directory is the worktree. Use hidden Start-Process with logs for detached execution if a process must be relaunched. Approval escalation may be needed for worktree/C:tmp writes.

1. `node --expose-gc window1-watch/unsupported_rests/run.mjs --worker 1 --workers 2 --skip-event KXATPCHALLENGERMATCH-26JUL14GANZIN` resumes shard1. Shard0 same with worker0 if dead. GAN separate `--event ...GANZIN`. Skip already-complete receipts only after hash/input validation (harness does so). Updated runner reuses saved pass1 digest and repeats pass2. A `STOP_AT_GAME_BOUNDARY` file in run root stops updated runners after a game; already-running original processes did not load this feature.
2. After replay workers no longer consume RAM, `node window1-watch/unsupported_rests/build_faces.mjs` builds100 (can resume); ALT already complete. Original selected print extracts are `C:/tmp/tune_expansion_20260914/selected_prints`; source/hash receipt in that old root. Do not extract again. ALT local extract and ALT_PRINTS_RECEIPT.json are in new root. Unknown five spans get FACE_UNAVAILABLE rather than fabricated data.
3. `node window1-watch/unsupported_rests/finalize.mjs` — creates unknown minimal cards and current selected100 scoreboard, sentence projections, per-game facts, verification. Adapted from original runner, not yet exercised end-to-end; fix orchestration/report bugs only, not grading logic. It permits exactly100 + ALT extra run dirs.
4. `node window1-watch/build_fault_taxonomy.mjs C:/tmp/unsupported_rests_20260914 C:/tmp/tune_expansion_20260914` — unchanged class definitions, new optional RUN_INPUTS and print root. Original input selection pins stay original. Requires finalized scoreboard. Count/ESS/weight parity when reconstructing B top members remains mandatory.
5. `node window1-watch/unsupported_rests/report.mjs` — stream-audits support decisions, pass digests, unchanged grader/ruler, builds before/after/cards/sample/receipt under data/unsupported-rests. Newly written and syntax-tested, not yet full-run tested. Ensure no false audit failure from unchanged tokens moved in diff. No success record before verification. This receipt gates demo packaging.
6. Rebuild `build_scoreboard.mjs` after fault audit so safety-excluded fills are reflected in served scoreboard. `finalize` initially builds before the new fault hashes exist, so rebuild after classification explicitly.

## What changed / invariant cautions

Engine uses fresh resolved sentence emitted at current receipt with future deadline, exact level, existing veto-selected target. Missed/superseded unsupported rest -> normal CANCEL_REST action with PULL_UNSUPPORTED reason and separate accountability record. Valid supported replacement may supersede. Pre-print overdue reviews use previously observed state only, can hold exact or cancel but cannot reprice to catch incoming print. At deadline equality the existing fill ordering remains. Final bell review clears unsupported rests.

Builder uses later corrected ruler bell if old cutoff was earlier; verified grading span is UNCHANGED. New late fills outside span are observed but uncredited. Unknown spans/bells unchanged. New metadata bell_source_used/bell_alignment is hash-bound into face provenance via face_contract.mjs. This is not permission to alter floors, spans or denominators.

First new PLACE with a nonfuture X is not silently prohibited by this order; count it in receipt as an inherited conduct limitation. Existing rearm remains unchanged. `conduct_changed:false` in renewal record describes the observer itself, not actual cancellation actions (documented in FIELDS).

## Baselines and assets

- BEFORE_OS.js, BEFORE_BUILDER.js, BEFORE_FAULTS.json, BEFORE_SCOREBOARD.json in run root.
- `before_compact/` has592 compact files plus original grades history and full tune-expansion reports. Do NOT overwrite the backup.
- Prior why audit `C:/tmp/ganzin_why_20260914/REPORT.md` and AUDIT.json: GAN old rest20 blocked from Q31; oldbell08:34UTC, corrected14:25UTC, +350.9271minutes. Prior card F with only ZIN72 filled,0/19.
- Full generated stages/renewals ignored/local. Some legacy tracked stage files are dirty despite .gitignore; never stage them. Raw prints remain local.

## Deploy / push (only after all checks)

Preserve other dirty bench/minute-rebind work. Stage explicit engine/builder plus new unsupported_rests code, minimal face_contract/gloss/FIELDS, build_fault_taxonomy/build_scoreboard changes and selected compact assets/reports/history. Prior selected100/fault deployment changes are authorized but still uncommitted. Review each staged path; do not stage engine functionable/minute binding, raw prints, stages, accountability streams or unrelated original-game dirty copies.

Production `https://omi-window1-watch.vercel.app`; branchface/window1-watch-20260903. Vercel CLI `C:/Users/omigr/AppData/Local/npm-cache/_npx/69f9afb961c37556/node_modules/vercel/dist/index.js`; project config under shell/.vercel. Existing5 baseline assets in shell/.demo-public remain checked by manifest. New prepare-tune-demo-data.mjs reads data/unsupported-rests/RECEIPT.json when present to ship newOS selected100 + ALT, preserve other3 original games (104total). Do not accidentally ship dirty rejected-minute baselines. History remains preserved. Compact face/grade/oracle/pressure/fault assets only.

`npm run build:demo` then `node scripts/package-demo.mjs` from shell. Package is content-addressed, lossless gzip routes. Run Vercel prebuilt preview then verify; deploy production/alias and verify. Use scripts/verify-fault-demo.mjs against hosted URL (100picker, grade/oracle, scoreboard, missing-stage404). Also explicitly verify ALT grade C,60/38,2/4 and newOS hash; screenshots. All current per-tick details exceed50MB and stay out.

No commit, push or new deployment had occurred when this handoff was written. Final user report must contain actual commit/newOSsha, safety-audited before/after, ALT/GAS and GANZIN cards, ten worst and hosted URL. Delete the quiet automation only after completed delivery or when genuinely obsolete.

## Heartbeat 2026-09-15 01:14 UTC

GANZIN finished both passes identically; verified trace SHA `e48f1586b658e16cf965011846a10f52d3a0fb41cc9b8abfcc847edb44868124`,48,088,169bytes. Still only ZIN72 filled; GAN unfilled. It has7,132 decision stages,3,519 PULL_UNSUPPORTED records and3,527 assumptions. This exposes substantial inherited cancel/rearm churn; do not hide it or change new-placement/rearm rules to improve results. report.mjs counts newly posted nonfuture deadlines. Face/grade still pending. Watch disk headroom before materializing many additional stages.

GAN PID14908 exited. Shard0 PID24384 alive on GRESAN pass1. With4.69GB RAM free and only one replay running, resumed shard1 hidden at PID16144, logging to `C:/tmp/unsupported_rests_20260914/worker1-resume-20260915-0114.stdout.log` and `.stderr.log`. Verify fresh process identity next time. At most two replay processes; heavy face coordinator remains stopped. All existing completed receipts, source pins and GAN trace digest were checked before resuming. No commit/push/deploy yet.

## Reattachment 2026-09-15 15:55 UTC — supersedes old process status

User asked to resume after overnight interruption, keep the 30-minute follow-up, use all safely available workers, and report ETA once a fresh rate is measured. There are 41/100 selected games complete twice, plus the separate ALT/GAS check. All 41 selected trace hashes, source pins, full two-pass digests and citation/action/pair invariants were reverified before restarting. No FAILURE.json was found. Last completed game was ARNPOH at 2026-09-15T03:53:35.879Z. Latest durable pass checkpoint is BOUZHA pass1 at 04:52:36.419Z; STRSHE pass1 at 04:46:41.763Z. Neither has PASS_2.json: their in-flight second-pass work was not saved and must repeat, but pass1 is preserved.

Prior shard processes were absent. Hidden detached replacements: shard0 PID13304 and shard1 PID19644, same two-worker commands and GANZIN skip. Logs: `C:/tmp/unsupported_rests_20260914/worker0-resume-20260915-1550.stdout.log` and corresponding stderr; same pattern for worker1. Both logs confirm REUSE_PASS_1 and REPLAY_START for pass2 at 15:51 UTC, BOUZHA (81,271 input rows) and STRSHE (187,820). Process identity and rising CPU confirmed. Both stderr logs empty. Do not launch duplicates or repartition their live shards.

Machine reports 6 physical / 12 logical CPUs, about16GB RAM; the two replays use5.8GB combined and only1.8GB system RAM remained at 15:54 UTC. Two is the currently safe worker count. Do not add replay or face workers without fresh headroom measurement. Boot timestamp alone does not prove the prior worker exit cause.

Automation `finish-unsupported-rests-and-bell-alignment` verified ACTIVE every30minutes, unchanged. The user's latest explicit request permits ONE rate/ETA notification once a post-resumption pass completes; report its measured rate and distinguish replay ETA from exports/grading/verification/deployment. Then return to quiet completion/failure/needed-decision notifications. Prior pass1 durations were about59minutes (BOUZHA) and73minutes (STRSHE); these are provisional evidence, not a fresh measured rate. Full remaining duration cannot be inferred reliably from raw input rows alone: completed games vary dramatically in rearm cost. No commit/push/deploy yet.

## Heartbeat 2026-09-15 16:59 UTC — requested rate update delivered

45/100 selected games now complete twice. BOUZHA pass2 finished16:50:20.528Z, identical to saved pass1 (`c0836c7f…`); full trace `c7c1514c0bc753320d6bf728b4e70561bf670172fe2632722bf615a004160aea`. Elapsed59m11s for81,271 input rows,22.9 input rows/s. Shard0 subsequently completed FORCUI, FEAWAL, WESSEL and is on ZHUYUN. STRSHE shard1 still in pass2, process alive with increasing CPU. Both stderr logs empty; source pins unchanged. All four newly completed traces were independently rehashed, full pass digests compared, and citation/action/pair invariants checked. No failures. RAM free2.76GB, C free22.30GB; no additional heavy worker.

ONE requested ETA/rate notification was delivered with this heartbeat. Replay-only rough estimate about5 more hours, low confidence: prior41 completed games averaged10.12 worker-minutes/game;55 remaining /2 workers projects4.64hours, but remaining tour mix differs (4ATP_CHALL,15ATP_MAIN,17WTA_CHALL,19WTA_MAIN) and cost varies strongly with rearm. This is not an end-to-end delivery ETA; exports, grades, audit and deployment remain pending. Resume quiet healthy-progress notifications after this one update. Automation stays active. No commit/push/deploy.
