# V52j Iteration 9 - role-conditioned level selection

Date: 2026-08-14

V52j changes exactly clause 3/N4 level selection over the frozen V52h
behavioral lineage. V52i's symmetric depth refinement is reverted and is not
inherited. At each receipt the two existing machine reads plus already
licensed coherence assign receipt-local roles. `FALLING` may lower the frozen
evidence-backed target with the existing G-grid discount and G3 gradient;
`RISING` keeps its own near-support target; `SETTLED` and insufficient reads
keep the frozen V52h target. Role assignments and read evidence are recorded
in every license and are re-evaluated on every receipt. Priors inform and
never gate. No new constant is introduced.

Clauses 1, 2, 4, 5, and 6, the disagreement referee, trades-as-truth
crediting, scavenger OFF, and `REFLEX_POST=0` are frozen. The two depth assets
remain under validation; this observation does not validate or promote them.

The source implementation identity is
`a7e4c1114251e22a42c3207fcc20496aacf2534c`. Its seed SHA-256 is
`1b7d0b9e5a74eefd6c4019d71eedea3872ab335bc8726a533f3a0ec48ff82784`;
the resulting 30-event list SHA-256 is
`6c8374ed9570beefc7f6494936002b80781bbeb8dda57d361c621ae8bf536e02`.
The population is the five frozen pins plus 25 fresh events with zero overlap
against every V52b through V52i fresh cohort. GUEGOM is replayed separately as
an explicitly reused named observation and is not part of the fresh 25.

On the 30-game observation, V52h and V52j are identical on outcomes: 16
complete-at-delta pairs, zero complete-at-loss, 45 credited legs, 41 cents of
locked delta per contract, and frontier 2/2/4/16/16 at <=93/<=95/<=97/<100/
any. V52j records 343,483 role receipts, 17,366 role flips, and 1,844 target
re-aims. Twenty-two credited legs are classified FALLING at fill/terminal;
their median fill delay versus V52h is zero seconds, median entry-minus-later-
floor is two cents, and 7 of 22 land within zero or one cent. Thus the
pre-stated faller-later/near-floor claim does not validate. No V52h credited
climber leg is lost, so the inverse-error preservation claim passes. Mean
banked delta remains 2.5625 cents and does not rise. No new one-sided exposure
is created and none is resolved.

GUEGOM does not convert. Both variants leave GOM uncredited and credit GUE at
62; the recorded terminal role on both legs is RISING. This failed named
observation did not authorize or cause a policy edit.

All five pins are unharmed and `REFLEX_POST=0`. Every complete pair remains
strictly under 100; every game retains exactly one complete pair-budget record.
Two final clean builds are byte-identical across 149 files. The complete
19-file inherited-and-new suite passes 620 assertions with zero failures,
omissions, or deselections. Full receipt diaries are retained in 10,000-row
gzip chunks so no Git object exceeds the transport ceiling.

This is a 30-game observation only. No disposition-804 run, sealed population,
deployment, authorization, live, network-runtime, order, position, exit,
settlement, or DCA action occurred.

Canonical package:

- `.claude/window1_live_v4_replay/v52j_role_conditioned_level_selection_20260813/`
- `.claude/window1_live_v4_replay/v52j_guegom_named_observation_20260813/`
