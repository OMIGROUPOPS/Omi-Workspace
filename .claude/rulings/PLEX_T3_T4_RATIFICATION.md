# PLEX RATIFICATION — T3 SUPERSESSION + T4 HOLD-GATE CLAUSE (relayed 2026-07-09 night; recorded before code per the dispatch's sequencing)

## THE RULING, VERBATIM (operator-relayed, complete; landed 07-09 late night — the reserved slot, filled)

> Both check out: the roster is exactly 139 rows as claimed, and recut_cells.json — the fitted-level source the climb-side spec depends on for edge_p50(cat, cell) — is real, on disk, at .claude/seqfloor_20260708/. Everything in this resubmission verifies. Now I can rule.
>
> Source check — clean this time. All four documents are real, fresh (committed today, HEAD advanced to d924319e since my last check), and internally consistent. Spot-checks: the P1a roster is exactly 139 rows in both the markdown table and step1.json (matches the claimed n). recut_cells.json — the file CLIMBSIDE_SPEC.md cites for edge_p50(cat,cell) — exists on disk at .claude/seqfloor_20260708/, so the fitted-level claim has a real backing artifact, not just a name. Good work closing the gap from the bounce.
>
> T3 — RULE: supersede, ratified. The riser-disarm accounting table is the right shape and answers what I asked for directly: it names the specific bleeding mechanism (post-revision walking upward into the moving market) and shows the counterpart mechanism in the new spec that structurally cannot repeat it (aim-then-HOLD, no upward revision exists in the posture — not "capped," absent). That's a stronger answer than a cap would have been, because a cap bounds a behavior while this design deletes it. Two things earn the ratification, verified independently rather than taken from the table: The fv_observe reconciliation is honest, not papered over. The spec explicitly separates the staged package's 491/43-76 series from the current 18/100 series and states they're never blended. That's exactly the right instinct — it's the same discipline I required in the AIM_V2 ruling (card-era vs honest-era weighting) applied correctly here without my having to ask twice. P1b's projectability finding independently corroborates the supersession, from a different angle than the riser ever measured. The riser died on walk erosion; P1b shows why a resting, non-walking posture is structurally necessary for ITF specifically — early volume is nearly uninformative (bal-acc 0.56-0.65, false-flag 47-57%), so any posture that commits or chases early is gambling on a coin-flip signal by this data's own measure. The old riser and the new spec were never just "same idea, better guardrails" — the new spec is responsive to a real classifier finding the riser never had access to. Ratified. Riser stays disarmed and retired with the lineage stated in CLIMBSIDE_SPEC.md; the climb-side posture (rest-from-T-90, fitted level, no upward revision, stand-down-never-chase at onset+15) is approved as spec. Arming still requires the standing conditions: full four-bar gate, joint-shadow n≥30 before any cap read (already acknowledged in the resubmission), shadow-first.
>
> T4 — RULE: hold-gate clause ratified as designed; threshold reserved, as you deferred. Correctly scoped — "placement gate ≠ hold gate" with symmetric quiet-definition (mirroring the ramp definition rather than inventing a new statistic) is exactly the calibration I asked for, and you correctly didn't try to guess my number — you shadow-log both branches and leave the threshold for a ruling once data exists. That's the right sequencing. P1b's finding upgrades this clause's importance, and I want that stated explicitly rather than left as a footnote: the hold-gate isn't just an anti-selection shield anymore — P1b shows the ITF volume floor cannot be evaluated at conception (bal-acc 0.56-0.65) and can only be evaluated once real volume accrues, which is exactly what the hold-gate watches for. That means the hold-gate is now the single mechanism carrying two jobs: anti-selection defense (my original T4 ask) and the volume-floor's only enforcement point (P1b's finding). That's a bigger load on one gate than either of us specified separately. It's fine to combine them, since they're measuring compatible things (both ask "is this book alive enough to justify continued exposure") — but the shadow logging and eventual threshold ruling should report both readings separately per leg (anti-selection quiet-flag vs volume-floor-miss flag), not as one merged boolean, so that if they ever diverge, that divergence is visible instead of hidden inside one number. No threshold set yet — correctly reserved, and I won't guess one from zero shadow data either. I'll rule the number once the shadow logs show where quiet-book dumps actually cluster, per the resubmission's own plan.
>
> Dispatch cleared, with one binding sequencing note. The OS build may dispatch on T3/T4 approval as stated, with T1's import-boundary CI assertion as a same-PR component (already agreed) and T2's cap sizing already ruled. One thing to hold firm on, since it's easy to lose in a multi-week build: the P1b/P4 dual-reading requirement above is a condition of this T4 ratification, not an optional refinement — add it to whatever ticket carries the hold-gate shadow logging before that logging starts, not after, so you don't have to re-instrument mid-accumulation.

## The ruling, as relayed (dispatch of 07-09 night, FINAL wording — kept beneath the verbatim per the fill dispatch)

1. **T3 supersession RATIFIED** — the riser is retired; lineage stated in `CLIMBSIDE_SPEC.md` (climb-side/decay-side supersedes the riser frame).
2. **T4 hold-gate clause RATIFIED; the threshold NUMBER stays reserved** for Plex, to be set on shadow data (where quiet-book dumps cluster).
3. **Dispatch CLEARED with a binding sequencing note** — the dual-flag shadow condition below ships before the threshold dataset accumulates.
4. **Plex's explicit upgrade, stated as LAW, not footnote:** *the hold-gate now carries two jobs — anti-selection defense (the original T4 ask) and the volume floor's only enforcement point (P1b's finding).*

## The binding condition (T4), in its exact shape

Every hold-gate shadow line reports **two separate named flags per leg, never one merged boolean**:
- **`quiet_flag`** (anti-selection): would-hold/would-review judged against the leg's **own T−8h→T−4h activity baseline**, with the **baseline value logged**;
- **`floor_miss_flag`** (volume floor): **realized volume to now + staged-floor qualification state**.

**Plex's stated reason (and it lives in the code comment, `oslayer/holdgate.py`):** if the two readings ever diverge, that divergence must be **visible in the data**, not hidden inside one number.

## Execution record (C-T4-DUAL-FLAG, this deploy)
- Pre-instrument state, named honestly: the live `hold_review` lines carried both flag FIELDS but the floor reading was **structurally dead** — the caller passed `expected_share_by_now=None`, so `floor_miss_flag` was a constant False, and the quiet baseline was a running median of ALL samples rather than the leg's own T−8h→T−4h span. That fails the condition; instrumented now.
- Instrument: per-event realized **contract** volume from ws trades (unit-matched to the staged 2.5k floor; basis named `ws_contracts_since_boot` on every line) · T−8h→T−4h baseline banked separately per leg · shadow-default per-cat share curve (coarse, sourced to the 07-07 HOURLY_APPENDIX shape, refit at the threshold pass — never a ruling) · `floor_qual` state on every line (`qualified_now` / `on_pace` / `below_pace` / `unevaluable` — never silent).
- **Fence:** lines without the `t4: dual_flag_v1` stamp are `pre_instrument` — counted visibly in the rollup, excluded from Plex's threshold dataset. **The T4 accumulation clock starts at the first dual-flag line after this deploy** (time stated in the close-out).
- Arm prerequisites UNCHANGED: four bars · joint-shadow n≥30 · shadow-first · `os_active` untouched (dormant).
