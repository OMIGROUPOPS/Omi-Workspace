# OPERATOR RULING — the ≤2¢ branch (relayed 2026-07-06; closes the PENDING item)

**Provenance:** operator's ruling as relayed 2026-07-06, on the Job-1 evidence table
(ONE_AIM_FIX_20260706.md: DAMHUE / POPSAN / BUEPOR). The a/b menu offered with that
table (rest-at-own-dip-capped vs keep-cancel) is REJECTED.

---

## THE RELAYED TEXT (verbatim)

NO new ruleset. Per-leg conditional patchwork — cancel thresholds, rest floors,
bound-derived sibling levels — is BANNED AS A CLASS: every such branch is scaffolding
around the missing joint model, and leg 2's bid must never be a function of leg 1's
accounting. The cancel branch stays as-is, marked INTERIM-ARCHAIC in the ledger,
dissolved by the aim build when its data trigger fires (the pair priced as one state,
both bids from one read — the June atomic-pairs conclusion and the operator's 90/10
frame, converged). The 3 orphan losses + the live same-mechanism cases (ZEBAND, XUXBER,
LINMAR) are logged as the interim's known cost. This ruling governs the ≤2¢ branch
ONLY — the running jobs (shape accumulator, v2 spec, harness, z-score shadow) proceed
as dispatched, and none of them may add sibling-level conditionals while they build.

---

## Binding constraints extracted (C45 grep surface)
- **BANNED AS A CLASS: per-leg conditional patchwork** — cancel thresholds, rest floors,
  bound-derived sibling levels. Leg-2's bid is never a function of leg-1's accounting.
  (This extends the June-12 paired_cap grave from "the veto" to the whole conditional
  family; any future proposal in this shape is DEAD-IN-DRAFT by this ruling.)
- The `reaim_sibling_lower` ≤2¢ cancel branch: **UNCHANGED, status INTERIM-ARCHAIC** —
  it dissolves when the aim build's coverage trigger fires (AIM_V2_SPEC §3), replaced by
  the joint model: the pair priced as ONE STATE, both bids from ONE READ.
- Interim's known cost, logged: DAMHUE/POPSAN/BUEPOR naked-single losses (−$1.65 settled
  + forfeited completions, all three pulled legs won) + ZEBAND/XUXBER/LINMAR live
  same-mechanism cases at ruling time.
- Scope: the ≤2¢ branch ONLY. Accumulator / V2 spec / harness / z-score shadow proceed
  as dispatched — **and may not add sibling-level conditionals while they build.**
