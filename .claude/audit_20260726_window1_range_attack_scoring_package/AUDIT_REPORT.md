# INDEPENDENT WINDOW-1 RANGE-ATTACK SCORING/EXECUTION PACKAGE AUDIT @ f774d906 — RULING: BLOCKED

**BLOCKED — four reproduced defects.** The package's frozen laws are largely sound and fully reproduced (lineage, sole guarded fill source, census, metric law, conservation, one-attempt safety, 102/102 tests) — but (1) the guarded-fill adapter **silently truncates and accepts fractional quantities and prices** against the exact-five/integer-cent contract; (2) the execution authorization gate is **self-referentially unsatisfiable** — it requires a commit whose own tree contains its own SHA; (3) the adapter **fail-crashes on the package's own frozen fill source** (3 legs carry multiple fillable interval rows and the adapter raises rather than lawfully selecting); (4) the reference adapter resolves same-timestamp price ties by an **invented lexicographic UUID ordering that is outcome-relevant on 64 legs**. No scoring occurred; the holdout was not touched. Do not execute this package.

Date: 2026-07-26 · Branch: `audit/window1-independent` (child of `d413f231`) · Auditor: independent CC session
Method: detached worktrees at `f774d9060acc70efd4a80d48bfa7d4c6b1b9daf1`; `--mode execute` never invoked; no real-population C/PC/S/IC computed; results directory never created; synthetic scorer fixtures, adversarial adapter probes, a synthetic Git repository for the gate proof, validation-mode/code inspection, and non-metric source censuses only.

## 1. Lineage and package identity — PASS

Parent exactly `85134634`; all-refs children scan: **sole child**; local/remote `codex/window1-definition` equality verified. **21 additions, zero modifications/deletions** — both frozen V2 candidates and every inherited surface unchanged by construction. All source hashes verify: 16/16 SOURCE_HASH_MANIFEST committed inputs and 12/12 PACKAGE_ARTIFACT_MANIFEST rows (28 total; CRLF-basis accepted only where blob-equal — see the portability caveat in §9). **Input-bundle hash exact**: `canonical_sha256(input_bundle_payload) = 549717d97f08bff4b9e57a7eef06c576de5c9cb2cd28e77fd6556b41e021f2d8`, matching the manifest and the expected value. Execution ID `w1-range-attack-v2-dev-20260712-20260720-grid1` bound in the runner constants, gate file, and command template. D=804 and 1,608 event-leg identities enforced in code (`_event_legs` hard-fails otherwise); the sealed-date firewall refuses July 24–26 at the adapter, reference, and role-resolution layers (`holdout role path refused`).

## 2. Sole guarded fill source — PASS census / **defect in the one-fill law (see §3b)**

Census reproduced exactly from the sole source: **969 guarded PRICE_REACHED + 26 guarded STRICT_ASK_CERTAIN_FILL = 995 FILLABLE_AT_X interval rows; macro-hold 502 / macro×micro 493**; the 283 after-right and 53 boundary-unprovable strict-ask actions are structurally excluded (they exist only in the stream artifacts, which the scoring package never reads). Sole-source proof: the runner's fill input is exactly the role `guarded_fillability_receipts → .claude/window1_range_attack_prerun_v2_strict_ask_20260725/PRICE_FILLABILITY_RECEIPTS.jsonl.gz`; no manifest role and no runner code references the stream shards, `causal_policy_fill_state`, raw strict-ask actions, post-cutoff actions, or exact-ask touches; the adapter's `_forbidden_keys` recursively rejects raw causal-state keys (`FORBIDDEN_RAW_SURFACES`) in any input row. **However, the census framing "995 eligible leg fills" is wrong: the 995 rows span only 991 unique candidate/event/legs** — see §3b.

## 3. Guarded-fill adapter

### 3a. Numeric semantics — **BLOCK (silent truncation, adversarially demonstrated)**
All acceptance conditions of the row contract are otherwise enforced (identity allowlists, dates, score-free source, positive provable boundary with supported class, evidence within `[opened, evaluated_right]` and cutoff, fill price == exposed X, band 1..99, evidence-type truth flags, print positive-size at/below X, strict ask strictly below X). But `_finite` accepts any finite float and every quantity/price passes through `int(...)` **truncation**: adversarial probes through the real `adapt_fillable_row` show **ACCEPTED** for accounting quantity **5.9** (→5), **5.0001** (→5), target/fill **40.9** (→40), fill 40.9 alone, fractional print evidence price 39.7, fractional strict-ask evidence target 40.9, and fractional ask 39.9 — 8/8 fractional cases accepted (control also accepted). The contract requires exact five and integer cents; a value merely truncating to them must not pass. Full case table in `ADVERSARIAL_GATE_NUMERIC_RECEIPT.json`.

### 3b. Duplicate-leg law — **BLOCK (fail-crash on the frozen source)**
The frozen source itself contains **3 legs with multiple FILLABLE_AT_X interval rows (7 rows)**: macro-hold `…LANRAD/RAD` (X=81 then X=80, distinct evidence), macro×micro `…BOHBOU/BOH` (three intervals, X=3/2/3, same print evidence ts), macro×micro `…LANRAD/RAD`. `adapt_fillability_rows` **raises** `"multiple eligible intervals for one candidate/event/leg"` on the second row, and the execute path feeds all rows straight in — the package **cannot process its own frozen input**; execution would fail-crash before scoring. (Fail-closed is better than silent inflation, but a frozen scoring package that cannot run on its frozen source is defective, and the "one fill per leg" claim is presently a crash, not a law.)

## 4. Reference adapter — mechanics PASS / **BLOCK on the tie law**

Verified: only guarded-cache-v3 `prints` reach the reference path (carried last-trade state and book snapshots are structurally absent from the print iterator); positive-size, exchange-identified (`trade_id`), deduplicated true prints only, with **conflicting duplicate receipts raising**; T−8h floor = frozen `scheduled_start_exchange_ts` − 8h; upper bound = the exact V5 guarded cutoff with the full class/guard-id/seconds/anchor arithmetic (`official-point-strict-60s-v1` 60s, `causal-interval-strict-60s-v1` 60s on the interval lower bound, `te-calibration-central-93pct-asymmetric-v1` 900/600 with the committed-cutoff cross-check; `cutoff = anchor − positive_guard`, direction `anchor_minus_positive_guard`); future prints excluded; schedule cannot substitute (non-positive classes return unavailable); missing reference keeps C as a fill fact and prevents PC/IC (verified in §5). Two flaws: fractional true-print prices are `int(...)`-truncated (same class as §3a), and the choice rule `sorted(by ts, receipt)[-1]` resolves latest-timestamp ties by **lexicographically greatest receipt UUID**. Census over the real caches (1,408 legs with known cutoffs): **271 legs have multiple receipts at their latest admitted timestamp; 64 of those have DIFFERING prices** — on those 64 legs the Window-1 close reference, and therefore individual/combined delta and PC/IC, depends on an invented UUID ordering with no authoritative source. **Unsupported and outcome-relevant → BLOCK.**

## 5. Metric law — PASS (synthetic fixtures through the real scorer)

`FEE_CENTS = 0`. Fixtures through `score_event`: C requires both legs' single guarded five-contract fills; individual delta = X − W1 close; combined = sum; **PC = C ∧ combined < 0** (combined exactly 0 → `completed_non_PC`); **S = C ∧ cost < 100** (cost exactly 100 → S false while PC can be true — no aliasing); **IC = C ∧ both deltas < 0**; PC true with IC false reproduced; S true with PC false reproduced; **missing reference → C retained, PC/IC false, delta null, class `completed_reference_missing`**. No inherited PC-as-cost-below-100 exists anywhere in the package modules (grep: zero matches; zero legacy scorer imports).

## 6. Event and aggregate conservation — PASS (structural)

`aggregate_candidate` hard-fails unless exactly **804 rows** per candidate with unique candidate/event identities; classification census restricted to the closed six-class set (`completed_PC`, `completed_non_PC`, `completed_reference_missing`, `naked_single`, `no_fill`, `censored_boundary`) and must sum to D; the completed-decomposition **IC + PC-but-not-IC + non-PC = C** is emitted; no ranking or selection anywhere; the EXPECTED_OUTPUT_SCHEMA carries the evidence, reference, and metric fields. Real two-candidate aggregates were not computed.

## 7. Authorization-gate satisfiability — **BLOCK (formally unsatisfiable)**

`authorize_execute` requires: full-SHA `audit_commit` ancestored on `origin/audit/window1-independent`; then reads the report **from that commit** (`git show <audit_commit>:<report_path>`); then requires that report to contain `PASS`, the package HEAD, the execution ID, the bundle hash, **and the exact execution command formatted with `FUTURE_PASS_AUDIT_SHA=audit_commit`** — i.e. the report stored in commit A must contain A's own SHA. A commit's SHA is SHA-1 over its tree (which contains the report), parents, and metadata: the requirement is a hash fixed point. **Synthetic-repository reproduction:** six iterations of write-report-citing-best-known-SHA → commit → observe new SHA never converge (each tree can only cite the previous SHA); the formal argument (SHA-1 preimage/fixed-point infeasibility) closes the proof. **No ordinary finite audit workflow can produce an accepted report/commit pair — execute mode can never lawfully run.** The remaining gate properties are correct and verified: ancestry check, package/ID/bundle binding, wrong-parent and dirty-worktree refusal, altered-package hash failure, existing-results-directory refusal, and validation-only mode structurally incapable of invoking adapters/scorer or loading the real population (`fill_adapter_invoked: false` by construction). The gate must not be weakened; it must be made satisfiable (e.g., binding the report by path+content-hash or a later-commit attestation rather than self-referential SHA embedding) — not repaired here.

## 8. One-attempt and output safety — PASS (read-only inspection)

Results directory uniqueness enforced (`exists()` → refuse); `retry_or_resume_permitted: false` and `retry_count: 0` frozen in receipts; failure preservation and deterministic output hashing follow the stdout-safe series pattern; only the declared results directory is writable (role resolution refuses escapes and holdout paths); exactly two guarded print sites; no network/live/production interface in any package module.

## 9. Tests and regeneration — PASS with a portability caveat

**102/102 tests** (22 new package tests + 80 inherited). **Two fresh package builds are byte-identical to each other (11/11 files)** — internally deterministic. Post-commit regeneration vs the committed artifacts: 7/11 match on a CRLF checkout, 6/11 on an LF checkout; every difference is **hash-basis only** — the committed receipts mix CRLF disk-basis hashes (the builder's original Windows checkout) with LF blob hashes, so regenerated manifests differ in recorded sha256 strings while **every underlying blob is content-identical** (verified row-by-row: `committed-basis: CRLF, regen-basis: LF` on the same blobs). This is the recurring benign newline-basis pattern of the series, now surfaced as regeneration checkout-dependence — the corrected package must record newline-normalized (git-blob) hashes. 27+ source/manifest hash rows verify. The outstanding strict-ask V2 regeneration check was **already independently confirmed** in the finalized addendum at `d413f231` (17/17 artifacts byte-identical) — no repeat needed; recorded separately as required, and it cures nothing here.

## RULING

**BLOCKED — do not execute this package.** Reproduced defects and affected surfaces:
1. **Numeric truncation acceptance** — `window1_range_attack_guarded_fill_adapter_v1.py` (`int(_finite(...))` on quantity/target/fill/evidence-target; fractional print/ask evidence accepted as floats): 8/8 adversarial fractional cases accepted.
2. **Unsatisfiable authorization gate** — `window1_range_attack_scoring_runner_v1.py:305-343`: self-referential SHA fixed-point; execute mode can never lawfully run.
3. **Duplicate-leg fail-crash** — the frozen fill source's 3 legs / 7 rows (LANRAD×2 candidates, BOHBOU) crash `adapt_fillability_rows`; the package cannot process its own frozen input, and the 995-leg census actually covers 991 legs.
4. **Invented outcome-relevant reference tie law** — lexicographic UUID tie-break at the latest admitted timestamp: 271 tied legs, **64 with differing prices** where delta/PC/IC depend on it.

Everything else — lineage, bundle binding, sole-source discipline, guard arithmetic, metric law, conservation, one-attempt safety, tests, internal build determinism — independently reproduces. No correction was implemented; no scoring occurred; the results directory does not exist; July 24–26 remains sealed.
