# PROOF — C-INCUMBENT-SUNSET v1 (the convicted machinery gets a death date)

**Candidate SHA: 5c41acbb** (NO-CALL-posture path-mode dark branch + orientation_live dark layer + DEFAULT-GO machinery in the packet clock + deploy/auto_cutover.sh + nightly INCUMBENT-COST/SUNSET LEDGER + knob, all committed at that SHA).
**Prior-art (C45):** the conviction's three witnesses — the week yield headline (−1.1% actual vs 22.6% at path under the selector, SELECTOR_WEEK.txt) · the Kalshi export reconciliation (−$32.35; avg loss 2.74× avg win at 72.2% win rate, breakeven <2.60) · ROVIEM — plus the packet clock (C-GOLD-NOW Part 3) and the migration doctrine (shadow → graded → cutover → legacy DELETED), of which this dispatch is the enforcement arm.
**Operator decree, cited verbatim:** "…the cutover is default-GO: the notification arrives with the numbers and a single word — your 'stop' halts it, silence for 12 hours after the packet means the flags flip on the next boot with full audit. The burden of proof reverses: the proven design doesn't wait for ceremony; the convicted one needs a reason to survive another night."

## What changes tonight (Lane 2: $0 by construction)
- **Nothing acts.** `trendpath_live=false`, `orientation_live=false`, `sizing_live=false` — the NO-CALL-posture branch and the orientation layer are dark code behind flags; the boot is behavior-identical on the placement path.
- **The DEFAULT-GO machinery is armed but conditionally inert:** `auto_cutover.sh` no-ops until (packet fired) ∧ (deadline passed) ∧ (no STOP file) ∧ (flag still false) — none true tonight. When it does run, it performs the FULL gate itself (config commit = candidate SHA → proof stamped doc-only → lint+smoke+boot audit) and notifies success or GATE-FAILED loudly (a failed gate means the incumbent keeps running — fail-safe direction).
- **Reporting only:** INCUMBENT-COST (attribution via the `trendpath_live_aim` stamp — until cutover, all entered legs bill to the incumbent) and the SUNSET LEDGER (each organ, replacement status, distance-to-activation).

## The freeze (Part 1, doctrine)
The incumbent — static aim tables, join-the-market placement, 97-remainder completion pricing — is FROZEN: no further tuning, no patches, no new knobs. It runs as-is only until replaced. Any future dispatch proposing to tune it must first overturn the conviction in the ledger.

## The cutover's own proof, pre-staged
PROOF_CUTOVER.md ships as a template; `auto_cutover.sh` stamps the flip SHA and the packet's numbers at execution — the outcome proof for that deploy IS the packet, exactly as the decree frames it.

## Deploy gate
[1/2] lint PASS (at commit; `bash -n` on auto_cutover.sh) · [2/3] smoke (gate-run) · [3/3] this file, OUTCOME_PROOF_SHA=5c41acbb · [4/4] C50 push (the decree verbatim in the vault; THE CONVICTED INCUMBENT declared in the ledger) · [5/5] constraints surface.
