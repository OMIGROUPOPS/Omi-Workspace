# V52e Iteration 4 - N9 Palantir wiring

Date: 2026-08-12

## Step 0: reuse, do not rebuild

V52e reuses the existing consultation spine. `live_v4.py::_entry_dossier`
remains the dossier/provenance contract; the C-ONE-TRUTH single-registry law
is re-pointed to the pinned `MACHINE_PALANTIR.store_CLEAN`; the replay
builder's existing `gitShow` is the only byte reader; and the frozen N8
incumbent chain remains the pre-license decision source. The new CLEAN-store
module validates already-read bytes and cannot discover or fall back to a
parallel source.

The old hot-mtime aim/ATLAS loader, hot cohort loader, and BCASC loader are
retired for this package because they are not hash-bound CLEAN N9 assets. The
complete component-by-component disposition, including source lines, is in
`STEP0_REUSE_INVENTORY.json`.

## CLEAN-only boot binding

The manifest is pinned to
`9929e91802dc0e0f7ed1af50c5526b2c9a730c7c`. The boot adapter loads only P1,
P2, P4, P6, P11, P12, P13, and P14 from `store_CLEAN`, and applies the stricter
operator law that accepts only `VALIDATED` and `VALID-NARROW` status. Loaded
UNVALIDATED, QUARANTINED, SUPERSEDED, and fallback counts are all zero. Every
source is bound to a full Git commit and SHA-256 in
`CLEAN_SOURCE_BINDING.json`.

N2 records category/cell base rates and validated signal priors beside the
causal read. N4 records THE GRID discount and zone reference beside live
evidence. The grid lookup uses the current qualifying ask as the decision-time
cell and explicitly refuses the ex-post W1 close as a lookup key. N5 records
mirror-coherence and one-eyed-vindication rates and may inform only an exact
tie left by V52d's in-game referee. Priors inform and never gate; no incumbent
authority is withdrawn.

## Thirty-game validation

The cohort is five frozen pins plus a fresh 25 stratified by category and
paired queue/formation/reflex census stamps. It has zero overlap with the
V52b, V52c, and V52d fresh cohorts. Seed SHA-256 is
`50dc079d2c200f824004f8c4e0b3549df2edcefb72d298728fd6c13198493a03`.

- All 361,432 V52e decision rows carry continuous N2/N4/N5 receipts and
  compact asset/status/SHA provenance.
- N4 performs 3,953 live-evidence-bounded rescues. On the shared receipt
  universe, grid-covered `MACHINE_READ_LEVEL_AUTHORITY_NOT_EARNED` abstentions
  fall from 2,093 to zero.
- N5 is consulted on all 361,432 rows. This cohort contains zero frozen V52d
  exact ties eligible for N5 resolution; no V52d strict winner is overridden.
- Every frozen clause-mechanics check passes, `REFLEX_POST` is zero, scavenger
  is OFF, and all five pins are unharmed.
- V52d and V52e each complete 17 of 30 pairs, all under par. These are
  observations only; they are not a disposition or full-population result.

Two clean builds are byte-identical across 31 pre-determinism artifacts. Ten
focused and inherited V52-family test files pass 286 assertions with zero
failures, omissions, or deselections. No 804 run, deployment, authorization,
holdout or live access, network runtime, order action, or position action
occurred. The full-804 exam remains gated until the operator reads the traces.
