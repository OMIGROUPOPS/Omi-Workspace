# PART 1 — WHERE THE READING STOPS (C-READ-THE-TAPE v2: every decision site from discovery to fill)

The rubric: *a 75/25 game is not stagnant till the end — window 1, corridor, window 2; best bid, best ask, traded price per tick; it's never just one single number.*

| # | decision site | reads-tape / FROZEN | vaulted knowledge the site ignores (lifecycle cited) |
|---|---|---|---|
| 1 | discovery pricing (v4_place anchor/current_price) | reads tape ONCE at conception → the basis freezes | own-moment capture (GAME_LIFECYCLE wake step); flow state at later ticks (the dossier logs it; the price never consumes it) |
| 2 | **the path aim (entry price)** | **FROZEN** = discovery − page p50; moves only on re-conception/band change | **the leg's position on its OWN path slice this minute — ATLAS `path[slice]` quantiles exist per 12 clock slices and are never consulted after conception**; sequential divots |
| 3 | selector / DROP-AS-PAIR | FROZEN at the discovery cell | contention re-pricing as the leg migrates cells intra-window (re-runs only on re-conception) |
| 4 | pair verdict at entry | sibling = 100−p estimate, FROZEN | the sibling's actual book at entry (the seesaw fixes this at RE-AIMS only) |
| 5 | seesaw at re-aim | **READS LIVE sibling book** ✓ | — (the one tape-reading entry organ) |
| 6 | place_order locks (gun/cycle/chase/pair/clamp) | read live state ✓ | — |
| 7 | walk/repost | up-moves DELETED (cutover law); protective moves read book | lawful-frozen BY RULING (get paid, not filled) — not a defect row |
| 8 | **completion pricing (s0/x)** | **FROZEN at attempt**; path-priced bids rest and skip freshness | the sibling's tick position on its path at completion moments; flow state at the attempt |
| 9 | **flatten / taker verdicts (M15 frame)** | **NO TAPE READ AT ALL** (the 07-15 census conviction: honest verdicts on tape-blind inputs) | best bid/ask THIS tick vs basis — the KOPPRI class (sold a 100-settler into a steady 74 tape) |
| 10 | exit bands | FROZEN at fill — **lawful** (§0A fitted anchors, operator ruling) | — |
| 11 | gun sources (7) | continuous tape reads ✓ | — |
| 12 | **the os_shadow lane** | **READS EVERYTHING PER TICK** — regime, timing, wake gate, flow_open, role postures, levels, citing GAME_LIFECYCLE 2–5 on every line — **and the live engine consumes none of it** | THE HEADLINE ROW: the tape-reading brain already exists, in shadow, logging what it would do at every review |

**The frozen gaps to price by replay (Part 2): GAP-A the aim freeze (row 2) · GAP-B completion freeze (row 8) · GAP-C the tape-blind flatten (row 9) · GAP-D the os_shadow lane (row 12, the composite).** Rows 7 and 10 are frozen BY OPERATOR RULING and are not gaps.
