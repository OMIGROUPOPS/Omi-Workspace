# Window-1 game plan — 2026-09-03
Rulings: range = traded range from formation; overlap = share; Q from overlap members is not a camp (membership is this game's live range and phase); bed four (GIUBAR URSPAL LAJSVA DANPRA) = exam, ALTGAS = tune game; no 804 without operator word; one prompt per seat at a time; every prompt carries a layman's non-redundancy note.
Layers: macro = the pile (which library paths still overlap ours at this fraction); micro = the sentence from those members (Q = weighted mode over members' futures, X = members' floor fractions on our clock); micro-micro = this game's subsecond book, veto only, never an author.
Steps: 1 path index (this order) · 2 overlap organ (replaces neighbor_count 7 and the rung gate; membership by live-range overlap at fraction, weight = share × similarity scalars × coverage) · 3 tune on ALTGAS via window1-watch/rerun_altgas.ps1, pass = ALT bid stops walking away from a rising tape and GAS carries a sentence toward its floor before it prints · 4 loop: operator points, counsel writes the order, Codex patches the organ, rerun, refresh, sitting stamp in artifacts/review_mirror/ · 5 exam on the bed four, no tuning on them · 6 804 measure-only, sentence-scored, on operator word.
Prior art this plan does not repeat: July DRIFT ATLAS (trendpath_build.py: page percentiles by category × discovery cell × side, onset clock, not consumed by v54); range_spectrum_v1 (per-game poll paths, onset right edge, F-VS-050); FUTURE_LOW_RETURN_LIBRARY (per-leg lows only); v54 seven-neighbor scalar match; same-state population (empty) and phase-category population (no range condition).
