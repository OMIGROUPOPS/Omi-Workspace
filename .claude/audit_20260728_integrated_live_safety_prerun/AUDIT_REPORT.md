# INDEPENDENT AUDIT — INTEGRATED P0 v1–v4 + CASUKA D1–D3 PRE-RUN @ 1dd5787c — RULING: PASS (all 7 sections)

**PASS. Every ruling below is independently reproduced, not inspected. The integrated candidate blob `d7d7cd1d` (SHA-256 `62614501…`, 1,047,115 B) decomposes exactly into the three audited deltas with zero residual bytes; both disclosed same-boundary insertions are mechanically necessary and behavior-neutral; every P0 and CASUKA safety law holds simultaneously on the integrated bytes with neither able to suppress the other; the single historical-parity difference is the disclosed CASUKA fail-closed fixture, whose mechanism I reproduced (`sell_guard_api_fail`) and whose replacement test proves no regression; the deployment-control law is executable and fail-closed on all nine required ceremony properties; two clean regenerations are byte-identical to all 23 committed artifacts; and live containment is fully intact with zero deployment artifacts created. The prior P0 binding defect (my FINDING F1 at cac1a144) is corrected exactly as required.**

Date: 2026-07-28 · Branch: `audit/window1-independent` · Additions-only child of exactly `cac1a144342de6d99c0d2701e355ce63745063b0` · Auditor: independent CC session
Under audit: package `1dd5787cfe00f8c69488fff1c9c93fbb7c3eb0a1` (branch `codex/integrated-p0v4-casuka-prerun`), parent exactly `765083b9bce6940d11a778a862bbd7df14967da4`.

## 1. Lineage and containment — PASS

Every supplied 40-hex identity validated with `git cat-file -t` (never bare `rev-parse` echo): 1dd5787c/765083b9/11e70454/995a8817/904a1993 are commits; d7d7cd1d/363e1c8a/ebd29103/949f6995/f1857199 are blobs. Parent links from commit objects: 1dd5787c→765083b9; 11e70454→995a8817; 904a1993→11e70454. Remote tip equals 1dd5787c. Containment: the PRE-RUN is **32 files, all additions, zero modifications/deletions** — the tree's `live_v4.py` remains the v4 blob 363e1c8a; the frozen `deploy/deploy_live_v4.sh` is untouched. The file-only integration commit changes **exactly** `arb-executor/live_v4.py` from f1857199 to d7d7cd1d; the parked rollback commit changes exactly that path back to f1857199. Integrated blob re-hashed from the object store: 1,047,115 B, SHA-256 `62614501cb9708bb3c3c2b35823ba8431b2e95acdc027f659a4b37a66a777034`.

## 2. Prior P0 binding defect — PASS (corrected as required)

The operative running parent is exactly `bb085ce06db5932049af85f927a7f9316ad76816` (verified: real commit; true parent of v1 `eca101c6…` from the commit object). The former nonexistent `bb085ce04191e2…` appears in the new package **only** as `superseded_nonexistent_value` inside `P0_BINDING_CORRECTION_RECEIPT.json` with an explicit supersession law, plus unchanged inherited v4-package history from 765083b9's tree; the new builder even asserts the retracted value **fails** `git cat-file -e` (negative existence check). No verifier, runner, rollback, ceremony, or control artifact consumes `running_lineage_parent` from the v4 receipts (grep across all new executables: zero consumption). Ancestry/linearity is computed from real parent/tree/blob relationships with recorded `cat-file`/`rev-parse --verify` exit codes — the receipt states no literal `linearity_verified` boolean is accepted as evidence. All three binding conditions from my v4 PASS at cac1a144 are met.

## 3. Independent byte algebra — PASS (zero residual bytes)

From the six real Git blobs (preimage f1857199, P0 v1–v3 949f6995, P0 v1–v4 363e1c8a, v1–v3+CASUKA 1809085d = `94be4113:live_v4.py` verified, CASUKA-only ebd29103, integrated d7d7cd1d), computed with my own sequence alignment (no package tooling):

- **integrated − P0 v1–v4 = CASUKA delta exactly:** diff(363e1c8a→d7d7cd1d) content-line multisets ≡ the audited CASUKA D1–D3 delta (+340/−19), which is itself identical across both audited bases (94be4113 form ≡ d256c491 form).
- **integrated − (v1–v3 + CASUKA) = P0 v4 delta exactly:** diff(1809085d→d7d7cd1d) ≡ the audited v4 patch (+517/−3).
- **integrated − CASUKA-only = v1–v3∘v4 exactly**, and **integrated − preimage = v1–v3∘v4∘CASUKA exactly**, under the sequential composition law in which the only cancellations are the three `_entry_start_gate` call-site lines (added by v1–v3, replaced by v4) — verified to be exactly those 3 lines and nothing else. Equivalently: integrated − CASUKA − v4 = 949f6995 and integrated − every authorized delta = f1857199 with **zero unexplained bytes**.
- The frozen composer (`compose_integrated_p0v4_casuka.py --repo .`) reproduces the integrated identity exactly.

**Same-boundary insertions (both disclosed, both lawful):** (a) base line 23 — v4's `import hashlib` and CASUKA's `import functools`, adjacent stdlib imports, order-neutral; (b) base line 5851 — the final pre-POST boundary carries the P0 v4 buy-revalidation block first, then the CASUKA D2 sell clamp, then `api_post`. The two blocks guard **disjoint actions**: for buys, no await separates the P0 revalidation from POST (the sell block is skipped), preserving v4's final-instruction law; for sells, the P0 block is a no-op and the clamp remains the final operation with its own post-await exchange-truth read, preserving D2's law. Each block's line content is byte-exactly the audited delta content (proven by the multiset identities). No fuzzy merge, no retuning, no third-source byte.

## 4. Semantic coexistence — PASS

Run against the integrated bytes (candidate loaded from `11e70454:arb-executor/live_v4.py`): integrated interaction suite **8/8** (SHICHA fires REAL_START, sweeps the entry buy, preserves the exit; PENDING blocks buys while authoritative exits post; stale intent cannot cross REAL_START or the sell guard; 804 PENDING events independently fail-closed) plus **19 CASUKA adversarial probes inside 4 test groups** (D1 four serialization orderings, D2 nine clamp probes + pagination/repeated-cursor, D3 six classifier probes); CASUKA repair suite **12/12**; my **35-probe P0 battery: 35/35** on the integrated candidate (state machine, monotonic REAL_START, gate matrix, boundary/dedup/ambiguity/ceiling laws, SHICHA 651-print fixture, per-event isolation, buy-only gating AST proof re-verified at the integrated line numbers). My own six cross-suppression probes: oversell 8>5 refused pre-POST with zero sell POSTs; covered 5-lot exits POST under both P0 PENDING and REAL_START (P0 cannot suppress exit paths); buys refused under PENDING despite a healthy exchange surface (CASUKA cannot bypass P0); a REAL_START raced in during the exchange-truth awaits is caught by the pre-POST revalidation (`p0v4_pre_post_refused`); zero-quantity sells refused. 804-event cold start on the integrated candidate: 1.00 s, all 804 INSUFFICIENT, 6,432 rows, ~2.7 MB peak — identical bounded behavior to the v4-only audit.

**Parity line independently reproduced:** all **85** scripts run on a byte-exact 765083b9 checkout versus the integrated overlay — **84 exact identities** (0 signature mismatches, 0 repaired failures) and exactly **one** difference: `test_p0_real_start_v4_boot_tape.py` fails one test (`test_exit_sell_remains_live_while_tape_pending`) on the integrated candidate. I reproduced the mechanism directly: the fixture's isolated P0 stub provides no authoritative exchange GET surface, so the CASUKA D2 clamp fails closed with `sell_guard_api_fail` before POST. That is the clamp's required behavior, not a regression — the integrated replacement test proves the same exit POSTs when authoritative truth exists, and my I2/I3 probes confirm covered exits post under every P0 state. **Lawful and causally required.**

## 5. Deployment-control package — PASS (audited as executable law, no deploy mode entered)

The control contract, verifier, one-shot ceremony runner, rollback script, and outcome contract enforce all nine required properties fail-closed: (1) T0 requires `live_v4_process_count_zero` with the engine stopped; (2) exact inhibited-crontab (`0e2af22e…`) and original-backup (`4c38967f…`) hashes — and the rollback script binds the **raw spool path** `/var/spool/cron/crontabs/root`, which I verified live hashes to exactly `0e2af22e…` (the raw file carries the 3-line header, so the binding is canonicalization-correct); (3) zero tennis entry buys + reconciled holdings/exits required at T0; (4) single-file scope — integration commit touches exactly `live_v4.py`, extra-file integrations rejected by the verifier; (5) exactly one boot (`execute_once`; dry-run failure path models single-rollback-no-retry); (6) cron remains inhibited through post-boot validation; (7) restoration of the original crontab is the **last** post-boot requirement, only after every other invariant passes, with no additional restart; (8) on any failure the rollback restores f1857199 byte-verified (backup hash+size → atomic temp+rename → target hash+size), gracefully stops the candidate within a 200 s window, hard-fails if the process survives, and **leaves the engine stopped with the inhibited cron still installed** — it never boots the preimage and never touches the original cron; (9) `MUTATION_STARTED` marks candidate-retry FORBIDDEN and authorization reuse is rejected. The **27/27** deployment-control tests (9 runner/builder + 18 verifier, including all 12 adversarial refusal classes, valid-pass, no-self-SHA, journal append-only, outcome-environment exactness) pass in my environment; dry-run paths produce zero invocations and zero results. The superseded CASUKA-only authorization (8a142c86) is bound as `STRUCTURALLY_SUPERSEDED_UNUSABLE_ATTEMPTS_ZERO` with its five defects enumerated; the new design fixes each (executable verifier, active pre-deployment outcome contract, environment-bound gate).

## 6. Tests, hashes, determinism — PASS

Compile clean. Suites on integrated bytes: 8/8 interaction, 12/12 CASUKA, 23/23 P0 focused on the exact v4 candidate (component law) with the one explained integrated difference, 27/27 deployment-control, 35/35 + 6/6 auditor probes, 85-script parity as above. **Two clean regenerations** of `build_integrated_live_safety_prerun.py` produced **23/23 artifacts byte-identical** to each other and to the committed package. All **22 artifact-manifest rows** verify (manifest self-excluded, `self_hash` present); all **11 source-manifest rows** verify — including the integrated candidate bound via its `commit` field to `11e70454:live_v4.py` (an initial mismatch reading was my parser ignoring that field, corrected), the verifier/runner/rollback SHA-256s, and the unchanged `deploy_live_v4.sh`. No event-ID hardcoding can exist in the candidate: the byte algebra proves every integrated byte comes from the three previously audited deltas (each already scanned). No new thresholds (decomposition exact), no hidden mutation path (33-caller census + single chokepoint re-proven at integrated line numbers), no inherited receipt used as an expected answer (all counts recomputed here).

## 7. Live containment — PASS (read-only)

Engine process count **0** (full `ps` argument list, not pgrep). Keepalive relaunch line still commented with the `TEMP_MAINTENANCE_SCHEDULE_LIAR_CONTAINMENT_20260728` marker; zero active relaunch lines. Raw spool crontab hashes **exactly** `0e2af22e…`; immutable backup hashes exactly `4c38967f…`. Live `live_v4.py` blob = `f1857199` (nothing deployed). **Zero** resting tennis entry buys; 10 resting exit sells totaling 40.00 against 40.58 held across 9 markets (one market settled exchange-side since the prior audit's 45.58/10 — no engine action; the 0.58 ARSANN fractional residue persists), zero tickers with sells exceeding held. **No deployment backup (`live_v4.py.pre-integrated-p0v4-casuka.*`) and no results directory (`integrated_live_safety_results_*`) exists on the host.** Nothing was cancelled, posted, restarted, or edited.

## Canonical PASS receipt

Per the frozen instruction, `PACKAGE_AUDIT_PASS_RECEIPT.json` in this directory is the template with **only** `FINAL_V2_PACKAGE_COMMIT` replaced by `1dd5787cfe00f8c69488fff1c9c93fbb7c3eb0a1` (receipt SHA-256 `b2bc9727108765f2186b830b0be3284f34e91d5788fe890e493c790d01b4f7d2`). The frozen verifier consumes that exact receipt from this separately supplied PASS commit.

## RULING

**PASS on all seven sections.** This audit does **not** authorize deployment, restart, cron restoration, live mutation, scoring, or T2 work. A future ceremony requires a separate authorization binding the exact package (1dd5787c), this PASS commit, the integration commit (11e70454), the deployment ID (`integrated-p0v4-casuka-deploy-20260728-attempt1`), the stopped-state and outcome contracts, and the complete ceremony command literal — verified by the frozen executable verifier at execution time.
