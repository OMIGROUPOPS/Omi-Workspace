# V52g 2,094 reconciliation and flip-trace addendum

The `e17af2dc` analytical receipt published 2,094
`AT_OR_ABOVE_UNCREDITED_TIMING` moments but did not publish their identities.
An independent receipt-grain reconstruction of the named 346-game population
materializes 2,093 exact print identities. At every one, the runner's leg was
already terminal and credited before the cited floor print. The decision-only
export stopped at credit and carried the last standing target forward, so all
2,093 verdicts are `EXPORT_GRAIN_ILLUSION`; zero is a
`CREDITING_DEFECT`.

The one-count difference is not reconciled because no source moment ledger
exists at `e17af2dc`. It is frozen as an unexplained source measurement residue,
and complete 2,094-of-2,094 certification is BLOCKED for operator review. No
corrected four-state census or capture is emitted. Original scores are retained
unchanged, policy imports and invocations are zero, and no behavior is edited.

The same package freezes rows-only V52f/V52g divergence evidence for CRIJEA and
BRAVON, the two shared-cohort delta completions lost by V52g: both machines at
the divergence receipt, all clause-6 differential rows, V52g pair-budget
revisions, forward true prints, and terminal four-state observations.
Interpretation remains with the analysis seat.

Canonical package:
`.claude/window1_live_v4_replay/v52g_reconciliation_2094_and_flip_traces_20260813/`.
