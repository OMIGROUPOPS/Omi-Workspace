# INDEPENDENT WINDOW-1 RANGE-ATTACK SCORING-PACKAGE V2 AUDIT @ e7e7b907 — RULING: PASS

**PASS.** All four defects of the controlling BLOCK (`3811a772`) are repaired exactly and nothing else changed: exact-integer numeric validation (adversarially re-proven through the real adapter), a construction-frozen unique guarded-fill ledger that my independent rebuild reproduces with zero differences, the reference tie law replaced by a named evidence-ambiguity class with the 271-vs-272 discrepancy exactly resolved, and a finite, satisfiable, fail-closed authorization gate. 123/123 tests; all receipts reproduce; no scoring occurred. **This PASS authorizes only the separately bound scoring execution — it does not itself execute the benchmark.**

Date: 2026-07-26 · Branch: `audit/window1-independent` · Auditor: independent CC session
Method: detached worktrees at `e7e7b9071b9238868d0599a2e5f24bb92dcc9bdd`; read-only; no execute mode, no real-population metrics, no results directory, no holdout/live/production access; adversarial probes through real code paths; synthetic Git repository for the gate; raw-input reproductions for censuses.

## 1. Lineage and containment — PASS

`e7e7b907` ("Freeze Range-Attack scoring package V2") has parent exactly `f774d906`; all-refs children scan: **sole child**; local/remote `codex/window1-definition` equality verified. **23 additions, zero modifications/deletions** — the two frozen candidates, the PASSED strict-ask instrument, and every other inherited surface are byte-identical by construction. No strategy, headroom, fill, PC/IC/S, D=804, or reference-window law changed: `scorer_v2` delegates the metric law verbatim to the fixture-verified V1 functions (adding only per-leg tie-receipt reporting); `reference_adapter_v2` preserves the T−8h floor, V5 guard classes/arithmetic (60/60/900-600, `anchor − positive_guard`), print admission, and dedup-with-conflict-raise; the adapter consumes the same guarded FILLABLE_AT_X facts.

## 2. Exact numeric validation — PASS (adversarial, real adapter path)

`exact_integer` replaces truncation: through the real `adapt_unique_fill_row` on a real ledger row, **REJECTED with named errors**: quantity **5.9**, **5.0001** ("not an exact integer"), target/fill **40.9**, fill 49.9, **boolean** quantity, **NaN** ("not finite"), **infinity**, out-of-band prices **0** and **100** ("below 1"/"above 99"), fractional print evidence price 48.7, fractional strict-ask ask 38.9 and evidence target 40.9, and quantities **4** and **6** ("below 5"/"above 5" — credited quantity must equal exactly five). ACCEPTED only: the real control row and exact-integer-valued floats (5.0, 49.0 — the documented JSON-representation allowance, value-exact, never truncating). Full case table in `ADVERSARIAL_NUMERIC_AND_LEDGER_RECEIPT.json`.

## 3. Unique guarded-fill ledger — PASS (independent rebuild, zero differences)

Rebuilt from the frozen sources: **995 raw guarded rows → 991 unique candidate/event/leg fills** (4 excess duplicate rows removed); **macro_hold 501, macro_micro 490; print-backed 965 + strict-ask-backed 26 = 991**; maximum one ledger fill per leg (991 unique keys). My rebuild rule — select per leg the interval matching the causal stream's `simulated_fill_order_interval_id`, with **every** credited quantity/price/timestamp/evidence/boundary fact taken from the guarded FILLABLE_AT_X receipt — reproduces the committed ledger with **zero differences**, including the three duplicate resolutions exactly: macro_hold LANRAD/RAD → interval0005 X=80; macro_micro LANRAD/RAD → interval0005 X=80; macro_micro BOHBOU/BOH → interval0007 X=3 (each equal to the causal interval identity). All 26 guarded strict-ask fills present. Double credit impossible: unique-key law in construction plus the runtime adapter's duplicate refusal. **Runtime sole-consumption is enforced in the strongest form**: `validate_package` hard-fails if `guarded_fillability_receipts`, `candidate_order_streams`, or `raw_causal_fill_state` appear among runtime roles; scoring reads only the `unique_guarded_fill_ledger` role, hash-bound in the source manifest.

## 4. Latest-timestamp reference ties — PASS (271↔272 exactly resolved)

Independent recomputation from raw development inputs (event-level boundaries from the frozen receipts; prints from guarded-cache-v3): my interval-derived map covers 1,408 legs → **271 ties = 207 same-price + 64 differing** — and the missing tie is **CREMAT/CRE**, the no-BBO event with a positive boundary, zero exposed intervals (hence invisible to interval-derived maps, the exact blind spot of my `3811a772` census), and **3 receipts at its latest admitted timestamp, all price 63** — verified directly from the raw cache. Adding CREMAT's legs gives **1,410 positive-boundary legs → 272 ties = 208 same-price + 64 differing-price: the committed census is correct**, and its `source_reproduction_delta_from_audit: 1` documents the delta honestly. The 64 differing-price set is identical in both computations. **Zero authoritative exchange sequence, preserved raw ordering, or other bound ordering fields exist** for the 64 (census field verified on all 272 rows; no such surface exists in the caches). The V2 reference law: no UUID/lexical/file-order/price-preference/averaging/median/volume selection exists anywhere in the code (`"""…without receipt UUID, price, or volume tie-breaking"""` and no such path); same-timestamp/same-price ties are **reference-available at the shared price with all receipts retained** (`reference_supporting_receipts`); differing-price ties are **unavailable with exactly** `ambiguous_latest_timestamp_multiple_prices_no_authoritative_sequence`, retaining tie count, distinct prices, and all receipts. These 64 legs remain in D, preserve C/S evidence, and cannot count as PC or IC (the fixture-verified missing-reference law), and the per-leg tie fields (`reference_latest_timestamp_tie_count`, `…_distinct_prices`, `…_supporting_receipts`) make them a **separately reportable evidence-ambiguity class** distinguishable from ordinary reference-missing rows.

## 5. Authorization — PASS (finite, satisfiable, fail-closed)

The self-reference is gone: the report must contain `PASS`, the exact package commit, execution ID, bundle hash, and the **frozen command template with placeholders** (`<AUDIT_COMMIT_SUPPLIED_SEPARATELY>`), while the audit commit SHA is supplied separately at run time and the report is loaded from exactly that commit (`git show <commit>:<path>`) — no report ever needs its own SHA. **Reproduced a finite successful workflow in a synthetic repository**: write report → commit on the audit ref → supply the SHA → **AUTHORIZED**. Fail-closed negatives all refused: wrong package commit, wrong execution ID, wrong bundle hash, missing report path, wrong command template, non-ancestor audit commit, malformed SHA — plus the retained checks (audit-branch ancestry, sole-child parent `HEAD^ == f774d906`, clean worktree, package-hash binding, results-directory uniqueness). **No authorization check was weakened**: every V1 gate condition survives except the impossible self-reference, which is replaced by the template binding.

## 6. Determinism and integrity — PASS

- **22/22 source/blob receipts** and **14/14 package artifact receipts** recompute exactly (git-blob basis — the V1 CRLF-basis defect is gone; the committed NEWLINE_PORTABILITY_RECEIPT records LF-canonical text identity and two byte-identical clean builds).
- Input-bundle identity recomputed: `canonical_sha256(input_bundle_payload)` matches the manifest exactly.
- **123/123 tests pass** (21 new V2 package tests + 102 inherited).
- Independent dual-checkout rebuild: see the regeneration addendum at the end — canonical artifacts byte-identical to committed from both newline configurations.

## 7. No execution — PASS

C/PC/IC/S and every performance field null everywhere (`NO_EXECUTION_NO_METRICS_RECEIPT`: all metrics null, `real_population_scorer_invocations: 0`, results directory absent, no ranking/selection); no scorer or benchmark invocation, no holdout/live/production/order/position/exit/settlement/DCA/Window-2 access in any V2 module (grep + role firewall + receipts); this audit created no results directory and computed no real-population metrics.

## RULING

**PASS.** Every requirement independently reproduced with exact evidence; all four `3811a772` defects repaired narrowly and verifiably; containment total. This PASS authorizes **only** the separately bound scoring execution of this exact package (`e7e7b9071b9238868d0599a2e5f24bb92dcc9bdd`, execution ID `w1-range-attack-v2-dev-20260712-20260720-grid2-scorepkg-v2`, bundle `21b4db24ac041d4f…` per the manifest, under the frozen command template with a separately supplied authorization commit). It does not itself execute the benchmark; the execution's results must return for independent audit.

## Regeneration addendum (confirmed)

Dual clean-checkout rebuilds completed: the CRLF-configured and LF-configured worktrees each produced all 12 regenerable canonical artifacts (gate, schema, identities, cache hash set, no-exec receipt, report, tie census, source order trace, input manifest, source hash manifest, ledger derivation receipt, unique ledger) **byte-identical to each other AND to the committed artifacts (12/12; gz compared on decompressed content)**. Regeneration is newline-configuration-independent — the V1 portability defect is repaired.
