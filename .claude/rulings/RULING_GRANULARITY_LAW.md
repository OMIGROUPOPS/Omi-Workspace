# OPERATOR RULING — THE GRANULARITY LAW (2026-07-08, verbatim-as-relayed; completes the two-element frame)

The HISTORICS (per-minute corpus) are the MACRO instrument — cell edges, drift shapes, floors, dip timing distributions, the assumptions. They can never time an entry.

The SUB-SECOND LIVE TAPE (the three observables per tick, non-self chain, ask-hold/divot conditioning, flow state) is the MICRO instrument — the entry mechanics, the validation, the trigger.

Every entry decision = macro assumption × micro confirmation; either alone is a failed entry (HUIAHN = micro ignored; a reprice-chase = macro ignored).

Corollary: no per-minute-derived number may ever be consumed as a timing trigger (the bell-recovery training-grade/certification-grade split is this law applied); no live-tick pattern may override a fitted cell edge without the study that refits it.
