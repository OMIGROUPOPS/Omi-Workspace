# PLEX REGRESSION RULING — aim-layer regression program (relayed 2026-07-06)

**Provenance:** text as relayed by the operator, 2026-07-06 (Fable↔CC relay). The relay
delivered the converged plan below, which cites the ruling's numbered items (①, ②, ④)
inline. If a fuller verbatim Plex body exists beyond this relay, this file reserves its
slot (REANCHOR precedent, slot discipline of 0a17ce03) — the operative constraints are
in force as relayed either way.

---

## THE RELAYED TEXT (verbatim)

Converged plan, in order:

(1) THE NIGHTLY SHAPE ACCUMULATOR — the critical path is honest-era training data: stand
up the nightly job that folds each slate's tape into the shape corpus (per cat × bucket ×
T-minus, honest clock), with a coverage counter per cell so we can SEE the honest-era
corpus filling in — the re-run trigger is defined as coverage, not vibes: when the
early-T cells the fix targets reach the min-n floor, the median re-derivation re-runs
automatically and reports.

(2) THE V2 SPEC, written NOW per Ruling ①, against the median table's exact schema
(drop-in replacement): monotone splines/GAM on log-odds per bucket · hierarchical pooling
toward the cat-level path within cat (shrinkage ∝ n) · HARD minimum-n gate — cells below
floor render null/parked, never silently interpolated (impose this discipline on the
median script TODAY too — Plex named its silent interpolation as the inherited failure
mode) · n + residual spread as first-class output fields. Committed as
.claude/rulings/AIM_V2_SPEC.md.

(3) THE VALIDATION HARNESS per Ruling ②, built now, run when data permits: walk-forward
by week on the 14k, honest-clock-filtered/reweighted (card-era flagged), census-161 as
secondary confirmatory OOS; Lane-1 = joint gap shrinks / lazy-leg-1 rate shrinks /
participation holds; the standing bars restated in the harness doc: beats −$20.31 on the
new basis, walk/repost interaction explicitly modeled (the riser's bar-e erosion pattern
as the test), walk-cap anchor ships with or before any arm.

(4) THE Z-SCORE, per Ruling ④: own gated flag, default-OFF, shadow-first
(riser_post_revision/C-KALSHI-OCC precedent), consuming whichever table version is live;
spec + shadow build staged, its own gate later.

(5) The ledger updated: the aim fix's state = BLOCKED-ON-DATA with the coverage trigger
defined; the operator's ≤2¢ branch ruling still PENDING on his desk (evidence table
delivered — rest-at-own-dip-capped vs keep-cancel); ZEBAND/XUXBER/LINMAR noted as live
same-mechanism cases. Push everything.

---

## Binding constraints extracted (for the gated-flags inventory / C45 greps)
- Re-run trigger for the aim median re-derivation = **coverage floors, not judgment** (accumulator §1; floors defined in AIM_V2_SPEC).
- **No silent interpolation anywhere in the aim-table lineage** — named as the inherited failure mode; below-floor cells are null/parked; the median script carries the same discipline effective today.
- `n` and residual spread are first-class table fields (never stripped).
- The z-score is its own flag, default-OFF, shadow-first, gated separately; it consumes the live table version, whichever that is.
- Standing arm bars unchanged: beats −$20.31 on the new basis · walk/repost erosion (riser bar-e pattern) as an explicit test · walk-cap honest anchor ships with or before any arm.
- Implemented by: `.claude/rulings/AIM_V2_SPEC.md`, `arb-executor/analysis/shape_accumulator.py`, `arb-executor/analysis/aim_v2_harness.py`, z-score shadow (gated `aim_zscore_shadow`, default OFF).
