# OUTCOME PROOF — DUAL-DIVOT STEER (combined-primary seal, gate deploy #2)
Candidate: `ecd7b116` (the ruling's C50: sealed object consumer + WHEN-FLAT
config + meter re-grammar). C46 two-lane, judged separately.

## LANE 1 — MECHANISM (primary, luck-free)

### The construction delta, exactly
One consumer added at `_v4_entry_anchor` (after cohort steer, before
target_bid): where `pair_class_steer_enabled` AND the cascade reads the pair
FLAT_FLAT AND the leg's called band carries a SEALED row in
`state/pair_policies_sealed_v1.json` (sha `b2f0b670…`), the entry offset is
SET to the band's frozen dip_p90. Everything else — mirror/neither pairs,
ITF, violent-faller REFUSE, floors, W1 law, leg2_reshuffle, downstream
clamps — byte-identical paths. `expiration_wire_enabled` remains false.

### The outcome replay IS the seal's gate record (deterministic, per-game)
The exact policy this steer wires — flat-classed legs fished at
median−dip_p90, final-2h, stability guard — was replayed on the full unseen
week (200 pairs, `analysis/loop8_exam_week.py`, artifact
`WEEK_RESCORE_COMBINED.md`):
- FLAT-FLAT class: **7/7 duals SUB-PAR** (6 ≤93, 1 ≤95), 100% in BOTH eras
  (virgin 3/3, selection 4/4) and in every cat with duals; medPairD −10.
- Per band-pair receipts (duals): ATP_MAIN-B1+B8 2/2 · WTA_MAIN-B3+B3 2/2 ·
  ATP_CHALL-B1+B6 1/1 · ATP_CHALL-B4+B4 1/1 · ATP_MAIN-B3+B5 1/1 — all
  sub-par.
- The classes the steer does NOT touch stayed exactly as scored: mirror
  0/66 sub-par (REFUSE — the fader drill's target), neither 9/26 (REFUSE).
- Mastery meter (standing, never pass/fail): flat-flat 14% dual-negative —
  the drill gap the nightly now reports per class/band.
Grade-distribution/≤97-completion impact at the pair level is the point:
every dual this policy produced on the week landed inside the lawful span.
Volume baseline banked: 16 sub-par duals / 200 slate pairs (8.0%).

### Smoke
The gate's smoke replay runs the steer's code path (flag on in the deploy
config) on the recorded slate hour — runtime proof rides the gate itself.

## LANE 2 — SETTLEMENT P&L (secondary)
No settled cohort under the sealed policy exists yet: **INSUFFICIENT
SETTLEMENTS — flagged, never the verdict** (C46). The nightly's mastery +
completion lines are the accruing evidence stream.

## Verdict
Lane 1: the wired policy's own full-week deterministic replay is the
strongest construction evidence this operation produces — 7/7 sub-par duals,
both eras, all cats, with the untouched classes proven untouched. Lane 2: no
claim. DEPLOY.
