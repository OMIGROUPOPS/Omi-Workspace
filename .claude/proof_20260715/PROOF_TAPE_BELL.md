# OUTCOME PROOF — C-TAPE-BELL v1 (07-15)

**PROVEN SHA: 23a097ac** (cited post-commit per the gate's ancestry law).

## PRIOR ART (C45 gate)
- C-FLOW-REST-SEED v1 (07-15, operator R1 GO): `_flow_rest_refresh` REST trades cache exists, consultation-local, proven on 19,141 replayed consultations; its comment reserved the gun input as "its own word." **Delta: THIS dispatch is that word — the cache becomes gun source 8 `tape_flow`.**
- C-REALITY-BELL (07-10) fallback bell + C-PERCAT-GUN (07-10/12) fitted trigger: both read the WS deques (`_trade_times`/`_trade_prices`) — the −0k class (ALHVUX, KOAYAZ, FOMCHA: REST tape loud, WS silent) is exactly their shared blind spot. **Delta: tape_flow reads max(WS, REST).**
- C-WINDOW-LAW (07-14): `_window_phase` evidence set — tape_flow is NOT in `_BURST`, so it closes the corridor (evidence-grade), by design.
- C-DELETION-GATE / flatten-leash / taker-cap named-line patterns: Part 2/3 refusals reuse the same emit shapes.
- C-DISCOVERY-FLOOR v1 (07-15, this morning): the ≥1,500 floor + `event_lifetime_vol` organ — Part 3a's ITF fallback composes with it.

## PART 1 ACCEPTANCE (MANDATORY) — the morning's W2 fills replayed against the exchange tape
Method: for every `entry_filled` stamping W2 between the 2:36:35 AM boot and 12:30 PM — anchor = the honest clock where the live code had one (gun `tts_honest_min` / `bell_missing` anchor), else the schedule stamp; fill instant = the EXCHANGE fill time where the portfolio fills API knows the order (bookings lag); tape_flow would-fire = first instant ≥ anchor where the event's rolling 30-min print count (both legs, exchange tape) crossed the per-cat threshold and held 60s later (sustain 2 polls); CATCH = would-fire + 300s grace < fill instant.

**Results (per-row citations: `.claude/tapebell_20260715/ACCEPTANCE_TABLE.txt` + `acceptance_rows.json`):**
| measure | count / 77 | detail |
|---|---|---|
| tape_flow FIRES | **76** | 47 started-anchor + 29 rise-escape (the LEOTSI lying-late class) |
| fires BEFORE the exchange fill | **54** | median **13.6 min earlier**, p25 3.4 / p75 30.8 |
| full chain catches (fire + 300s grace < fill) | **37** | the sweep completes before the fill |
| fill IS the flow onset | 22 | margins ≈ −1 min — the fill print rides the opening burst; structurally uncatchable by any flow evidence |
| grace-eaten | 17 | bell fired 0.2–4.6 min pre-fill; the 5-min FERCER grace let the fill land — **named knob tension: grace width vs W2 zero-tolerance, the operator's word** |
| no-fire | 1 | GUTSHA (lying schedule stamp, no 5¢ rise) |

The comparison that matters for the defect class: this morning's ACTUAL guns fired at/after these fills (that is why they stamped W2). The tape-bell replaces a trigger that fired ~0 min early with one that fires median 13.6 min early on the same tape.

## PART 2/3 BEHAVIOR
- Part 2 (`w2_fill_violation`): pure emit at the single `_log` chokepoint — no decision-path change; monitor handler added (FORENSIC_w2_fill).
- Part 3a (W1-PREFERENCE): today's replay basis — the 9 corridor buys (all mains) refuse under the rule; ITF corridor placements today: 0, the fallback clause costs nothing on this tape.
- Part 3b (cross cap): DELXIL (combined 102) replays REFUSED at `cross_over_combined_cap`; SCHHOU's capped taker verdicts unaffected (cap fires first).
- Part 3c (healer): JONURG/TOPGEN/DARCRI replays: `_avg4` resolves via fingerprint price (69/59/16) where exchange exposure was falsy — booked instead of halted; MASDUT replays `settlement_pending` FLAG (market status determined at the audit instant).

## HONEST LIMITS
1. tape_flow arms only PAST the anchored start — a lying-LATE clock with no honest join leaves the bell dark until the fallback bell's rise-escape (ALHVUX's W1-stamped mode; named residual, reach-refit territory).
2. Mains threshold (16/30m) is a DECREED interim — no fitted mains flow bucket exists; cited in knob_citations; refit when a mains corpus lands.
3. The acceptance replay's rolling window uses the full exchange tape; live tape_flow sees max(WS, REST-100-trades-cap) — very deep 30-min tapes can undercount REST-side (the fitted thresholds are far below the cap; named).
4. W2 fills whose flow onset IS the fill (no prior sustained tape) are structurally uncatchable by any flow bell — named per-row in the table.

## WATCHES
- tape_flow fires on the nightly gun scorecard (premature-fire = FERCER regression: tune constants, never disable).
- `w2_fill_violation` per night → 0 bar; each carries booking source.
- `corridor_refused_w1_preference` / `corridor_itf_fallback_placed` counts; a starved lawful ITF fallback = tuning evidence, dossier-named.
- `cross_over_combined_cap` / `cross_ev_not_above_flatten` / `fill_book_skip` / `settlement_pending` — all named lines, monitor-rendered.
- 429 pressure (REST polls bounded: past-start gunless events, 45s/ticker cooldown, 6h age window).
