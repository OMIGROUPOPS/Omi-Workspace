# PROOF — THE CUTOVER, EXECUTED NOW (operator word waiving the default-GO deadline)

**Candidate SHA: bf7f81a1 (re-cited after rebase: eb1d7784 -> bf7f81a1, the SHA-re-cite class) (bdd215dd + the smoke-sandbox atlas staging — the gate refused the first run exactly as designed: an atlas-blind smoke placed zero orders)** (path-mode unconditional + incumbent entry organs deleted + standing-law nightly, committed at that SHA).
**The operator word, verbatim:** "waive the default-GO deadline: execute the cutover NOW through the full gate. trendpath_live: true — path-mode entries, pair-law, selector enforcement, NO-CALL posture until orientation's own bar clears. Full boot audit; first live path-entry cited with its page; incumbent organs deleted per the migration doctrine — deleted, not disabled. Sizing stays dark pending my separate word."
**The outcome proof IS the packet** (RULING_PACKET.md, fired 07-14 10:40 AM ET, all three doors PASS at n≥300 with CI off zero):
- PATH: n=487, delta +$85.49 (CI [+34.75, +139.82]), yield 11.5% (pess 11.1%) — PASS
- REACH: n=487, delta +$63.89 (CI [+17.74, +114.80]) — PASS on its delta door (yield 3.9% grades the incumbent's own aims — the conviction restated)
- SELECTOR: n=337, delta +$73.90 (CI [+33.03, +115.63]), yield 24.3% (pess 24.2%) — PASS
Actual over the same graded window: −$44.65 — the incumbent's bill.

## What is DELETED (the migration doctrine, literal)
1. **Static-aim entry:** the path aim is the unconditional entry price; a leg with no fitted PATH page gets NO entry — `no_path_page_refused`, named (measured cost: thin pages were 3 skips of ~410 era-clean legs in the week corpus). The `trendpath_live` flag itself is removed from config and code — the branch is law, not a setting.
2. **Join-the-market walking:** entry up-moves die at `path_mode_hold` — the bid rests at its fitted aim (get paid, not get filled). Protective down/equal moves and the completion organ's own repricing are untouched.
3. **97-remainder-as-TARGET completion pricing:** when the sibling has a fitted page, the completion bid prices AT its path aim (`completion_path_priced`, rests; freshness re-walks skipped). **The C-BOUND remainder CEILING (operator adjudication 07-05) is RETAINED as the cap** — an operator ruling is not mine to delete; the vault names this scope.

## What stays dark, per the word
`sizing_live=false` (the operator's separate word) · `orientation_live=false` (its own n≥300 bar; NO-CALL posture until then). Pair-law and selector enforcement continue as deployed.

## The standing law (vaulted verbatim)
"the goal is solid yield on solid capital wagered — the nightly's first line is forever YIELD-ON-WAGERED versus the 8% bar; below-bar nights print their named reasons, diagnosed, never explained away." Implemented: the header carries the law; below-bar nights print the settled-vs-exited split, worst categories, and the diagnosis line.

## Deploy gate
[1/2] lint PASS (at commit) · [2/3] smoke (gate-run) · [3/3] this file, OUTCOME_PROOF_SHA=bf7f81a1 · [4/4] C50 push (the word + the standing law verbatim in the vault) · [5/5] constraints surface. auto_cutover neutralized (cron removed; PACKET_STATUS marked CUTOVER-DONE by operator word) — the 10:40 PM machinery stands down because the human word outran it.
