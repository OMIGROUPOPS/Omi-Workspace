# PROOF — C-TAPE-GATE v1 (two operator words: the flatten tape gate, armed on its own proof · the timing recut on the gun clock)

**Candidate SHA: 40004188 (c070a158 + the builder ordering fix — the SHA-re-cite discipline).**
**The lineage (third ruling on the same defendant, cited):** the flatten-below-basis census (25 sells, one mechanism) → C-ADJUDICATION-READ's leash (N=36, −$0.264/leg, every giveaway a WINNER) → GAP-C's replay price (+$20.06, 43/47 against the tape) → this gate.

## Part 1 — THE FLATTEN TAPE GATE: SHIP-CONDITION MET
**(a) Counterfactual method (the required line):** hold the leg; the resting band exit stays UNMOVED at basis+band_x (each leg's real posted value); BAND-CASHED = the tape printed at/above the exit level after the skip moment (tape-touch, size-blind — named); otherwise the actual settlement. Below-basis flattens only — the gate's scope.
**(b) The founding table (FOUNDING_TABLE.txt, committed, per-leg):** **25 skips → 23 BAND-CASHED, 1 SETTLED-WON, 1 SETTLED-LOST, 0 unsettled. Actual −$7.25 → counterfactual +$8.25 = GATE DELTA +$15.50, REAL OUTCOMES ONLY.** The honest restatement: the +$20.06 headline was the wider 43-action cohort including above-basis flattens outside the gate's scope; **the gate's own scope earns +$15.50 and ships on that number, not the headline.** Twenty-three of twenty-five dumps were followed by the tape printing through the very exit the flatten had torn up.
**What ships:** one input inside the unchanged leash — before any below-basis flatten, trailing-15-min traded median ≥ basis → SKIP (`flatten_tape_skipped`, median and basis on the stamp; the one-shot is NOT consumed, so a later honest tape-turn lets the flatten proceed); median < basis → the flatten proceeds under the existing leash (8/day, −3¢ floor, untouched). **Self-grading forever:** the nightly prints SKIP/KEEP counts and the skip cohort's realized forward outcomes — re-earned every night or revoked by its own line.

## Part 2 — THE TIMING RECUT (gun clock)
The nightly builder joins evidence guns from the day-logs at build time: every atlas page gains `timing_gun` (dual-stamp pre-gun quantiles + **lawful_share**); library cells gain `gun_axis`; **LAWFUL_HARVEST_MAP.txt prints per category where the dips live before the gun** — the fitted "when" every aim consults forward. The w1_cohort dossier caveat lifts ONLY where the gun column exists (n_dual ≥ 8); tour pages keep the −0k axis with the caveat standing. **Depth quantiles untouched; no aim values change in this dispatch.**

## Lane 2
The gate is subtraction inside an existing leash on the twice-convicted branch; the recut is fitted-input production. Entries, participation, aims, sealed exit bands, tail, and leg range untouched.

## Deploy gate
[1/2] lint PASS · [2/3] smoke (gate-run; atlas staged) · [3/3] this file, OUTCOME_PROOF_SHA=40004188 · [4/4] C50 push (both words verbatim; the founding table; the lineage) · [5/5] constraints surface.
