# OUTCOME PROOF (C46, two-lane) — C-COMPOSER-G1 v1 (the composer live-side; in-process shadow; trades nothing)

**Candidate SHA: `39490f33`** (conviction_shadow in _os_shadow + config kill switch + composer purity in the lint gate + LIVE-vs-REPLAY AGREEMENT nightly section).

## Prior art (C45)
- **MIGRATION DOCTRINE / organ: blind aim table → composer conviction** — this wires the replacement beside the incumbent; no cutover.
- **The composer's 4/4 gate** (honesty/posterior/era/range_layer) — this build consumes, invents nothing (#1).
- **M15 range layer + M16 ITF_M margin** — the only surfaces consulted (#11: registry-cited or NO-OPINION).
- **_os_shadow scaffolding** — the shadow rides its dedup (300 s/site) as the compute budget; the posterior's own math makes each evaluation O(1): final confidence = f(prior, n_eff, latest print) — no tape re-scan in-process.

## LANE 1 — MECHANISM
- **Part 3 (replay-harness law): 200 synthetic tick series — the live incremental form equals the offline full form with 0 mismatches** (identity by construction; the offline posterior's final value is exactly w·latest + (1−w)·prior). Honest caveat named: the live n_eff counts prints (the offline adds 0.25-weight tight-book mids); the nightly agreement check's 0.10 tolerance covers the residual and NAMES any real drift.
- **Part 2 (same-instrument law, live edition):** the nightly adjudication now checks every trade's live shadow verdict against the replay's on the same tick — opinion mismatches and confidence gaps >0.10 are named VIOLATION rows.
- **Part 4 (G1 stays honest):** no mains model invented. Mains lines print NO-OPINION with G1 named. **The distance to fitting G1, as numbers: 371 settled mains legs in the graded corpus (183 ATP_MAIN + 188 WTA_MAIN) and 20,332 mains tape samples banked in the shape corpus** — the accumulator keeps banking toward honest n.
- Purity: composer imports json/pathlib only — now ASSERTED at the lint gate beside the oslayer boundary; kill switch `composer_live_shadow_enabled`; every evaluation wrapped, never raises, never trades.

## LANE 2 — SETTLEMENT P&L
$0 claimed. Opinions beside decisions; the nightly meter grades them.

## Regression watches
conviction_shadow lines/night per site+cat · LIVE-vs-REPLAY divergences (must trend zero; any = the shadow is measuring nothing) · mains NO-OPINION share stays 100% until G1 is fitted at honest n · loop-lag unchanged (the O(1) budget claim, checked against loop_lag_sample).
