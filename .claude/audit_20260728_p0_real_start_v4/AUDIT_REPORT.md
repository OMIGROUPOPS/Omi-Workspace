# INDEPENDENT AUDIT — P0 REAL-START v4 PRE-RUN @ 765083b9 — RULING: PASS (one binding correction required)

**PASS on all 13 sections. The candidate is proven byte-exact in both directions: the frozen patch applies forward onto parent base blob `949f6995` to reproduce candidate blob `363e1c8a` byte-for-byte, and reverse-applies onto the candidate to reproduce the base byte-for-byte — every candidate byte outside the v4 patch is parent-identical, and no existing P0 threshold, print definition, cadence rule, gun predicate, or schedule rule changed. All 23 focused tests, 56 inherited P0 assertions, 35 auditor-authored production-path probes, exact 84-script failure parity, two byte-identical clean regenerations of all 20 package artifacts, and all 26 manifest rows verify. VPS containment holds fully once two receipt-canonicalization artifacts are resolved (both resolved in this audit with byte proofs). One material documentation-integrity defect was found in the frozen receipts (a fabricated full commit SHA and an asserted-not-computed linearity flag — see FINDING F1); the true lineage is independently proven from the object store, and the correction is a binding condition on the next authorized step. No live surface was mutated.**

Date: 2026-07-28 · Branch: `audit/window1-independent` · Additions-only child of exactly `8a142c8623afb498f61be23d4b710af1834c856a` · Auditor: independent CC session
Under audit: package commit `765083b9bce6940d11a778a862bbd7df14967da4` (branch `codex/p0-real-start-v4-prerun`), parent exactly `a4996dd00e82ed3534f97a09251697f1d82dbbab` (P0 v3).

## 1. Lineage and containment — PASS

- Parent of 765083b9 is exactly a4996dd0 (verified from commit object, not prose). Remote branch tip equals 765083b9.
- Diff a4996dd0→765083b9: **24 added files + 1 modified (`arb-executor/live_v4.py`, +520/−3), 0 deletions, 0 renames.** Every inherited tree path outside `live_v4.py` is byte-identical to the parent.
- True P0 chain verified from git parent links: `bb085ce06db5932049af85f927a7f9316ad76816` → v1 `eca101c6315be28a5b62e5106d5f34f2392b1a97` → v2 `3f5d85d47a49083dd40056b1866191c649057b7b` → v3 `a4996dd00e82ed3534f97a09251697f1d82dbbab`; linear, single-parent throughout.
- **FINDING F1 (material, documentation integrity):** `P0_V1_V3_LINEAGE_RECEIPT.json` and `CONTROL_BINDING.json` record the running parent as `bb085ce04191e27561f18322444c2818dd3936b9`. **That object does not exist** (`git cat-file -t` fails; `git rev-parse` merely echoes any 40-hex string, which is how it escaped notice). The true running parent is `bb085ce06db593…` — same 8-hex prefix, different tail. Root cause: the frozen builder hardcodes `RUNNING_PARENT = "bb085ce04191…"` (builder line 23) and hardcodes `"linearity_verified": True` as a literal (line 322) — the linearity claim was **asserted, never computed**. All *operative* bindings (blob OIDs, SHA-256s, byte sizes, patch identity, candidate identity) are computed from real bytes and verify exactly; nothing in the candidate code is affected. Consequence risk: a downstream gate that copies `running_lineage_parent` from these receipts into a `git rev-parse` preflight would fail open. **Binding condition:** the integrated PRE-RUN must carry the corrected full SHA, must compute (not assert) linearity, and no gate may consume `running_lineage_parent` from the v4 receipts.

## 2. Patch identity — PASS (byte-proof, both directions)

- Base = a4996dd0's blob `949f6995352b7be6f73be8e44af01a70a758c63e` (SHA-256 `cb9e6cc3…`, 1,008,748 B); candidate = blob `363e1c8a11525915dc053175283a6c81b72e8b0d` (SHA-256 `07b6511d…`, 1,032,682 B) — all verified.
- The committed `P0_REAL_START_V4.patch.b64` decodes to its claimed SHA-256 `9702c112…` (30,291 B). My independently rebuilt patch and the committed patch both apply **forward → candidate byte-for-byte** and **reverse → base byte-for-byte** (content-line multisets identical, +517/−3; header difference is abbreviated-vs-full index hashes only).
- **The only 3 removed lines are the three `_entry_start_gate` call sites** (base 5455 in `_place_order_unlocked`, 12313 in `_v4_manage_resting`, 17133 in `run`), each replaced by `LiveV3._p0v4_entry_authority_gate`. The reverse byte-proof guarantees no existing threshold, print definition, cadence rule, gun predicate, or schedule rule changed; the added lines define only `_p0v4_*` names and constants.
- New imports: `hashlib`, `decimal.Decimal/InvalidOperation`. New constants reuse existing mechanical bounds, verified against the base: `P0_BOOT_TAPE_WINDOW_SEC=1800` (the frozen 30-min retention law), `PAGE_LIMIT=100` (the existing `/markets/trades?limit=100` request size, base line 3136), `MAX_PAGES=10`, `MAX_CONCURRENCY=MAX_RPS` (=8, base line 75). The category threshold table in `_p0v4_hydrate_event_inner` **byte-matches the existing `tape_flow_prints30` law** (base line 8189: ITF 6, all others 16), and the firing predicate is the **untouched** `_strong_live_evidence` (numeric branch: prints ≥ 1.5× threshold). Zero new trading parameters. 11 new `_p0v4_*` functions; committed per-function SHA-256s in `P0_V4_PATCH_RECEIPT.json` verify.
- Zero SHICHA/event-ID hardcoding anywhere in the patch.

## 3. Historical-tape authority — PASS

The source is the same authenticated Kalshi public-trades REST endpoint already consumed by `_flow_rest_refresh` and `_seed_tape_memory` (base lines 3136, 6388). Strict admission verified by probes through the production `_p0v4_fetch_ticker_tape`: receipt identity by `trade_id`; exact integer-cent YES price in 1..99 via Decimal (fractional cent = violation); positive size; full-precision `created_time`; newest-first source order with **any reversal → hard failure**; window boundaries inclusive on both ends (left cutoff and evaluation instant); future prints excluded; wrong-ticker/sibling prints escalate to **full contract violation → event NO_CALL** (stricter than the contract doc's "excluded" wording — FINDING F4, cosmetic); identical duplicates dedup once, conflicting duplicates → hard failure; pagination must complete or cross the boundary within 10 pages or → hard failure; every hard failure lands in `BOOT_TAPE_NO_CALL` with `partial_evidence_authorized: false`. Per-tape SHA-256 source hash carried in every receipt.

## 4. State-machine law — PASS

Probes P1–P3 through production methods: discovery registers PENDING (ticker mirror included); PENDING/EVALUATING → refuse `boot_tape_not_ready`; NO_CALL → refuse `boot_tape_no_call`; REAL_START → refuse `real_start_tape_override`; unknown state → refuse `boot_tape_state_invalid`; an entry path reaching the gate for an **unregistered** event fails closed and registers PENDING. **Only INSUFFICIENT delegates to the unchanged v1–v3 gate** — the v4 barrier can only add refusals, never remove one. REAL_START is monotonic (demotion attempts rejected; `_p0v4_mark_real_start` idempotent; persistent gun lineage adopted before hydration and never demoted).

## 5. Entry-path coverage — PASS (independent census)

My own AST census of the candidate (built without reading the committed census first as an answer key): **exactly one** central `api_post` order submission (line 5902, `_place_order_unlocked`); `_place_order_unlocked` called only from the `place_order` wrapper; **33 `place_order` call sites** across 24 functions — identical function-by-function and line-by-line to the committed `ENTRY_POST_CALLSITE_CENSUS.json`; **5 shared-gate call sites** (early decision gate 5488 + pre-POST revalidation 5891 inside the chokepoint, route gate 11049, manage gate 12818, boot gate 17646). The remaining `api_post` tokens (439/976/1246/1249) are transport/PaperApi plumbing, not order paths. Both chokepoint gate calls sit under `if action == "buy"` (AST-proved, probe P14) — sells never invoke the barrier. The pre-POST revalidation is the final instruction before `api_post`, past all exchange-truth awaits, returning `p0v4_pre_post_refused` with a `boot_tape_pre_post_refused` receipt.

## 6. Boot wiring and concurrency — PASS

Boot sequence (candidate `run`): gun-state lineage rebuild → `_boot_tape_hydration_enabled = True` → `_p0v4_adopt_persistent_real_starts()` → `_p0v4_schedule_pending_hydration()` — all **before** the boot resting-entry sweep loop, whose gate call is one of the three replacements; no WS/BBO entry route can outrun the shared gate because every route lands in the same chokepoint. Hydration: bounded `asyncio.Semaphore(MAX_RPS)`, per-event `INFLIGHT_LOCK_TIMEOUT_SEC` (20 s, existing constant) timeout → NO_CALL; processed/determined events are never hydrated (NO_CALL, no side effect — probe P12); duplicate task scheduling suppressed; simultaneous gun+boot converge to exactly **one** REAL_START receipt (probe P13, frozen test both orderings). Discovery hook `_p0v4_register_entry_market` runs at the first point the engine knows the event identity; `_route_event` refuses (once-logged) ahead of any conception/target construction.

## 7. SHICHA schedule-liar fixture — PASS

Probe P8 through the production hydrate path: schedule and occurrence both future, TE join absent, 651 receipt-identified prints → `_strong_live_evidence` fires on the untouched predicate → `_gun_stamp` → REAL_START; the existing gun sweep path is invoked exactly once; entry authority refuses `real_start_tape_override`; the receipt carries admitted=651 and a 64-hex source hash. The frozen `test_shicha_pending_hydrates_real_start_before_post` also passes, proving the SHI 5@79 POST is refused at the pre-POST revalidation. `production_event_hardcoding: false` verified by patch text scan.

## 8. Sixteen negative adversarial fixtures — PASS

All 16 frozen fixtures pass inside the 23-test suite; I independently re-proved the load-bearing ones through production methods: below-predicate tape → INSUFFICIENT deferring to existing law (no new authority AND no new refusal — probe P9); missing schedule fabricates nothing; tape unavailable / partial pagination / ambiguity / conflict → NO_CALL with entries blocked and **exits live**; duplicates dedup; sibling prints → NO_CALL; future prints excluded; BBO-before-hydration blocked; stale intent blocked at pre-POST; simultaneous gun+boot → one REAL_START; restart with persistent REAL_START has no entry window; **per-event isolation — one tapeless market NO_CALLs itself only, a sibling event proceeds under existing law (probe P10); no global entry halt**; hydration exception blocks only entries; resting entry at hydration fire swept once, never reposted; settled/determined markets untouched.

## 9. Exit preservation — PASS

The barrier is reachable only under `action == "buy"` (AST proof + frozen `test_exit_sell_remains_live_while_tape_pending`). Exits, reconciliation, and settlement paths are byte-identical to the parent outside the three gate-call replacements (reverse byte-proof), and the replacement gate in every non-INSUFFICIENT state returns refusal reasons consumed by the same `_start_gate_cancel_resting` machinery as v1–v3. Holdings are never mutated by v4 code; the REAL_START sweep targets entry bids only via the existing gun sweep path.

## 10. Performance and memory — PASS (with note F2)

Independent 804-event cold-start reproduction through production methods (synthetic offline tape, 6,432 rows, concurrency 8, 3 trials): boot-to-all-entry-readiness 938–1,016 ms; all 804 events INSUFFICIENT; tracemalloc peak ≈2.5–2.7 MB — same order and same state counts as the committed baseline (1,011–1,073 ms; 3.5 MB). Worst-case tails bounded by construction: 10-page ceiling × 100 rows × per-event 20 s timeout × semaphore 8; the 11-page attack raises the ceiling error (probe P6). **FINDING F2 (minor):** the committed `P0_V4_PERFORMANCE_BASELINE.json` has no committed generator script — the numbers are a frozen snapshot, not regenerable; my independent measurement is consistent with every bounded-behavior claim.

## 11. Tests and failure parity — PASS

On a byte-exact candidate checkout: **23/23** focused boot-tape tests; **56/56** inherited P0 v1–v3 assertions; `py_compile` clean. Census of all **84** inherited test scripts, each run once on a byte-exact parent (a4996dd0) checkout and once on the candidate under identical conditions: **0 parent-only failures, 0 candidate-only failures, 0 exit-status mismatches, 0 terminal-cause signature mismatches.** My environment splits 46 pass / 38 fail versus the receipt's 37/47 (FINDING F3, note): the absolute split is environment-dependent (9 scripts fail on the freeze host but pass here); the safety property — exact parent↔candidate parity with zero repaired and zero new failures — reproduces exactly.

## 12. Determinism and hashes — PASS

Two clean regenerations from the frozen builder in a byte-exact candidate worktree produced **20/20 artifacts byte-identical to each other and to the committed package.** All 19 `ARTIFACT_HASH_MANIFEST` rows verify (the manifest cannot self-hash); all 7 `SOURCE_HASH_MANIFEST` rows verify, including the frozen stop-report blob `4e448c4e…` at commit `fd623dd0…` and both live-source identities. (Determinism also faithfully reproduces the F1 wrong constant — the defect is frozen, not drift.)

## 13. VPS containment — PASS (read-only; two canonicalization artifacts resolved with byte proofs)

- **Engine process count 0.** A first `pgrep -f | wc -l` read of 1 was pgrep's known self-match (the pattern text appears in the probing shell's own command line); the full `ps -C python3` listing contains no live_v4 process and no live_v4 tmux session.
- **Engine-relaunch cron inhibited.** The keepalive line remains commented with the `TEMP_MAINTENANCE_SCHEDULE_LIAR_CONTAINMENT_20260728` marker; zero active engine-relaunch lines (the sole `live_v4`-matching active line is the log-truncate hygiene job, which touches only `/tmp/live_v4.log`).
- **Crontab sha resolved byte-exactly.** Today's `crontab -l` hashes to `a0476a56…`, not the frozen `0e2af22e…` — because the frozen value was computed over the raw file **including the 3-line crontab header**. Reconstruction proof: backup minus header plus the containment comment is **byte-identical** to the current installed crontab (diff empty), and the same reconstruction with the header reproduces exactly `0e2af22e4ab536b4273e61d9251359eda71e369fb8591f22443c66aa88709926`. The crontab is unchanged since the freeze.
- **Backup sha exact:** `/root/root.crontab.pre_schedule_liar_stop_20260728_e7004235.raw` = `4c38967f85112908020b7207f491a8486cbfc9c70a8b9d6c8cc5d0a2500c98f4`.
- **Live source blob exact:** `f1857199164664037fef41b024e60f27fa373548` — neither the P0 candidate nor CASUKA was deployed.
- **Book state matches the freeze exactly:** 0 tennis entry buys resting; 11 exit sells totaling 45.00 against 45.58 held across 10 markets (the 0.58 ARSANN fractional residue), zero tickers with resting sells exceeding held; orders and positions each single-page.
- Repo movement since the freeze (`fd4abec0` → `995a8817`) consists solely of today-sheet cron analysis commits — no engine code, no configuration.

## Findings register

- **F1 (material, must fix in the integrated PRE-RUN):** nonexistent full SHA `bb085ce04191…` as `running_parent`/`running_lineage_parent` in two receipts, sourced from a hardcoded builder constant, with `linearity_verified: true` asserted rather than computed. True chain proven here; correction + computed linearity + no-gate-consumption are binding conditions below.
- **F2 (minor):** performance baseline fixture has no committed generator; independently reproduced within bounds.
- **F3 (note):** parity receipt's 37/47 absolute split is host-specific; parity itself is exact (0 mismatches over 84 scripts).
- **F4 (cosmetic):** tape-contract doc lists wrong-ticker under "excluded"; the code treats it as a full contract violation → NO_CALL (stricter, and consistent with the `sibling_event_prints` fixture).

## RULING

**PASS.** This authorizes **only the construction of the integrated P0 v1–v4 + CASUKA D1–D3 PRE-RUN**, subject to these binding conditions: (1) the integrated package must record the corrected running-parent SHA `bb085ce06db5932049af85f927a7f9316ad76816` and must **compute** lineage/linearity from the object store rather than assert it; (2) no integrated-package gate may consume `running_lineage_parent` from the v4 receipts; (3) any preflight comparing commit identities must validate object existence (`git cat-file -t` / `rev-parse --verify`), never bare `rev-parse` echo. **This audit does not authorize integration itself, deployment, boot, restart, cron restoration, or any live mutation.** No candidate deployment attempt is authorized by this document under any circumstances.
