# CLASS LEDGER — defect/doctrine classes, founded 2026-07-10 (RULING_PAIR_ECONOMICS; one entry per CLASS, instances counted, open until the closing build ships)

## CLASS — DOCTRINE CONFLICT
Two laws steering one decision. **Census evidence added 07-11 (C-KNOB-CANON): four pair-97 knobs live in production — `combined_goal`, `completion_combined_ceiling`, `completion_all_cells`, `completion_reprice` — recorded as WITHIN the operator's interim clause (pair-97 = dumb safety bound until the replacement proves itself in shadow), BOUNDED BY C-COMPLETION-POLICY v1 shipping next: the interim clock now has a named bound. Pin-overlay evidence: the `reshuffle_pinned` exemption population ran 5,373 lines on July 10 — the pin IS goal−basis arithmetic riding inside the churn-fix.** **Instance 1: settlement math inside an exit-first machine** (pair-97's "guaranteed under 97" vs §0A exit-posts-at-fill; realized in Exhibit 1: +$0.25 winner band, −$3.70 loser ride). Status: OPEN — closes when per-leg exit economics + priced completion ships and pair-97 is DELETED (not disabled).

## CLASS — ADVERSE-SELECTION STRAND
The kept leg loses 65 percent; taker flow hits the side the market is dumping and starves the side it is accumulating; a maker bid on the winning sibling is structurally starved (ADVERSE_SELECTION_STRANDED, June). **June: 93 priced instances (−$50.65 realized, COMPLETION_FUNNEL). July 10 (FULL-SLATE REVIEW): 55 live instances filed, $6.25 measured weight — #1 in the ranked fix queue; 70 never-filled legs total, 44 strands (winner-verification limited to determinable settlement rows — 1 verified; method noted).** Verdict verbatim: cross to complete, or never hold the kept leg naked. **CLOSING DESIGN SHIPPED 07-11 (C-COMPLETION-POLICY v1, shadow):** per-leg economics on every one-sided pair; never-hold-naked verdicts + gated taker branch (operator_taker_word) logging would-have-dones nightly; 44/44 of July-10's strands verdict+priced on replay. Status: SHADOW — cutover on the operator's word closes the class; the nightly COMPLETION-SHADOW section is the evidence clock. **07-13 FIRST CALIBRATION GRADE (C-ADJUDICATION-READ): the flatten branch NEGATIVE on real outcomes (N=36, −$0.264/leg vs ride; giveaways = winners, the win-ride-residual exclusion named as frame bias) → the pre-authorized leash applied (`flatten_daily_action_cap=8` + 3¢ EV-margin floor, named lines). The taker branch stays at its own cap; the never-hold-naked doctrine stands, now margin-gated.** **07-12 THE WORD IS GIVEN (`operator_taker_word=true`, `completion_live_enabled=true`, deploy `9b26fc17`): the verdicts ACT — never-hold-naked enforced live; first live day graded against the shadow record (COMPLETION LIVE-vs-SHADOW, divergence = named violation). KILLED claim = clean live-vs-shadow nights + the strand count falling on the nightly meter.**

## CLASS — UNBOOKED FILL
A fill arrives that nothing books — no entry_filled, no exit posts, the audit halts conceptions. **Instances on record: 13 tickers July 10, five halt episodes, 276.8 minutes of conception downtime (the day's dominant operational cost); parent race: TUPMAK July 9 11:24 pm (cancel success=false → replacement placed anyway), GHASPI recurrence 1:31 pm July 10.** BOARD −0f and −0c fold into this class. **Closing design (C-BOOK-THE-FILL, 07-10): (1) a held-no-exit leg our own order lineage explains is BOOKED at the audit — routed through the proven naked-recovery path (books the entry, posts the native band exit, stamps lineage) — never halted on; the halt remains for genuinely unexplained holdings. (2) The race dies at the source: a failed cancel with the order still live returns HELD-INVENTORY-UNTIL-RECONCILED ("unresolved"), never "cancelled" — the replacement is never placed.** KILLED claim = HALT-MIN and UNBOOKED-FILLS-BOOKED at zero on the nightly scorecard footer, or the class reopens itself.

> NOTE (07-10, C-NIGHTLY-ADJUDICATION): no new class — loop infrastructure per the MIGRATION DOCTRINE. Every class's nightly numbers (HALT-MIN, UNBOOKED-FILLS-BOOKED, BELLS-MISSING, the MIGRATION METER) now print without a dispatch; a class claiming KILLED re-opens itself the night its number is nonzero.

## CLASS — EXCHANGE-TRUTH DAYLIGHT (founded 07-10, FULL-SLATE REVIEW Part 3)
Any daylight between the bot's report, the log, and the exchange's own order/fill history is a named violation row, never a footnote. **Founding instances: 17 on July 10** (first: VANKOI-KOI log 7.0 shares vs exchange 5.0 — qty daylight; full rows in FULL_SLATE_REVIEW_20260710.md). The nightly instrument reconciles three ways every night; the browser-based exchange-eye stays queued behind the #8 token rotation — this REST column is its forerunner. **ROOTED 07-10 night (C-DAYLIGHT-ROOTS v1): log double-count 0 (suspect INNOCENT — increments correct, no dedupe built for a phantom) · report error 6 (reconciliation summed cumulative qty, fixed at its line; VANKOI reconciles 5.0-vs-5.0 on the rerun) · exchange truth 11 (UNBOOKED FILL history — the halt roster; healed forward, healer bookings now count as log truth).** Status: rooted; KILLED claim = zero violations on the nightly re-count or it reopens itself.

## CLASS — GUN FIRE-STARVATION (named 07-10, C-PERCAT-GUN v1; the seventh-time class)
A match goes live and no bell rings — seven instance-fixes on record across the lineage (volume-burst prematurity 06-19 · fused multi-source gun 6d84f27e · gun persistence · te_live resurrection + lock fix · truth-join 07-09 · fallback bell 07-10 · per-instance halts/containments), each fixing an instance, none shaping the trigger to its market. **The class-kill design: ONE trigger shape per category, FITTED — the volume ledger's own OPEN thresholds (ITF ≥6 prints/30m · CHALL ≥16/30m · MAINS OFF: "volume does not open mains", fill flat ~1% at every volume state), the same fitted surface the flow gauge reads — one source of truth, registry-cited (M3/M13).** Shipped SHADOW-FIRST (percat_gun_shadow, migration doctrine: shadow → grade one night vs the live gun → cutover on the operator's word → legacy burst constant DELETED). **07-12 OPERATOR WORD: ARMED as an ADDITIONAL source (`percat_gun_armed`, gun source seven `percat_fitted`) under the fused freeze; legacy deletion rides the 07-13 6:10 regrade scorecard.** KILLED claim = the July-8 replay coverage number + nightly BELLS-MISSING at zero.

## CLASS — UNTRACED LIVE KNOB (founded 07-11, C-KNOB-CANON v1)
A live money-path constant or flag with zero citation anywhere. **Instance 1: `marketable_stale_pin_exempt` (live_v4.py:1713, live=True)** — traced 07-11: born `aba83afa` 07-04 (C-CHURN-FIX b), exempts reshuffle-PINNED bids from the marketable-stale kill (LEG-49 exhibit: the bound held 10 cycles then was murdered); **it fires SILENTLY — no log line when it holds a bid, so its July-10 firing count is UNOBSERVABLE from the tape. That unobservability is the class's teeth.** Population: **the census's 52 NAKED knobs.** Kill bar: every NAKED knob gets a study, an operator ruling, or deletion — **no fourth option**; the count prints in the census regeneration (`analysis/knob_census_check.py`; mechanical count today: 119 knob keys, code∪config).

## CLASS — MONITOR-BLIND (founded 07-11, C-MORNING-TRIAGE-0711)
Nothing watches the watcher: a monitoring instrument that crashes every cycle is indistinguishable from a quiet night. **Instance 1: the live monitor rendered nothing 04:09 am → 12:01 pm 07-11 (~7h52m)** — the first live `bell_missing` hit a violation item built without `cls`, `forensic_check` threw, and the per-cycle catch swallowed it silently every 10 minutes; the replay exposed two more assumed-key sites (`detail`) in the same family. All three fixed failing-then-passing (`d81a0709`, `c55f576c`). **Structural kill shipped with the founding: 3 consecutive crashed cycles → the BOT_DOWN ntfy channel.** Kill bar: one fired tripwire proving the channel works, or a crash-free month.

## CLASS — TRUTH-JOIN DRIBBLE-ONSET (founded 07-11; closing build SHIPPED 07-12 `476b4326`, awaiting the clean regrade night)
The GRADING instrument, not the gun: `gun_scorecard.tape_onset` accepted the first minute with prints in ≥5 of the trailing 15 bins **at any size** — sustained overnight premarket dribble passed 75–115 min before real match onset. **Instances: the 16 `SUSPECT(truth_predates_fresh_fire)` rows of 07-11** (every one a correct te_honest-anchored fire graded against noise; forensic `.claude/triage_20260711/`). **Closing build (−0k, `476b4326`): flow-step rule — trailing-15 prints ≥ max(8, 3× window-first-hour baseline) + 30-min forward sustain + symmetric `onset_lags_fresh_fire` quarantine (>20 min lag).** Validated fail-before/pass-after on the same tape: 10/16 suspects → clean joins within ±12 min of the tape-proven fires; the 6 heavy-premarket ambiguities all QUARANTINE instead of averaging; good rows 11/12 unchanged. Kill bar remaining: **one clean regrade night** (tonight's 6:10 am scorecard rides the fixed rule) — then shadow-gun grades are admissible for the cutover decision.

## CLASS — CAP-VOID (flagged 07-10 GHASPI; defined-stamp 07-10; **closing build SHIPPED 07-12 C-CHASE-KILL `8a857ecf`**)
A live buy that no cap ever looked at. The lineage's proof instance: **CORBRU 07-12 — 14 buy placements laddering 41→65¢ over 90 minutes on a LIVE match** (rungs = `v4_move_repost`), while (i) `premarket_walk_cap`'s anchor (`_window_open` cell) was ABSENT → the walk cap silently never engaged (the void is the missing ANCHOR; the honest-anchor fallback built for this hole sits staged OFF — **−0j escalated from ruling to defect**), (ii) the cycle cap counts only COMPLETED cycles (a ladder is one pursuit, zero cycles), (iii) no bell rang. **The cap, DEFINED (the deferral ends):** conception stamp RETROACTIVE to the event's first order (no order precedes conception, by definition); UPWARD buy placements per leg cap at `reentry_cycle_cap` (2, DECREED cycle ruling 07-08) from the FIRST buy, early window included, while neither leg has fills; beyond cap = `chase_cap_refused`/`chase_cap_hold`, monitor-rendered. Plus the self-fill bell (gun source six): own buy activity rising ≥4¢/30 min = match tape → fused-gun freeze. **KILLED bar: zero chase ladders (3+ upward re-places pre-fill on one leg) on any nightly tape for 7 nights, with the refusal lines PRESENT on at least one (proving the lock fires, not that chases stopped arriving) — then the bar prints in the slate review and the class closes.** Replay: `.claude/replay_20260712/replay_chase_kill.py` (fail-before 17/17 accepted = the tape; pass-after frozen at the 51).

## CLASS — OVER-BROAD LOCK (founded 07-13, C-CAP-SCOPE v1; filed under the CAP-VOID lineage)
A protection that redefines the operator's ruling: the chase-kill closing build turned the DECREED CYCLE cap (buy→cash→re-buy, cycle ruling 07-08) into a PLACEMENT cap from the first buy — and strangled premarket market-making within hours of arming. **Founding instance: the 07-12/13 refusal window — 931 refused re-aims across 20 legs / 15 events, 7 bids stranded stale at count time (WONBOW 6¢ vs mid 42.5 = +36¢; SARBOV 11¢ vs 51 = +40¢; SARANG ×2 +34¢; BONFAB +23¢; HASZAG +21¢; DUHGAT +13¢), 539 sidelined leg-minutes; 215 refusals on one leg in 45 minutes (the organ retry cadence against a wall it should never have met).** Scope correction shipped same night (`22039f30`): `_pursuit_armed` — the cap counts from the leg's first fill or the event's first bell, whichever first; pre-fill pre-bell upward re-aims unlimited, each bounded by the armed honest-anchor walk cap; three replay lanes PASS (CORBRU still freezes; WONBOW shapes pass; post-arm 3rd re-aim still refuses). Kill bar: the 6:10 honesty footer shows zero cap refusals on unarmed legs for 3 nights, with post-arm refusals still present somewhere on the tape (the lock alive, correctly scoped). Class lesson, standing: **a lock's scope is part of the decree — tightening beyond the operator's words is a defect, not prudence.** **INSTANCE 2 (07-13, one night apart — C-BELL-SCOPE): the self-fill bell's 4¢/30-min threshold was calibrated against the pre-arming 4¢-walk world; the −0j arming (ITF allowances 14–20¢) invalidated it SAME-DAY — 24 events frozen 1:30–3:09 am on sanctioned walk-cap-compliant re-aims (1,780 cumulative frozen-minutes, 42 refused entries). Scope fix `0a6ab782`: the bell fires only on (a) post-fill pursuit, (b) rise exceeding the ARMED walk allowance read from the LIVE table (`_sanctioned_walk_cents_cat` — re-ratifications can never strand it again), or (c) tape corroboration (≥5 prints in-window); boot unfreeze audit lifts evidence-free freezes with the true state logged. The lesson EXTENDS: a lock's threshold must scope against currently-sanctioned behavior DYNAMICALLY, never against constants other deploys change.**

## CLASS — COMBINED-CAP BLIND SPOTS (founded 07-12, −0k appended scope (c))
The pair-97 constant's two enforcement holes, both organic on 07-11 (combined 99 each; `.claude/triage_20260711/ZEROK_REPORT.md` §c). **Instance 1 — the race second (WALBAD, 2 shares at 28+71):** legs rest at targets summing >97 by design (the cap binds the SECOND leg after the first fills); when both legs fill in the same second, the correct `reaim_sibling_lower` cancel loses the `cancel_fill_race`. The tape also shows the exit sell placed BEFORE the sibling cancel — free race-window shave available. **Instance 2 — the conception-void ledger (MIRMAL, 87+12):** pre-conception fills (MAL 11–12¢, ~8:00 am, stamp 2:00 pm) are invisible to pair-basis accounting, so MIR rested/filled at 87 uncapped (should have been ≤85) and the machinery then re-aimed the ALREADY-FILLED leg — the 07-06 autopsy's conception-void hole expressed in the combined-cap organ. Kill bar: operator's remedy word (resting-sum clamp vs doctrine · cancel-before-exit ordering · pre-conception basis into pair accounting), then the closing build with its replay.

> C-BOOK-REPLAY v2 (07-11): AIM/TIMING MISSES' closing design ran — the aim refit FAILED held-out and was refused entry (the class stays open on real evidence, not on a surface that would have made it worse); conviction-gap calibration produced M16 (ITF_M 8¢, shadow) — the July-10 '2¢ miscalibrated-tight' finding held for ITF_M and was REFUTED for ITF_W (the 2¢ constant won there).
