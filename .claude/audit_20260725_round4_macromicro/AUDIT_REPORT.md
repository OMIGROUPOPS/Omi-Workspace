# INDEPENDENT ROUND-4 MACRO×MICRO PRE-RUN AUDIT @ 84959172 — RULING: BLOCKED

**BLOCKED — five confirmed semantic defects.** The mechanical substrate (lineage, last-trade preservation, five-event D-membership, headroom arithmetic, fill law, receipts, tests) reproduces cleanly, but the composition layer misbinds its own authoritative sources: the fitted macro target is re-anchored to the moving external bid, the climb/decay law flattens category-specific crossovers, aim-then-HOLD is a label over a bid-following chase, claimed volume/cadence consumption does not exist in the executable, `composed_macro_micro` annotates actions the composition never gated, the flow/pressure direction mappings are unsourced, and the headroom bias can bypass the join/improve-by-1¢ expression invariant. No scoring package may be constructed against this SHA.

Date: 2026-07-25 · Branch: `audit/window1-independent` · Auditor: independent CC session
Method: detached clean worktree at `84959172330a6659df4ef7db04d971ec0bb8d893`; pinned development inputs; no implementation, scoring, ranking, tuning, holdout, or live access; committed receipts accepted only after independent reproduction.

## 0. Prior art (C45)

`8fc80e35` (V2 audit — PASS, scoring not authorized; macro×micro composition named as an open omission), `abe543e3` (Item-5 amendment), `84cdf87a` (V1 audit). Delta: first audit of the macro×micro composition PRE-RUN.

## Item results

### 1. Lineage, scope, determinism — PASS (with regeneration noted)
Parent exactly `781b6d6f`; all-refs children scan: sole child; remote tip verified. **28 additions, zero modifications/deletions**; all 26 inherited V2 paths byte-identical by construction (additions-only diff). All 27 artifact-manifest receipts verify (sha256/blob OIDs; CRLF disk-basis accepted only where blob-equivalent); the source manifest pins my controlling V2 audit report blob (`44257fa4…`). Metrics null everywhere (`metrics: null`, `scored: false`, C/PC/S/IC unpopulated); scorer neither imported nor invoked (grep of all four new modules; forbidden-access receipt corroborated); no ranking/selection/tuning; no holdout/live/production access (all inputs pinned dev caches; zero holdout rows). Tests: **12/12 pass** from clean state. Fresh-directory regeneration of the stream artifacts was launched and still running at report time; it is a determinism check only and cannot affect this ruling — the committed DETERMINISTIC_REGENERATION_RECEIPT's claim is noted as **not yet independently confirmed**, and must be re-verified in the corrected PRE-RUN's audit.

### 2. Last-trade preservation — PASS (independently reproduced)
Raw census over all 804 caches reproduces the committed numbers exactly: **13,479,418 raw snapshots; 12,773,011 positive-last-trade snapshots; 3,025,875 positive prints**. Normalized: 1,741,891 books; 1,555,991 positive last-trades = **1,051,712 VERIFIED_PRINT_TIMESTAMP + 504,279 CARRIED_EXECUTION_TIME_UNKNOWN** (exact sum); 70,247 carried before the first Window-1 print; 7 legs carried-with-no-window-print; `last_trade_created_print_count: 0`.
**Why 13.48M → 1.74M:** (a) the majority of raw snapshots lie **outside the lawful policy corridor** (e.g. CREMAT: 5,347 snapshots, 0 in-window); (b) in-window snapshots are thinned by the **inherited Round-2 normalizer** (`window1_round2_real_capability.py:145-176`): keep on (best_bid, best_ask) price-state change or new minute, plus the pre-print snapshot and boundary checkpoints — sample measurement 15,579 normalized / 65,735 in-window (23.7%). The in-window discards are BBO-unchanged intra-minute rows — duplicates under the frozen BBO/print/checkpoint decision law; the only residual class (intra-minute size-only top-5 changes) has been excluded by this inherited normalization since Round 2 and is consistent with the matrix's PROXIED depth classifications. Not silent evidence destruction.
**Timestamp matching:** raw match requires identical ts AND identical bids/asks content; the `matches[-1]` fallback **never engaged** in a 15,579-observation sample, and by construction the chain state is built from the observation's own levels — only `last_trade` could come from a same-ts sibling snapshot, and never across tickers or timestamps. **Proofs hold:** last_trade creates no print volume (`last_trade_is_fill_volume: false` everywhere), never substitutes for BBO (`_target_price` reads external bids only; dedicated test), never claims a new execution from carried state, and `execution_at` is set only when last_trade equals the price of the latest lawful in-window print at-or-before the observation (else CARRIED with no execution claim).

### 3. Macro target meaning — **BLOCK (semantic source-binding failure)**
The generating study (`.claude/seqfloor_20260708/SEQFLOOR_RECUT.md`) defines the grid keyed by **each leg's OWN W1-closing price cell** and the aim as `leg's W1-close − edge_p50(cat, cell)`; the ratified CLIMBSIDE_SPEC (§2) states placement at the FITTED level "`own W1-close-projection − edge_p50(cat, cell)`" and "the level is fitted, not walked." The implementation (`window1_round4_macromicro_instrument.py:467`) computes **`fitted_target = bid − edge`** — the *current external best bid* minus the edge — and re-evaluates it on every book/print event. The current bid is nowhere authorized as a close projection; this transforms the fitted historical level into a **moving BBO-relative target** — precisely the riser anti-pattern the spec's own disarm table forbids. The commit's own witness P02 documents the defect: external_bid 60→61 moves the target 59→60. Secondary: the cell key uses `birth_anchor` (current price at birth) where the study keys by own W1-close cell — an unauthorized proxy choice compounding the misbinding.

### 4. Category×cell climb/decay law — **BLOCK (category law flattened)**
Implementation (`:414`): universal `CLIMB_SIDE if birth_anchor >= 50 else DECAY_SIDE`. The authoritative surface: crossover **~cell 50–55 (ITF), ~cell 80 (CHALL)**, mains 1–2¢ flat (par-locked, no deep-climb zone); cells with edge_p50 = 0 are JOIN-policy by the study's own boundary. Independent census over all 3,196 macro-bound legs: ATP_CHALL 686 climb / 782 decay, WTA_CHALL 266/278, ATP_MAIN 288/300, WTA_MAIN 294/302 — and **284 legs are labeled CLIMB_SIDE in cells whose fitted edge_p50 is 0** (ATP_CHALL 134, WTA_CHALL 96, ATP_MAIN 32, WTA_MAIN 22): unsupported climb-side labels under the actual fitted category-specific surface, concentrated exactly where the Challenger ~80 crossover is flattened to 50.

### 5. Aim-then-HOLD vs reprice — **BLOCK (HOLD is a label; upward walking dominates)**
Reproduced from the frozen streams: chain candidate **108,537 reprices / 108,972 cancels**, chain+flow **64,304 / 64,722** (exactly the reported numbers) against only 1,598 placements each — ~68 reprices per leg. Direction census: **up-moves are the majority** — 68,487 up vs 40,050 down (chain), 38,569 vs 25,735 (flow); the modal step is **+1¢ (93,771 occurrences)** — the fitted aim following the bid upward one cent at a time. The ratified posture says "upward revision does not exist." Reason attribution: 126,707 `macromicro_fitted_cell_chain_transition` + 8,583 `macromicro_actual_flow_transition` reprices are the overlay's own bid−edge re-anchors (each implemented as cancel+replace — **every one surrenders the resting queue**, the antithesis of HOLD); 37,299 are inherited headroom-ladder; 252 inherited divot recuts. The climb-side "fitted resting aim before the dip, then HOLD" does not exist in the executable: ordinary BBO motion turns the aim into a moving chase, gated only by the last_trade/pressure/flow confirms.

### 6. Volume, cadence, sibling flow — **BLOCK (claimed BOUND, actually labels)**
The mechanism matrix claims `actual_positive_print_flow` is BOUND with normalizer "deduplicated **taker side, volume and cadence**." The executable stores exactly three fields per leg — `last_positive_print_receipt`, `last_positive_print_taker_side`, `last_positive_print_ts` (`:797-803`) — and the flow gate consumes **only taker_side** (`:706-713`). Executed size, trade count, inter-print cadence, rolling volume state, and sibling-leg flow are stored nowhere and read nowhere; perturbing them while holding taker side constant cannot change any action (no code path exists), and the commit's own witness P06 perturbs only taker_side. Volume/cadence/sibling-flow must be reclassified from BOUND to receipt-only/ABSENT — or actually bound. Primary fill volume (item 12) is genuinely consumed; this defect is about the *flow-confirmation* claim.

### 7. Microstructure binding vs annotation — **BLOCK (over-claimed annotation)**
The overlay's own gating is real for its transitions: 126,707 + 8,583 confirmed reprices plus withheld-transition rows and `micro_hold_count`. But `_annotate_decisions` stamps **`composed_macro_micro: true` on every place/reprice/cancel** flowing through `_place`/`_cancel`/`_replace_at_price` — including **purely inherited actions the composition never gated or selected**: 37,299×2 `causal_headroom_ladder` reprice+cancel rows, 853 `declared_policy_horizon` cancels, 252×2 divot-recut rows, and all 3,196 initial placements. Reason-count tables (reproduced): inherited-parent actions 41,851+; overlay-confirmed 135,290; overlay-withheld recorded as `macromicro_transition … withholds` rows. Bid/ask chain transitions, depletion/replenishment, and pressure are *computed and receipted* per book, but only three chain quantities actually reach a decision (last_trade vs bid, top5 pressure sign, taker side) — the remaining transition census is a stored hash/receipt, not BOUND consumption, exactly what the matrix's own PROXIED rows admit for walls/slopes/persistence.

### 8. Controlled perturbation receipts — **BLOCK (fixtures, not production-path witnesses)**
The nine committed witnesses are bare `{input_A, output_A, input_B, output_B}` rows with **no event identity, no receipts, no invocation lineage** — consistent with manually constructed formula examples. The test suite does drive some of these through real code paths (role-swap, last_trade/top5 perturbation, coherent-pair, headroom tests — 12/12 pass), but the committed receipts themselves carry no production-path evidence and no real-corpus coverage counts, P02 documents the defect of item 3, and P06 does not test the claimed volume/cadence at all. Insufficient as BOUND-mechanism proof under the required standard.

### 9. Flow and pressure semantics — **BLOCK (unsourced mappings)**
The four directional mappings — climb confirm `last_trade ≤ bid` / decay `last_trade ≥ bid`; climb pressure BID/BALANCED / decay ASK/BALANCED; climb flow taker "no" / decay "yes" — have **no fitted or ratified source** anywhere in `.claude/rulings/`, the seqfloor study, or the Vault (searched). The taker_side data lineage (G9 archive) documents the field's existence and per-ticker meaning, not a climb/decay confirmation doctrine. These are newly invented gates. (No YES/NO inversion was found — the "no"=sell-flow orientation is consistent with the G9 convention — but consistency is not ratification.)

### 10. Pair chronology and expression — mechanics PASS; **expression invariant bypass — BLOCK**
Coherent pair read verified: both initial placements ride one content-bound two-leg pair receipt (`pair_read_id`; 3,196 `coherent_pair_first_lawful_bbo` placements); later leg clocks independent; no future sibling evidence found in receipts. Headroom: first exact-five d1 activates the strict joint budget; **all 129,173 receipts re-verified — `b2_max = floor(−d1 − fee − 1)` with zero mismatches, zero strict-budget violations**; 29,695 accepted improvements; positive sibling delta allowed within budget; no IC or S gate anywhere. **However:** `_target_price` adds `sibling_bias_cents` *after* the join/improve cap and clamps only to ask−1 — and **27 executed ladder actions (17 chain, 10 flow) rest above nonself_bid + 1** (e.g. price 36 vs bid 34) by their own attached micro receipts. The bias path can and does bypass the ratified join-or-improve-by-exactly-one-cent expression invariant.

### 11. Evidence availability classification — PASS with one required correction
BOUND/PROXIED/ABSENT/RETRACTED rows reconcile with the data and decisions **except** the item-6 flow row. Top-20: ABSENT is correct for **raw causal availability** (the guarded cache binds five levels; named TOP20 NO_CALL emitted per leg) — but the classification must not flatten history: the frozen feature ledger carries historical checkpoint top-20 availability on 1,017 legs (checkpoint, not chronological decision evidence); the matrix reason states the raw-cache basis, which is accurate, and the distinction should be stated explicitly in the corrected PRE-RUN. Bookmaker/FV and Pinnacle: ABSENT for causal decision use (prior partial checkpoint coverage remains history-only); proved full depth ABSENT; borrowed AIM shape RETRACTED (correct — and consistent with item 3's requirement that no borrowed mapping substitute for the unproxied close projection). Raw-causal vs historical-checkpoint vs decision-consumption vs unavailable are otherwise properly distinguished.

### 12. Five-event and fill-law preservation — PASS
D = **804 per candidate** (recounted from the four stream shards); the five no-lawful-BBO events retained in both candidates with named steering + market-evidence NO_CALLs, **zero placements and zero fabricated prices** on all ten legs, non-censored terminals. Fill law inherited unchanged: cumulative positive-size print filling at limit-or-better; no displayed-depth, single-print-size, queue, trade-through, IC, or S gate (code + dedicated test + P07 semantics); headroom receipts arithmetically clean (above).

## Discrepancy classification

- **Semantic source-binding failures (BLOCK):** items 3, 4, 5, 9, and the item-10 bias bypass.
- **Claim/label inflation (BLOCK):** items 6, 7, 8 — mechanisms and annotations claimed beyond what the executable consumes; receipts without production lineage.
- **Measurement/mechanical:** none found — lineage, censuses, arithmetic, and preservation all reproduce.
- **Legitimate missing evidence:** top-20/full-depth/Pinnacle/FV/schedule-revisions (named, unproxied); cohort inherited.

## RULING

**BLOCKED — DO NOT BUILD A SCORING PACKAGE AGAINST `84959172`.**

Narrow corrections required (do not implement here; each must arrive in a superseding additions-only PRE-RUN with fresh independent audit):
1. **Item 3:** bind the fitted target to an authorized own-close projection (or an explicitly ratified stand-in with source lineage), keyed by the study's own-close cell — not `bid − edge`, not `birth_anchor` as cell key; the aim must be a level, re-anchored only on authorized triggers.
2. **Item 4:** replace the universal 50-split with the category-specific fitted surface (edge_p50 > 0 defining climb-side support; CHALL crossover ~80; mains par-locked), eliminating all 284 unsupported climb labels.
3. **Item 5:** implement aim-then-HOLD as behavior: no upward revision on climb-side, queue preserved absent an authorized trigger; reprice census must show the chase eliminated.
4. **Item 6:** either actually bind volume/cadence/sibling flow (with fitted, sourced definitions) or reclassify them as not consumed; the matrix must match the executable.
5. **Item 7:** `composed_macro_micro` only on actions the composition logic actually gated; inherited actions labeled inherited.
6. **Item 8:** perturbation witnesses regenerated through production paths with event identity, receipts, and corpus coverage counts.
7. **Item 9:** every directional confirm mapping either sourced to a ratified fitted study or removed.
8. **Item 10:** apply the join/improve-by-exactly-1¢ invariant after all bias terms (zero actions above nonself_bid+1).
9. Deterministic regeneration must be independently confirmed in the corrected PRE-RUN's audit (the check was still running at this report's ruling and is immaterial to the BLOCK).

## Determinism addendum (post-ruling)

The fresh-directory regeneration completed after the ruling was committed: **all 10 regenerable artifacts are byte-identical to the committed git blobs** (four stream shards, FIELD_CONSUMPTION_RECEIPTS, LAST_TRADE_PRESERVATION_CENSUS, HEADROOM_RECEIPTS, ORDER_DIFFERENCES, REAL_CAPABILITY, FIVE_EVENT proof; gz compared on decompressed content). Zero nondeterminism. The DETERMINISTIC_REGENERATION_RECEIPT claim is now independently confirmed. This strengthens item 1 and does not alter the BLOCKED ruling, which rests entirely on the semantic defects of items 3-10.
