# OUTCOME PROOF — THE RESCHEDULE LAW (gate #4; capture-standard rider P4)
Candidate: `f7aea7bb` (engine change `2bb4fe15`: reschedule re-conceive;
plus analysis/panel-only commits — no other engine delta). C46 two-lane.

## LANE 1 — MECHANISM (primary, luck-free)

### Scope, exactly
One branch added inside the existing `schedule_corrected` site, fired only
when a TRUSTED source moves a stored start by >60 minutes (either
direction) with `reschedule_reconceive_enabled` (DECREED): unfilled entry
bids cancel through `_cancel_entry_and_resolve` (race-safe — a raced fill
books with its exit, the proven path), tombstones clear so the router
re-conceives in the corrected window, and the cascade's read-side state
re-births (fresh band_call at the new conception). Moves ≤60min, filled
legs, exits, weaker-source corrections: byte-identical behavior.

### The replay evidence (the class this law kills, from the record)
- The Phase-A indictment (07-17): the reschedule class is REAL and split
  at ~24h — Kalshi's own moves; old stamps fed whole matches to fits as
  premarket (BROBAL the walked exhibit).
- **The founding charge for this rider: OLISCH (Olivieri vs Schunk)** —
  the Schunk leg's 5@30 filled 11:57 AM against a schedule that later
  read TOMORROW 10 AM (clock-liar class, adjudicated in the post-bell
  recon). Under this law, the >60min correction re-conceives the pair:
  the unfilled sibling cancels and re-prices in the real window instead
  of resting against a game that no longer exists; the filled leg and
  its exit ride untouched — exactly what the recon's containment did by
  hand, now structural.
- Construction delta on games with NO >60min move: zero, by the guard.

### Riders in the same push (non-engine)
Provenance tags (panel rows carry the chokepoint's authority stamps;
pre-stamp orders read `pre-law`) and the whole-market capture instrument
(analysis lane) — neither touches the engine's paths.

## LANE 2 — SETTLEMENT P&L (secondary)
Not applicable — placement-governance; no claim (C46). The
`reschedule_reconceive` lines are the accruing evidence.

## Verdict
Lane 1: the law converts the recon's by-hand containment of the
reschedule class into structure, race-safe, on the site that already
owns schedule truth. Lane 2: no claim. DEPLOY.
