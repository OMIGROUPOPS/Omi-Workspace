# Fresh holdout exam: boundary/admission frozen, execution not started

Date: 2026-08-06

The corrected touch-law seal supplied 171 untouched post-July-26 events with
event-list SHA-256
`06ede0264a196bbebc005785c3ffdee5a840afe1a617f86f0354eedf65ac4313`.

Stage 1 froze one pre-match boundary per event by the V3 precedence required
by the operator: official exact start where a live/interrupted official receipt
exists, otherwise a policy-blind live-by onset, otherwise the exchange
occurrence schedule. The resulting classes are 11 exact, 14 live-by-only and
146 schedule-only. The boundary-ledger SHA-256 is
`70f8b28749d8e1fd60e64af6e3ced41e556b2a20e5898ec692f0e4f7081b7c0a`.
Self-fill, fallback-bell and per-category fitted bells are explicitly excluded.

Stage 2 reverified every frozen tape hash and reapplied the exact V36
floor-pass witness law. All 171 events pass, zero are excluded, and zero
capture spans overlap the old recorder's July-30 through August-6 degraded
reconnect interval. The admitted event-list hash remains the seal hash. Two
authoritative clean builds are byte-identical.

Stage 3 did not start and the standing one-run authorization is not consumed.
The exact frozen sources prove that V35 and V36 hard-bind their 804-event
development base, while R3 consumes frozen V28/R2 development result ledgers.
None exposes an executable interface accepting an external event list,
boundary ledger and tape root. Creating an unverified adapter inside the exam
would not be running the three frozen brains unchanged. Therefore the lawful
state is fail-closed: zero exam-process invocations, zero brain invocations,
zero retries and zero score rows.

Before an exam may run, a population-parametric input adapter for all three
exact commits must be frozen and independently shown byte/semantic-equivalent
on the development corpus. The sealed 171-event population remains unscored.
