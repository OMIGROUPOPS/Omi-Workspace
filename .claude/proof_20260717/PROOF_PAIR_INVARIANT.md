# OUTCOME PROOF — C-SHELF-CONSOLIDATION Part 0: the pair-completeness invariant
Proven SHA: **431bfcaf**
Operator word (07-17, verbatim): "Every conceived game = 2 resting-or-refused-named bids, checked every cycle. A game with one leg resting and the other neither resting nor refused-by-name = DEFECT ping to the phone. This is the alarm that makes the half-drop impossible to return quietly."
Prior art (C45): PAIR-PRIORITY ENFORCEMENT 03e68139 (the half-drop killed at the seesaw) · C47-CONTINUOUS steady-cadence audit (the check's home) · SIGNAL LAW (DEFECT-grade ping) · OVER-BROAD LOCK class (why this is a FLAG + ping, never an audit-FAIL conception halt).

## Lane 1 — per-game replay (tonight's own book)
The invariant reads, per audit cycle, every 2-leg tracked event NOT gunned: each leg must be FILLED, RESTING (exchange-truth buys), or REFUSED BY NAME (the new `_refusal_named` registry banked at the single `_log` chokepoint — below_leg_floor / no_path_page / discovery_floor / corridor_w1_pref / selector_drop / gun_buy / conception_horizon / pair_seesaw / chase_cap / cycle_cap / aim_unresolved / skipped). A leg ABSENT while its sibling rests or is filled = `pair_incomplete_violation` + `flags` row `pair_incomplete` + one phone ping per event per boot (`/root/notify.sh warn`).
- Tonight's expected founding row: **GUNKUZ** (KUZ filled 5@59, the GUN side neither resting nor refused this boot on the postponed match) — the one-sided-sibling-unworked shape (the YIBYUN watch class) now alarms itself.
- Tonight's half-drop shapes (ALHYAZ/CHALIM/FOMALK/KAMNGU before the exemption) would each have pinged within one audit cycle — the alarm this dispatch orders.
- Gunned events are exempt (post-gun absence is the no-buy law); refused-named legs are lawful absences (the refusal IS the record).

## Lane 2 — behavior isolation
- Read-only + logging + ping: the invariant places nothing, cancels nothing, halts nothing (FLAG, never a failure row — the OVER-BROAD LOCK lesson applied at birth).
- Gated on `pair_invariant_enabled` (default true, DECREED-cited); `notify_script` path guarded (absent = log-only).
- The `_refusal_named` registry is a dict write at the existing log chokepoint — no new log events except the violation itself.

## Gate
lint + smoke via deploy/deploy_gate.sh (this file is OUTCOME_PROOF; OUTCOME_PROOF_SHA=431bfcaf).
