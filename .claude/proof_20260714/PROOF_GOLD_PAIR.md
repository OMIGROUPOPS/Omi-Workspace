# PROOF — C-GOLD-NOW v1 + C-PAIR-LAW v1 + AMENDMENT (one deploy; the seesaw is the thesis)

**Candidate SHA: 08e1c8ed** (pair-law orientation enforcement + seesaw lift + orientation tells fitter/reader + dark path-mode + dark sizing + realization ledger + consolidated clock; code across 609f58a0 → 2a505246 → 08e1c8ed, cited at HEAD).
**Prior-art (C45):** C-CONTENTION-LAW 1733bdd5 (the fitted pages this law reads) · the two witnesses that convicted the DROP bucket (week −$17.95; export −1.3% on 76% winners) · C-BAND-CLAMP make-it-stick chokepoint doctrine · `_untombstone_entry` free-the-leg lifecycle · the lazy-leg-1 / starved-partner lineage (join-trial night-1, ALTMED, MIRMAL) · the entry-blend arc (directional entry DEAD as a *taker* thesis — the orientation tells here are a *placement-shape* read, shadow-clocked, never a chase).
**Operator rulings, cited verbatim:** C-GOLD-NOW Part 1 enforcement DECREED · C-PAIR-LAW: "the entry unit is the pair, never the leg" · THE AMENDMENT: "the seesaw is the thesis, not the risk… DROP-AS-PAIR (reserved only for events where no orientation composes under 97 — the true garbage, not the hard calls)." Both amendments folded before the gate ran — one deploy.

## Part 1 (final semantics) — orientation-composition enforcement, LIVE tonight
Permission is per-event at the discovery chokepoint. An orientation = buy the riser near now + cast the faller deep at its page's fitted deep quantile (`depth_p75`); it composes when riser-now + faller-deep ≤ 97. **DROP-AS-PAIR fires only when NO orientation composes — the true garbage.** PAIR-COMPOSED and PAIR-NO-OPINION continue current live behavior on both sides. The chokepoint half in `place_order` covers the router and every re-conception path. **The seesaw guard (fitted): at entry and every re-aim, the sibling's LIVE book price is priced on its own page at the deep quantile — my bid + sibling deep-cast > 97 = unreachable → entry refused (`pair_seesaw_refused`) or the resting bid LIFTS (`pair_seesaw_lifted`: cancel-and-free via `_untombstone_entry`; re-conception re-passes the law — entry returns when the seesaw does, dynamic, page-cited).** One-sided fills stamp `pair_law_violation` at the emitter from tonight.

## The oriented read — SHADOW, its own clock
`orient_fit` in the nightly builder (walk-forward: tour < Jul 10; ITF live-era): per event pair, the fitted tells on the discovery hour — f_drift (hour-end vs first-hour median), f_range (hour-range position), f_flow (dog's share of pair prints); **spread asymmetry is SILENT in this corpus (candles carry no book) — named, not faked.** Truth = pre-onset drift ≥ 2¢. Operating point (named, v1): bucket n≥10, call at rate ≥0.65 / ≤0.35, else NO-CALL. Live `_orient_read` consumes ORIENT_V1 at discovery and logs `orientation_shadow` per event (TRADE-ORIENTED riser=X / NO-CALL, bucket cited). **The tells' accuracy accrues its own n≥300 clock before any oriented placement touches money.**

## The outcome replay (PAIRLAW_WEEK.txt, amended semantics) — LANDED
**THE VOLUME BRACKET, measured: blanket pair-viability cut 72% (245/878 events); the amended orientation law cuts 0% (878/878 enter — this week's cells hold NO "true garbage": a deep cast always composes under 97).** The two laws bracket the space; tonight's live enforcement is therefore risk-free on current cells (it blocks nothing until a genuinely non-composing event appears) — and the DROP-bucket bleed the witnesses convicted (−$17.95 week / −1.3% export) is NOT captured by the amended letter; it lives between the brackets, and the packet + the tells' clock decide where the line goes. **Naked singles 263 → DROP-AS-PAIR 0, seesaw-lifted 2, RESIDUAL 261 (named: orientations composed, seesaw never breached pre-fill — the fill-timing class, the completion machinery's jurisdiction).**
**THE ORIENTED GRADE (the tells' clock opens): 103 events graded, 8 called (8% coverage), 5 correct (62%) — n=8 of 300. Weak at v1 and said so.**
**ROVIEM Lane-1 (26JUL14, run individually — it postdates the week window): the operator's orientation IS in the tells' direction — dog IEM drift +1, hour-end at range top, market confirms (ROV 52→47) — but the fitted read is NO-CALL: 3 prints of tape, bucket `1|hi|lo` thin/absent. The honesty gate refused to call on that evidence; the exhibit's direction shows in the tells, not yet as a fitted call.** IEM tape absent → complement-mirrored proxy, named.

## Part 2 — the money half, DARK through the same gate
`trendpath_live: false` — path-mode coded (path-target entries, pair capped ≤97); flag-off = zero behavior; cutover = flag flip + boot audit. `sizing_live: false` — sizing v0 coded (contention-scaled 1×/2×/3× lots, drawdown-bounded at the PLACEHOLDER −1500¢ day floor — named; the operator's sizing word precedes arming), `sizing_shadow` logged, local qty override only. Lane 2: $0 by construction while dark.

## Part 3 — the clock automates itself
`analysis/consolidated_packet.py`, 2h cron (:40): one settlement pull, one tape pass, three pre-registered letters (PATH aims · REACH law on live aims · SELECTOR slate, leg-frame as pre-registered; the pair-law and orientation replays ride beside it, never silently swapped in). First n≥300 crossing on all three → RULING_PACKET.md + ntfy push (severity/title/message interface verified against /root/notify.sh). PACKET_STATUS.json feeds the nightly countdown.

## Part 4 — the realization ledger
Nightly headline: REALIZED-BY-SUBTRACTION — DROP-AS-PAIR refusals graded at what the legs did (dollars-not-lost), seesaw refused/lifted counts, one-sided VIOLATIONS, packet countdown n/300.

## Deploy gate
[1/2] lint PASS (at commit) · [2/3] smoke (gate-run) · [3/3] this file, OUTCOME_PROOF_SHA=08e1c8ed · [4/4] C50 push (both rulings verbatim in the vault; ONE-SIDED PERMISSION class founded) · [5/5] constraints surface.
