# OPERATOR RULING — SETTLEMENT VOCABULARY (2026-07-09, verbatim-as-relayed)

A market SETTLING = the game ending (universal, meaningless). A SETTLED POSITION = an exit that never filled — a FAILURE state, the F-bucket, never neutral bookkeeping. The doctrine lifecycle never touches settlement: fill W1 → cash W1/corridor → flat before resolution.

No report may use "settled" as a synonym for "concluded" when describing OUR positions; realized-P&L tables split "cashed via exit" from "rode to settlement" as separate columns ALWAYS.

(Part 1's "+$3.87 settlements" line = mostly failure-state residue by this definition — the healthy pairs' profits live in the EXIT fills, which is exactly why the recovery table looked empty.)
