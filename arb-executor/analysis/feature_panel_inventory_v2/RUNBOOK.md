# Eleven-block recovery — completed screen; history below

## 2026-09-13 — final CHALL publication verified; all four passes complete

The retry queue at `C:\tmp\feature_panel_inventory_v2\restart_20260912T1721Z`
finished MAIN twice then CHALL twice. All four pass exit markers and
`SCREEN_QUEUE.exit` are 0; queue PID18476 and final worker PID2692 are absent.
MAIN completed 1353/1353 evaluations per pass (928 primary +425 frozen June),
16688 gate receipts. CHALL completed 3245/3245 per pass (2287 primary +958
frozen June),27234 gate receipts; final pass elapsed11600.844s.

The unchanged guarded inventory publisher compared CHALL's full population
across both passes before writing `ATP_CHALL/`: DETERMINISTIC and exact
unchanged-FIRST parity in all60 side/gate cells. Its final results SHA256 is
`1b403de9659b3108f1d098f11b4208e7e43d3d63a81fbaea0a2bcc0f2057e3fc`.
The raw pass2 receipt SHA256 is
`b46ece9e6cae39decb25ac10b9a37e0a6cca873c793af73d952b1f4a0f8e3277`.
`ATP_CHALL_PASS1/` remains the historical provisional report; it is superseded
by this verified final publication, without deleting or changing its files.
`OPERATOR_REPORT.md` now presents both verified tours and the final verdict.

No variant qualifies under the filed rule; June remains separate and cannot
select. No full-cadence finalist is advanced. No new scientific test, engine,
face, input, or definition change was made to close this run. Raw private
extracts and per-query outputs stay outside git. After the compact reports
are committed and pushed, remove the completed 30-minute follow-up.

Engine pins reverified at completion:
- OS: `70b68856cb4f0329048f9ae951e01c19c7d7465e3cc0ee183e325ce6c1fb0346`
- Functionable: `83f44682de821f754e99ecfdc0b56fd4ac8a03d65b0ee986f39c47f7df4e6c74`
- Builder: `6136c8ca9cd2b6742f7a6235a27169cdc97b6b6543368a29d08f18c3185f5a8c`

## 2026-09-13 — MAIN published; CHALL first-pass report only

The operator requested publication now. MAIN pass1 and pass2 both exited0,
1353/1353 each,16688 gates. The guarded inventory publisher verified full
population determinism and all60 unchanged-FIRST side/gate cells. Public
`ATP_MAIN/` is final for this screen (not full-cadence proof); no qualifiers.
CHALL pass1 exited0,3245/3245,27234 gates. `ATP_CHALL_PASS1/` is explicitly
**DETERMINISM PENDING**, with validated sources and exact FIRST parity; it
does not authorize advancement. Do not mistake it for a final tour receipt.
The guarded final publisher is unchanged. The separate pass1 reporter exists
only for this requested interim publication and requires a `_PASS1` directory.

CHALL pass2 PID2692 and queue18476 remain active in the retry root below.
Latest completed evaluation at this handoff:2883/3245 (primary1925/2287 plus
frozenJune958/958); elapsed7923.75s at the last query. Now FIT_MONTH July,
967 training games. The ETA in QUERY_COMPLETE excludes this fit. Logs/model
files are not resumable per-query score checkpoints. Preserve the worker.
On completion: use the existing guarded publisher with pass2 and v1 prior
FIRST into `ATP_CHALL/`, then update the report's pending label with evidence.
Never rerun completed MAIN to accomplish this. Source/model/engine unchanged.

New face-only `build_lab_pressure.mjs` projects existing decision-stage reads
into compact `*.pressure.json`. No recovery averages joined onto named games.
No flow/maker estimate substituted for missing readings. The five existing
face/grade/oracle files are unchanged. LAB preserves the existing design;
the pressure panel explicitly labels carried state and non-author readings.

Verification this turn:228 feature-panel regression tests passed125.051s,
3 pressure projection tests, TypeScript, static build, headless Chrome at
480m and the GAS fill. OS/functionable/builder pins below remain unchanged.

## AMENDMENT 2026-09-11 — authoritative recovery (current order wins)

### 2026-09-12 17:18Z — interrupted screen, fresh retry

**18:23Z checkpoint:** retry MAIN pass1 completed exit0,1353/1353 evaluations,
16688 gates. Raw pass1 receipt SHA
`7e730b28da5c44b03d30c84ce0e9a9b9e898e6967b00d9bfe97d6de09c0bc3de`.
Read-only validate_screen passed: PRIMARY928/11518 gates; frozenJune425/5170.
Exact unchanged-FIRST parity passed all60 side/gate cells vs v1 scoreboard
`2ebbdf568d47d5cc77a27ba67e386d4ccf1b00f3e8125758fbb40899e0d09799`.
MAIN pass2 PID14416 alive (293/1353 at check), no errors. No tour-completion
report yet: wait for full second pass and deterministic comparison. CHALL is
queued after MAIN2. Old interruption remains preserved as described below.

The07:40Z queue15372 and worker11592 are both absent. Old queue lock is free;
no other Python screen worker found. No exit marker, Python traceback, or
completed score receipt exists. Last log:441/1353 MAIN evaluations,5497 gate
receipts, then FIT_MONTH June (436 training games), at07:50:06Z. Only April/May
MODEL/SCALES files are present. These are not a resumable score checkpoint;
do not pretend the441 logged evaluations survive as verified aggregate state.
All original logs/partials remain untouched. Cause is unestablished: the
Windows boot timestamp alone did not prove a restart; no matching error or
shutdown event was returned by the scoped log check.

Rechecked exact gate gzip53c4851a/receiptca39df33, recovery receipt770e2db9,
four scientific adapter/helper code hashes and all three engine pins: unchanged.
Physical RAM available7102064KiB, C: free5688946688bytes. No cleanup.
Relaunched the identical queue into a fresh private output root:
`C:\tmp\feature_panel_inventory_v2\restart_20260912T1721Z`.
Retry launched17:21:20Z: queuePID18476, MAIN pass1 workerPID8624. At17:21:48Z
worker alive,1.59GB private memory, no stderr/exit marker; validating inputs.
Interruption and fresh retry reported to operator in this17:18Z follow-up.
Its SCREEN_QUEUE.log/.err/.exit and pass1/pass2 subfolders are authoritative
for this retry; the earlier output root is retained as interruption evidence.
Same order MAIN twice then CHALL twice, one shared-cache atlas coordinator
(runner rejects8),512/128MiB caches,batch16. No reuse of incomplete score/model
outputs, no code/definition/engine changes. Check the retry queue/worker PID
in its log before any future restart. Report MAIN after both complete passes,
population determinism and exact FIRST parity; do not wait for CHALL to report
that milestone. Completion publication still requires all four passes.

### 2026-09-12 completed recovery; gate export in progress

**Update07:32Z: gate export completed07:31:48Z, PID2068053/code0.**
3215 pairs =928 MAIN +2287 CHALL;26 initial-price ties excluded as in FIRST.
Transferred only derived gate gzip14961827bytes SHA
`53c4851ad03bbe86e82555c5dc13075ec7e150a0a56718f4b7a385de45b86273`,
receipt SHA `ca39df332501d67739cc2a562c8d431fd3d7ce8a5068d084bca615f7bd6bbeca`
to `C:\tmp\feature_panel_inventory_v2\`. Local hash/size/status/counts passed.
Both-category --verify-only preflight PASSED, exit0: MAIN928/1856 legs and
CHALL2287/4574 legs;1056664 positive witnesses,6482 source/witness/auxiliary
legs reconciled. One original source object remains explicitly unverified
in the unchanged v1 lineage (one book-wake exclusion), not silently repaired.

**Screens launched07:40:33Z.** Hidden detached PowerShell queue PID15372;
`C:\tmp\feature_panel_inventory_v2\SCREEN_QUEUE.log` / `.err` / `.exit`.
Order MAIN pass1, MAIN pass2, CHALL pass1, CHALL pass2. Per-pass logs:
`ATP_MAIN_pass1.log` / `.err` / `.exit` etc. Private outputs:
`pass1\ATP_MAIN`, `pass2\ATP_MAIN`, `pass1\ATP_CHALL`, `pass2\ATP_CHALL`.
One shared-cache coordinator,512MiB training/128MiB exact-state cache,batch16.
C: free3664719872bytes before launch; no cleanup. Do not relaunch while
queue/child owns its lock. It will stop at any failed pass, no automatic
publication/commit. All four complete results plus FIRST parity required.

**Verification amendment (execution-only):** use
`feature_panel_inventory_verify.compare(pass1,pass2)`, not generic
`feature_panel_run.compare_runs`. Both retain exact scientific results. The
generic verifier overlooks that ATLAS_SCREEN_RESULTS.receipt_sha256 depends
on the receipt's elapsed-runtime field. The new verifier first checks each
raw hash link, then substitutes the canonical receipt SHA (elapsed only
excluded); no scientific hashes/clocks/weights/results are dropped. Tests
reject bad links, changed scores/clocks/binaries/file sets. Publisher now
REQUIRES `--second-pass <private pass2>` AND `--prior-first <v1 tour output>`;
it verifies determinism before any publication and stores that proof in the
public receipt. This does not change screen definitions or fitting code.
Verifier SHA `baecbb8ab241a5370a775f20997fcfb49f192edadb81af0190d95a302f18c772`;
publisher SHA `13d6b9d5b6e5194b30850440d488cac5a8e7c0ad39bdeff388bd84de97056f91`.
Five new verifier/publication-guard tests passed; prior223 regression tests
remain the scientific-code proof. No claim of population determinism yet.

Recovery PID2067453 exited0 at07:01:53Z. Full receipt SHA256
`770e2db9c165f479f8afd52d31aef119a56b9e8d05420bacae9fd1e7a35db1a7`.
Independent read-only nice10 audit rehashed every one of6482 parts, verified
sizes/ready metadata/full bindings and6482 unique tickers, and reconciled all
part and month/tour partitions:1056664 positive prints =693025 API-authoritative
+227076 arithmetic-only +136563 unresolved +0 confirmed off-book. No failures.
Unknown block status is not confirmed on-book; arithmetic-only never feeds B3.
The compact receipt is locally at
`C:\tmp\feature_panel_inventory_v2\RECOVERY_RECEIPT.json`; raw parts stay remote.

Gate PID2068053 began export07:01:53Z, reached1050 oriented pairs by07:10Z,
and remains active. Its old GATE_RUN.exit is from2039036, not this run.
Do not consume the gate gzip until its full verified receipt exists. Screens
have NOT started. C: free3.70GB at07:12Z; no files deleted. Expected gate pair
universe is928 MAIN +2287 CHALL; tied initial-price pairs are not oriented
queries, matching the unchanged baseline. Earlier launch notes below are
historical, not the current process state.

### Latest continuation — interrupted transfer, exact resume

Old recovery2033698 failed22:58:44Z at1595/6482 with gzip EOF on the next
leg's source read. Fresh reads of BOTH referenced BICMON-MON objects passed
gzip/Content-Length/filed MD5 ETag; see TRANSPORT_RESUME_20260911.json. This
supports an interrupted transfer, not a license to exclude the source.
Resumed exact code/CLI/output with PID **2067453**, nohup/nice10. Existing1595
ready parts are rehashed/binding-checked and reused; failed leg starts clean.
Confirmed resumed worker passed the failed leg and reached1598/6482; API
requests4 at that checkpoint, so the1595 reused legs did not refetch labels.
Failure state/exit preserved in amended_production/interruption_20260911T225844Z/.
An old RUN.exit may remain while the resumed PID is alive: compare marker PID
with RUN_STATE/current process; never mistake that prior exit for a new failure.
No source/code/model change; do not blindly resume an incomplete source object.
Old dependent gate2039036 failed safely on missing full receipt. Requeued as
PID **2068053** after confirming the new extractor owns WORKER.lock; same
code and amended_gates_boundary output, old failure preserved under
dependency_failure_20260911T225844Z/. Check the marker's PID here too.
Checkpoint replay's logged legs/elapsed and ETA include reused legs, so they
are not a new extraction-rate estimate. Notes below retain
initial launch PIDs for provenance; this section is the active continuation.

The operator approved Astra's verification amendments. Do not use the older
arithmetic-substituted `production/` as a scientific extract. Old PID2014102
was terminated after753 hash-ready legs; all complete source checkpoints were
preserved. They can supply verified books, never old inferred flow labels.

Current recovery code directory on droplet A:
`/mnt/omi-trading-data-nyc3/library/feature_panel_inventory_v2/code_amended_f8bc227e/`.
Current output: sibling `amended_production/`. Check its `RUN_STATE.json`, PID,
`RUN.log`, `RUN.exit` and `RECOVERY_RECEIPT.json`; an exit marker alone is not
completion (clean checkpoint stop can return0 without a receipt).
Live PID at launch: **2033698**, nice10, nohup; parent2033696 is the detached
launcher shell, not a second extractor. At319.123s it had529/6482 legs;
this warm-start rate reuses prior books and is not a full-run ETA.
Latest verified handoff checkpoint:880/6482 complete at1032.976s; extractor
alive at nice10. This is still an incomplete scientific extract, not a table.
Frozen extractor SHA `f8bc227e9bc5b743cb677e300bc61a4f5db92531e3cd6feb7cb53badc3bfca29`;
public client `c221ec82a920e4c879dab1f98d1685a100cf2dfffbb16cd4b8b0f3547f78d275`;
direction helper `690e609f471921dda9444c3a00d60d04972cbe3d39a638c14c498e428359bec3`.
Never edit these running remote files. Any fixes require a new code binding.

CLI is the original recovery CLI below, with new code/output paths and
`--reuse-recovery <root>/production`; same transport limits. Runs detached
under nohup, nice10. SIGTERM or an output `STOP_AFTER_LEG` marker finishes the
current leg atomically. Remove a stop marker only when operator-authorized to
resume; verify no duplicate worker. Do not restart WS.

Amended rules:

- API canonical `taker_outcome_side`/`taker_book_side`, exact unique identity,
  and explicit `is_block_trade=false` are required for authoritative flow.
  Fetch pages for all positive prints, even when arithmetic is classifiable.
- Arithmetic remains a separate estimate with a top-quote-change-in-print-
  second flag and completed-second availability. It never substitutes in B3.
- Report exclusive API / arithmetic-only / unresolved label counts by month
  and tour, plus off-book excluded, so totals reconcile to original positives.
  Library paths/counts/spans/targets stay immutable; exclude off-book executions
  from the new flow/maker features, not silently from the baseline library.
- Maker is end-start+on-book executions, per level/both sides/distance from
  receipt last. Missing consumed-side/block/interval attribution stays missing.
  Last TWO OBSERVED snapshots are required, not last two changed states.
  Fresh V3 outputs include all observation epochs; reused V2 parts require the
  hash-bound v1 feature part's `book_observation_epochs` joined by ticker/span.
- CSV asks are already YES asks: verified writer live_v3.py converts NO
  snapshot/delta prices with100-price before writing ask_i. Writer hash
  `f1a54d3c939bafa8c7c43ab23d9fd88f99ccf56ed97f30aed8cd8a98dae66f93`;
  exact April deployed revision is unfiled. Do not invert CSV asks again.
  New maker/imbalance/queue features fail closed on locked/crossed captures;
  retain explicit invalidity diagnostics, no guessed sequence reconstruction.
- Entire WS integration DEFERRED_NOT_CHEAP:406 archives/6,570,463,552 bytes;
  measured safe decode48.892s/object implies roughly5.5h, alongside the source
  recovery on1vCPU. Explicitly exclude `ws_20260623_08.jsonl.gz`: truncated gzip
  EOF; old WS PID2019044 exited1 after3 completed objects. No partial WS result
  feeds this screen. B11 snapshot residuals still run. Decoder/exclusion tests
  are code readiness, not WS reconciliation evidence.

New code (local; verify final hashes before remote gate deployment):
`feature_panel_inventory_gate_extract.py` builds compact exact15-gate states
from completed amended recovery + sealed spool + legacy observation parts.
It streams one pair at a time, preserves first-observation/orientation/span,
and writes no raw prints/IDs in gate outputs. Uses a baseline receipt's
organ_contract for gates/minute_seconds; never hard-code a new grid.
Queue Q is deliberately deferred to the desktop adapter so fitting/PRIMARY/
frozen June use their own unchanged-FIRST universe.

Final gate code has been uploaded independently to
`<root>/gate_code_amended_boundary_1919f82e/` (do not use old gate copies in
extraction directories or gate_code_amended_20260911). Gate exporter SHA
`1978bcc675d8090e06df9ebed5a601cad8c88f72ba5ee668c3f11fdcc4ad5618`;
feature functions `01c2c71f5ebd1e55361d5819ce8e07e491cbb753a4bb3adeee51f691f9f2eda6`;
book functions `1919f82e134970c8f2c456ddf5e55dc836757f387023bef04a30c74cf454ef19`.
Independent audit found and fixed a fractional boundary-second attribution
bug: at receipt120, a known print100.5 cannot be ignored when the last snapshot
is labeled100. Affected residuals are now missing; at receipt100 that future
print is not consulted. Receipt cutoff is explicit and reported. Targeted
maker26 tests and gate4 tests pass. Final regression: **223 tests passed in
119.281s** after this fix, including the two-pass adapter fixture and exact
unchanged-FIRST fixture. This is not population determinism evidence.
Same directory contains public `TUNE_BENCH_RECEIPT.json` SHA
`03a2fb61a5e6c6396ab6b16efca0b256e6b44d76df8f7d7d10975c3ed10e5a8b`,
whose organ_contract supplies the exact gates/seconds. No private audit metadata
was uploaded. Queue/adapter tests50 passed12.430s and full regression219 tests
passed119.922s before the independently audited boundary-second amendment.

Dependent gate job PID **2039036** is detached under nohup/nice10. It waits
on amended_production/WORKER.lock (single batch dependency), then requires the
complete hash-bound amended receipt before exporting. Output directory is
`<root>/amended_gates_boundary/`, with GATE_RUN_STATE.json, GATE_RUN.log and
GATE_RUN.exit. Old waiting gate PID2036360 was terminated before any export;
its old amended_gates/ directory is preserved, not a result source. Do not
restart the extractor: PID2033698 and its three frozen code files are unchanged.

Gate CLI: `--library ... --spool ... --recovery-receipt .../amended_production/RECOVERY_RECEIPT.json
--recovery-parts .../amended_production/parts --legacy-source-parts .../feature_panel_v1/production_sources/parts
--baseline-receipt <copied public corrected ATP_MAIN receipt> --categories ATP_MAIN ATP_CHALL
--out <new private>/INVENTORY_GATE_INPUTS.jsonl.gz`.
It writes sibling `INVENTORY_GATE_INPUTS.jsonl.receipt.json`. Full recovery
required; smoke is deliberately refused. Copy only these derived compact
inputs plus receipts locally; no original archives/prints/IDs.

Desktop launcher: `feature_panel_inventory_atlas_run.py`, all existing atlas
arguments plus `--inventory-gates` and `--inventory-receipt`. The new adapter
registers23 variants FIRST/C1-C11/L1-L11, exact cache keys including active
member-universe freeze; B3 authority-only overrides and its interactions;
new B7-B11. Publisher `feature_panel_inventory_atlas_publish.py` adds full
monthly lineage coverage. Existing v1 launch arguments in lower section;
use new output directories, not old scores/models. `--verify-only` first.
Both full-universe tours, June separate; original criterion and optimizer
outcomes unchanged. Require full population determinism twice and exact FIRST
parity before publication; synthetic tests alone are not that proof.

Prepared but NOT launched: `feature_panel_inventory_screen_queue.ps1`
`-GateFile <verified local compact gzip> -GateReceipt <its receipt> -OutputRoot C:\tmp\feature_panel_inventory_v2`.
It runs MAIN pass1/pass2 then CHALL pass1/pass2; hidden child windows, one
shared-cache coordinator per screen, explicit log/exit markers, exclusive queue
lock. This is the established atlas execution mode, not the earlier full-
receipt 8-worker mode. It refuses existing screen destinations; inspect any
partials before a clean new-version retry, never delete them without authority.
Run launcher `--verify-only` first with identical arguments. After runs,
`feature_panel_run.compare_runs` must prove each tour's two full screens.
Then versioned publisher --prior-first points to the v1 atlas run for exact
FIRST parity. Check1GB-ish local free disk before copying/running; no cleanup
is authorized. Raw per-level/print ID extract remains only on the droplet.

Tests during amendment:213 feature-panel regression tests passed125.673s,
then219 passed119.922s before the final boundary fix;
final two-leg remote smoke406 positives:275 API,26 arithmetic-only,105
unresolved,0 off-book. Smoke is NOT population coverage. Real pair gate smoke
passed, then revealed crossed book diagnostics; final gate/code tests must
include the fail-closed patch. Do not claim completed screens yet.

Handoff safety check:OS70b68856cb4f0329048f9ae951e01c19c7d7465e3cc0ee183e325ce6c1fb0346,
functionable83f44682de821f754e99ecfdc0b56fd4ac8a03d65b0ee986f39c47f7df4e6c74,
builder6136c8ca9cd2b6742f7a6235a27169cdc97b6b6543368a29d08f18c3185f5a8c unchanged.
C: free1,017,241,600 bytes; check space again before compact transfer/screens.
No deletion authorized, no code/result commit yet.

Only after complete verified outputs: commit `recovery (amended) + eleven-block screen`,
push, coverage tables first, both screens/criterion/optimizer and Astra verdict.
Quiet30-minute follow-up updated to this amended order. No raw/private data,
unrelated WIP, or engine edit. The skill used to update the existing follow-up
was OpenAI Docs; it did not alter the scientific workflow.

## Earlier setup (superseded where conflicting)

Worktree `C:\Users\omigr\omi-w1-face`, branch `face/window1-watch-20260903`.
Baseline `d29e9d83` is the six-block atlas screen (no qualifiers). No new result
has been committed. Preserve unrelated `package-lock.json`,
`window1-watch/face_contract.mjs`, and all other unowned WIP.

## Operator-approved definitions

- Eleven blocks: original B1–B6; B7 imbalance, B8 queue, B9 spread behavior,
  B10 spike/reversion, B11 maker. B3 gets recovered taker flow.
- Missing trade ID: UNIQUE exact ticker / recorded second / YES price / size
  match only. Multiple matches remain unresolved. No fuzzy/nearest join.
- Preserve observed aggressor; arithmetic is a lagged-book estimate. API is
  the resolver for arithmetic-ambiguous rows. Report agreement separately.
- Queue uses unchanged FIRST Q for query AND each member at its own receipt.
  Displayed depth is not queue priority. Use the same current Q at both clocks
  when computing decay, so a moving reference level cannot invent decay.
- Spread rate and queue decay use the previous ORIGINAL receipt (native pair
  changes plus atlas), not the previous sampled gate. Spike uses the latest
  completed minute. Earliest tied largest move; retracement not clipped.
- Refill ratio = positive net additions / executed contracts at the same
  observable levels and snapshot interval. Zero denominator => STORE SILENT.
  Below previous bid and above previous ask separate; net adds/pulls beside.
  Net residual = current displayed - previous displayed + executed contracts.
- Quiet 30-minute completion follow-up approved; resume verified checkpoints,
  finish screens, commit/push only after verification, report only completion,
  failure or needed decision. Disable afterward.

## Current code and tests

New uncommitted files in analysis: `feature_panel_taker_recovery.py`,
`feature_panel_public_trades.py`, `feature_panel_inventory_extract.py`,
`feature_panel_book_recovery.py`, `feature_panel_ws_recovery.py`,
`feature_panel_inventory_features.py`, `feature_panel_recovery_audit.py` and
three corresponding test modules. Twenty new tests passed. The feature
functions are NOT yet integrated into a bench launcher. The full feature-panel
regression suite passed: 189 tests in 107.139 seconds on 2026-09-11.

## Inputs and remote jobs

SSH `root@104.131.191.95`, BatchMode, ConnectTimeout15. Run nice10. Remote root
`/mnt/omi-trading-data-nyc3/library/feature_panel_inventory_v2`; code in `code/`.
Never edit code in use by a production worker; version fixes explicitly.

Library `/mnt/omi-trading-data-nyc3/library/RANGE_OVERLAP_LIBRARY_TICKS.jsonl.gz`:
`c823d1725cf4af8b561eabaae48fc540379fa774571cc3fdea49e5ffcd052461`.
Receipt `533c3271d8734caa6f1ccd7dc36563a6726a4c540060d3a1b33cfa011596a282`.
Read-only sealed spool (NOT live store):
`/mnt/omi-trading-data-nyc3/library/feature_panel_v1/production_spool/PREPARED_SOURCES.sqlite`,
SHA `a09e25084cb78af08d24660373502c7ca453930477f13ec13e93bc0a7e102fa0`.
The spool already has exact cutter-deduplicated rows/rowids; never dedupe again.
Source receipt `.../feature_panel_v1/production_sources/FEATURE_EXTRACT_RECEIPT.json`,
SHA `c52187b9c03405fda698c6830b22bc6c7b61b81e0baf1215a908b41a93a8b1fe`.
Frozen core `code/feature_panel_extract.24bfeb8f.py`, SHA
`24bfeb8f39157d0a1ddcf72cc1e173f84a75cfc4af3793bff47daf7b955d081f`.
It validates final counts and every library path volume point.

Original/taker worker launched detached PID **2014102**, output `production/`:
`RUN.log`, `RUN_STATE.json`, `RUN.exit`, `parts/`, then `RECOVERY_RECEIPT.json`.
Total6482 legs,1056664 positive prints. Check live process, not historical PID
alone. Same CLI/code binding resumes per-leg hash-ready checkpoints.

```
feature_panel_inventory_extract.py
--library <library above> --spool <spool above>
--source-receipt <source receipt above> --core <core above>
--repo /root/Omi-Workspace --out <root>/production
--pause-seconds 0.5 --page-limit 1000 --retry-limit 5 --timeout 60
```

These are transport limits, not scientific cutoffs. `smoke/` passed two legs:
406 positive prints;104/104 arithmetic/original,78/78 arithmetic/API,275/275
original/API agreement. Not population coverage. The first100-ish legs are
April; do not project a final coverage rate from them.

WS inventory `/tmp/spaces_ws_depth.lst`: Jun23–Jul10 has406 archives,
6570463552 compressed bytes. Stream; never save raw archives locally.
`ws_smoke/` is an obsolete successful eager-materialization smoke: DO NOT use
as production (gap invalidations were added afterward). `ws_smoke_final/`
passed: 13,890 valid library-second states on 114 legs; source size/MD5/SHA and
stored identity/clocks verified. Batching took 48.892 seconds versus 143.177
for the eager version. WS production launched detached PID **2019044** into
`ws_production/`; check it before any relaunch. Same library/source-receipt/core/repo and
`--inventory /tmp/spaces_ws_depth.lst`; omit `--limit-objects`.
WS checkpoints entire objects transactionally, including decoder state, only
after CRC/size/MD5/SHA verify. Gaps/restarts emit explicit invalidation rows;
never carry stale depth through a gap. Actual local WS receipt clock retained.

Desktop private v1 inputs remain `C:\tmp\feature_panel_v1\`, unchanged.
Do not copy its locally pinned odds receipt remotely (earlier transfer denied).
Desktop free space fluctuated from 0.47 to 1.01 GiB; no deletion authorized. Keep full derived
per-second data remote and create compact exact-gate feature sidecars before
copying. Droplet last had9.3GiB disk,~1.2GiB available RAM. Monitor both.

## Remaining work (do not report extraction as a screen)

1. Both production extracts launched; check current PID, exit marker and
   hash-bound checkpoints before any resume. Verify all source
   recovery hashes, accepted/positive counts, coverage and API agreement.
2. Finish per-second maker derivation and WS/snapshot overlap reconciliation.
   Snapshot ET-second clocks and WS actual receipt clocks are different: name
   both; don't claim equivalence. Boundary-second prints have unknown ordering
   relative to snapshots; affected residuals are missing (tested).
3. Build compact gate inputs REMOTELY from recovered parts: current and prior
   ORIGINAL receipt books, positive completed-minute features, maker metrics,
   recovered flow. V1 book_observation_epochs retains unchanged refreshes;
   don't mistake last changed book for last actual observation. Preserve exact
   member/mtb/span hash keys. No rounded cache keys or rederived spans.
4. Create versioned adapter/launcher for B1–B11, C1–C11 and L1–L11. Keep v1
   code/artifacts intact where feasible. Missing NaN, neutral likelihood with
   coverage reported, no member deletion. B7 depth imbalance relates to B3
   depth ratio; B9 spread duplicates B2: record collinearity, don't overread LOBO.
5. FIRST queue Q comes from conduct_scoreboard_v2.ReceiptProjector, not new
   model Q. In frozen June, freeze that reference's member library too. Cache
   must include universe/freeze binding, so primary June outcomes cannot leak
   into holdout queue features. Same causal category/day/self exclusions.
6. Reuse feature_panel_atlas*, optimizer reporting, non-June selection. Full
   MAIN928 + June425; CHALL2287 + June958;15 filed gates. June strictly frozen
   weights/scales/library, report-only. No named-check fitting.
7. Report every block/month optimizer outcome, missing April/May inner
   validation explicitly. Filed n>=100,strictly closer>=half criterion loaded
   from receipt. Matched scorable cells + all-eligible coverage. Only qualifiers
   advance to full receipt-cadence/full-universe/two-pass proof.
8. Full tests, causal counterexamples, determinism, unchanged FIRST comparison,
   engine hashes and no tune/raw data in Git. Publish coverage first, then both
   screens, June separate, criterion and optimizer tables. Commit only task
   code/receipts/compact summaries: `recovery + full-inventory atlas screen`;
   push. No raw prints/depth/API ID sidecar, secrets, unrelated WIP or partials.
9. Astra conclusion: a null block test does NOT identify an irreducible tape
   ceiling. Report attained FIRST error and filed diagnostic bounds honestly.

## Engine pins

- OS `70b68856cb4f0329048f9ae951e01c19c7d7465e3cc0ee183e325ce6c1fb0346`
- functionable `83f44682de821f754e99ecfdc0b56fd4ac8a03d65b0ee986f39c47f7df4e6c74`
- builder `6136c8ca9cd2b6742f7a6235a27169cdc97b6b6543368a29d08f18c3185f5a8c`

Public docs: Kalshi historical trades, historical_data, order_direction,
websockets/orderbook-updates. Cutoff API returned trades_created_ts July13;
use actual hash-bound response, not this note, as proof.

Automation `finish-eleven-block-feature-recovery-and-screen`:30-minute task
heartbeat. Desktop-scheduled; recovery workers themselves are droplet-detached.
