# OUTCOME PROOF (C46, two-lane) — C-PERCAT-GUN v1 (the seventh-time class's class-kill candidate; SHADOW ONLY this deploy)

**Candidate SHA: `bbcb613c`** (the shadow code was authored at ae623bcf and is byte-identical through this HEAD; the intervening commits are the closed-out range-layer/viewer builds — analysis-side, already C50'd; re-cited at HEAD for the gate's staleness check).

## Prior art (C45)
- **CLASS — GUN FIRE-STARVATION (CLASS_LEDGER.md)** — seven instance-fixes on record (volume-burst prematurity 06-19 · fused gun 6d84f27e · gun persistence · te_live resurrection + lock · truth-join · fallback bell · per-instance containments); each fixed an instance, none shaped the trigger to its market.
- **VOLUME_LEDGER 07-09 (M3/M13)** — THE fitted thresholds, one source of truth, verbatim: ITF OPEN ≥6 prints/30m (fill crosses 20–50%) · CHALL ≥16/30m (fill 17–30%) · **MAINS: the gauge stays OFF — fill flat ~1% at every volume state ("volume does not open mains") — their fitted line IS off.**
- **Constraint #11** — no decreed constants: every threshold registry-cited; nothing invented.
- **MIGRATION DOCTRINE** — shadow → graded one night vs the live gun → cutover on the operator's word → the legacy burst constant DELETED. No exceptions for urgent nights.

## LANE 1 — MECHANISM (replay-harness law: the July-8 post-gun forensic tapes, coverage before vs after, per category)
| cat | n (Jul-8 tracked) | fire coverage BEFORE | coverage AFTER (fitted thresholds) |
|---|--:|--:|--:|
| ITF_M | 73 | 14% | **100%** |
| ITF_W | 56 | 7% | **98%** |
| ATP_CHALL | 25 | 24% | **96%** |
| WTA_CHALL | 10 | 40% | **100%** |
| mains | 2 | 0% | 0% (gauge OFF — the fitted line) |
The 73–83%-silent ITF cohort dies on the fitted thresholds. **The class-kill claim = this coverage + nightly BELLS-MISSING at zero — claimable only AFTER the shadow night grades and the cutover arms.**
- Shadow mechanics: trailing-30-min prints per event (the 30-min `_trade_prices` window), threshold per category from config (registry-cited), `percat_gun_shadow {would_fire, prints_30m, threshold, live_gun_source, live_gun_delta_min}` once per event per boot. ZERO behavior: no stamp, no cancel, no refusal — logging only; the scorecard grades the night.

## LANE 2 — SETTLEMENT P&L
$0 claimed. Shadow only.

## Regression watches
percat_gun_shadow fires/night vs live gun (delta distribution = the cutover evidence) · premature-fire check vs tape onset (scorecard) · mains must produce ZERO shadow lines (any = defect) · cutover decision = the operator's word on the graded night.
