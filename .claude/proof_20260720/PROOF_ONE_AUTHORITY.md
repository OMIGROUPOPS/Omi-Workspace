# OUTCOME PROOF — THE ONE-AUTHORITY GATE (deploy #3, CONSOLIDATED)
Candidate: `0585d8d6` (one-authority law `7c09adaf` + gauge-label rider
`b6ecedc9` + re-anchor duty `8679643d` + the INVENTORY LAW / tmux receipts
— the gate was killed twice mid-smoke and consolidated: ONE gate, ONE
restart, the deploy-law mirror intact). C46 two-lane, judged separately.

## LANE 1 ADDITION — the inventory law
The authority reads the book before fishing: a held (≥ lot) sealed-class
leg resolves SEAL-HELD — its fish is REPLACED, the chokepoint refuses any
stack (`hold_replaces_fish`), and the re-anchor duty WITHDRAWS resting
stacks on holds instead of re-pricing them (`authority_hold_as_is`, with
held basis + sibling fish + the honest combined printed). Replayed against
today's book: the Shick hold (5@48) under the duty draws NO new buy and NO
re-priced stack — its sibling (Noguchi) fishes at its own sealed number;
the pair completes at 48 + fish, graded honestly. No averaging-down, no
doubling — by construction, not by advice. The tmux receipts (ruling doc)
prove the stop/boot signal path against the actual book-holder lineage.

## LANE 1 ADDITION — the re-anchor duty (sweep dispatch)
The duty converts the audit from flag-only to flag→heal: the three named
overpay games' sealed-class resting bids (and any future leak) re-anchor to
the fish within 2 cycles (~2 min), through the chokepoint where every
standing gate re-checks; refusals retreat pair-aware; era-orphans withdraw
on engine lineage only (3 cycles); unattributable orders are flagged and
NEVER touched (the manual-book law). **THE SWEEP the operator ordered IS
this code's first post-boot cycle** — one owner, no side script, census
line (`authority_sweep_census`: touched/held/retreated/orphans/cents
removed) on the panel. Queue wisdom is structural: the mismatch filter
touches only wrong-priced orders — at-number orders never churn.

## LANE 1 — MECHANISM (primary, luck-free)

### The founding charge, replayed deterministically against the day's own book
The defect this gate closes is ON THE EXCHANGE right now, reconciled from
truth (`SEAL_BOOK_RECON.md`): three sealed-class legs held above their
sealed numbers — Shick 5@48 vs fish 37 (**+11¢**), Ilagan 5@39 vs 29
(**+10¢**), Trotter 5@73 vs 70 (**+3¢**) — 120¢ of overpay at 5sh, every
one placed by the legacy `path_aim` authority. Replay of the chokepoint
clause against those three placements: all three would have been
**clamped to the fish at placement** (`authority_clamp`: 48→37, 39→29,
73→70) — the construction delta is exactly the 120¢ the book bleeds today,
per game, deterministic, no settlement involved.

### Structural coverage
- `place_order` is the single placement chokepoint for every organ
  (path_aim, orientation-park, window-truth joins, sibling reposts,
  completion) — the clause governs them all at once: SEAL-owned games
  cannot be priced by anything but the seal, by construction.
- LEGACY-owned games byte-identical (authority stamped, price untouched).
- The 60s audit closes the loop on RESTING state: any pre-law bid or any
  future leak = `authority_mismatch_defect`, RED, every cycle. Expected
  first-night behavior: the three named overpays' sibling bids and any
  sealed-game legacy bids flag red until reposts re-anchor through the
  clamp — the meter that proves the subordination is working.
- Deploy-law mirror satisfied IN THIS GATE: the same push that armed the
  seal's authority subordinates the legacy paths (the addendum's
  requirement, founded).
- Gauge-label rider: `pair_class_read` now stamps the ruling's frame
  (combined-vs-par primary; mastery meter named) — superseded-frame defect
  retired from the operator-facing surface.

### Smoke
The gate's smoke replay exercises the chokepoint clause + resolver on the
recorded slate hour with `one_authority_enabled` true in the deploy config.

## LANE 2 — SETTLEMENT P&L (secondary)
No settlements involved in a placement-governance law: **NOT APPLICABLE —
no claim** (C46). The panel's `seal_overpay` and `authority_mismatch_defect`
lines are the accruing evidence stream.

## Verdict
Lane 1: the law's replay against today's own book converts 120¢ of real,
named overpay into clamps at placement, and the audit makes any future leak
red within 60s. Lane 2: no claim. DEPLOY.
