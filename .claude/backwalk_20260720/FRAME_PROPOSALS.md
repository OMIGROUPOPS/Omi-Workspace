# FADER FRAME PROPOSALS — the depth family is exhausted (v1+v2 walls: no
# variant clears combined anywhere). Next candidates are NEW FRAMES,
# proposed here per the dispatch: fitting ground = graded_pairs_v2.jsonl
# (the wall bank); judge = the MASTERY METER; nothing arms without
# clearing combined on unseen walls AND the operator's word.

## Candidate 1 — TIMING-LED: the deceleration cast
The depth family priced WHERE the fader lands and missed WHEN. Receipts:
the floor lands post-ramp in the last ~20 minutes (pair-story 07-07);
loop-7 fallers overshoot every projected terminal while still moving.
**Frame:** arm the fader cast only when the fall DECELERATES — trailing
median slope rising through −1¢/30min after a ≥5¢ decline — then fish
the local divot of the fallen price (current med − called-band dip_p50),
not a projected terminal. The cast chases nothing; it waits for the
knife to stick and buys the bounce-back level.
**Fit on the bank:** per-pair nets + close paths give slope/terminal
pairs per band; judge dual-negative (mastery) then combined on unseen
walls.

## Candidate 2 — BOOK-LED: the collapse join
Fader fills in the depth family were bid-side fishing; the post-fill
classifier's standing receipt says decisive fader liquidity arrives as
ASK-SIDE REPRICE, not bid-side dips (ITF_W 67% REPRICE; NO_UNDERCUT the
strongest class). **Frame:** join the falling leg's declining best bid
(bid+0, queue-aware per the wall law) only while the ask reprices
downward faster than the bid retreats — ride the collapse rather than
guess its floor. **Fit on the bank + the subsecond store's
book_transition era ONLY (JUL 12+; decision-grade tape — the era
boundary is named, this frame cannot be fit on backfill fidelity).**

## Candidate 3 — RECOGNITION-LED: conviction-gated full depth
The depth family hedged conviction with shallowness (net_med+2) and paid
for it 131/131. **Frame:** cast ONLY when the recognition re-call names
a faller band at purity ≥ 0.7 (a higher bar than the 0.5 live
convention) AND that band's net p25–p75 straddle is narrow (< 12¢ —
the terminal is predictable, not just the direction); then cast at FULL
net_med depth, no shallow offset. Depth follows conviction instead of
substituting for it. **Fit on the bank:** recognition-cell purity vs
realized terminal dispersion per band is directly computable from the
graded pairs.

## The law over all three
Violent-faller REFUSE stands above every frame. Each candidate fits on
the wall bank, walks the rolling-origin walls unseen, reports
combined-primary + mastery per wall — and arms NOTHING without clearing
combined out-of-sample and the operator's word. The mastery meter is the
drill's nightly grade either way.
