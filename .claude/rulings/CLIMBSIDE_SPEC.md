# CLIMB-SIDE SPEC — the T-90 resting posture (Plex T3 reconciliation; committed 2026-07-09; STEP 1 P3)

**SUPERSESSION CLAUSE: this spec SUPERSEDES the riser package (riser_post_revision, disarmed 07-06 by its own pre-registered rule). The disarm ruling STANDS until Plex re-rules on THIS document.**

## The posture
1. **Scope:** climb-side legs (cells 75–94; 50–74 shallow variant) on volume-qualified matches (lifecycle step 1; staged qualification per STEP1 — placement-light, hold-hard).
2. **Placement: aim RESTING from T−90** (before the p25 climb start at T−70/−81) at the FITTED level: `own W1-close-projection − edge_p50(cat, cell)` per `recut_cells.json` (volume-stamped rows; thin-tape flagged). The bid must pre-exist the ~3-minute median dip window — placing on the signal is structurally late (EXTENSIVE P3).
3. **Hold: aim-then-HOLD.** The level is fitted, not walked. Through volume-onset ±15 min the bid HOLDS (the dip prints 11–15 min before onset at median). Downward re-aims on sibling-basis arrival remain law (C42); **upward revision does not exist in this posture.**
4. **Stand-down: no fill by onset+15 → the climb owns the tape** — revert to join-at-close posture or skip. NEVER chase the ascent (cost of missing: med 17–21¢, p75 32–33 — priced, not guessed).
5. **Volume gates:** entry commitment requires the gauge's fill-probability states (ITF OPEN ≥6 prints/30m); the P4 hold-gate governs continued rest on dying books.

## The riser-disarm accounting (mechanism-by-mechanism, as demanded)
The riser package died 2026-07-06 by its own scoreboard: **walks ate ~60% of the riser's captured edge (the Jul-6 erosion)**. Why THIS posture is not that package:

| riser-package mechanism (what bled) | this spec's counterpart |
|---|---|
| **Post-revision walking**: each riser revision re-priced UPWARD toward the moving market — paying the climb it was built to capture | **Aim-then-HOLD at a fitted level: upward revision does not exist.** The level comes from the corpus (edge_p50), not from chasing the tape (granularity law: macro sets levels) |
| Unbounded walk journeys compounding the chase | **Walk caps live per Plex T2** (`walk_cap_honest_by_cat` staged at the ruling's sizes: ITF_W 20 / ITF_M 14 / CHALL 2 / mains 1); the joint shadow reads the cap × expression interaction — **no cap read before n≥30 (operator, 07-09)** |
| Chasing into the ascent after the dip passed | **Stand-down-never-chase at onset+15** — the ascent is a no-trade zone by spec, with the cost table attached |
| Revision-count telemetry ambiguity | **fv_observe count lineage, stated: the staged riser package counted 491 fv-graded observations with 43–76 in the bounce-eligible class (staged-era numbers, as relayed through the Plex review); the CURRENT accumulation (fv_observe since 07-05, 18/100 at week open) is a different series with a different eligibility rule — the two are never blended; the Plex bounce reads the current series only** |

**Arm path:** this spec ships nothing. It is the document Plex rules on; implementation joins the cell-aim ramp (AIM_V2 arm gates unchanged), with the joint-shadow n≥30 cap read and the P4 hold-gate shadow as named prerequisites.
