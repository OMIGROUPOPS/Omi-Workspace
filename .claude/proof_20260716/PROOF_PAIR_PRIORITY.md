# OUTCOME PROOF — PAIR-PRIORITY ENFORCEMENT + milestone whitelist hotfix
Proven SHA: **03e68139**
Operator word (07-16 late night, verbatim): "no new doctrine — Vault Priority 1 verbatim: 'rest bids on both legs of every tracked game, always'… The law says both rest from conception… if the engine is sequencing existence instead of management, that's the defect — name the code site."
Prior art (C45): C-PAIR-LAW 07-15 (drop-as-pair, seesaw) · THE PAIR-97 GRAVE (≤97 = scoreboard, never a pricing instruction) · C-GOLD-NOW amendment.

## Lane 1 — per-game replay (tonight's own singles)
The named code site: `place_order` chokepoint seesaw refusal (`pair_seesaw_refused`, site `place_order_chokepoint`) — a PAIR-level law implemented PER LEG. Tonight's tape: six conceived games half-dropped — ALHYAZ (YAZ placed, ALH refused), CHALIM (LIM placed, CHA refused), FOMALK (ALK placed, FOM refused), KAMNGU (KAM placed, NGU refused), ALHMOG + ABDVUX (favorite refused). Each refused favorite was a fitted per-cell aim (cell 92 recut rows, n=11–21 — FITTED, not default-on-gap; the range_cell_m15 GAP is the separate M15 entry-band intake, not an aim input). Under the exemption these legs PLACE on the router's next pass — both legs rest, the seesaw line logs as `pair_seesaw_scoreboard` with identical fields (the measurement is kept, the existence gate is removed). USMMAS (both legs placed tonight) is the control: unchanged.

## Lane 2 — behavior isolation
- Gated on `pair_seesaw_conception_exempt` (DECREED, cited to this dispatch). Flag off = byte-identical refusal behavior.
- Filled events were never touched by this site (never-hold-naked completion path unchanged).
- Milestone hotfix isolation: the fire condition NARROWS (whitelist live/P replaces blacklist not_started) — strictly fewer fires; the ABDVUX-class misfire (ms_status "SCH", start 4 min in the future, fired 11:06:57 PM tonight) is impossible by construction; unknown statuses log `gun_feed_ambiguous` once and never fire.

## Gate
lint + smoke via deploy/deploy_gate.sh (this file is OUTCOME_PROOF; OUTCOME_PROOF_SHA=03e68139).
