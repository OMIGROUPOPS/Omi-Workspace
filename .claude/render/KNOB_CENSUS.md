# KNOB_CENSUS — the canonical knob inventory (Plex; C-KNOB-CANON v1)

> ⚠ **VERBATIM BODY PENDING — the paste channel dropped Plex's census file** (third instance of the class; checked git + disk). Slot reserved word-for-word; the relay owes it. **The census's binding numbers as relayed: 124 knobs — 52 NAKED / 42 DECREED / 28 FITTED / 2 FITTED-but-UNARMED.** Named instance: `marketable_stale_pin_exempt` (live_v4.py:1713, live=True — traced below in the vault 07-11 entry).

## CODE-DRIFT HASH (Part 4 mechanism, live now; the verbatim census inherits this slot)
The census is pinned to the source it inventoried. Regenerate + verify: `python3 analysis/knob_census_check.py` — stores/compares the hash below; the viewer flags DRIFT when they differ. The map cannot silently rot.
