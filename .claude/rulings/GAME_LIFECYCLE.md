# THE GAME LIFECYCLE — the 8 steps, each citing its fitted source (committed 2026-07-09, per Plex review; STEP 1 P0)

**Terminology, DEFINED (supersedes fav/dog per THE GRID):**
- **CLIMB-SIDE** = the leg whose price cell carries depth and upward W1 momentum (cells ~50–94, concentrated 75–94): it dips at the wake, then climbs into the bell (climb med 17–21¢, EXTENSIVE P3). Formerly "fav/walking side."
- **DECAY-SIDE** = the leg whose floor IS its close, arriving in the final minutes (cells <50: edge_p50 = 0, floor T−9..−17 min): it decays toward the bell; its game is timing, never depth. Formerly "dog/waiting side."

## The 8 steps

| # | step | the law | fitted source |
|---|---|---|---|
| 1 | **QUALIFY the match** | ITF volume structure decides tradability; sub-2.5k W1 = 37% never wake, worst margin per ticker under every accounting convention. Qualification is STAGED, not called at conception (early density is a weak predictor — bal-acc 0.56–0.65 ITF): light at placement, hard at hold/commit (step 4). | NEVERWAKE + RECONCILIATION + STEP1 projectability |
| 2 | **CONCEIVE inside the horizon, on the anchored clock** | No conception beyond T-8h honest-anchored; anchor hierarchy gun_truth > te_honest > kalshi-last; clock liars get conservative gates. | C-CONCEPTION-HORIZON + EARLY_CANVAS (nothing before T-8h ever pays) + C-ANCHOR |
| 3 | **POSITION by regime, per cell** | Climb-side: aim RESTING from T−90 at the fitted level (own close − edge_p50(cat,cell)) — the bid must pre-exist the window. Decay-side: join-at-close posture, late window. Mains: join-at-touch only, depth forbidden (the cliff). | SEQFLOOR_RECUT + FILL_REDO + CLIMBSIDE_SPEC |
| 4 | **THE WAKE = the dip** | Volume onset and the dip are the same event (dip starts 11–15 min before onset); the climb-side bottom dwells ~3 min at median (ITF). Gauge states are fill probabilities, per cat. HOLD-GATE: a resting bid keeps resting only while the book stays above its own quiet-threshold (P4 clause). | EXTENSIVE P3 + VOLUME_LEDGER/FILL_REDO + STEP1 P4 |
| 5 | **CAPTURE at each leg's own moment** | Climb-side fills in the onset window or stands down (never chase the ascent — missing costs 17–21¢ med); decay-side completes near its close in the final minutes. Divots are sequential (gap p50 41–62 min, climb-side first 2:1). | EXTENSIVE spec + SEQFLOOR |
| 6 | **THE GUN closes the entry** | Fused sources fire (te_scoreboard > schedule_live > tape_latch > divergence); NO new buy after any fire; grace governs exits only; state survives restarts. | C-FUSED-GUN + C-GUN-PERSIST |
| 7 | **CASH W1/corridor — flat before resolution** | Exits at sealed bands; band-touch odds are front-loaded (half of touches inside 1h of fill, plateau by 4h); the exit machinery is airtight when the band prints ($0.36 total counterfactual on 288 legs). A settled position is the F-bucket, never bookkeeping. | CHAIN_PROOF + SETTLEMENT VOCABULARY + 0A |
| 8 | **GRADE the decision, read the receipt** | Dynamic-S per leg (own W1-close − edge_p50), provenance clause, anchored window stamps; the combined prints WITH its fitted floor (contamination rule); category law on every aggregate. | COMBINED-PRICE CLAUSE + DYNAMIC-S ruling + CATEGORY LAW |

Every threshold above states its denominator (depth-relative amendment); every step's numbers are per-cat (category law); macro sets the levels, micro times the triggers (granularity law).
