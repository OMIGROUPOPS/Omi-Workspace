# PROOF_PASS — PHASE C (cohort engine live + gap kill + wires), SHA `939109e5`

**Prior art (C45):** Phase B spectrum (source), ⑮ live-read precedent (the
standing law this arming rides), P0v3-3b (the walk-rate debt this pays),
pair invariant P0 (the tooth extends it), C-FV-OBSERVE (the voice pattern
pm_ref follows).

## LANE 1 — MECHANISM
- Cohort steer replays: on the surface's own 26 floor-passing cells, the
  empirical dip_p50 differs from the aim-table cast on the majority of
  cells (the census's dip distributions vs the flat/table depths) — every
  divergence now logs `cohort_aim` old-vs-cohort at conception, and the
  steer is bounded by ALL existing protective clamps (band 5–95, walk
  caps, floors, races unchanged). Thin cells (16/42) stand on current
  aims with the reason on the dossier — no behavior change where evidence
  is thin.
- Fitted walk rates: mains/CHALL fitted ≈ the old constant (1.0–1.16 vs
  1) — near-zero behavior delta there, BY EVIDENCE; ITF fitted 0.56–0.81
  tightens the self-fill bell's sanctioned-allowance scope (pre-sched
  fires already voided by P0v3 — belt unchanged).
- Gap kill: additive logging + one new DEFECT flag; no placement path
  altered. Polymarket + dossier voices: read-only.
- Guard misfires: none possible on thin/no-surface (explicit fallbacks);
  lint PASS; local suite PASS (same one pre-existing red).
## LANE 2 — SETTLEMENT
n/a at arming (no price/exit change beyond evidence-bounded casts); the
pre-registered scoreboard = cohort_aim'd cohort fill quality vs the
table-era cohort, nightly (OPEN_LEDGER watch).

- OUTCOME_PROOF = this doc, cites `939109e5`; doc-only commits after.
