# V52l causal stability-onset addendum — 2026-08-14

## Status

V52l is an observation-only iteration over the frozen V52h behavioral lineage. It replaces clause 1 only. Adoption or hold remains an operator decision at dock; this addendum does not make V52l operative.

Part A is the separately committed and pushed grading-only binding at `fc17d0d3ec3db4795d2e25a986bb9bfa1806714b`. Historical replay windows and decision streams remain untouched. Floors, fill validity, offer denominators, and deltas bind solely to `W1_GROUND_TRUTH_TABLE` from `c0056976c446afcb4d9603796a2e06c068ee94d6`, SHA-256 `f7bc71d8e615859db272d841e125bc4836a685d82bb2d6769762c9bc19e56729`. `UNKNOWN_BELL` rows are non-gradeable and remain a separate class.

## Clause 1 causal law

The A/B onset vocabulary is preserved:

- A: spread collapse plus cross-leg mid-sum settle.
- B: sustained trade-cadence arrival.

Each candidate is now evaluated on sequential receipt prefixes. At any evaluation moment, the organ may consume only receipts at or before that moment. The first supported prefix fixes causal recognition. The clause reads neither the right edge nor a full-span fit. Pre-onset exported receipts do not disclose a future selected onset.

Clauses 2 through 6, the disagreement referee, pair-budget record, N9 continuous CLEAN-store consumption, trades-as-truth crediting, scavenger OFF, and `REFLEX_POST=0` remain frozen V52h behavior.

## Observation set and receipts

The deterministic cohort is five standing pins plus 25 fresh games, with zero overlap against the V52b–V52k fresh cohorts. Seed SHA-256: `eda16b3de53e586ad230e04edd099f0cb9a60f6a5ae9b3bcce6f596e5dbb81c1`.

Right-edge independence passed for all 30 games under `-86400`, unchanged, and `+86400` second perturbations. Across 60 legs, 59 have comparable old/new onsets and one gains a causal onset where the interim method had none. New minus old onset seconds: min `-61980`, p25 `-29580`, median `-18180`, p75 `-3240`, p90 `-60`, max `360`; old-present/new-absent `0`, old-absent/new-present `1`.

The observation-only four-state result is:

| State | frozen V52h | V52l |
|---|---:|---:|
| COMPLETE_AT_DELTA | 14 | 15 |
| PARTIAL_FOR_REASON | 10 | 10 |
| NEITHER_FOR_REASON | 4 | 3 |
| UNKNOWN_BELL_NON_GRADEABLE | 2 | 2 |

Pin outcomes are observations rather than outcome bars. Pin license lawfulness passes. No `COMPLETE_AT_LOSS` row appears. Every game has one complete `PAIR_BUDGET_RECORD` revision chain.

## Verification and safety

Two clean builds produced 141 byte-identical artifacts. The focused V52–V52l lineage suite covers ten test files and 269 assertions with zero failures, omissions, or deselections. The generated-package verifier adds 55 passing assertions. No full-804 run, sealed-population run, deployment, authorization, live access, order, or position action occurred.

The frozen package is `.claude/window1_live_v4_replay/v52l_causal_stability_onset_20260814/`. Its report, onset-shift ledger, right-edge-independence receipt, four-state observation, per-game outcome table, trace manifest, test receipt, determinism receipt, and hash manifests are the controlling evidence for this observation.

VAULTED: Part A binds grading only to the verified-bell ground-truth table while preserving historical decisions; V52l replaces clause 1 with receipt-prefix-causal onset, proves right-edge independence, freezes clauses 2–6, and remains observation-only pending the operator's adopt-or-hold ruling.
