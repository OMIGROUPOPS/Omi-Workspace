# PROOF — C-ADJUDICATION-READ v1 (intraday calibration grade → the pre-authorized flatten leash)

**Candidate SHA: b7abdc5b**

## Part 1 — the calibration number (Lane 1, real settled outcomes, not shadow assumptions)
Every flatten action Jul 12–13 with a settled or terminal-priced counterfactual: **N=36 · flatten-minus-ride = −$9.50 total · −$0.264/leg · flatten beat the ride on 16/36.** The structure, named: **all 20 giveaways were legs whose kept side went on to WIN** (worst: BATSYC −$4.40, GIZVLA −$4.40, LINHON −$4.10 — dogs at 12–16¢ flattened flat that settled at 100); the 16 saves were real (+$0.35…+$3.25, all ride-to-zero legs). Root cause identified for the composer's next fit: **the two-term EV frame excludes the win-ride residual BY DESIGN** (`win_ride_residual_excluded`), so winners' upside never enters EV_hold and high-probability legs systematically read negative → flatten. The +$1.90-savings claim tested and REFUTED as an unconditional number: it holds only on the legs that lose.

## Part 2 — the bound, applied (the operator's word embedded in the dispatch; the number decided)
Negative at n≥20 → the leash: **`flatten_daily_action_cap = 8` + `flatten_ev_margin_cents = 3.0`** (verdicts with EV_hold above −3¢ defer — 3¢ is the noise band where today's graded deltas straddle zero). Named lines, never silent: `completion_flatten_deferred` / `completion_flatten_capped`, monitor-rendered. Counts survive restarts (boot rebuild counts today's `flattening` actions — today already ran 32, so the cap binds from this boot). Sunset: the nightly grades the leashed branch; calibration positive lifts it by word. Legacy exit path untouched (§0A).

## Part 3 — the band-collapse decomposition (today, by exit path)
maker_band vs engine_flatten avg band per category: ATP_CHALL +15.5¢ (n=2) vs −1.7¢ (n=9) · ATP_MAIN +13.6¢ (5) vs 0.0¢ (3) · ITF_M +10.8¢ (12) vs −1.6¢ (7) · ITF_W +9.8¢ (6) vs −3.1¢ (14) · WTA_CHALL +7.9¢ (9) vs — · WTA_MAIN +13.8¢ (5) vs +2.0¢ (1). Today's 4.8¢ blended band = the engine's near-zero flatten bands averaging against normal ~10–15¢ maker bands. The nightly's GOVERNOR SPLIT now prints avg band per governor so tomorrow's read is this one table.

## Lane 1 replay (at the SHA on the box): REPLAY PASS 8/8
The two new cases: flatten with EV −1.2 (above the −3 floor) → zero orders, `completion_flatten_deferred` named; flatten at the daily cap → zero orders, `completion_flatten_capped` named. All six prior cases unchanged (taker cross + gun exemption, dedup, word gate, flatten mechanics, hold, taker cap).

## Lane 2 — economics
Strictly loss-bounding on a branch measured at −$0.264/leg: the margin floor removes the noise-EV flattens (where the giveaways concentrated at −0.6…−2.5¢ EV), the cap bounds residual miscalibration at ≤8 × spread × qty/day, and the saves the branch genuinely makes (deep-negative-EV riders) stay available.
