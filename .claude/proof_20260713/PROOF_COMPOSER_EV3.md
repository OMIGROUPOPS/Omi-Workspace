# PROOF — C-COMPOSER-EV3 v1 (the three-term frame, shadow stream; the acid test reported as it fell)

**Candidate SHA: 07cf29a2**
**Class cited:** the win-ride-residual exclusion (rooted C-ADJUDICATION-READ; `win_ride_residual_excluded` in every two-term verdict) — the under-broad brain, OVER-BROAD LOCK's cousin. This is its closing DESIGN, shipped to shadow; the cutover bar is NOT yet met and the nightly is the judge.

## Part 1 — the frame (Lane 1 code, pure module, registry-cited)
`leg_ev3` / `completion_verdict3` in oslayer/leg_econ.py:
**EV_hold = p_exit_fill × band + p_win_ride × (100 − basis) − p_loss_ride × basis** — every probability an M15 cell share (`cashed/n`, `win/n`, `loss/n`; the 3-way partition was in the building — the two-term frame never consumed `win/n`). Posterior hook: when a conviction posterior is supplied, the ride mass re-splits by shrinkage toward it (K_POST=8, the composer's own prior weight); live wiring passes None for now (the composer's conf lives in its own shadow lines; the nightly joins them). NO-OPINION on thin cells unchanged. Shadow stream: every `completion_shadow` line now carries an `ev3` sub-record (verdict3 + all three probabilities + cell + blend source). **The acting path is byte-identical in behavior: the leashed two-term branch keeps its cap and floor (Part 3 doctrine).**

## Part 2 — the acid test (walk-forward by construction; run at the SHA on the box)
M15's cells were built on the pre-Jul-10 era corpus; the 36 graded legs are Jul 12–13 — nothing was fitted to them (the Plex frame: losers don't enter the registry; nothing here entered it either).
**Result, not smoothed: winners flipped to hold/defer 9/20 · true saves still flatten 6/16 — THE BAR IS NOT MET.** Eleven giveaways still flatten (all cited in the run: the high-basis leaders — NAGCHY 93/−7.7, GIAMEN 91/−8.08, IVAKOP 91/−5.7, TIMCAR 88/−6.33, MOHKEZ 90/−7.08, PRICRI ×2, VILGAN 91, CAKVOZ 71, RAJHUT 74, PULWIR 31): at basis ≥88 the cell's ride-loss share prices −basis so heavily that no historical win share rescues it. Ten true saves now defer (their EVs sat at −0.6…−2.6¢, inside the noise floor). **The structural finding, stated plainly: the acid set's separator is the settled RESULT, and ex-ante that information is mostly the price itself — a market-posterior ride is EV≈0 by identity, so no EV threshold on these inputs can flip all 20 winners while keeping all 16 saves. The frame's real value is decidable only on forward tape — exactly what the shadow stream and the nightly grading exist for.**
**Corollary that stands on the same tape: the leash's 3¢ floor nets +$11.00** (blocks 8 shallow-EV giveaways worth $24.20 against 10 forgone shallow saves worth $13.20) — the bound applied yesterday is validated by today's grade.

## Part 3 — doctrine unchanged
Shadow before hands: EV3 rides in the shadow stream; the two-term actor keeps cap 8/day + 3¢ floor; the nightly's COMPLETION section now has both frames' verdicts per event to grade against settled outcomes; **cutover only on a positive nightly grade + the operator's word.** No live behavior change in this deploy (the `ev3` record is a passenger on an existing log line).

## Lane 2 — economics
$0 by construction: pure-module addition + one shadow sub-record. §0A untouched; the legacy exit path untouched; every probability cited (M15 cell shares + composer K_POST).
