# INDEPENDENT AUDIT — T2 SCORING PACKAGE V3 @ a7582791 — RULING: PASS (three-phase blind protocol)

**PASS. The Phase-2 blind reconstruction (frozen at `c5f3a3a2` with ZERO of the 22 V3-added files opened) matches the frozen V3 package row-for-row and field-for-field everywhere, with no reinterpretation: every comparison below prints the blind value first and the package value second. The V2 execution failure is fully explained, reproduced, and repaired at the exact seam; the repaired runtime consumes only the strictly validated frozen reference ledger; preflight and execute are proven mechanically equivalent through the first scorer call; the provenance sidecar is live and exactly consistent; determinism, tests, and every null/forbidden-access guarantee hold. No scorer execution, no results directory, no authorization was created.**

Phase-2 blind commit: `c5f3a3a2e7180ea23b6dd6517ae6a8d2568e0afd` (never amended). Under audit: V3 `a758279184ab0f367a3ce74a69da851608645e19` (22 additions) on parent `b73679edc9186eb72236cd1bee5f886ac141cac4`; preserved failed V2-results commit `3f8fa0fb372c9e89fa97f89fd26156892745afe1` (5 additions, same parent). Remote branch tips equal both commits. Audit-lane parent `e4e57bac…`; shared dirty workspace and LIVING_VAULT untouched; no branch merged.

## Blind-versus-package comparison (blind first, package second)

- **Boundary contract (804 raw V5 rows):** statuses 705/85/14 (positive/censored/contradictory) ↔ 705/85/14; classes proxy 453·official 234·interval 31·live_by 52·sched 20·contradictory 14 ↔ identical (per-leg ×2 in census: 906/468/62/104/40/28); flat-vs-raw mismatches **0 ↔ 0**; `source_record_sha256` reproduced **804/804**; all 804 flattened rows lack `guard_band` ↔ V3's compatibility receipt declares the normalized ledger "not valid raw-V5 input to the reference adapter". Guard/cutoff/guard-id conservation: my recomputation ↔ receipt `field_mismatch_count: 0`, `cutoff_conserved: true`.
- **Frozen reference ledger (1,608 event-legs):** key sets equal both directions (0 missing / 0 extra); **zero field mismatches** on availability, price, timestamp, and supporting receipts across all 1,608 rows. Availability 1,307/301 ↔ 1,307/301; unavailable reasons 170 boundary-not-positive + 28 contradictory + 39 no-print-in-window + 64 ambiguous-differing-price ↔ identical; tie-size census identical (272 tie rows = 208 same-price + 64 differing ↔ `latest_timestamp_tie_row_count: 272`); zero schedule/carried/midpoint/receipt-ID substitutions ↔ all four negative-attestation fields false on all 1,608 rows and all four census counts 0.
- **V2 failure reproduction:** first affected event `KXATPCHALLENGERMATCH-26JUL12ALVVAN` (first positive-class row in ledger order); its flattened row raises exactly `positive boundary lacks V5 guard artifact` under my own transcription of the frozen guard law ↔ the preserved STDERR shows the same error at `reference_adapter_v1.guarded_cutoff` line 121; its full raw-V5 row derives status positive, cutoff 1783846800.0 (= proxy clock − 900 s), and **both leg references derive successfully through the repaired seam (78 and 22, available, unscored)** ↔ V3's frozen ledger rows for that event are identical.
- **Population joins to the scorer boundary:** events 804, legs 1,608, boundary ids ≡ event ids, fills 3,252 unique in-D, floors ≡ the 1,608 leg keys, regret chains 12,864 = 8×1,608 unique, provenance 4,170 ⊂ chain keys, fit 525 / post-fit 279 ↔ the live `preflight-no-score` run reports exactly 804/1,608/12,864/1,608/3,252/1,608/12,864/4,170/525/279 with `scorer_boundary_reached: true`, `scorer_invocations: 0`, `results_directory_absent: true`.

## Runtime-seam proof (§11)

Both `execution_readiness_no_score` and `execute` consume the frozen ledger exclusively through `adapt_frozen_reference_rows` — a strict validator that performs **no reference re-derivation**: schema pin; identity/duplicate/extra/unknown-ticker refusal; forbidden-date refusal; raw-V5 ledger SHA pin (`c6204d01…`); the four substitution attestations required false; score-free law required; availability typed boolean; available rows must carry an exact 1–99 integer price, timestamp within [T−8h, cutoff], reason null, non-empty receipts, `distinct == (price,)`, `tie_count == len(receipts)`, single-receipt identity equality, and same-price ties must NOT select a single receipt; unavailable rows must carry no price/receipt and a named reason, with complete tie evidence for ambiguity and none otherwise; optional normalized-boundary cross-check (class/cutoff/guard agreement); and exact key-set equality with the expected 1,608 legs (fail-closed on missing/extra). `derive_window1_close_reference` is builder-side only; the normalized ledger is passed solely as the cross-check mapping, never as adapter input. The first-failure event has complete lawful coverage in the frozen ledger.

## Preflight/execute equivalence (§12 — mechanical, not the readiness receipt)

AST-derived role-loading census: execute loads {claim_fence_manifest, event_ledger, frozen_reference_ledger, normalized_boundary_ledger, oracle_leg_floor_ledger, regret_chain_input_ledger, unique_T2_fill_ledger} — **a strict subset of preflight's set**, which adds only the two validation-extra roles (fit_postfit_ledger, provenance sidecar). Both call the identical `validate_package(verify_private_inputs=True)`, `adapt_frozen_reference_rows`, `adapt_t2_unique_fill_rows`, `require_claim_fences`; `score_t2_event` appears **only** in execute; the preflight cannot skip or substitute any input execute consumes (the proof fails if any execute-only role appears). The audit-only regression (committed script) recomputes this censu and the live preflight run exercised every real join to the boundary with zero scorer calls.

## Provenance sidecar (§13)

All **4,170** sidecar rows agree **exactly** (4,170/4,170, 0 disagreements) with the `target_level_authority_d2_provenance` embedded in the regret-chain rows actually consumed by the runner; **zero** chains outside the sidecar carry any omitted-lawful-target entry (all 6,381 omitted entries live inside sidecar coverage; 627 sidecar rows lawfully cover zero-omission legs); `omitted_d2_inferred_from_successful_fill` is false everywhere, and execute stamps it false into scored rows. The result contract partitions omitted lawful targets per candidate × {aggregate, fit, post_fit} × authority × d2 sign with a runner-enforced conservation check, and every corresponding result field is null pre-execution. The sidecar is live and consistent — not dead.

## V2 failure bindings (§14)

All 5 preserved failure artifacts hash-verified against the `3f8fa0fb` blobs (0 bad); one invocation, retry_count 0, exit 1, `scorer_invocations: 0`, ~1.1 s runtime; the consumed authorization `e4e57bac…` is explicitly **rejected** by the V3 runner ("consumed V2 authorization is rejected by V3") and the V2 execution ID is bound as consumed; V3 carries the distinct ID `…scorepkg-v3` with a fresh results path that is absent locally and on every remote ref (no V3 results branch exists). The authorization timestamp-label discrepancy is **preserved, not repaired**: the document printed "10:43 PM ET" while Git metadata records 6:44:29 PM ET and execution began 6:53:49 PM ET — chronology derived from Git/UTC evidence proves authorization preceded execution, and the consumed report blob (`6aa24afd…`) is byte-identical at `e4e57bac` and at the current audit tip (never amended). V3 is additions-only from its stated parent (22 A / 0 M / 0 D — my git-level proof, matching the inherited-byte-identity receipt's 0 modified / 0 deleted).

## Determinism, tests, hashes (§§15–16)

Two clean V3 regenerations from a byte-exact parent worktree with frozen V3 sources placed untracked: **A ≡ B and 15/15 regenerable artifacts byte-identical to the frozen package** (the 16th file is the freeze-stage receipt). V3 artifact manifest 14/14 rows verify; source manifest 30 rows verify (0 bad; one row is the private-inputs directory reference, whose 805 member files were hash-verified in the prior audits and revalidated live by `validate_package(verify_private_inputs=True)` in the preflight run). Test enumeration from the frozen source manifests: the three scoring suites collect **76 tests (35 V1 + 24 V2 + 17 V3)**; the complete collected set runs **76/76 PASS with zero omitted, deselected, or filtered tests**, including the real-input preflight tests that load the full population and the original failing event (an initial 2-failure run was an auditor-harness path artifact — a Temp-sited worktree could not resolve the `../OMI-Window1-private` sibling — rerun cleanly from a correctly sited worktree; not a package defect).

## Null and forbidden-access guarantees (§17)

`validate-only`: `real_population_loaded: false`, no fill/reference adapter, no scorer, authorization args forbidden. `preflight-no-score`: authorization args forbidden, scorer boundary reached with 0 invocations, results dir absent. All C/PC/IC/S, frontier, regret, attribution, ranking, selection, and performance fields null in every receipt and ledger row; no holdout date opened; no live/network/exchange/order/position/settlement/DCA/Window-2 access; no V3 results directory or branch exists; no authorization was created by this audit.

## RULING

**PASS.** Package readiness only. This authorizes nothing: scoring V3 requires a future separate independent PASS and a new separately bound one-execution authorization, neither of which is created here.
