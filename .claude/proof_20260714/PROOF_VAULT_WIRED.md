# PROOF — C-VAULT-WIRED-ENTRY v1 (the entry consults the consolidated vault, and every line proves it)

**Candidate SHA: 92472906** (_entry_dossier organ + refusal-site wiring + reach-law hot loader + nightly CONSULTATION CENSUS with AUTO-GAPS + render dossier section, committed at that SHA).
**Prior-art (C45):** the cutover (path aims = the law) · the reach law LAW.json (the fill judge, now consulted at every entry) · ATLAS_V1/contention/pair-law (already enforcing) · the M15 range layer (completion frame — its ENTRY mapping is the census's first named GAP) · the HARVESTABLE MAP (flow buckets, now logged per entry) · the governor-stamp precedent (the same one-emitter standard this law extends to entries).
**THE LAW (operator, verbatim, vaulted):** Part 1 and Part 5 as dispatched — every placement and refusal consults every applicable registry surface with the dossier on the line; a silently-ignored surface is a named violation; the census's GAP column is the vault's intake list; vault conclusions are citable, not sacred — challenged by tape, re-validated by the same walk-forward standard that admitted them.

## Part 1 — wired (ten surfaces per line)
`_entry_dossier` fires at the placement site and all three refusal sites (`no_path_page`, `drop_as_pair`, `pair_seesaw`): atlas page (values) · contention selector (verdict/pct/tier) · pair state (composition) · **reach law — fill probability AT the aim (flow bucket, depth X, rate/hr, p_fill_1h)** · M15 range cell (**GAP, named: entry-band mapping unfitted — completion-frame bands only**) · dip timing (+ the timing shadow delay) · flow state (prints/30m, bucket, harvest rate at 5¢) · refuse margins (8% bar, pair-97) · fill regime · honest clock. Aim-CHANGING consultations ride as SHADOW variants on the same line (`shadow_range_shape` = the path-slice p25 aim at the current clock; timing delay) — logged today, cut over on the standing bar only (Part 3, §0A untouched).

## Part 2 — the census, standing
The nightly prints the site × surface grid (CONSULTED / SHADOW / NOT-APPLICABLE-with-why / GAP); **any GAP auto-appends to BOARD.md under the AUTO-GAPS marker (idempotent)** — the intake list, printed, never a 3 pm discovery.

## Part 4 — the replay exhibit (LANDED, committed)
Today's 7 post-cutover decisions re-emitted through the dossier arithmetic (`.claude/dossier_20260714/REPLAY_DOSSIERS.jsonl` + summary): **atlas/contention/pair/reach-law/dip-timing/flow/margins/clock CONSULTED 7/7 · fill_regime NOT-APPLICABLE in replay (named — the live organ carries it) · range_cell_m15 GAP 7/7 (the intake item) · shadow_range_shape SHADOW 7/7.** Sample: COXHAM-HAM placed:path_aim, discovery 11 → aim 7, page ITF_M|underdog|le25 (n=26, bottom [2,4,11]), contention 44.4%, reach-law consulted at the aim. The render shows dossier lines from its next cycle; the census prints tonight.

## Lane 2 — $0 by construction
Parts 1–2 are logging and reporting; no placement, price, cancel, or size consumes the dossier. Part 3's aim-movers are SHADOW fields only.

## Deploy gate
[1/2] lint PASS (at commit) · [2/3] smoke (gate-run; atlas staged) · [3/3] this file, OUTCOME_PROOF_SHA=92472906 · [4/4] C50 push (the law + Part 5 verbatim in the vault; registry updated) · [5/5] constraints surface.
