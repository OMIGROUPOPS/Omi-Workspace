# INDEPENDENT AUDIT — WINDOW-1 T1 POST-FIRST-LEG RESPONSE AND PERSISTENCE PRE-RUN @ 88b0eae8 — RULING: PASS

**PASS. The frozen T1 PRE-RUN faithfully implements the authorized T1 correction. Independent verification re-derived every law on every committed row — 6,432 overlays, 476,710 episode-keyed decisions, 51,186 headroom target constructions, 27,816 persistence receipts, 1,472 strict-ask credits — with ZERO true violations; the 48/8/34/2 fixture migrations reproduce independently from the controlling attribution; first-leg and no-fill semantics are identical to the byte-bound baselines on all rows; two fresh clean regenerations are byte-identical to each other and to the committed package; 135/135 tests pass; every hash verifies; no forbidden activity.**

Date: 2026-07-27 · Branch: `audit/window1-independent` · Additions-only child of `b96873c9a5eb340a7abb0eda9bffd6f0cedb4341` · Auditor: independent CC session
Under audit: `88b0eae8620172f41e2f5d45320408357de24c6f` ("Freeze Window-1 T1 post-first response") on `codex/window1-definition`.

## 1. Lineage and containment — PASS

88b0eae8's parent is exactly `7fe299e5` and it is the **sole child** across all refs; remote `codex/window1-definition` = 88b0eae8. Diff: **53 additions, 0 modifications, 0 deletions**; full `ls-tree` comparison proves every inherited candidate, instrument, scorer, result, test, and audit blob **byte-identical**. The package contains only the score-free T1 instrument, builder, spec, tests, and 45 receipts/artifacts. **No event-specific hardcoding**: the T1 sources contain zero event IDs (the only "TON-SPI" string is prose in the report template; `FIVE_NO_BBO` is imported from the inherited frozen baseline builder).

## 2. Candidate matrix — PASS

Exactly **eight** T1 candidates (hold/micro × response-only, target-completeness-only, persistence-only, full-stack) with the frozen switch matrix, `free_numeric_parameters: []`, `candidate_additions_after_freeze_allowed: false`, and simulated quantity 5. The two baselines remain **byte-bound references** (BASELINE_BYTE_IDENTITY receipt verified against the frozen stream files; zero regenerated baseline rows). `candidate_policy` derives every variant from the identical baseline policy plus the three switches — no unreported parameter differs. **D=804 per candidate on all eight** (verified by streaming all 6,432 overlays); no event dropped for missing placement, BBO, capacity, or decision call (the five no-BBO events retain membership with zero fabricated evidence — 40 proof rows verified against my recomputed baseline placement counts).

## 3. Post-first-fill causality — PASS

Re-derived on every committed row, not trusted: T1 actions exist only after the credited first fill (0 pre-fill post-action violations across all overlays); first-leg fill semantics (leg, price, timestamp, receipt, d1, reference) and the **entire pre-fill action prefix** are identical to the baseline on **all 6,432 rows** (compared against my independently recomputed baseline facts, 0 mismatches). All 476,710 episode-keyed decisions are strictly later than the first fill, bind a non-empty trigger receipt, are unique per (candidate, event, leg, receipt), and carry `new_action_fill_eligible_on_trigger_receipt: false` (fixture-tested: a trigger receipt cannot fill the action it creates; new fills need strictly later evidence). Same-timestamp ordering is deterministic (timestamp, then book-before-print, then leg, then receipt) with the same-second BBO selection inherited from the passed law — no older favorable row selection (0 target/persistence law violations, which would expose any). Every decision is an explicit HOLD / PLACE / REPRICE / NO_CALL; on this data all 476,710 are **HOLD** — at every decision point the sibling already carried active exposure, so the explicit decision retains it; the PLACE/NO_CALL branches are law-verified and fixture-tested. No silent episodes: decision counts reconcile per overlay exactly.

## 4. Combined-headroom law — PASS

On all 51,186 target-construction receipts (plus every construction embedded in decisions): `b2_max = -d1 - fee - 1` (= `floor(-d1-fee-1)` at integer cents, verified on all 476,710 decision rows), every alternative's five checks recomputed exactly, strict `d1+d2+fee<0` on every lawful row, **no both-negative or IC gate** (spec `IC_gate: false`; AST test proves no IC/S names; C/PC/S/IC null on all rows), no combined-zero admission (strictness verified), fee 0 applied once, contemporaneous bid/ask from the lawful non-crossed book only (crossed books rejected; prints never substitute — a missing BBO produces `MARKET_EVIDENCE_NO_CALL`), all arithmetic exact integer cents (fractional cents raise). **Accepted-target d2 distribution: 16,759 selected targets, 82 with d2 > 0 (0.49%)** — positive sibling d2 admitted inside combined headroom, published without any PC/C scoring.

## 5. Target-completeness law — PASS

`X_headroom = min(ask-1, bid+min(1,b2_max))` reproduced on every construction; all conditions verified per row (positive-size bid/ask, non-crossed, 1..99, X<ask, X−bid≤b2_max, strict combined negativity, ≤+1-cent improvement); **exactly one active five-contract sibling exposure** (0 interval-overlap violations across all 6,432 overlays); when an existing macro target is also lawful the selected X is the **maximum** lawful maker price (selection = max over lawful alternatives, verified on all rows); an unlawful macro target is never retained (it simply loses lawfulness and cannot be selected). No volume/cadence/queue/five-contract capacity condition appears anywhere in the target law.

## 6. Persistence and queue law — PASS

All 27,816 persistence receipts re-derived: **26,532 HOLDs** (active X re-verified lawful on all six checks — corridor, positive cent, maker-safe, inside b2_max, strict combined, non-crossed — with my own recomputation; queue never surrendered on a HOLD), **872 receipt-backed changes** (law lawful=false, surrender flagged true), **412 guarded-cutoff extensions** (every extension's cutoff equals the authoritative START_LEDGER boundary for its event — all 804 boundaries recomputed via the frozen boundary contract; my initial 132 flags were an audit-side artifact of checking against the V2 boundary receipt, which only covers census events, and were refuted by the authoritative source). Suppressed authorities on HOLD prove the non-surrender guarantees: **CAUSAL_PAIR_HEADROOM 24,794, LIVE-AIM 1,550, ATLAS macro target 188** — new targets, headroom suggestions, and LIVE-AIM do not surrender lawful queue position. After the first fill the guarded Window-1 cutoff controls termination for persistence variants (295 `guarded_window1_cutoff_after_first_fill` closes vs 620 base-horizon closes on non-persistence variants). Every cancel/reprice cites exactly one lawful receipt-backed cause (census: headroom plus-one, LIVE-AIM authorized, maker-safety, ATLAS target adoption, policy horizon/guarded cutoff — nothing else).

## 7. Strict-ask ordering — PASS

Reproduced globally (all overlays, not only the committed proof): **1,472 strict-ask credit actions, 0 same-timestamp cancel/reprice conflicts on the credited interval**. The committed STRICT_ASK_CREDIT_BEFORE_REPRICE_PROOF (4 fixture-event rows, 0 violations) reproduces exactly. Inherited law verified by test: ask < X credits at the original exposed X; ask = X routes to maker-safety, never auto-credit; no double print/strict-ask credit.

## 8. Fixture migrations — PASS

The controlling 24/4/17/2 attribution fixtures were re-identified independently from the attribution package (audited last session) and each acceptance re-derived from the committed T1 artifacts, not the migration receipt: **48 response rows** (24 × {response-only, full-stack}: episode-keyed decisions exist), **8 target rows** (4 × 2: a lawful complete construction exists), **34 persistence rows** (17 × 2: old X retained through the opportunity, filled first, or receipt-backed resolution present), **2 capacity rows** (carried once, no variant multiplication, `T1_credited_fixture: false` — uncredited without proven exact-five capacity). Multiplier: each fixture row is tested in exactly the two variants that arm its switch. Fixtures act as acceptance witnesses; no event-conditional code exists.

## 9. Semantic identity — PASS

Zero first-leg mismatches and zero prefix mismatches on all 6,432 rows against my independently recomputed baseline facts; **216 baseline no-fill streams × 4 variants = 864 no-fill rows, zero mismatches** (baseline no-fill hashes recomputed by my own semantic-hash implementation); the five no-BBO events retain D membership with zero orders; all changed streams descend from a first fill (T1 switches unreachable otherwise — both-no-fill events inherit the complete baseline by content receipt, verified); the changed/unchanged census reproduces exactly per candidate (**697/107 macro-hold, 695/109 macro-micro**, identical within regime across variants) including the exact changed event-ID lists, from my recomputed baseline full-stream hashes.

## 10. Integrity and determinism — PASS

All 48 artifact-manifest rows, 47 determinism-inventory rows, 19 committed-input hashes, 2 controlling-audit blob pins (verified against `b96873c9`), and **805 private input hashes** (events ledger + all 804 caches) verify; all 21 gzip payloads parse; D conservation, receipt conservation, decision/action/changed counts all reproduce. **135/135 tests pass** (16 T1 + 119 across eight inherited suites: range-attack, strict-ask V2, round-4 ×4, census V2, attribution). **Two fresh clean regenerations** from frozen inputs in a scratch clone at 88b0eae8 (LF checkout): builds A and B byte-identical to each other and **all 47 regenerable artifacts byte-identical to the committed package**; committed receipts were never used as expected answers.

## 11. Null metrics and forbidden access — PASS

C/PC/IC/S and every metrics/performance field null with `scored: false` on every row of every artifact (recursive scan, 0 violations); no scorer/benchmark import or invocation (AST + grep + receipts), no result package, no tuning/ranking/selection, no holdout (sealed dates enforced as rejections), no live/network/production, no order/position access, no Window-2/exit/settlement/DCA, no mutation of the frozen parent package (inherited tree byte-identical).

## RULING

**PASS.** The T1 PRE-RUN implements exactly the authorized correction — explicit post-first-fill sibling response, complete event-specific combined-headroom targeting, and lawful persistence of sibling exposure and queue position — with no generic fill-rate optimization, no silent PC-to-IC tightening, no first-leg or no-fill behavior change, and no future information. This PASS authorizes **only** construction of a separately frozen scoring package for these eight T1 candidates. It does **not** authorize scoring or execution.
