# PROOF — C-LIVE-AIM v1 (the fourth aim design: per-market live conditioning, shadow now, learning daily)

**Candidate SHA: da3deec7**
**Class cited: AIM/TIMING MISSES, fourth closing design.** Three refusals on record (07-11 refit · guidebook v1 static depth · guidebook v2 clock windows) — each convicted toward this: the 07-11 refit lacked conditioning entirely; v1 proved static depth = concentrated adverse selection; v2 proved the timing axis fixes WHO fills (partition 8/4) but that the discount is not a CLOCK moment. The operator's spec verbatim: every market needs its own tailored analysis in real time upon discovery.

## Part 1 — the aim posterior (Lane 1 code, every input registry-cited)
`_liveaim_shadow` at every v4_place decision: **aim depth = prior page × this market's live state.**
- Prior: GUIDEBOOK_V1 page (M1 recut depth p50 + deep tier p25 — refused for ACTING, citable as a PRIOR).
- Live state, from this market's own tape: `flow_ratio` (prints/30m vs the category's fitted OPEN threshold, M3/M13 accrual curve) · `depth_trend` (bid-side book building/hollowing since last read) · `spread` · `print_sig` (trailing-window direction).
- Conditioning v1: prints rising → **NO_BID_CHASE_GUARD** (the chase lesson, structural); flow at curve + (falling prints OR hollowing book) → **AIM_DEEP(moment_forming)** at the deep tier; quiet + wide spread → **AIM_SHALLOW**; else the prior. Every line logs the full input vector + citation.

## Part 2 — accrual-aware honesty
Every line prints `lib_conf` = the category's banked-tape confidence (trades-archive file count / 500, capped 1.0, recomputed each boot as archive-sync banks tape). ITF starts humble and hardens as a NUMBER; below `liveaim_conf_floor` (0.1) → NO-OPINION, never a guess.

## Part 3 — graded forward from night one (the pre-agreed gate)
No historical replay gates this build — the three refusals already graded everything history can say (the dispatch's own words). The nightly gains **LIVE-AIM SHADOW**: would-have-aims vs live aims vs tape truth, dollars per category, n accruing. **Cutover word only after the nightly shows it beating live aims at n≥300 shadow-graded aims with the interval clear of zero — the same statistical bar EV3 wears.**

## Part 4 — nothing acts (Lane 2: $0)
Shadow only; there is NO acting path in this build at all — the log line is the entire output. The two-term leash, the bells, the caps, live placement: all byte-identical in behavior. The deploy gate's smoke exercises the v4_place path with the shadow live (swallowed-exception construction, same as every shadow before it).
