# V54 L19a neighbor-voted composition — pass 2

Pass 2 was authorized in writing and executed from `e5c6212c0a9a7ed1b1ece082cae884aa9d41f20c`. This is the one filed adjustment after pass 1.

The 16-cell error-fit was retired. The replacement materializes 18,000 bounded leg records across 9,000 library games. Each record identifies derived timing depth as the specialist before its own bounded floor fraction and own-tape presence at touch as the specialist at-or-after that fraction. Runtime weights are the leave-self-out neighborhood's continuous similarity × coverage × this-leg evidence-match votes. There are zero cells and zero global coefficients.

Before the full replay, the filed pass-1 validation sets were scored independently. The candidate recovered 5 of the lost 258 and left 253 lost. It retained 6 of the gained 24 and surrendered 18, so the pre-stated recovery-without-surrender direction failed.

The full honest ruler produced 38 valid completes / 680, 123 locked cents, and 3.236842105263158 cents mean game delta. Standing-benchmark identity retained/lost/gained was 23/288/15 against 311. The initial disposition treated that comparison as a stop; the ONE-OS rider below supersedes that disposition.

The four-game read was GIUBAR 89c / Δ11, URSPAL 98c / Δ2, LAJSVA 97c / Δ3, and DANPRA 97c / Δ3. GIUBAR held its 7c report-only ratchet; URSPAL and LAJSVA missed 3c and 6c.

Case study v10 is emitted as a truthful Pass-2 record. Two clean builds are byte-identical across the specialist table, validation receipt, scorecard, 804 outcomes, tripwires, 30-story draw, case study, and the 61,400-row external sentence archive.

Historical disposition: `SELF_STOP_HARD_FLOOR_BREAK`; superseded by the ONE-OS rider below. Sealed, live, and deployment surfaces were untouched.

Rider poison check: the executed 61,400-row trace contains zero `OWN_TAPE_PRESENCE_AT_TOUCH` basis rows at depth 1. Available presence rows use depth 0 (at the evidenced touch); unavailable rows use null. The 1c literal is confined to the explicitly named frozen current-level fallback, not the presence basis. No active fit-error surface or clamp exists. Enrichment records are committed; per-decision vote surfaces are externally custodied and hash-bound. LOST-258 is the primary validation population (5 recovered); GAINED-24 is secondary ruler-side evidence (6 retained). No restart was warranted; the then-current floor disposition was later superseded by the ONE-OS rider below.

ONE-OS gate reframe: the prior 311 stop is superseded by reported benchmark context. Pass 2 is complete at 38/680 valid completes (5.588235%), 123c locked, and 3.236842c mean game delta. All 38 valid pairs and all 76 credited legs bind to positive leave-self-out specialist vote mass at their fill receipts; none binds to the underived frozen current-level fallback. Every rest level covered the credited at-or-below trade price. The only remaining stop classes are law violation, determinism failure, sealed contact, and missing reports; none fired. The policy, decisions, and score bytes were not changed by the reframe.
