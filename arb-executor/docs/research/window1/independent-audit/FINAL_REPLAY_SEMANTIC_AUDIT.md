# Independent semantic audit — corrected Window-1 development replay

Audited commit: `38d82a8a4ffc596cee93136b9cf3877110c7ca85` ("Publish corrected Window-1 development replay", codex/window1-definition, 2026-07-23 22:01:06 -0400), with predecessors 74535382 / 0060331e / 9ef16819.
Review date: 2026-07-23. Read-only; no Codex files modified; no tuning, candidate search, or scoring performed. Four independent recomputation passes over EVENT_RESULTS.jsonl (804), EVENT_LEG_RESULTS.jsonl (1,608), the policy-decision ledgers (7,256 rows), the frozen inputs, and the runner code.

## 1. Is the numeric replay PASS justified? — YES

Every reported raw count reproduces exactly from the ledgers: D=804 (2 legs × 804, unique keys); strict dual exact-five Window-1 completions = 0; proven-W1 exact-five legs = 13 (each satisfies every clause of the proof rule — exchange placement+fill clocks, completion 649–24,496 s before an exact/clean-interval safe cutoff — and they sit on 13 *distinct* events, which is why duals = 0: 10 siblings are receipt-proven nonfills, 3 lifecycle-censored); nonfill events/legs = 544/870 (sibling decomposition 326×2 + 158 + 54 + 6 = 870 exact); proven non-W1 = 66/75 per the code's rule; other-quantity = 12/12 incl. the retained 10-contract overfill; timing-censored = 728; feature-censored = 804; dip/catch 10/5/9; dynamic-floor evaluated 0; decision ledger 3,318/42/3,574/308/14; mismatch ledger empty. The 240 optimistic upper bound reproduces with zero per-event rule mismatches and is the *identical event set* to the earlier T-8 possible-C 240 (both inherit the frozen lifecycle upper-bound flags). Instrument binding is now mechanical and predeclared: candidate/allowlist/adapter/metric-contract hashes verify against the committed blobs, the freeze receipt predates execution, `bind_policy()` raises on proxy/forbidden/unknown policies and on the prohibited LATCHCAL sha, and all four conditions from the calibration cross-review (adapter binding, non-null-clock law, 76/728 decomposition printed, private exchange clocks consumed) are satisfied.

Defects found (none change the verdict, all should be recorded):

- **D1 — 14 events wrongly excluded from the 240 by censoring-only evidence** (censor `private_fill_lacks_entry_lineage`, observed orders <5 lots): BICWAL, CHABOU, ECHHAR, IMAKIN, KYMCLA, TOKHUR, TROISO, VISNIK, WINARS, ZHESHE (ATP_CHALL 07-20), FERREN, GORMIN, LANRAD, TOTRUG (WTA_CHALL 07-20). Three of these carry a proven-W1 leg with a merely-censored sibling. The exclusion evidence is receipt-flavored but the leg status is a censoring code — a letter violation of "exclusion only by proven outcomes."
- **D2 — 18 events wrongly *included* in the 240** despite a receipt-proven non-Window-1 fill leg (the bound never consults `proven_non_window1_fill`). A receipts-only tight bound is **222–233**; every variant remains far below 603, so `target_proven_failed` stands for this candidate under any correction.
- **D3 — unsound disjunct in the non-W1 proof rule**: 4 of the 75 non-W1 legs are "proven" only by completion > safe-cutoff on clean-interval events while filling 2,867–9,900 s *before* the live-by bound — timing-ambiguous converted to a definitive negative. Strict-rule counts: 62 events / 71 legs. Descriptive only; none touches numerator or bound.
- **D4 — earliest-bound live-by selection under-proves negatives**: on 7 events (GRABER, HRUBUY, SCHHOU, PENFAL, NIJDEN, VANTAN, YANTRE) a usable public-tape onset precedes ≥1 fill completion but a weaker earlier candidate was selected; conservatism inflates the 240, never the lower bound.
- **D5 — one hash-chain anomaly**: FROZEN_HASHES pins `CAUSAL_TAPE_RECONCILIATION.json` at `bb423028…`/5,232 B — the working-tree rendition (verified byte-identical on the Codex worktree during the calibration review) which was never committed byte-exactly (blob is 5,063 B LF). Gate input, not a scoring input. The wider chain reconciles modulo git CRLF re-encoding, and two `EVIDENCE_RECOVERY_ARTIFACT_MANIFEST.json` entries remain genuinely CRLF-stale (the standing recovery-manifest test failure). Normalize hash basis to committed LF blobs.

## 2. The lawful English interpretation (certified)

On the 804 development events of 2026-07-12 through 2026-07-20, the OS strategy of record **as actually executed** — its real placements, cancellations, and non-placements — is proven, under lawful exchange-clock evidence, unable to reach the 75% strict dual exact-five Window-1 completion target: at most 240 (correctly 222–233) of the required 603 events could have succeeded even granting every evidentially unresolved event. **This verdict binds only that frozen historical execution.** It is not a full chronological-OS-family failure (no other policy was run, and the four proxy families were contractually forbidden from running); it is not a market completion ceiling (237 of the 240 bound events are timing-censored, and the tape was never used to bound fillable volume); and it is not proof the operator's 75% target is impossible (the provable-positive universe under current evidence is only 76 events, so no run could currently *prove* ≥603 — "unprovable with current evidence" is not "unachievable"). The verdict string `target_proven_failed` must never be quoted without this scope clause; the published REPORT.md leaves the scope implicit. Calling this a "full-OS replay" is improper: COMPONENT_COVERAGE shows exactly one component consumed for policy (historical_execution); the OS's feature-driven decision logic was reproduced from receipts, not re-executed.

## 3. Does the result reject only this frozen candidate? — YES

Confirmed no tuning, candidate search, threshold optimization, or policy-family comparison occurred: single policy_id throughout all 7,256 decision rows, one-shot execution enforced (`execution_ordinal: 1`, refused re-runs, explicit execution token), scope assertions verified against the ledgers. The 544 nonfill events are definitive failures only for the orders actually placed at the prices actually placed (including 308 causal non-placements); a different Vault-authorized policy could have acted differently on those events and is unbounded by this run.

## 4. Feature-censored = 804, explained

Per-leg attribution (recomputed): `aim_v2_shape_prior_excluded`, `raw_full_depth_unavailable`, `pinnacle_unavailable`, `policy_decision_feature_vector_not_exchange_clock_aligned` — each on all 1,608 legs / 804 events by construction; `bookmaker_unavailable` on 1,361 legs / 683 events; `bbo_top5_unavailable` on 10 legs / 5 events. The flag is a **disclosure counter that gates nothing**: fills, nonfills, timing rulings, and bounds derive solely from receipts, the start ledger, and the public tape. Dynamic-floor evaluated = 0 because it requires a strict dual (there are none); dip/catch = 10 because 3 of the 13 proven legs have no recut cell at their reference price (WTA_CHALL 56/82/86¢) — neither is caused by the blanket censors. Pinnacle, full depth, and AIM_V2 were **not** silently replaced: no fallback path exists in code; missing features yield None, never substitutes.

## 5. The 31 historical duals / 27 under-par — materially reclassified

The fills are unchanged and real (VWAP sums reproduce all combined costs exactly). But the new exchange-clock evidence **falsifies the "merely unprovable" framing for most of them**: 17 of the 31 (15 of the 27 under-par) now have ≥1 leg receipt-proven post-start (fill completion at/after a usable known-live-by on the public-trade exchange clock) — permanently non-promotable. 7 more are effectively stuck (an in-ledger tape onset precedes a fill completion; better evidence can only confirm non-W1). 7 remain open (YEVCAM, NEUPRA, MARBIT, ALCTAB, SMIILA, AKSVAL, DILFAL), but with fill-to-onset margins of 74–244 s on six of them, realistically **0–2 promotable, ~1 (ALCTAB, >3 h margin — and at exactly 100¢, so not under-par)**. Promotion mechanics confirmed: an upgrade to exact/clean-interval start with both completion clocks ≤ the cutoff mechanically yields a strict dual completion from the same unchanged fills; feature censoring is no blocker. None of the 13 proven-W1 legs belongs to the 31.

## 6. AIM_V2 — fully excluded, nothing independent disabled

Exclusion is mechanical (allowlist `prohibited_input_sha256` includes LATCHCAL `6183ddec…`; `bind_policy` raises if bound; no `--shape-prior` argument exists). The recut-cell surface consumed for reporting is `seqfloor_20260708/recut_cells.json` — realized-fill edge-percentile lineage (2,435 pairs → 4,870 legs, commit 2f59130e, 07-08), created independently of and prior-to-in-derivation from the AIM_V2 pipeline (LATCHCAL, c8c91b33, 07-06, estimator lineage). Decisive fidelity fact: the deployed OS consumed LATCHCAL only in a logging-only shadow path; the live order path keyed cells from `volume_20260709/recut_cells_volume.json`. Excluding LATCHCAL therefore breaks no Vault-authorized instrument with independent lineage.

## 7. The failing grid test — genuinely deprecated

`window1_policy_runner.py` is a dead duplicate of the pre-rename generation: referenced by no code, gated behind a permanently-false validation flag, iterating policy ids the allowlist now forbids. The candidate-family/ablation machinery the tuning lane needs lives in `window1_fit_benchmark.py` (17/17 tests green; pinned as `future_fit_runner` by the corrected replay, the freeze, and the adapter contract). Disposition: delete the runner + its test (or mark the test skipped with the supersession reason). Separately, the recovery-manifest hash test failure is genuine receipt drift (two CRLF-hashed entries) and must be repaired, not skipped.

## 8. Recoverable vs permanently unavailable Window-1 evidence

Recoverable (lawful, without changing fills or inventing outcomes): official provider start timestamps via a historical public-milestone pull (rejected on all 31 duals only by the deployed six-hour freshness corridor — the largest single lever on the 76-event provable population); VPS raw milestone_shadow rows (07-15+); VPS raw intraday tennis_db live scores (retention doubtful, "PARKED"); VPS raw live_v3 order/fill logs; strictest-usable live-by re-selection (fixes D4 with evidence already in the ledger). Permanently unavailable: Pinnacle/multi-book for the window; sequence-valid full-depth ladders; the 703 raw acceptance bodies; the 14 lost top-20 depth files; per-ticker ws_depth live transitions (0 valid); exact starts for events with no provider record.

## 9. Minimum requirements before development-only candidate tuning across the full Vault-authorized OS family

1. **Predeclare the family**: extend WINDOW1_POLICY_ALLOWLIST with the explicit Vault-authorized policy ids and freeze the candidate-spec sha256(s) in a receipt *before* any scoring; keep the proxy prohibitions and prohibited-input shas.
2. **Publish the corrected bound**: restate the optimistic bound receipts-only (222–233), consulting proven non-W1 legs and excluding censoring-only disqualifications (D1/D2), and repair the D3 disjunct and D4 bound selection.
3. **Grow the provable-positive population first**: execute the historical official-start pull + VPS evidence transfer and re-issue the real-start ledger before scoring, so tuning verdicts are not pre-capped at 76/804 provable events.
4. **Causal feature basis**: candidates may consume only exchange-clock-reconstructable features (true prints, top-5 ticks, seqfloor recut cells, non-LATCHCAL shape data); Pinnacle/full-depth/AIM_V2 stay censored, never proxied.
5. **Hygiene**: normalize all hash receipts to committed LF blobs (re-pin CAUSAL_TAPE_RECONCILIATION; fix the two stale manifest entries); retire the deprecated policy-runner test so the lane starts green.
6. **Scope law**: development window 2026-07-12→20 only; the Jul 24–26 forward holdout stays sealed (single evaluation, operator ratification still outstanding); every tuning verdict carries the strategy-scope clause and the dynamic-floor gap beside any fixed line, per Vault law.

— Independent cross-review (Fable seat), read-only. Recomputation scripts and outputs retained in session scratch, outside git.
