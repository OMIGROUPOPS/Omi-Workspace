# V52k Iteration 10 - library-backed level evidence

Date: 2026-08-14

V52k changes clause 3 only over the frozen V52h behavioral lineage. A
receipt-local level may be supported by the game's own post-onset tape or by
the hash-bound G-grid cell discount and G3 dip-recovery gradient. Library
evidence may license a level below the game's shown range, but never below the
library-supported floor, never at or above the current ask, never without the
frozen clause-2 read, and never outside clause 6's joint pair-sum law. The
prior identity, value, status, source commit, and SHA are recorded on every
library-backed receipt. No constant or timing rule was added.

V52i and V52j remain reverted and non-validating; V52k imports V52h directly.
Clauses 1, 2, 4, 5, and 6, the disagreement referee, trades-as-truth
crediting, scavenger OFF, and `REFLEX_POST=0` remain frozen.

The source implementation identity is
`8dfcfbcdfb41e6ef9b54a0780b9199dc52593cc3`. Its cohort seed SHA-256 is
`697627cf6f2b025cf85d4bdc716bc8d44a143957b2614d052057be5e4493dc99`, and
the resulting 30-event list SHA-256 is
`96e0750296b438cc778779eb737c989aabadeb77d439dfa7943c707734b86d4d`.
The population is the five frozen pins plus 25 fresh events with zero overlap
against every V52b through V52j fresh cohort. GUEGOM is replayed separately as
an explicitly reused named observation and is excluded from the fresh 25.

On the 30-game observation, V52h and V52k are identical on outcomes: 16
complete-at-delta, zero complete-at-loss, 12 partial, two neither, 44 credited
legs, and 33 cents of locked delta per contract. Both frontiers are
1/1/3/16/16 at <=93/<=95/<=97/<100/any.

V52k evaluates 437,510 library-evidence receipts. Library evidence is
applicable on 33,603 receipts. Actual final rests stand below the game's shown
range on 1,021 receipts across two legs in one game; every such rest is one
cent below the shown minimum. This nonzero count passes the clause-activity
claim. There are 2,055 library-backed rest mutations and two behavior-changed
leg streams; all changes start at or after the authorized clause and there are
zero frozen pre-clause differences.

The faller claim does not validate. Seventeen faller fills have median delay
zero versus V52h and median entry-minus-later-floor two cents. The sole delayed
case is SANDAN's SAN leg, delayed 22,215.007 seconds and filled at its recorded
floor. Climber preservation passes: 26 V52h credited climber legs and zero
lost. Mean banked delta remains 2.0625 cents and does not rise. V52k creates
zero and resolves zero one-sided exposures. All five pins are unharmed.
GUEGOM does not convert; that observation caused no edit.

The floor comparison remains explicitly `CURRENT_PRE_RE-CUT`, sourced from
the `22441e05` post-onset offer census. Its statistics are not a final grading
ruler and await analysis-seat regrading.

Two clean named builds are byte-identical across 35 files. Two clean cohort
builds are byte-identical across 167 pre-determinism files. The complete
21-file inherited-plus-V52k suite passes 766 assertions with zero failures,
omissions, or deselections. Full traces are retained in bounded gzip chunks.

This is a 30-game observation only. No full-804 run, sealed population,
deployment, authorization, live, network-runtime, order, position, exit,
settlement, or DCA action occurred.

Canonical packages:

- `.claude/window1_live_v4_replay/v52k_library_backed_evidence_20260814/`
- `.claude/window1_live_v4_replay/v52k_guegom_named_observation_20260814/`
