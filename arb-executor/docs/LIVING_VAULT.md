# THE LIVING VAULT — the one chronological ledger (est. 2026-07-07)

> **THE CHRONOLOGY LAW: entries closer to today trump anything prior; older entries are context, never instruction.** When two entries collide, the newer one governs and the older one belongs in RETIRED. Nothing in this file is append-anywhere — new entries go ON TOP.
>
> **READ ORDER, every session:** `.claude/BOARD.md` (the standing queue) → **this file, top down until you hit dates you've already lived** → `docs/LESSONS.md` (A–G principles + C-law arc) → `.claude/rulings/` (verbatim law). The prior-art gate (C45) greps ALL FOUR surfaces.
>
> **Merge provenance:** consolidates JUNE_VAULT §0A–0E/§1/§4E–4I/§5 (canonical copy frozen on `blend/agent-derivation` @d2faa9c3), the two VAULT_PENDING docs, and the July working record (inventory: `.claude/INVENTORY.md`, 2026-07-07). Each surviving lesson appears ONCE, at its most-current form, with lineage. Every dead view is in RETIRED at the bottom — graves are findable, never re-discoverable as new insight.

---

# FRONT PAGE — THE ENTRY DOCTRINE

**This page IS the contract AIM_V2's arm gets judged against.** (AIM_V2 state: built + operationalized under the Plex ruling, GATED-OFF, ramp forecast pending honest n≥30 per tier — see ledger 07-06.)

## §0A — THE OPERATOR'S FRAME (verbatim, from the June Vault; unchanged and in force)

⛔ STANDING ORDER — EXITS ARE OUT OF SCOPE UNTIL THE TWO ENTRY PROBLEMS ARE SOLVED. (2026-07-02, operator.)
- The exit strategy is SOLVED and VALIDATED: positive at every level on the 14k-N foundation (Atlas / Druid's foundation). Those projections were CONSERVATIVE — scored on TAKER trades at the T-20m mark (the worst entries: crossed, late, the sucker's anchor per A49). Real maker entries earlier in premarket can only beat that baseline.
- Any "exit looks negative on live trades" reading is a SYMPTOM OF BROKEN ENTRIES, not evidence about the exit. Do NOT gauge exit policies on live trades until entries execute per strategy. P6's E2-negative-frontier reading is this artifact — struck as exit evidence.
- THE CAUSAL CHAIN: the ENTIRE premarket is the entry canvas; the historics show drift edge FOR ENTRY PURPOSES — enter at discount on the drift → the tailored exit becomes an EASIER SWING → the validated exit does the rest. Entries exist TO GAIN AN EASIER EXIT.
- THE RECURRING FALLBACK, named: whenever entry work stalls or a study surfaces an exit-shaped number, analysis drifts to "maybe the exit is the lever." It is not. The tell: any sentence proposing to change/score/re-target exit policy while combined ≥98 fills and one-sided strands persist. When the tell appears: STOP, cite this order, return to (1) fill BOTH sides in W1/W2, (2) fill at combined ≤97. **[RETIREMENT STAMP 07-15 (C-ONE-RECORD): the ≤97 clause survives as the pair SCOREBOARD only; as a leg-2 pricing MECHANISM it is buried — see THE PAIR-97 GRAVE below + C-PAIR-LAW (orientation-composition). Chronology law: the newest ruling governs.]**

⛔ PRIORITY 1 = PAIR: rest bids on both legs of every tracked game, always — never skip a game or leg on a projected combined. PRIORITY 2 = pair at good cost: ~~the combined cap binds the SECOND leg's target after the first fills~~ **[RETIREMENT STAMP 07-15: this sentence taught the buried mechanism — cap-binds-leg-2 is THE PAIR-97 GRAVE (operator: "IT NEGLECTS LEG 1 AND FORCES LEG 2"); the governing frame is C-PAIR-LAW orientation-composition + each leg at ITS OWN divot. The ≤97 SCOREBOARD stands.]** — a walk constraint, never a participation filter. Operator: "cant gage a trade if we dont even trade it."

**DEFINITIONS (canonical — correct all prior usage):**
- **DISCOUNT / DISTORTION = the pair-level measurement of entry success:** the combined cost of the two legs. **≤97 = success. 100 = par. >100 = TOTAL FAILURE regardless of fills** — no tailored exit can save a pair that cost more than it can pay. It is the scoreboard of the entry, not a per-leg event.
- Operator verbatim: `distortion is just the combined cost of 2 legs. its a measurement of our entry success. sure we may fill, but if the combined is anything over 100c its a total failure.`
- **The three observable prices: best bid, best ask, last traded.** Nothing else exists on the tape. Never anchor analysis on constructed mids.

**ENTRY DOCTRINE (sequential, inverse-aware):**
- Operator verbatim: `key word divots and inverse relationship. 2 legs firing at the same time isnt necessarily a good idea - especially if we know there is a shape that will weaken one of the sides`
- The legs are **inversely related** (seesaw, paired_mid_sum ≈ 0.998): the weak moment for one side is the strong moment for the other. Therefore **fill the two legs AT DIFFERENT TIMES, each at ITS OWN divot, in Window 1.** Simultaneous both-leg posting at static targets is NOT the doctrine.
- This **CORRECTS Build 1**: "always-lay-both" = always be **WORKING** both legs (each hunting its own divot), **not** "post both simultaneously at cell targets."

**ENTRY HAS TWO ELEMENTS:** 1. **Historical analysis → the assumptions** (cell tables, shapes, drift tendencies). 2. **The current tape → the validation** (best bid / best ask / last traded, live). A fill that satisfies element 1 but ignores element 2 (HUIAHN: target 75, tape offering 41-54) is a **failed entry**.

**THE WINDOW STRUCTURE (settled, §5):** Window 1 (premarket drift) + corridor + Window 2 (in-match). Only W2 is dictated by the game. A discounted entry has **three shots at its tailored exit, two before the game can hurt it.** Late/expensive entry = W2-only = the knife.

**§5 EARLY QUIET IS VOLUME-CONDITIONAL, NOT TIME-CONDITIONAL (operator doctrine, 2026-07-08):** the clock does not open a game's window — **flow does**. The per-game flow-state (prints/min + spread-tightening, per cat) is the primitive; clock distance (T-8h, T-4h, any T) is only ever a proxy for the flow-state distribution at that distance. Corollaries: (1) the CONCEPTION HORIZON below is a map edge on unstudied territory, never doctrine about time itself; (2) conception/aim-activation should ultimately key on observed flow-state, not clock distance — the early-canvas study fits the transitions; (3) **distributions over medians** — medians locate mass, never fences; a fence (when does the window EVER open) is a tail question and gets tail statistics.

**OPEN ANALYSIS DEBTS (named, tracked on BOARD):** FV-PER-LEG (never built right) · THE EXIT IS WINDOW-BLIND (found in code, unfixed) · tts@fill vs scheduled start (re-anchor on tape onset — largely addressed by the honest clock/flip, residual on BOARD) · PRE-T-4H (first look done 07-06: B3 §4 T-8h coverage sized; exploitation spec = week deliverable (c)) · THE MISSED-OPPORTUNITY LEDGER (half-built: the B3/gold counterfactual machinery is its first real installment).

**THE TWO-PROBLEM FRAME (June's entire scope, operator's words):** (1) not filling in Window 1/2 on both sides of a market; (2) when we do fill, not filling at a good price — where good = combined ≤97. Everything else is a sub-issue of these two. **[STAMP 07-15: ≤97 here is the MEASUREMENT of (2), never a leg-2 pricing instruction — see THE PAIR-97 GRAVE.]**

**OPERATIONAL INVARIANTS (2026-07-01, Plex, verbatim):** 1. Tier aggregation is doctrine-hostile. Report tier-partitioned or don't report. 2. Boundary contamination invalidates window-scoped scoring. Old numbers do not carry forward. 3. Ceiling results outrank contamination caveats. 4. Peak-concentration ≠ policy scope. 5. Closure over deferral.

**THE OPERATOR REPORTING LAW (2026-07-08, operator):** every operator-facing time — reports, close-outs, ledger entries, monitor lines, artifacts — is **EASTERN, 12-hour am/pm, converted at the edge** of the report. UTC/24h lives internally only (code, jsonl, cron). The CC close-out template inherits this law; a UTC timestamp handed to the operator is a defect, not a style choice.

## THE GOLD CENSUS RECIPE — tight / early / under-the-wall (measured, 07-06/07)

What separates GOLD legs (W1-filled AND W1/corridor-cashed) from RODE legs, in their own tape terms (SLATE_LEDGER §7 + B3 §5, n=58 vs 44):
- **TIGHT:** gold fills land **med 4¢ above their own W1 lows [1–11]; rode lands 11¢ [-2–23]**. Discount-to-low is the strongest single separator between the classes.
- **EARLY:** gold's low prints med **+29min AFTER its fill** (in position before the divot completes); rode's low is +5min and 16/44 fills chased a dip already gone. The riser fills early by doctrine; the faller is late by doctrine — role-relative (AX2 reissued), never per-leg-cheapest (that gauge is RETIRED).
- **UNDER-THE-WALL:** the aim is the SIZE-BACKED sell-flow dip level (A49 / dip_hunt convention), not FV and not a hard offset — a resting bid paid a discount by the flow, below the quoted wall, not behind it (queue starvation: ALTMED grave) and never above it (chain-chase: TANKAW exhibit).
- Band distance at fill is 7¢ [4–13] for BOTH classes — the band is not the separator; the entry is. The discount explains who ENDS UP gold; it converts only the ~quarter of rode legs whose bands ever arrive (B3 §2) — the rest is the sibling/band-arrival story.

## THE B3 DECOMPOSITION (07-06, B3_DISCOUNT_COUNTERFACTUAL — Lane-1 ratified bar)

B3 = both legs filled, ≤97, but ≥1 leg RODE to settlement. The rode legs vs their own lows: filled AT their lows, **32/44 still never see their band pregame** — the +$53 NET at LOW is mostly cheaper-basis riding (smaller losses), not cashing. **The discount is HALF the recipe: it cuts the bleed ~40% but does not manufacture band-touches.** The other half is completion timing — night-1 leak decomposition: **half_timing dominates (~131¢/night): the fader's dip passes BEFORE leg-1 fills, so the bound doesn't exist yet.** Sequential completion structurally forfeits the early fader divot → the pre-T-4h posting-window spec (week deliverable (c)).

## THE 11¢-MISSED-LOWS FINDING (07-06)

The one number that indicts current placement: **rode legs fill med 11.0¢ above their own T-4h window lows** (28/44 lows print AFTER our fill — we buy, then it gets cheaper). Gold misses by 4¢. AIM_V2's job is to close that 7¢ gap with per-cell dip aims; its arm is judged by gold-leg production + B3-conversion (ratified Lane-1 bars), NOT by settlement P&L at small n (C46 Lane-2).

## THE EXPRESSION INVARIANT (Plex-ratified 07-07; carry live, clamp gated OFF)

- **JOIN/IMPROVE ratified:** a buy target is expressed vs the NON-SELF chain (`_book_ex_self`) — rest AT target when target ≤ bid_ex_self; above the chain: join it or improve by EXACTLY 1¢. A walk step above bid_ex_self+1 is structurally impossible with the flag on. Invariant = step law; walk cap = journey bound (cap sizing rides separately, Plex pending).
- **NEVER-MARKETABLE: unconditional, armed** — no maker buy posts at ≥ best_ask, every placement site.
- **Mid-spread creation: DEFERRED** (census: 0.3% of placements, $0.85/day — the T47 coin; ruled later with shadow evidence).
- The census that grounds it (SPREAD_EXPRESSION, 4,158 placements): **54% of our book rests BELOW the chain at a 9.1% fill rate vs join's 17.2%** — the below-chain/join split is where posture policy lives. All postures fill 19–30¢ above eventual window lows: the corridor-timing story is posture-independent.
- Ex-self observe fields (`bid_ex_self`, postures) are LIVE in every aim_shadow line since 02:34:33 ET 07-07 (the "never landed" verdict was a console-truncation artifact — C47).

## RAMP / GUN STATE (as of 2026-07-07)

- **AIM_V2:** built + operationalized (LATCH-CAL canonical clock, w=0.25 prior, dip ITF/CHALL only ≥3¢ floor, mains NULL full stop, hierarchical fallback ⑦ with borrowed_from + inflated resid_sd). **GATED-OFF.** The RAMP = a dated schedule to honest n≥30 per (cat,Tbin) tier — arm waits on it plus this front page's bars. **First named live test (GOMOFN, 07-07): the table's 16-17¢ aim would have MISSED the fill — 16-17 never printed pre-bell (min 19¢); the 21¢ fill was within 2¢ of best available.** Evidence AGAINST deeper thin-book pre-bell aims; depth is not free.
- **Gun/latch:** tape latch is the live-cancel authority (match_live via volume-burst + latch_tape_override armed); honest clock (per_match_clock) LIVE since the 07-06 flip (Ratification #20, post-flip audit clean); scale_gun stays SHADOW (C46 first proof: SHADOW-INSUFFICIENT-N); grace-kill armed; bell detection 65% (BELL_FEASIBILITY) — the tape remains the only start-time source (START_TIME_JOIN), with observed_starts now banking real bells collector-side (C-RETENTION, 07-06).

## THE S-TIER, CONSOLIDATED (operator, 2026-07-08 — supersedes every prior S form, incl. the 07-07 per-leg redefinition)

**S is earned by three tests, all mandatory:**
1. **Both legs W1-filled.**
2. **Both cashed in W1/corridor.**
3. **Combined ≤97 achieved BY LOGIC** — each leg priced **independently off its own fitted value and its own divot**, on a **full input read**: both prices, category, non-self chain, **honest clock, flow state**. **[STAMP 07-15: this test is the ANTIDOTE to the buried mechanism — the provenance clause already convicts 97−basis pricing; the grave below names it in the operator's words.]**

**THE PROVENANCE CLAUSE:** any leg priced from the sibling's accounting — goal−basis, cap-arithmetic, any INTERIM-ARCHAIC form — is **S-INELIGIBLE regardless of outcome**. A cap-forced 96 is the 97-wall in costume. **The grade certifies the DECISION, not the luck.**

**Honest-era regrade under the clause: S = 0.** The 07-07 per-leg S=3 all die on provenance: BARSIM leg-2 "aim" 75 = 97−22 · WALVAL reaim to 65 with explicit `goal: 97` − basis 32 · SCHZID goal 27 = 97−70 with `aim: null`. Era-wide: **511/662 (77%) of sibling placements are priced at exactly 97−leg1_basis** — the completion leg is cap arithmetic almost everywhere, so the S tier is empty **by construction** until independent per-leg pricing exists. The near-miss shape exists (EKSLUX 07-08: both legs independently priced joins, combined 96 — killed by the input read: lying clock, no flow state). **S-per-night is the arm's truest scoreboard.**

**DYNAMIC S (ratified 07-08 night, `RULING_DYNAMIC_S_CELL_AIM`):** the S price reference is **Σ per leg (own W1-close − edge_p50(cat, cell))** — the pair's own cells define its bar. The 79/79/92 constants RETIRE to corpus-median footnotes. Honest-era regrade under the dynamic bar: **S = 0 unchanged** — every candidate dies on the provenance clause before the price bar is consulted; the dynamic bar governs the ledger renderer from 07-08 night forward. **CELL-KEYED AIM ratified same ruling:** AIM_V2 aim key = (cat, cell) — <50 JOIN · 50–74 shallow · 75–94 deep (edge_p50 per cell); `recut_cells.json` = the aim-surface prior with per-cell timing priors; **arm gates unchanged**.

## THE GRID (2026-07-08, operator — the ONLY analysis frame)

**90 cells/cat ([5,95) 1¢ price cells) × 6 cats. Every leg keys by its OWN price cell.** The fav/dog binary is **RETIRED as a frame** — it was a proxy for cell (depth is a property of the cell: edge p50 = 0 below ~cell 50 in every cat; crossover ~50–55 ITF / ~80 CHALL; the heavy zone 75–94 carries the depth — SEQFLOOR_RECUT 07-08). **ITF exit provenance, PINNED: ITF_M grades on the ATP_CHALL exit surface, ITF_W on WTA_CHALL (`ITF_EXIT_BORROW`, live_v4.py:288; the durable exit dir holds exactly four native parquets, no ITF) — label borrowed bands wherever they grade; an ITF readout that silently blends borrowed-exit grading is contaminated.** Lineage: **April 56-cell → VERSION B 41-cell → current 90×6.** Older frames are history-only under the chronology law; **any future analysis quoting a retired grid (56-cell, VERSION B, fav/dog-as-frame) is a C45 failure.**

## THE GAME LIFECYCLE (pinned 2026-07-09 — the 8 steps; ruling verbatim at `.claude/rulings/GAME_LIFECYCLE.md`)

**QUALIFY (staged, volume) → CONCEIVE (horizon, anchored clock) → POSITION (by regime per cell: climb-side resting T−90 at fitted level / decay-side join-late / mains join-at-touch) → THE WAKE (= the dip; hold-gate) → CAPTURE (each leg's own moment, never chase the ascent) → THE GUN (fused, no new buys) → CASH W1/corridor (flat before resolution) → GRADE (dynamic-S, the combined is the receipt).** Each step cites its fitted source in the ruling. **Terminology: CLIMB-SIDE / DECAY-SIDE replace fav/dog** (defined in the ruling; cells define sides per THE GRID).

## THE COMBINED-PRICE CLAUSE (operator, 2026-07-08 night — supersedes EVERY prior 97-framing; ruling verbatim at `.claude/rulings/RULING_COMBINED_PRICE_CLAUSE.md`)

**No fixed number defines a good entry. The combined price of a pair is a MEASUREMENT with exactly one honest reference — what THIS pair's tape actually offered — computed from the historics per cell, never decreed.** The derivation (honest axis, lineage cited): (1) each leg's achievable discount is FITTED — edge_p50(cat, cell), the median fillable dip below the leg's own W1-close (SEQFLOOR_RECUT, 4,870 legs, recut_cells.json); (2) the structure is cell-conditional — edge ≈ 0 below the ~50–55 crossover (those legs' floors ARE their closes, arriving in the final minutes), rising to 6–19¢ med in the 75–94 heavy cells (where the bleed and the opportunity coincide); (3) **the pair's DYNAMIC FLOOR = Σ both legs (own W1-close − edge_p50(cat, cell))** — each leg hunted at ITS OWN divot at ITS OWN time (inter-divot gap p50 41–62 min) — **different for every pair: a 30/70 CHALL pair floors near 92; an 8/92 ITF pair near 79. One fixed line cannot grade both**; (4) success = each leg within N¢ of its own fillable low (gold N=4), independently priced (provenance clause), at the divot not the reprice — **the combined lands at the floor as a CONSEQUENCE. Grade the decision per leg; read the combined as the receipt.**

**THE ROLES, fixed:** 97 = the goal-law BOUND (interim completion arithmetic — dies at the cell-aim ramp; its 94%-census re-prints nightly as DRIFT-3, a countdown, never an achievement) and the emergency ceiling thereafter — nothing else · 100 = par; >100 = total failure regardless of fills (operator verbatim, unchanged) · progress = the gap between achieved combined and the pair's dynamic floor + the per-leg dip-catch rate.

**CONTAMINATION RULE: no report, renderer, or agent may headline a "≤97 rate" (or ANY fixed-line rate) without the dynamic-floor gap printed beside it** — a fixed line grades a 96 and an 85 identically while the fitted floors say most 96s underpaid the tape. **RENDERERS INHERIT:** every combined printed carries its dynamic floor alongside (position_grade live ✓; DRIFT-5 cross-tab refresh adopts it; future surfaces refuse fixed-line grading at review). The §0A "≤97 = success" line above reads as the historical frame this clause refines: the measurement stands, the fixed line retires to the two roles named here.

## THE DEPTH-RELATIVE AMENDMENT (operator-caught, 2026-07-09 — the fixed-line grave's FIFTH costume)

**Fill probability is depth-RELATIVE to the book's own spread, never an absolute cent line.** The 3¢ absolute threshold in the first volume pass graded a 6¢-spread ITF book and a 1¢-spread mains book with one ruler — mains' "~1% at every volume" measured DISCOUNT SCARCITY (3¢ never prints inside a 1¢ world), not fill probability. Canonical depths: JOIN (the standing touch) · touch−1 · 1×spread below mid · 2×spread below mid. **The fixed-line grave's costume lineage, dated: (1) 97-as-goal/combined-as-target — killed 07-07 · (2) cat-constant S-lines as the bar — 07-08 · (3) per-leg S on price alone — 07-08 · (4) fixed-line combined grading — 07-08 · (5) absolute-cent depth — 07-09, operator-caught.** Every future threshold states its denominator.

## THE CATEGORY LAW (operator, 2026-07-09)

**Each category is a DIFFERENT MARKET with its own physics** — fill mechanism, opportunity shape, volume regime, spread scale, time signature. **Nothing generalizes cross-cat without per-cat evidence. No metric, threshold, gauge, posture, or grade is defined once and applied everywhere. Any cross-cat aggregate prints WITH its per-cat decomposition or fails review.** The flattening-grave lineage, six kills, dated: **07-01** tier aggregation ruled doctrine-hostile (Plex invariant 1) · **07-07** the mains-early hypothesis refuted ("per-cat mechanisms differ, never flatten" — FLOOR-BY-HOUR) · **07-07** per-cat S-lines replace the single 97 target (pair-story) · **07-08** fav/dog flattened into the cell key (THE GRID) · **07-08** per-cat gauge thresholds, mains excluded (EARLY_CANVAS) · **07-09** the absolute-depth line across a 6:1 spread spectrum (this amendment's own trigger). The MAINS REGIME question (thin-edge high-frequency at the touch vs ITF's fat-divot low-frequency) is this law's first live application — if two regimes exist, the consumption layer carries two postures, per the cell: macro says which regime, tape says when.

## THE SETTLEMENT VOCABULARY (operator, 2026-07-09 — ruling verbatim at `.claude/rulings/RULING_SETTLEMENT_VOCABULARY.md`)

**A market SETTLING = the game ending — universal, meaningless. A SETTLED POSITION = an exit that never filled — a FAILURE state, the F-bucket, never neutral bookkeeping.** The doctrine lifecycle never touches settlement: fill W1 → cash W1/corridor → flat before resolution. **No report uses "settled" as a synonym for "concluded" for OUR positions; every realized-P&L table splits "CASHED VIA EXIT" from "RODE TO SETTLEMENT" as separate columns, ALWAYS.** The law's own proof-case, same day: the EXTENSIVE's settlement-only attribution read +$3.87 and concluded "nobody paid us back" — the CASHED-VIA-EXIT column for the identical window is **+$163.41 across 323 exit fills** (ITF_M +69.85 / ITF_W +63.10 / ATP_CHALL +20.77). The exits paid us back; settlements measured the residue of failure. One vocabulary error inverted a day's read.

## THE GRANULARITY LAW (operator, 2026-07-08 — completes §0A's two-element frame; ruling verbatim at `.claude/rulings/RULING_GRANULARITY_LAW.md`)

**The HISTORICS (per-minute corpus) are the MACRO instrument** — cell edges, drift shapes, floors, dip-timing distributions, THE ASSUMPTIONS. **They can never time an entry.** **The SUB-SECOND LIVE TAPE is the MICRO instrument** — the three observables per tick, the non-self chain, ask-hold/divot conditioning, flow state: the entry mechanics, THE VALIDATION, the trigger. **Every entry decision = macro assumption × micro confirmation; either alone is a failed entry** (HUIAHN = micro ignored; a reprice-chase = macro ignored). Corollaries, binding: **no per-minute-derived number may ever be consumed as a timing trigger** (the bell-recovery TRAINING-grade/CERTIFICATION-grade split is this law applied); **no live-tick pattern may override a fitted cell edge without the study that refits it.**

## THE W1→W1 MULTIPLICATION MANDATE

The money machine is a PRODUCT, not a sum: **P(both legs fill in W1, each at its own divot) × P(the exit cashes in W1/corridor)** — E18/B23's bilateral funnel (multiplicative composition, ~21-23% unconditional double-cash ceiling measured on the foundation). Vault §0E made it the grading law: **A requires the W1 shape; BOUHAR (both legs W1_CASHED) is the headline metric; baseline ZERO across 257 pre-flip legs, 2/357 at the last full cut.** Every entry build is judged by whether it multiplies BOTH factors — a deeper fill that pushes the cash past the corridor, or a faster cash that costs the fill, fails the mandate. This is the frame that unifies the recipe (tight/early = factor 1) and the B3 decomposition (band-arrival/timing = factor 2).

---

## THE RECORD MANDATE (operator order, 07-15, VERBATIM AND PERMANENT)
**The consolidated record — LIVING_VAULT, the rulings stack, the class ledger, the constraints file, LAW.json, the registries — is the operation. Every organ, every seat, every model anchors there; nothing anywhere speaks or trades from memory when the disk disagrees.** Enforcement: retirement stamps ride every superseded frame on every operator-facing surface; an unstamped superseded frame is a NAMED DEFECT CLASS, swept at every C50; doctrine_registry.json is machine-rebuilt at every C50 and every seat shows its receipt before doctrine-touching output.

## THE PAIR-97 GRAVE (operator verbatim, 07-15)
**"IT NEGLECTS LEG 1 AND FORCES LEG 2."** The mechanism, named: the conviction leg (leg 1) is demoted to accounting — its own value read abandoned the moment it fills — and the completion leg (leg 2) is COMPELLED at a subtraction price (97 − basis), post-divot, into the dumped side. 511/662 (77%) of era sibling placements priced at exactly 97−leg1_basis; the S-tier is empty BY CONSTRUCTION under that mechanism (the provenance clause). The ≤97 combined survives as the pair's SCOREBOARD — the measurement of entry success — never as either leg's pricing instruction. Governing frames: C-PAIR-LAW orientation-composition (entry unit = the pair, each leg at its own divot) + DYNAMIC S (each leg off its own fitted value).

# THE LEDGER (newest first — the chronology law applies)

### 2026-07-15 · −0l FIXED SAME NIGHT (operator word: "size the stop window to the book, full gate, now") — both stop paths sized; the abandonment branch deleted by construction

**The build (`c6163580`):** bot drain budget = 5s + 0.25s per resting entry bid (floor 10s, cap 180s), computed at signal time from the drain's own criterion, printed with the count; `_force_exit` prints the sized budget. Script `STOP_WINDOW_SEC=200` (strictly above the bot's cap) waits for actual process death and prints the measured stop time; the only reachable failure branch is old-bot-alive-past-its-own-watchdog → BOT_DOWN-channel alert with the OLD code still trading — **never a dead book**. **The proof's teeth:** tonight's 10s budget expired before `shutdown_drain_begin` ever logged (LOOP_LAG 3.4s at the time; no manifest → the drain-replay had nothing) — the incident wasn't a slow drain, it was a budget that couldn't fit the drain's preamble. New budget on the same book ≈55s, inside the 200s window with margin at every point of the curve. **Lane 2 = this deploy's own gated restart; measured stop time + boot verdict below.** LIVE-LANE: STOP-RESULT-HERE. Watches: `stopped in Ns` per deploy; drain begin/done pairs mandatory on every graceful stop.

### 2026-07-15 · C-FLOW-REST-SEED v1 DEPLOYED (operator R1 GO word, same night as the founding exhibit): the consultation gauge gains an exchange-truth input — REST trades cache at the dossier moment, consultation-LOCAL, the gun's fitted WS counter untouched

**The word:** "R1 word: GO — REST-seed the flow gauge, full gate, tonight." **The build:** `_flow_rest_refresh` (REST `/markets/trades`, 45s cooldown on `tried`, data freshness on `ts` — a failed fetch never re-stamps stale trades as fresh; fail-soft) + fire-and-forget warm in `_route_event` (in-window events only, never blocks the decision slice) + the `_entry_dossier` merge (`p30 = max(ws, rest)` — REST only ADDS true prints; richer-tape median; `gauge_src`/`prints_30m_ws`/`prints_30m_rest`/`flow_bucket_ws` stamped on reach_law + flow_state; `flow_rest_seed` event on bucket flips, deduped). Flag `flow_gauge_rest_seed` DECREED, cited. **Boundary honored: `_trade_times`/`_trade_prices` never written — the percat gun reads its fitted input unchanged; a gun-input change is its own word.**

**THE OUTCOME REPLAY (per-game, tonight's slate, every consultation recomputed on the exchange tape at its own instant — `.claude/proof_20260715/PROOF_FLOW_REST_SEED.md`):** **19,141 consultations / 228 tickers replayed on the exchange tape: 171 bucket flips (~0.9%) — rare, and concentrated EXACTLY at onsets** (ITF_M 29: 17 quiet→warm / 7 quiet→open / 5 warm→open; ITF_W 141: 61/26/54; CHALL ~0). Certified founding rows: **KOA ws=1/rest=20 → quiet→open, p_fill 0.000→1.0**; YAZ ws=1/rest=3 → warm/0.010 (the amendment). The FOMCHA class is the extreme: ws=0 against a 179-print tape. Undercount warnings named per ticker (deep tapes bias AGAINST the seed — flip counts are floors). Behavior isolation proven: placed:path_aim dossiers aim==page path_aim **201/201** — the gauge feeds the record, not the aims.

**THE RESTART INCIDENT (owned, named, healed):** the gate PASSED clean ([1/1] lint · SMOKE PASS · outcome proof OK · C50 OK · constraints OK) but the restart step FAILED — the script's 20s stop window lost the race against the bot's own drain (255 resting orders; the bot's 10s shutdown budget overran → `[STOP] shutdown budget (10s) exceeded -> hard exit` landed AFTER the script gave up) → **the bot was DOWN ~8 min (02:28:35→02:36:35 AM), unnoticed by the script's own exit path**. Manual restart at the gated SHA in tmux; `last_deploy_sha` stamped `1cfb28b3`. **BOOT VERDICT: audit PASS (context boot, 8 positions / 255 resting orders, 0 failures / 0 flags); gun rebuilt n=100 all-sourced; drain-replay had no logged outcomes (hard exit outran the manifest) — the book verified whole by the exchange-truth audit instead.** NEW DEFECT NAMED → BOARD: **DEPLOY-STOP-WINDOW vs DRAIN-BUDGET mismatch** — the stop window must exceed the drain budget by margin, and the script must WAIT for the process to actually die rather than fail-and-abandon (the abandonment left a dead book with no restart — the blackout class in miniature).

**FIRST LIVE STAMPS (02:38:53 AM, ~2 min after boot):** 11 dossiers carry `gauge_src: rest_seeded` with `prints_30m_ws`/`prints_30m_rest`/`flow_bucket_ws` present; p_fill printing honest nonzero quiet-bucket values; `flow_rest_seed` flip events 0 so far (2:39 AM lull — flips expected at Euro onsets; the nightly watches them).

**TWO HONESTY CATCHES ON THE WAY (both filed):** (1) the exhibit's first draft put YAZ's honest bucket at open/0.449 — WRONG, amended to **warm/0.010**: the 30-minute trailing bucket LAGS a 26-second-old burst even on an honest input — **bucket onset-lag = a second named limitation, NOT cured by R1** (reach-law refit territory; evidence accrues from tonight's stamps). (2) the replay's own first run silently truncated the densest tape (20-page budget → KOA counted 0) — fixed with named truncation warnings + 429 backoff before certification; no-silent-caps applied to the instrument itself.

### 2026-07-15 · C-KOAY-EXHIBIT v1 (operator live catch, ~12:25–1:20 AM): the Koay/Yazdani autopsy = REACH-RECAL EXHIBIT #1 — the queued recalibration read EXECUTED; the reach law consulted a STALE FLOW BUCKET and answered p_fill 0.000 on a bid that filled 75 seconds later; the error's home is the INPUT, not the page, not the rates

**Safety first (exchange truth):** YAZ filled 5 shares @ 59¢ 12:27:35 AM **W1 (pre-gun by 37s)** with the exit posted the SAME SECOND (sell 5 @ 76, order `3a0e50d4`, confirmed resting at probe) — the kept leg was never naked. KOA's bid (31→29 pre-fill repost; the operator's "28" is the 29) was cancelled 12:33:27 by match_live_cancel on an evidence-grade gun — the pair went one-sided BY CANCEL. `pair_law_violation` correctly did NOT fire (fill-instant composition: 59 + KOA deep cast ≈16 = 75 ≤ 97) → **NAMED BLIND SPOT: cancel-made one-sidedness is invisible to the fill-instant stamp; the pair-law census must count cancel-stranded pairs beside stamp-flagged ones or it undercounts the class.**

**The autopsy (`.claude/reachrecal_20260715/KOAY_EXHIBIT.md`, full timeline + tape):** No completion re-aim ever ran (`completion_no_attempt: leg1_window_open_unset`); the sibling's own economics NEVER passed (M15 cross EV −5.67¢ then −4.53¢); the taker cross skipped on partial ask (1 share offered vs 5). The dog's dip (36–37¢) printed **50 seconds before the KOA bid existed** — the B3 half-timing shape verbatim. The operator's 38-low: **no 38 print exists on either leg through 1:18 AM** (the ~12:30 dip printed 46; first bottom 41 @ 12:39:45–56; running low 40 @ 12:51:19) — and by the coded WINDOW LAW it is **W2, not corridor** (the gun preceded the lying 5:00 AM schedule by 4.5h → the corridor was empty; `clock_liar` stamped).

**THE RECALIBRATION READ (where the rate error lives):** the dossiers consulted `flow_bucket: quiet (prints_30m=1)` → rate 0.0 → **p_fill 0.000 both legs**, while the exchange tape had ~15 KOA prints in the prior 71 seconds. On the honest bucket (`ITF_M|open`), the law prices YAZ 0.449 / KOA 0.346 over their gun-bound residencies — consistent with one fill, one miss. **(1) the flow-bucket INPUT is the error's home — the −0k WS-seen-vs-full-tape undercount class proven at the reach consultation site; (2) NOT the page depth — the p50 (16 deep) printed at the fill to the cent, deep tiers only post-gun (MIS-ANCHORED corroborated); (3) NOT the rate table.** Structural corollary re-proven: on ITF, discovery IS the onset and the gun follows in ~1–2 min — lawful p_fill mass requires presence BEFORE the burst (the W1 thesis). **REMEDY PRICED, awaiting the word: R1 = REST-seed the flow gauge at consultation (the C-TAPE-SEED pattern; flips the consulted 0.000 → 0.35–0.45; no decision-path change). NO aim change proposed; the R2 latch proposals are NOT advanced by this case (KOA's cancel was correct — the tape never returned below 41).**

**Record hygiene (same push):** the 12:20 nightly (`ADJUDICATION_20260714`) was committed on the VPS but never pushed — pushed (origin `80ce3751 → 738ced4c`, carrying the packet artifacts `RULING_PACKET.md`/`PACKET_STATUS.json` fired 07-14 CUTOVER-DONE, 12:40 AM refresh all doors PASS n=495/495/370, + census AUTO-GAPS). **The "40-mains-legs" HAS a disk record** (ADJUDICATION_20260714 §EARLY-CHALL: DEFECT, 40 pre-T-4h mains placements, 4 tickers named) — not struck; its adjudication stays queued. te_live tripwire (250 min): process alive, source shows 0 live matches (US-midnight lull) — no restart; the percat fallback caught KOAYAZ as designed.

### 2026-07-15 · THE UNFREEZE, VERIFIED (12:21 AM boot, gate take three — twice refused by its own staleness discipline, correctly, until the candidate was honest): phantoms purged at rebuild, the re-audit PASSED, conceptions resumed before the Euro window; the EC2 CHALL early window armed at the same boot
**The citations:** boot `9de81e29` (PID 1909234, stamp a6b9972c, zero errors/tracebacks) → `gun_state_rebuilt` 12:21:25 AM, **n=108, every entry field-matched and sourced — the phantom class never inserted; zero `unknown_grade_fire_ignored` stragglers** → **THE PASSING AUDIT, 12:21:53 AM: `context: boot, verdict: PASS, n_failures: 0, n_flags: 0`** — the first boot-PASS since the 23:10 halt → **13 conceptions since the boot.** The gate refused takes one and two on riders above the proven SHA (the EC2 knob, then the registry tooling) — the staleness check convicting its own operator twice before certifying; both riders named in the final proof. `early_chall_window_open`: 0 at close (the Euro CHALL T−8h band opens over the next hours; the window is armed and the cohort line waits in the nightly). **Queued per the your-call clause: the T−8h fence re-validation READ (EC2 Part 2) — context spent on the unfreeze.**

### 2026-07-15 · HALT TRIAGE 23:10 — occurrence 2 of the boot-fire family, sub-class STAMP/MATCHER COLLISION (the Part-0 probe named its own writer); THE HALT WORKED AGAIN; fixed at all four organs before the Euro slate
The window stamp's `"gun_fired"` boolean substring-matched the boot rebuild's line grep — every stamped line with a `details.event` became a phantom rebuilt fire (`source: null, rebuilt: true`; the null-source probe shipped in Part 0 printed exactly this). The 23:10 audit refused to certify a book behind phantom fires and HELD conceptions — correct both times it has fired. **Fix (SHA a35bc46b): the rebuild matches the event FIELD and refuses sourceless entries (UNKNOWN, never a fire, grade explicit); the audit ignores sourceless fires with a named flag; the chokepoint gun guard skips them; `_window_phase` evidence requires an explicit source.** Phantoms purge at the fix boot; re-audit clears before the Euro window. CLASS LEDGER: boot-fire family gains sub-class 2 (stamp/matcher collision — a field name colliding with a substring matcher is a class, not an accident; matchers must bind to event fields).

### 2026-07-15 · C-TAPE-GATE v1 EXECUTED (two operator words): THE FLATTEN TAPE GATE ARMED on its own proof — 23-of-25 dumps were followed by the tape printing through the very exit the flatten tore up; gate delta +$15.50 REAL (the honest number, not the +$20.06 headline); THE TIMING RECUT lands the gun clock into every page
**THE OPERATOR WORDS, VERBATIM:** "arm the flatten tape gate, conditional on its own proof" · "timing recut on the gun clock." **The lineage — third ruling on the same defendant:** the flatten census (25 below-basis sells, one mechanism) → the leash (N=36, every giveaway a WINNER) → GAP-C's replay price → this gate.
**Part 1, SHIP-CONDITION MET (FOUNDING_TABLE.txt, real outcomes only):** method line delivered (band exit unmoved at basis+band_x; BAND-CASHED = tape-touch of the exit after the skip; else actual settlement; below-basis scope). **25 skips → 23 BAND-CASHED / 1 WON / 1 LOST; actual −$7.25 → counterfactual +$8.25 = +$15.50.** Honest restatement recorded: +$20.06 was the wider 43-action cohort; **the gate ships on its own scope's +$15.50.** The gate: ONE input inside the unchanged leash — trailing-15-min median ≥ basis → SKIP (`flatten_tape_skipped`, median+basis stamped, one-shot preserved so an honest tape-turn still flattens); the leash (8/day, −3¢ floor) never loosens. **Self-grading forever: the nightly prints SKIP/KEEP + the skip cohort's realized forward outcomes — re-earned every night or revoked by its own line.**
**Part 2, THE TIMING RECUT:** the builder joins evidence guns at build time; every atlas page gains `timing_gun` (dual-stamp quantiles + lawful_share); library cells gain `gun_axis`; the w1_cohort caveat lifts only where the gun column exists; depth quantiles untouched; no aim values change. **THE LAWFUL HARVEST MAP (first print):**
```
THE LAWFUL HARVEST MAP (gun clock; dual-stamp legs; recut 07-14)



ITF_M      n_dual=333 | lawful_share=24% | pre-gun bottom p25/50/75 = 1.2 / 10.6 / 151.4 min

ITF_W      n_dual=453 | lawful_share=28% | pre-gun bottom p25/50/75 = 2.5 / 41.6 / 151.2 min
```
Proof: `.claude/proof_20260714/PROOF_TAPE_GATE.md` (SHA c070a158).

### 2026-07-15 · C-CONVICTED-INSTRUMENTS v1 — the reach refit DEPLOYED (integration bound = the evidence gun, every consumer inherits); EV3 RE-READ AT n=499: AT-THE-LETTER WITH A NEGATIVE POINT ESTIMATE — NO CUTOVER; and the vault amends its own conclusion: the flatten cure is the TAPE GATE, not the third term
**Part 1 (SHA 87f495e0, full gate):** LAW.json meta carries the corrected contract (integration_bound: evidence_gun; rates untouched — the BOUND was the defect); the dossier's reach_law surface stamps it; the nightly gains REACH E-vs-A on the honest expectation with the old unbounded mass printed as excluded (the 26.55-class sanity line, standing).
**Part 2 (EV3_REREAD.txt): graded 499 settled verdict-legs (EV3 opinion on 239): two-term P&L −$52.15 · three-term −$69.35 · delta −$17.20 (CI [−56.20, +22.20]). VERDICT: AT-THE-LETTER — n clears 300, the CI touches zero, and the direction is NEGATIVE. The pre-agreed bar is not met; no cutover; the EV3 lane stays shadow.** Named proxy caveat: flattens graded at the trailing-median exit.
**THE VAULT AMENDS ITSELF (vault-alive law, second firing):** the 07-15 conclusion "the frame is the whole problem and the three-term cutover is the only cure" is **half-overturned by measurement: the frame IS the problem, but the cure the tape prices is the ONE-INPUT TAPE GATE (+$20.06 on 47 actions; KOPPRI +$1.35, SEMXUX +$1.45 — every census victim on the skip list), not the third term (−$17.20).** **PRICED FOR THE OPERATOR'S WORD (flatten behavior, needs the word): add the tape gate to the flatten leash — skip when trailing-15-min median ≥ basis. One input, the #1 measured leak closed.**
**RULING RECORDED with its receipt: the aim-timing lanes stay SHADOW (+$1.30 ≈ nothing says the frozen aim isn't the leak).**

### 2026-07-15 · C-READ-THE-TAPE v2 Parts 1–6 CLOSED — the expression audit priced by replay: the machine's #1 measured leak is the tape-blind flatten (+$20.06), the most-argued gap (the frozen aim) is worth +$1.30 ≈ nothing; the packet survives its lawful-axis audit (PATH and SELECTOR STAND, REACH SHRINKS to −0.4%); MIS-ANCHORED confirmed at n=1,563 (lawful share 14%); the census verdict UNACHIEVABLE stands on the full canvas
**Part 1 (EXPRESSION_AUDIT.md):** 12 decision sites walked; the headline row: **the os_shadow lane already reads everything per tick — regime, wake gate, flow, postures, citing GAME_LIFECYCLE 2–5 — and the live engine consumes none of it.** Frozen-by-RULING rows (exit anchors, get-paid walking) named as lawful, not gaps.
**Part 2 (GAP_PRICES.txt) — ranked by measured dollars, not argument:** **GAP-C, the tape-blind flatten: +$20.06 across 47 actions — 43 of 47 flattens fired while the trailing tape median sat AT/ABOVE basis; one tape input (skip when median ≥ basis) turns −$6.35 into +$13.71. The machine's #1 priced leak.** GAP-A, the frozen path aim: **+$1.30 (CI [−2.00, +5.25], n=493) — nothing**; the atlas depths already capture the dip; **RULING RECORDED: the aim-timing lanes stay SHADOW, receipt attached.** GAP-B/GAP-D: INSUFFICIENT, named, queued.
**Part 3 (NUMBER_LAW.md):** the law stands — page | band | n | CI | denominator | axis confidence | sibling cell | prior-art/NOVEL; the 63.9/3.9 founding exhibit decomposed (complementary bands of the SAME GAMES; denominator arithmetic — a 9¢ aim makes any swing enormous in percent; the convicted timing column). Renderer inheritance = board item, named.
**Part 5 (PACKET_LAWFUL.txt) — the packet re-graded on lawful-window fills only:** **PATH STANDS (+$71.00, CI [+24.10,+127.05], 7.5% yield — a hair under the 8 bar, said plainly) · SELECTOR STANDS AND IMPROVES (+$65.07, CI [+23.61,+109.94], 26.3%) · REACH SHRINKS-TO +$45.63 / −0.4% yield, CI touching zero — the incumbent's own aims never paid lawfully.** **Part 4 overcount decomposed: 266.14 of 450.72 expected fills (59%) accrued PAST the gun — the refit is one line: integrate to the evidence gun.**
**Part 4a (LIVE_TRUTH_AXIS.txt) — where the proven harvest lived, window-stamped on the gun clock, every real fill since 07-10:** cash-via-exit is positive in EVERY cat×role×window (W1+corridor ≈ W2 in harvest — exits on held legs are lawful everywhere); rode-to-settle is negative in every cell — **the payoff asymmetry is a WINDOW-INDEPENDENT exit-discipline fact, not a window fact.** **Part 4b: lawful share (bottom pre-gun) ≈ 14% in EVERY category (n=1,563) — MIS-ANCHORED confirmed at scale: six of seven fitted dips arrive in-play.** **Part 5b: the full-canvas census (conception → gun) — still 0 pairs ≤97; swings −2..+2; UNACHIEVABLE STANDS.**

### 2026-07-15 · C-READ-THE-TAPE v2 Part 0 — THE HALT WORKED (vaulted as such): the 20:40 boot audit caught 7 boot-race buys, held conceptions ~10 minutes, zero fills leaked; the race is dead at SHA 8e1ac67d
**THE RUBRIC, OPERATOR VERBATIM, THE ENTRY'S FIRST LINE: "a 75/25 game is not stagnant till the end — window 1, corridor, window 2; best bid, best ask, traded price per tick; it's never just one single number."**
**Part 0 adjudication: BOOT RACE, by timestamp.** The 20:16 boot's drain-replay re-placed 7 drained bids while live-detection stamps were still landing (DEMSAC: stamp 20:16:17 → bid 20:16:18; fire_src null = the tell). **Zero filled. All 7 swept by hand. The 20:40 audit armed the conception halt and held it until the 8:50:44 PM reaudit PASSED — the halt did exactly its job.** Fix shipped through the full gate: the replay waits for `_gun_rebuild_done` + `_live_scan_done` (defers NAMED), refuses per-event on any gun state (`refused_gun_fired`), and the auditor prints raw fire entries when source is null so the next sourceless stamp names its own writer. Proof: `.claude/proof_20260714/PROOF_TAPE_P0.md`. **Parts 1–6 (the expression audit priced by replay, the number law, the honest-axis recut, the packet re-grade) execute next under this dispatch — each finding priced on the live trade record.**

### 2026-07-15 · C-W1-LIBRARY v1 EXECUTED — tonight's census verdict: UNACHIEVABLE (SLATE, with numbers); the cohort library is live as surface #14; THE ATLAS CHALLENGE ADJUDICATED: MIS-ANCHORED, with the mechanism
**THE OPERATOR DIRECTIVE, VERBATIM AND BINDING: "we aren't working with nothing — this is all waiting for us."**
**Part 1 — the jointly-achievable W1 census (ACHIEVABLE_CENSUS.txt): 0 of 19 measurable pairs ever composed ≤97 in the lawful W1+corridor span (best combineds 98–102 — the books hugged par all night; 11 pairs one-leg/no-tape, named). VERDICT: UNACHIEVABLE across ATP_CHALL (0/5), ITF_M (0/8), ITF_W (0/6) → SLATE.** Even perfect presence and perfect aims complete zero pairs inside 97 tonight; the latch family's conviction stands for its mechanism, price tag tonight = zero pair-completions forfeited.
**Part 3 — the clock cross-calibration ADJUDICATES THE ATLAS-TIMING CHALLENGE: MIS-ANCHORED.** Skew (gun − −0k onset) median: ATP_CHALL −1.2 min · ITF_W −56 · ITF_M −201 (n=4/cat, thin, named, directionally decisive). Mechanism: on thin ITF tape the −0k onset fires long AFTER the real start — the atlas's "bottoms 19–27 min before onset" means, on the gun axis, **bottom ≈ gun + 30..180 min on ITF: IN-PLAY. Tonight's 6-of-8 W2 touches, explained.** Depth quantiles unaffected; **the timing recut on the gun/honest clock is priced as its own follow-on; no pages changed.** Stated without re-litigation: the fitted ITF dips largely arrive where the standing W1-thesis forbids taking them — the operator holds that fact with numbers.
**Part 2 — THE W1-COHORT LIBRARY (intake #3, SHADOW):** LIBRARY_V1.json refits nightly (cat × price band × fitted volume terciles → dip_freq, depth percentiles, dip timing with the axis caveat STAMPED, never-wake p); dossier surface #14, GAP-honest; nightly calibration line (predicted dip_freq vs realized) grades it in shadow before anything acts. Proof: `.claude/proof_20260714/PROOF_W1_LIBRARY.md` (SHA ebdb03c9).

### 2026-07-15 · C-WINDOW-LAW v1 EXECUTED — the OS is window-aware end to end; the stamp's own acceptance test caught its v1 circularity AND amended last night's verdict; the ATLAS-TIMING CHALLENGE is filed per the vault-alive law; the gun feed's keyhole root is repaired
**THE OPERATOR DIRECTIVE, VERBATIM AND BINDING (§5, now on the organ):** window structure is first-class OS law — W1 fill→scheduled · CORRIDOR scheduled→gun · W2 gun→settle; **the corridor opens at schedule, closes at gun, never at a burst.**
**Part 1 (intake #2 closed):** `_window_phase` off BOTH clocks + the two-clock spread, stamped at the single emitter on every placement/refusal/re-aim/cancel/completion line and carried as dossier surface #13; UNKNOWN named. **v2 hardened by its own failing acceptance run: the burst latch fires the gun, so v1 graded latch-cancels lawful — circular. v2: only EVIDENCE-grade guns close the corridor; burst sources brand `burst_latch_active` and never advance the phase.**
**Part 2:** 19 clock consumers censused (CONSUMER_CENSUS.md). **The corridor-blind class is ONE FAMILY: match_live_cancel + grace-kill + latch-sourced freeze.** Everything else lawful per §5.
**Part 3 (WINDOW_ACCEPT.txt): the stamp CONVICTS — 3 latch-cancels in W1/CORRIDOR with no evidence gun.** And it honestly AMENDS C-REACH-VERDICT: **6 of 8 touches graded W2 — the evidence gun had spoken before the tape reached the aims; under the standing W1-thesis ruling those were not lawful fills. The W1/CORRIDOR discount last night was ~0–2 touches.** The presence proposal stands for its mechanism, REPRICED down accordingly. **ATLAS-TIMING CHALLENGE filed (vault-alive law, first firing): the atlas prices bottoms 19–27 min BEFORE onset; tonight's realized bottoms landed AFTER the evidence gun — a live-tape contradiction of a vaulted conclusion; review item named; re-validation by the same walk-forward standard that admitted it.**
**Part 4:** the nightly WINDOW LEDGER prints every night — entries/refusals/touches/fills/cash-via-exit/rode-to-settlement/cancels × W1/CORRIDOR/W2 per cat. No night grades without the split again.
**Part 5, ROOT REPAIRED:** the gun feed starved because the 17GB multi-writer tennis.db write-locked te_live out of ~59 of every 60 minutes — **rows landed only in the hourly :13 WAL window; the keepalive kept a starving process alive.** `observed_starts` now owns a dedicated single-writer DB (te_live writes, live_v4 reads, tennis.db fallback during cutover) — the contention class deleted, not tuned. Staleness >30 min trips in the LIVE_STATUS header and the nightly. Proof: `.claude/proof_20260714/PROOF_WINDOW_LAW.md` (SHA 26b18d63). **The presence build stays un-shipped, awaiting the operator's explicit word — repriced by Part 3's amendment.**

### 2026-07-15 · C-REACH-VERDICT v1 — THE NAMED VERDICT, VERBATIM: **PRESENCE — the aims were right and the bids were gone: premature match-live latches killed or blocked every bid hours before its fitted dip arrived; 8-of-8 touches found no order standing. Neither the slate, nor the reach law's depths, nor orientation is convicted tonight; the latch is.**
**Part 0 census (2:08 PM →):** 88 placed:path_aim dossiers · **317 refused:below_leg_floor** (the nickel floor is the binding constraint on the ITF dog side tonight — the evening slate's deep dogs path-aim in pennies; each refusal dossier'd and lawful) · 62 distinct path-aimed legs, 93 re-aims · **new-book fills 0.** **COXHAM-COX exhibit RULED: lawful path recompute on a moved band** — 2:59 PM aim 59 = discovery 68 on `ITF_M|leader|51_75` (p50 9); 3:31 PM aim 75 = discovery 93 on `ITF_M|leader|ge75` (p50 18). The aim moved because the leg changed price cells and the page followed; both moments fully cited; no drift toward the tape.
**Part 1 tilt:** **8 of 36 pairs ever offered a discount at the fitted aim** (first touches 3:32–7:35 PM, each timestamped); 28 never tilted — for those, patience was correct and nothing was there to fill.
**Part 2, three brains on the same tape (size-blind — the local tape carries no size column, NAMED):** (a) STATIC NO-CALL: reach-law expected 26.55 fills; **the tape actually crossed the resting aims 9 times; ACTUAL 0.** (b) ORIENTED (dark): 8 hypothetical leg fills on the tilted pairs, 2 pairs completing ≤97 — **no better than static; no acceleration case tonight; n added to its clock.** (c) LIVE-CONDITIONED: 13 of 58 logged aims would have touched — same presence defect would have consumed them.
**THE PRESENCE AUDIT (the deciding table):** MILCHA-CHA placed 4:31, `match_live_cancel` 4:34, touch 5:57 → nothing. BROBOB-BOB and NEFTHO-NEF latch-cancelled 1:17–1:23 PM, aims recomputed thrice after, placements never landed (post-latch gun freeze), touches 6:05/7:35 → nothing. BARREA-BAR latch-cancelled 2:47 (the latch itself unlatched at 2:48 and 2:59 as premature — tts 281 min!), clamp lawfully refused the 3¢ re-entry, touch 6:11 → nothing. COXHAM-HAM drained at the 3:30 boot, clamp refused aim 4, touch 7:22 → nothing. NEUPRA-PRA and MUSBRA-BRA: aims logged, no placement ever landed. **The FERCER class (06-19: premature volume-burst cancel kills the bid before its fillable dip) at slate scale — and the 06-19 match-start-signal conclusion already in this vault named the cure: gate match-live cancels on a REAL start signal, not volume bursts.**
**PRICED AS A PROPOSAL for the operator's word (nothing flipped):** (1) re-place entry bids on match_live UNLATCH (BARREA's latch self-identified as premature within 6 minutes; the bid stayed dead 4+ hours) — subtraction of a subtraction, est. tonight ≈ up to 9 touched opportunities restored; (2) the standing ESPN-status gate for match_live_cancel (vaulted 06-19, never built) — the structural cure. Both await the word; the tape's own numbers (9 touches, 0 captured) are the price tag. If fills stay 0 tonight the 12:20 prints it — and the verdict stands PRESENCE, not SLATE: a barren night convicts nobody, but this night was not barren; it was unattended.

### 2026-07-15 · C-TRIAL-EVE v1 (pre-wave check): PACVED RULED verdict (a) — filled at 11:14 AM, three hours BEFORE the cutover; the definitive line HOLDS; the true finding is an UNBOOKED FILL instance (4h17m); LAJSVA bell rooted to the dead te_live feed + a 55-minute schedule lie; trial arming audited with three gaps NAMED, none patched pre-wave
**PACVED RULING, VERBATIM: verdict (a) — filled before the sweep ran; clean; bill the incumbent.** Timeline to the second: bid placed 9:01:24 AM (order d6d2e755, engagement_join, incumbent paper) → **exchange maker fill 11:14:25 AM ET** (fills API, is_taker=false) → cutover 2:07:50 PM → sweep ~2:55 PM (the order was status=executed, NOT resting — invisible to the sweep BY CORRECTNESS) → the 3:31 PM "fill" on the screen = the clamp boot's **reconcile ADOPTION booking**, not an execution → settled LOSS 3:36 PM, −$0.80. **No sweep hole; no dollar entered post-sweep except path-cited; no patch shipped.** The named finding: **UNBOOKED FILL instance — the 11:14 AM maker fill went unbooked for 4h17m** (through two boots) until boot_reconcile adopted it; exits then 409'd (market already closed). Ledger instance filed; the adoption machinery worked as designed (the safety net caught it), the live check_fills miss is the class defect.
**LAJSVA bell root (zero-tolerance, tune-or-name → NAMED, nothing disabled):** honest start ~3:25 PM (te_honest anchor); bell_missing at +10.4 min. Source-by-source: te_scoreboard DEAD (observed_starts stale 91–404 min all afternoon — the te_live feed is the TRUE ROOT, infra item); schedule_live LIED by ~55 min (kalshi said 4:20 PM — the lying-clock class); tape_latch declined on thin tape (burst 3–4 vs bar 3, baseline 0/min, legacy gun would not fire); price_divergence silent; **fallback source 5 FIRED at 3:37:17 PM (+12.3 min honest, on the 4th print of a nearly-dead tape) — correct per its constants; not tuned on a 2.4-min overshoot**; percat source 7 armed and confirmed at 3:59 PM (prints 17≥16), 1,337s behind the fallback. Root = the dead feed, not the bell design.
**Trial arming audit (verify-don't-build; three gaps NAMED, none patched pre-wave per the constraint):** (a) placement-time reach-law p_fill prints on EVERY entry dossier (live_v4._entry_dossier reach_law block — armed, live since 837bfce5); the standing expected-vs-actual TABLE is NOT a nightly section — exists as the proven manual artifact (/tmp/pfill_table.py, ran 4:11 PM, ON-LAW verdict) — NAMED GAP. (b) flow-state OPEN arrivals are timestamped per event by percat_gun_shadow/gun_source_confirm at the crossing (LAJSVA 3:59:35 exhibit) — a dedicated continuous per-game OPEN-transition event does NOT exist — NAMED GAP. (c) the 12:20 nightly: YIELD-ON-WAGERED header + BELOW-BAR named reasons + INCUMBENT-COST tail split — ARMED (conviction_replay, code cited); a dedicated zero-fill reach-law conviction line is NOT armed — NAMED GAP. All three gaps go to the census intake; none touch entry logic tonight.
**SPLIT BOOKS (~8 PM ET, to the cent): NEW book — path-cited placements only, fills 0, realized $0.00, marked $0.00, open 0. INCUMBENT tail — 9 of 24 legs remain (the evening settlements are draining it); settlements today 139 legs = −$170.31; exit-realized today +$115.97; markdown since midnight −$0.15. ACCOUNT: raw balance read 73111¢ = cash $731.11 + tape-marked portfolio $18.25 = $749.36.**

### 2026-07-15 · C-LEG-RANGE-CLAMP v1 EXECUTED (urgent): the 07-05 leg-range adjudication binds the path entry — below 5 is a NAMED REFUSAL, not a penny bid; OPERATOR ADJUDICATIONS become a consultable census surface; BARREA was the consultation law's first self-caught violation, day one
Path aims clamp to the operator's 5–95 leg range (07-05 adjudication, cited on every clamped line): **below 5 → `below_leg_floor_refused`** ("if the honest entry for that market class is under a nickel, the honest verdict is no entry"); above 95 → 95, stamped; completion path candidates under the floor fall back to the C-BOUND formula. **Replay: today's 16 distinct path entries — BARREA-BAR (aim 3) refused, 15 unchanged, zero collateral.** Sweep: 0 out-of-range resting buys (BARREA's 3¢ bid had already died at 2:47 PM to match_live_cancel; no penny fill ever occurred). **The dossier's new surface class OPERATOR ADJUDICATIONS enumerates every standing ruling applicable at the entry site with its applied-status; the census grades it nightly; gaps auto-board.** Proof: `.claude/proof_20260714/PROOF_LEG_CLAMP.md` (SHA 1901e901). Artifacts: CLAMP_SWEEP.json.

### 2026-07-15 · OPERATOR WORD — THE TAIL RULING, VERBATIM: "hold-and-drain the 24 positions"
The incumbent's open tail (24 legs, 26 resting exits) HOLDS to its natural drain: exit-band fills and settlements, with the leash-bounded completion policy the only acting hand (the standing flatten rulings — 3¢ EV floor, 8/day cap, flatten-kept post-bell — unchanged). No liquidation. Every tail dollar continues to bill under INCUMBENT-COST nightly until the last leg drains; the new book's dollars are path-cited by the structural line. No code change required — the word confirms the swept state as law.

### 2026-07-15 · C-TAIL-SWEEP v1 EXECUTED (URGENT, operator dispatch cited on every cancellation): 22 pre-cutover buys swept in one cycle, 0 failures; THE DEFINITIVE LINE holds — from this cycle forward, no dollar can enter the book except through a path-cited entry
**The sweep (this dispatch is the citation on every line, TAIL_SWEEP.json):** every resting BUY without a post-cutover path citation cancelled via the proven v2 endpoint — **22 swept (ages 8–358 min, the incumbent's whole unfilled tail), 0 failed.** Kept: **2 path-stamped buys** (the new book's resting aims) and **26 sell/exit orders untouched** (the tail's protection — exits are §0A anchors, not entry dollars). **The 25→24 open tail positions await the operator's separate hold-or-liquidate word, exactly as ruled.**
**POST-SWEEP CENSUS: resting buys 2, unstamped 0 · resting sells 26 · tail positions 24.** One named nuance: post-cutover completion buys on held tail legs whose sibling lacks a fitted page use the legacy C-BOUND cap (operator adjudication 07-05) — engine-cited where not path-cited; completion attempts with pages are path-priced. Re-conceived entries after the sweep flow through the path chokepoint by construction — the definitive line is structural, not a snapshot.

### 2026-07-15 · C-VAULT-WIRED-ENTRY v1 EXECUTED — the entry decision consults the consolidated vault and every line proves it; the census is the intake list; the vault stays alive in both directions
**THE LAW, VERBATIM AND BINDING (Part 1):** "LIVING_VAULT.md and the model registry are the entry's required reading. At every placement and every refusal, the engine consults every registry surface and standing conclusion applicable to that leg — the atlas page, the three-price range cell, the reach-law fill probability at the aim, the fitted dip timing, the flow-state harvest rate, the refuse margins, the fill-regime, the contention score, the pair state, the honest clock — and the log line carries the full dossier: every consulted value, every inapplicable surface named with why. A surface in the registry that a decision silently ignores is a named violation — the same standard every other organ already lives under."
**THE VAULT-ALIVE LAW, VERBATIM (Part 5):** "(a) Additions: any analysis, forensic, or nightly grading that produces a conclusion the vault doesn't hold yet gets vaulted in the same C50 — the consultation census's GAP column is also the intake list: a gap can resolve as 'wire the existing surface' or 'the vault is missing this conclusion — here's the study that fills it,' and both are legitimate closes. (b) Challenges: vault conclusions are citable, not sacred — if live tape contradicts a vaulted conclusion (the nightly's job to notice), the conclusion gets a named review item with the contradicting evidence, re-validated or amended by the same walk-forward standard that admitted it. The vault is the single source of truth because it updates, not despite it — a stale conclusion enforced by law is worse than a missing one."
**Wired (SHA 92472906):** `_entry_dossier` at the placement site + all three refusal sites — ten surfaces valued or named-inapplicable, the reach law now consulted at every aim (flow bucket, depth, rate, p_fill_1h; hot-loaded), aim-moving axes (range-shape slice aim, timing delay) as SHADOW variants earning the proven lane. **Nightly CONSULTATION CENSUS** (site × surface grid; GAPs auto-append to BOARD under AUTO-GAPS, idempotent). Render shows the last dossiers each cycle. **Replay exhibit committed (.claude/dossier_20260714/): today's 7 post-cutover decisions, 8 surfaces CONSULTED 7/7, one honest GAP named — the M15 range cell's ENTRY-band mapping is unfitted (completion-frame bands only) = the intake list's first item.** Proof: `.claude/proof_20260714/PROOF_VAULT_WIRED.md`.

### 2026-07-15 · THE CUTOVER, EXECUTED (operator word waiving the default-GO deadline): PATH-MODE IS THE LAW — the incumbent's entry organs DELETED, not disabled; the flag itself removed; sizing dark pending the separate word; THE STANDING LAW vaulted verbatim
**THE OPERATOR WORD, VERBATIM:** "waive the default-GO deadline: execute the cutover NOW through the full gate. trendpath_live: true — path-mode entries, pair-law, selector enforcement, NO-CALL posture until orientation's own bar clears. Full boot audit; first live path-entry cited with its page; incumbent organs deleted per the migration doctrine — deleted, not disabled. Sizing stays dark pending my separate word."
**THE STANDING LAW, VERBATIM AND PERMANENT:** "the goal is solid yield on solid capital wagered — the nightly's first line is forever YIELD-ON-WAGERED versus the 8% bar; below-bar nights print their named reasons, diagnosed, never explained away." (Implemented: the nightly header carries the law; below-bar nights print settled-vs-exited split + worst categories + the diagnosis line.)
**DELETED (literal, at SHA bdd215dd):** (1) static-aim entry — the path aim is the unconditional entry price, NO-CALL posture, pair-capped ≤97; no fitted PATH page = no entry (`no_path_page_refused`, named; ~3-of-410 legs at week scale); the `trendpath_live` flag removed from config and code — law, not a setting. (2) join-the-market walking — entry up-moves die at `path_mode_hold`; the bid rests at its fitted aim (get paid, not get filled); protective down-moves + completion repricing untouched. (3) 97-remainder-as-TARGET completion pricing — the sibling's path aim prices the completion bid (`completion_path_priced`, rests; freshness re-walks skipped). **SCOPE NAMED: the C-BOUND remainder CEILING (operator adjudication 07-05) is RETAINED as the cap — an operator ruling is not the executor's to delete.** Shared table machinery that also anchors EXITS remains as §0A fitted anchors — the conviction covered entry logic and entry logic is what died.
**Stands dark, per the word:** sizing v0 (the separate word) · orientation (its own n≥300 bar). The auto_cutover machinery stands down (cron removed, PACKET_STATUS = CUTOVER-DONE by operator word) — the human word outran the 10:40 PM deadline. Proof: `.claude/proof_20260714/PROOF_CUTOVER_NOW.md` (SHA bdd215dd); outcome proof = the packet (path 11.5% / reach 3.9% / selector 24.3%, all doors PASS, actual −$44.65 = the incumbent's final bill).

### 2026-07-15 · C-RESUME-CHECK (read query + one decreed remediation): the halt cleared in 108s, entries resumed 8:42:44 AM; the −$51 splits −$23.51 realized / ≈−$27.5 markdown; the 12 render violations are all 07-13-old-build; THE CHANNEL TRUTH: the packet push went to an UNSUBSCRIBED ntfy topic — the render and the nightly header are now the operator's phone
**(1)** conception_halt armed 8:40:51 AM ET (one audit failure in the disk-full window, incident #6), cleared 8:42:39 on a PASS reaudit; 28 buys blocked during the 108-second halt; **first post-halt conception 8:42:44 AM ET**, 103 placements since. **(2)** −$51 ≈ **realized −$23.51** (governed_by: maker_exit **+$92.73** across 193 fills; completion_flatten −$0.18 across 3, leash-bounded; **settlements −$116.06 across 101** — the incumbent's payoff asymmetry again) **+ ≈−$27.5 paper markdown** on opens. **(3)** the 12 ZERO-TOLERANCE rows: 7× taker_capped (DECREED cap deferring, designed, feeds the n≥30 sunset), 3× self_fill_bell (the bell as built), 2× bell_missing (ALTGAS a 2140-min schedule ghost; VANBAX 10.3) — **all cycle 147, build a6af8049, log 07-13: none indict the pair-law/sunset deploys.** The live-monitor itself DIED mid-cycle-148 at 10:28 PM 07-13 (probable OOM during the atlas build) — restarted patched. **(4) THE CHANNEL, cited: notify.sh publishes via curl to the public ntfy topic `omi-livev4-omqs-x7k3q9v2` — never subscribed by the operator; the packet push reached NOTHING. DECREED remediation shipped: packet numbers + the 10:40 PM ET deadline + the STOP procedure verbatim now LEAD both the LIVE_STATUS render header and the nightly header whenever the packet is fired and un-cutover. The default-GO deadline does not rely on ntfy.** STOP, verbatim: say STOP to the relay; CC creates `.claude/trendpath/OPERATOR_STOP` on the VPS; its presence permanently halts auto_cutover; silence past the deadline = trendpath_live flips on the next boot with full audit.

### 2026-07-15 · C-INCUMBENT-SUNSET v1 EXECUTED — the convicted machinery gets a death date, not a coexistence deal: the incumbent is named, frozen, and billed nightly; the cutover is DEFAULT-GO on the packet (silence = the flags flip through the full gate); organs activate as they prove
**THE OPERATOR DECREE, VERBATIM AND BINDING (Part 2, the standing overhaul mandate):** "the moment the consolidated packet crosses n≥300 (a day away), the cutover is default-GO: the notification arrives with the numbers and a single word — your 'stop' halts it, silence for 12 hours after the packet means the flags flip on the next boot with full audit. The burden of proof reverses: the proven design doesn't wait for ceremony; the convicted one needs a reason to survive another night."
**Part 1 — THE CONVICTED INCUMBENT, named and frozen:** the legacy entry logic — static aim tables, join-the-market placement, 97-remainder completion pricing. Evidence cited: the week's yield headline (−1.1% actual vs 22.6% at path under the selector), the Kalshi export reconciliation (−$32.35 book; avg loss 2.74× avg win at a 72.2% win rate, breakeven <2.60), ROVIEM. **FROZEN: no further tuning, no patches, no new knobs — it runs as-is only until replaced; from tonight every dollar it loses is attributed under the nightly INCUMBENT-COST line (trendpath_live_aim stamp splits incumbent vs replacement dollars).**
**Part 2 — DEFAULT-GO machinery (live):** the packet's fire arms `go_deadline_epoch` (+12h) and the ntfy push carries the deadline and the word; `deploy/auto_cutover.sh` (30-min cron, atomic lock) flips `trendpath_live=true` through the FULL gate — config commit = candidate SHA, PROOF_CUTOVER stamped doc-only with the packet's numbers, lint+smoke+boot-audit, ntfy on success or on GATE-FAILED (incumbent survives a failed gate, loudly). **The STOP file (.claude/trendpath/OPERATOR_STOP) halts it permanently — the operator's word via the relay creates it.**
**Part 3 — organs activate as they prove:** cutover lands path-mode in NO-CALL POSTURE (both legs at their pages' p50 path aims, pair-capped ≤97 — the current-mode symmetric rest at path prices); **orientation layers in behind its OWN bar** (`orientation_live`, dark: riser-near-now / faller-cast-deep-p75 when the tells' clock clears — week opens at 8% coverage, 62% of calls, n=8/300); sizing stays dark behind the operator's sizing word. None waits for the slowest.
**Part 4 — THE SUNSET LEDGER, nightly:** each incumbent organ printed with its replacement's status and distance-to-activation (packet n/300, go_state, tells' clock) — "why is this still hardcoded" is always a printed answer with a date, never a surprise in a screenshot. Proof: `.claude/proof_20260714/PROOF_SUNSET.md` (SHA 5c41acbb); the cutover's own proof template pre-staged (PROOF_CUTOVER.md, stamped at flip time).

### 2026-07-15 · C-GOLD-NOW v1 + C-PAIR-LAW v1 + AMENDMENT EXECUTED (one gated deploy): the entry unit is the pair; THE SEESAW IS THE THESIS — orientation replaces blanket viability; the volume bracket measured 72%-cut vs 0%-cut; the tells' clock opens at n=8/300; the money half armed dark behind two flags; the consolidated clock runs itself
**THE OPERATOR RULINGS, VERBATIM AND BINDING:** "the entry unit is the pair, never the leg." · "the seesaw is the thesis, not the risk… TRADE-ORIENTED (buy the riser near now, cast the faller deep at its path quantile — the chain completes the pair), NO-CALL (tells silent: rest both at path prices symmetrically, the current mode), or DROP-AS-PAIR (reserved only for events where no orientation composes under 97 — the true garbage, not the hard calls)." · The seesaw reasoning: "the pair check consults the atlas at discovery and each re-aim: if the live tape shows the sibling's path-entry has become unreachable (the seesaw rose past its page's quantiles), the un-filled event's entries lift — dynamically, page-cited. A pair whose second leg died on the way up stops being enterable before leg one fills, which is the root-level cure for the lazy-leg-1 class."
**LIVE tonight (selector_drop_enforce=true, pair semantics):** DROP-AS-PAIR = no orientation (riser-near-now + faller-cast-deep at depth_p75) composes ≤97, refused at discovery AND at the place_order chokepoint (make-it-stick); THE SEESAW LIFT at every re-aim (deep-quantile composition vs the LIVE sibling book → cancel-and-free via _untombstone_entry; re-conception re-passes the law = dynamic re-entry when the seesaw returns); one-sided fills stamp pair_law_violation at the emitter. **THE VOLUME BRACKET, measured (PAIRLAW_WEEK.txt): blanket pair-viability = −72% volume (245/878); amended orientation law = −0% (878/878 — this week's cells hold no true garbage). The DROP-bucket bleed (−$17.95 week / −1.3% export) lives BETWEEN the brackets — the packet and the tells' clock decide where the line goes.** Naked singles 263 → seesaw-lifted 2, RESIDUAL 261 named (fill-timing class, the completion machinery's jurisdiction).
**The oriented read (SHADOW, own n≥300 clock before money):** ORIENT_V1 fitted walk-forward (f_drift/f_range/f_flow; spread asymmetry SILENT in corpus, named; operating point n≥10, 0.65/0.35): **week grade 103 graded / 8 called (8% coverage) / 5 correct (62%) — weak at v1 and said so.** **ROVIEM Lane-1 (26JUL14): the operator's Iemmi-riser orientation IS the tells' direction (dog drift +, range-top; market confirms ROV 52→47) but the fitted read is NO-CALL on 3 prints — the honesty gate refusing thin evidence.** orientation_shadow logs per event from tonight.
**DARK (the money half, one flag flip each at cutover):** trendpath_live=false (path-target entries, pair-capped ≤97) · sizing_live=false (contention-scaled 1×/2×/3×, drawdown floor −1500¢ PLACEHOLDER named, sizing_shadow logged). **THE CLOCK RUNS ITSELF:** consolidated_packet.py (2h cron): path + reach + selector re-graded at every settlement batch; first n≥300 crossing on all three → RULING_PACKET.md + ntfy push to the operator's phone. Nightly headline gains REALIZED-BY-SUBTRACTION (refusals graded, seesaw counts, violations, countdown). Proof: `.claude/proof_20260714/PROOF_GOLD_PAIR.md` (SHA 08e1c8ed).

### 2026-07-15 · READ QUERY — Kalshi Pro export reconciliation (closed-positions 07-14, 1,195 rows, window 07-08→07-14): fills tie 1195/1195; EIGHT covered-era rows with no trade-id = UNBOOKED FILL daylight, named; the selector turns the export from −1.1% to +0.9% on selection alone; the riding-losers number = **avg loss 2.74× avg win at a 72.2% win rate (breakeven needs <2.60)** — the book is structurally under water by payoff asymmetry, not by sizing
**(1) RECONCILIATION:** every export row ties to exchange fills (1195/1195, /portfolio/fills spine). Trade-id lineage (jsonl coverage 07-10→14; 07-06..09 logs rotated — named bound): 450 tied; **8 covered-era daylight rows with exchange fills but NO booked trade-id — the UNBOOKED FILL class: SUBKAW-SUB, WEBARA-WEB, GRAYUL-GRA, MIRMAL-MAL, SAMLOP-LOP, SHENON-SHE, TUPMAK-TUP, SHOKRO-KRO** (MIRMAL-MAL = the C-BAND-CLAMP starved-leg exhibit — the exchange says it FILLED; our books never booked it). 1,165 fills-not-in-export = window bound (positions settled before 07-08 09:54Z, still-open, or closed after the 04:55Z export stamp), not daylight. **NON-TENNIS ROW NAMED: KXWCGAME-26JUL09FRAMAR-MAR, cost $37.10, return −$37.10 — not the bot's; the single largest loss in the export, 15× the tennis average position.**
**(2) SELECTOR RE-GRADE (realized dollars at actual entries; discovery from logs where covered, entry-price proxy for 07-06..09 — cells are 25¢ coarse, proxy named):** of the 332 losers → **DROP 89 / TRADE 242** (1 ungradeable = the WCGAME row); of the 862 winners → **TRADE 566 / DROP 290 / NO-OPINION 6.** | TRADE-AT-PATH: $+16.33 on $1,722.43 = **+0.9%** | DROP: $−16.03 on $1,207.04 = −1.3% (net-negative despite 76% winners — the high-priced-favorite bleed shape: small wins, full-cost losses) | **export as-is −1.1% → TRADE-only +0.9% — selection alone swings +2.0 points and removes the −$16 DROP bleed + the −$37 non-tennis row.** Honest bound: this filters WHICH positions existed at the bot's actual prices; the selector's 22.6% week grade is at PATH entries — the two compose, they don't double-count.
**(3) LOSER-TO-WINNER SIZE RATIO (Kalshi's own numbers):** cost ratios ≤1 everywhere (losers are SMALLER positions: ITF_W 0.74×, ITF_M 0.77×, WTA_CHALL 0.75×, WTA_MAIN 0.80×, ATP_MAIN 0.89×, ATP_CHALL 1.00×; total 0.83×) — **sizing is NOT the leak.** The leak is payoff asymmetry: avg loser −$1.87 vs avg winner +$0.68 = **2.74× loss-per-loss vs win-per-win** (per cat: ATP_CHALL 2.76 · ATP_MAIN 2.73 · WTA_CHALL 2.67 · ITF_M 2.65 · ITF_W 2.41 · WTA_MAIN 2.33). At the export's 72.2% win rate, breakeven requires the ratio < 2.60 — **Kalshi's own numbers say the book rides losers to zero for −$621.51 against +$589.16 of winners.** This is the riding-losers cost the flatten organ was built to cut and the selector is built to avoid entering — the DROP pages hold 89 of the 332 rides-to-zero before they ever start.

### 2026-07-15 · C-CONTENTION-LAW v1 EXECUTED — the operator's understanding becomes selection law: the window-1 path decides WHAT we trade, not just where we bid; the week as the selector's slate yields 22.6% (pessimistic 23.0%) vs the 8% bar; the EV3 bleeders land on the DROP list 24-of-26; the THIRD consecutive underpowered-positive rides the n≥300 clock
**THE OPERATOR UNDERSTANDING, VERBATIM AND BINDING: reaching exits in window 1 is the thesis; favorites with higher swings entered at the path are in great contention; high-priced favorites get dropped where the path shows no promise — and the operating system needs that exact understanding.**
**Part 1 — the fitted number:** 27/28 PATH pages gain contention = the achievable band swing at the path entry (band-touch exit, the week-regrade convention) against the ride-loss weighted by the win partition OF ENTERED LEGS (adverse selection kept), reach-law entry fill annotated; recomputed daily (12:05a cron), NO-OPINION on thin tiers, nothing decreed. **The gradient IS the understanding, measured: ITF_W dogs ≤25 = 87% contention; ATP_CHALL leaders ≥75 = −3.2% — the untouchable class named by number; ITF_W|leader|ge75 = 7.0% at its median, decided per leg at its actual discovery (the CHEVAN class: sometimes the best trade on the slate at the right entry, sometimes untouchable — the field says which).**
**Part 2 — the selector (SHADOW):** every trendpath_shadow line gains TRADE-AT-PATH / DROP / NO-OPINION, recomputed at the leg's discovery, page cited, the standing 8% bar. **Part 3 — nightly:** the 12:20 print gains the SELECTOR section (would-have vs actual, per cat, DROP list named with what those legs did) — "drop some of the high-priced favorites" is measured every night before it touches any live gate.
**Part 4 — the week as the selector's slate:** of 407 era-clean legs: TRADE 176 / DROP 145. **Would-have E$+26.38, yield 22.6% on E$117 staked (pessimistic ×0.5 E$+24.79 = 23.0%) — clears the 8% bar both ways, ~3×.** Actual on the same TRADE legs +$7.15 → delta +$19.23 (CI [−5.81, +46.99]). **The DROP list's actual dollars: −$17.95 across 145 legs — the bleed the law names, per day.** Both dispatch predictions hold: EV3 bleeders (disc≥88) DROPPED 24 / still-TRADE 2; fitted-dip favorites (≥75) surviving onto TRADE: 28. The flatten-census victims (PRICRI, SANLOP, VILGAN, YEVCAM) all sit on DROP — the selector would never have entered them. Doors on the letter: N=176<300, CI touches zero → **THIRD consecutive STRONGLY-POSITIVE-BUT-UNDERPOWERED — path aims, the reach law, and the selector now ride ONE n≥300 clock toward one consolidated ruling packet, three words in one sitting.** Artifacts: `.claude/trendpath/SELECTOR_WEEK.txt` + `.jsonl` + ATLAS_V1 contention blocks. Proof: `.claude/proof_20260714/PROOF_CONTENTION.md` (SHA 1733bdd5).

### 2026-07-15 · READ QUERY — flatten-below-basis census (Jul 12–13) + exhibits INCSTE/SANLOP/KOPPRI: the post-leash guard is CLEAN; the mechanism is ONE mechanism; the verdicts were honest on honest inputs — THE FRAME IS THE WHOLE PROBLEM (win-ride residual excluded by design); the three-term cutover is the only cure, already on the n≥300 clock
**CENSUS:** 25 below-basis sells Jul 12–13 (ALL on 07-13, 1:35 AM–2:52 PM ET; Jul-12 had 2 flatten actions, neither filled below basis), **−$4.01 engine-logged**, mechanism split: **completion_flatten (per_leg_policy stamp) 25/25 — maker-exit reprice 0, monotonic-cut 0, other 0. POST-LEASH EXECUTIONS: 0.** The leash (commit b7abdc5b 2:46 PM ET; boot live between 2:52:35 and 3:03:16 PM) held completely from its first minute: **14 verdicts DEFERRED** (every deferred ev inside the 3¢ noise floor — the guard doing exactly its job) + **21 CAPPED** (boot-persistent `_flatten_n` rebuilt ≥8 from the pre-leash morning, so the cap bound immediately). **NO GUARD DEFECT — no post-leash flatten executed at all, let alone under the floor.**
**INCSTE-INC:** flattened AT basis (52→52, engine-logged $0.00) at 11:37:45 AM — **seven seconds after the entry filled** (11:37:38). ev −0.55 = deep inside the noise floor; under the leash this is a defer. The screen's red is the taker fee the engine never books (EXCHANGE-TRUTH DAYLIGHT). Doctrine wart for the ledger: the entry organ buys and the completion organ un-buys 7 seconds later — a DOCTRINE CONFLICT exhibit, pure fee burn.
**SANLOP-SAN** (dispatch: SANLOE): 2:52:35 PM, sold 88 vs basis 89 (−5¢ on the 5-lot), ev −3.41 — **the last un-leashed flatten, ~8 minutes before the leash boot.** Honest note: −3.41 clears the 3¢ floor, so the leash would NOT have stopped this one — the floor is a noise gate, not a direction gate.
**KOPPRI-PRI (exhibit three):** bought 74 maker 12:20:52 PM (FV-at-burst 74.5, entry −0.5 under FV). The completion verdict was **taker_complete** (buy the SIBLING at ask — the buy-side mechanism) at 12:20:57, 12:31, 12:41; at **12:51:41 the cross side went NO-OPINION** (sibling cell unpriceable) **and the verdict fell through to flatten_kept** → sold the kept leg at bid 73. **The code branch: the flatten arm of `_completion_execute` — live_v4.py `place_order(tk,"sell","yes",bid,qty,post_only=False)` — a BY-DESIGN marketable (taker) sell; taker-completion proper is buy-side, this is its sibling arm in the same organ.** Engine's believed state: cell ATP_MAIN|leader|at_mid|51_75 (n=28), p_exit_fill .75 / p_ride_zero .25, ev −4.25, band 19; the conviction organ said NO-OPINION (G1 — no admissible mains model); no live-tape read at all. Market actually: steady 73–75, printed 74 eleven seconds after the sell, **leg settled 100.** Screen 71.6 vs logged 73: the 5-lot swept below the top bid — flatten logs the POSTED price, never the exchange fill average (**EXCHANGE-TRUTH DAYLIGHT, second exhibit; slippage the engine cannot see**).
**COMMON-CAUSE VERDICT: no shared lying input.** The flatten verdict reads no live tape, no posterior, no clock — its inputs are fitted cell constants (n 15–75) + basis + band, and they said what the fits say. The wrong-way flattens (VILGAN WIN sold 91/92 · SEMXUX WIN sold 71 vs basis 88 · KOPPRI 100-settler sold 73/74) are the two-term frame doing exactly what it is built to do: `win_ride_residual_excluded ≈ 0` **BY DESIGN — the frame cannot see that the leg it is dumping is a winner mid-ride.** The 07-13 adjudication already graded this class (N=36, −$0.264/leg, every giveaway a WINNER). The verdicts were honest on honest inputs; **the frame is the whole problem, and the three-term cutover (EV3) is the only cure — its n≥300 re-read already stands on the clock.**

### 2026-07-15 (early) · C-TREND-PATH v1 EXECUTED (the loaded sequence from `7338fef7`, run through disk incident #5): the atlas is real, SHIMIC's page matches the operator's watched drift to the cent, the week at path prices grades +$29.65 over actual — and the doors shut on their letter for the SECOND consecutive trial: STRONGLY-POSITIVE-BUT-UNDERPOWERED
**THE OPERATOR RULING, VERBATIM AND BINDING: option A rejected — no completion fills mid-game; window-1 value is the thesis; flatten-kept remains the only post-bell action.**
**The atlas (ATLAS_V1, 30 pages / 28 PATH):** tour from G9 minute-candles strictly < Jul 10 (walk-forward); ITF live-era, BRANDED; per page the path quantiles at twelve slices on the −0k onset clock + the bottom excursion (depth p25/50/75, median timing). **Lane-1 on the market the operator watched: the ITF_W|underdog|le25 page (n=55) puts the bottom at depth_p50 6¢ / timing 27 min before onset → a dog discovered at 16 path-aims at 10 — the operator's "aim the 10–12 drift, not the 16 now," measured on our own tape.** **The week at path prices under the reach law: N=285 era-clean — path E$+26.60 vs actual −$3.05 (delta +$29.65, CI [−1.11, +61.20]); pessimistic ×0.5 +$24.95 (delta +$28.00); yield 9.0% vs the 8% bar BOTH WAYS; combined-at-path 74/76 pairs ≤97 — the design-to-complete-in-window property holds.** Doors on the letter: n=285<300 (07-10's tape lost to the disk prunes; 1,043 counterfactual-unsettled) and the CI kisses zero → no bar-drop; pessimistic strongly positive over the bar → NOT refusal six. **Second consecutive STRONGLY-POSITIVE-BUT-UNDERPOWERED — the pattern now has a name and a cure: settlements land daily; the reach-law and path re-runs clear n≥300 within days.** trendpath_shadow deployed (gated) — both legs' path-aims + the combined-at-path check log at every discovery from tonight. Disk incident #5 struck mid-sequence (100%, ws_depth_recorder now 8.2G — the STANDING eater, twice-flagged): the incident-4 verified-twin prune + gzip freed 4.5G; the ws archive-sync extension is now overdue infra. Artifacts: `.claude/trendpath/`. Proof: `.claude/proof_20260714/PROOF_TREND_PATH.md`.

### 2026-07-14 (~9:30 pm) · C-TAKER-REACH v1 LANDS: the strongest positive evidence of the arc — engine E$+39.61 vs actual +$11.20, yield 15.4–16.0% vs the 8% bar EVEN UNDER THE PESSIMISTIC ×0.5 BOUND — and the doors, ruled on their letter, still do not open: n=244 < 300 and the interval kisses zero (CI [−3.05, +58.03]). NO bar-drop, NO refusal — the forward clock stands, record marked STRONGLY-POSITIVE-BUT-UNDERPOWERED
The reach law fitted walk-forward (G9 < Jul 10 tour; live-era ITF, branded; memory-lean after the first run was OOM-killed — the in-group-window approximation is named and conservative). **The re-grade under it: 07-10 +$12.63 / 07-11 +$15.80 / 07-12 +$11.57 / 07-13 −$0.39 (pessimistic within $1 throughout); N=244 era-clean — 727 counterfactual-unsettled skips ate the sample; a re-run in days clears n≥300 as settlements land. The real-print deep partition (10/1) stands untouched.** **THE HARVESTABLE MAP replaces the mirror census (the 1¢-flicker table stays as the mirror-method exhibit): the harvest exists and it lives ENTIRELY in the OPEN flow state — ITF_W|open 94.7 one-cent-reach sweeps/hr · 68.0/hr at 5¢ · 44.8/hr at 10¢; ITF_M|open 79/54/34; CHALL|open comparable; quiet flow ≈ 0.03–0.1/hr = nothing. The mirror census undercounted the moment by two orders of magnitude because it counted printed depth instead of taker arrivals.** Doors: not a clear pass (n short, CI touches zero) — five-forward-nights does NOT unlock; not a pessimistic failure (+$25.97, 16.0%) — NOT refusal five. The fill judge for all future replays is the reach law (`.claude/takerreach/LAW.json`, registry-cited). Artifacts committed.

### 2026-07-14 (~8 pm, read-only, parallel) · C-SHIMIC-TRACE: the market the operator is watching, decoded — the pair is one-sided BY THE MACHINERY'S OWN LOCKS: MIC 5@16 held with a 20¢ maker exit; the SHI completion re-aim was cancelled-then-GUN-REFUSED at 7:46:35 pm (no Shibata bid exists; the 80s bid on screen is not ours) — and the completion engine has been saying `taker_complete` every 10 minutes since, DEFERRED at the taker cap (3/3 used)
Lifecycle cited: 7:27:44 move_repost 81 → **7:28:59 self_fill_bell (78→82, +4¢, condition `tape_corroborated`, allowance 20)** → gun fired (source six) → the trigger placement rested at 82 → 7:33:03 `move_repost_gun_hold` (83 refused, bid held — C-CHASE-KILL organ gate) → 7:46:34 MIC fills 16, maker exit 20 posted (`maker_exit` stamp) → 7:46:35 `reaim_sibling_lower` cancels the 82 to re-aim 81 (97−16 headroom, `pair97_bound` stamp) → **the 81 re-place `gun_buy_refused` → `sibling_repost_failed` → NO SHI bid since** (exchange confirms: SHI resting NONE; MIC sell 20×5 rests; SHI market 86/87, last 86). **The structural finding, named: the fused-gun guard starves the pair-97 completion re-place after a bell — the one-shot exemption covers only the completion-live TAKER cross, which the cap is deferring (`completion_taker_capped` at 7:46:40, 7:56:43, every cycle since; verdict `taker_complete` is RIGHT for this state).** The shadow's opinion, cited for the operator's comparison: at 6:36:21 pm (pre-bell, quiet tape) liveaim said AIM_SHALLOW 68¢ on SHI (prior 9, deep tier 21, flow 0.0, spread 8, lib_conf 0.84) vs the live 71 bid — the new brain wanted 3¢ shallower on a dead tape, then the bell froze everything before the moment could form. Rider executed: YIELD-ON-WAGERED headline added to the nightly (backfill 07-10 +1.6% · 07-11 +0.8% · 07-12 +0.1% · 07-13 −3.8%).

### 2026-07-14 (late night) · C-FULLWINDOW-REPLAY v1 (read-only, detached): the REAL trial — and the verdict is the THIRD door, said plainly: THE SLATE, NOT THE ENGINE. The moments exist in count (900+ episodes) and the engine picks them right (deep-fill partition 10/1, far above the 8/4 floor) — but they barely exist in SUBSTANCE: median depth available 0–1¢, median duration 2 minutes
The full window-1 re-aim cadence simulated tick-forward on the four days' own tape (state recomputed every 120s, deployed rules verbatim, fills only where tape printed at the resting aim during its residency; 148 no-tick legs excluded; depth_trend branded as the 120s reconstruction). **The grade: N=765 era-clean legs — engine +$8.40 vs actual +$11.20 (delta −$2.80, 95% CI [−38.50, +34.35]); yield 5.4% on $157 staked vs the 8% bar. No pass; no bar-drop; the forward clock stands.** **NOT refusal five — the engine's core mechanism is VINDICATED on selection: 647 mid-window deep episodes fired and the moment-conditioned deep fills ran 10 winners-or-cashed to 1 loss** (v1's collapse-skew fully inverted; v2's 8/4 floor cleared). **THE MOMENT CENSUS — window 1 in its entirety, ours, from our tape, measured for the first time:** ATP_CHALL 53 episodes · med 54 min before bell · med 2 min · med 0.0¢ depth; ITF_M 387 · 142 min · 2 min · 1.0¢; ITF_W 327 · 95 min · 2 min · 1.0¢. **The strategic sentence the operator asked history to write: on this slate's tape, the harvestable discount-moment is a one-cent, two-minute flicker — moment-timing cannot carry the entry edge here; whatever edge exists must come from the prior depth (already refused at static and clock conditioning) or from an axis not yet measured. The engine stays in shadow (its selection is real and free); its dollar ceiling on this slate is the census's, not its own.** Artifacts: `.claude/fullwindow/` (ROWS, MOMENTS, SUMMARY).

### 2026-07-14 (night) · C-LIVEAIM-BACKTEST v1 (read-only): the acceleration is REFUSED — engine −$1.70 vs actual on 741 era-clean rows, CI [−34, +34] — but the failure axis convicts the TEST, not the design: discovery instants structurally cannot show the moment-forming mechanism (1 AIM_DEEP fill in 741); the forward clock stands unchanged
Tick-forward on the week's own recordings (book-tick files carry full 5-level state; 148 legs excluded era-unclean/no-ticks, counted; 421 thin pages; depth_trend branded as a 2-min reconstruction). **N=741 era-clean rows: engine +$9.50 vs actual +$11.20 (delta −$1.70, 95% CI [−34.45, +33.90]); yield 5.6% on $169 staked vs the 8% bar; AIM_DEEP fired ONCE across the whole test** — the deep condition (flow at curve + falling prints/hollowing book) almost never coincides with a DISCOVERY instant, because discoveries happen on quiet early tape while the moment-forming mechanism lives at RE-AIM time throughout window 1, which the live shadow evaluates continuously and this replay could not without simulating the full re-aim cadence. **Per the pre-agreement: no bar-drop — the forward requirement stays n≥300 shadow-graded with the interval clear of zero.** Not refusal number five: the design ran statistically level with actual (−$1.70 on an $11 scale) while its core mechanism went untested — the live shadow is the honest instrument, and it is already running. Rows: `.claude/liveaim_backtest/ROWS_20260710_13.jsonl`.

### 2026-07-14 (pre-6:10) · C-LIVE-AIM v1 (`da3deec7`, gated deploy, SHADOW): the fourth aim design — the operator's spec verbatim, every market its own tailored analysis in real time upon discovery; learning daily, acting never until the forward bar
**The aim posterior per market:** at every v4_place decision, `_liveaim_shadow` composes the fitted PRIOR (GUIDEBOOK_V1 page — refused for acting, citable as prior: M1 depth p50 + deep tier) with THIS market's live state from its own tape: flow_ratio vs the category's fitted OPEN curve (M3/M13) · book depth building/hollowing · spread · print signature. Conditioning v1: prints rising → NO_BID_CHASE_GUARD (the chase lesson made structural); flow at curve + falling prints or hollowing book → AIM_DEEP(moment_forming) at the deep tier; quiet+wide → AIM_SHALLOW; else the prior. Full input vector + citation in every line (constraint #11). **Accrual-aware honesty (Part 2): every line prints lib_conf — the category's banked-tape confidence rising daily with the archive-sync; ITF starts humble and its hardening is a number you watch; NO-OPINION below the floor.** **Graded forward from night one (Part 3): the nightly's LIVE-AIM SHADOW dual line (shadow $ vs live $ vs tape truth per cat); the cutover bar is pre-agreed — beat live aims at n≥300 shadow-graded with the interval clear of zero, the same bar EV3 wears. No historical replay gates the build: the three refusals already graded everything history can say.** **Nothing acts (Part 4): there is no acting path in this build — the log line is the entire output; leash, bells, caps, placement all byte-identical.** AIM/TIMING MISSES: attempt four, earning it or not on paper, nightly. Proof: `.claude/proof_20260714/PROOF_LIVE_AIM.md`.

### 2026-07-14 (early) · C-GUIDEBOOK-V2 v1: THE THIRD REFUSAL, named — the timing-conditioned recut fixes WHO fills (partition 8 win / 4 loss vs v1's collapse-skew) but not the dollars (−$10.70 vs −$9.30 same-universe actual, held-out); nothing ships
**The recut (built, committed dark):** GUIDEBOOK_V2 from the G9 era tape (3.24M ticker-minutes, 20,018 tickers, train < Jul 10) — self-contained truth per ticker (−0k onset rule as start proxy, first-hour median as reference, terminal 97/3 prints as result); 48 pages (46 AIM) on cat × shape × price-cell with per-cat dip windows and in-window depth quantiles. **Two structural findings that outlive the refusal:** (1) **G9 carries no ITF** — the week's P&L engine has NO era-admissible deep corpus (consistent with G4: archive ITF anchors inadmissible); the guidebook family is data-starved exactly where the book trades most — a tape problem before a model problem; (2) **the fitted tour dip windows are DIFFUSE ([13,508] min CHALL, [91,996] MAIN)** — at this conditioning the discount is not a narrow moment on tour tape; a real v3 needs flow-signature triggers, not clock windows. **The week, conditioned (held-out): −$10.70 vs −$9.30 actual on the same universe; 12 deep fills on $34 staked (−4.1% yield); 605 legs skipped on the tape condition, 748 no-window.** The adverse-selection check PASSED (partition 8/4 — the axis both refusals convicted was the right axis for WHO) — and the dollars still don't clear, so per the pre-agreement: **third refusal, nothing ships, no deploy, no flag.** AIM/TIMING MISSES stays open on evidence, three refusals deep; the registry discipline held every time. Next direction named, not built: flow-signature (not clock) conditioning + the ITF tape gap closed by accrual before any v3.

### 2026-07-13 (late night) · C-GUIDEBOOK-AIM v1: BUILT, TRIED, AND REFUSED BY ITS OWN TRIAL — the week replay says the guidebook aims LOSE to the live aims on held-out dollars (−$9.90 vs −$0.62); per the pre-agreement, the close-out says so and NOTHING SHIPS (shadow gated OFF, `guidebook_shadow_enabled=false`)
**The build (kept, dark):** GUIDEBOOK_V1 (`9e1fd77c`, `.claude/guidebook/`) composes M1's recut depth DISTRIBUTIONS (edge_p25/50/75 = depth reached in ~75/50/25% of historical window-1s + dip timing t_deep + dip-first share) with the fillredo fill-regime grid and M6 bands — 538 pages, 261 AIM / 277 REFUSE_THIN; the doctrine flip named in code (GET PAID, not GET FILLED; yield vs the 8% bar; NAMED NO-BID; the walk may never chase past the page); shape axis = named v2 gap. **The trial (walk-forward — recut fitted 07-08, week 07-10→13 held out): 1,377 discovery placements → 420 thin-refused + 295 yield-refused (fewer-deeper-on-purpose DID happen) → 247 aims, 114 filled at depth, 133 honest no-fills. Dollars: −10.65 / +5.10 / 0.00 / −4.35 = −$9.90 on $226 staked (−4.4% yield) vs actual −$0.62.** The failure, named: static depth without flow conditioning is adverse selection concentrated — the fills that reach a fixed deep bid skew toward collapsing legs while the no-fills forgo the winners the live shallower aims captured; the entry-blend arc's law re-confirmed on fresh tape (dip-capture survives only WITH the tape condition). The unexploited half of the page is the TIMING column (t_deep_p50, dip_first) — v1 aimed at a depth while ignoring WHEN; the v2 recut must condition depth on the dip window and the three-price shape. **AIM/TIMING MISSES stays open on evidence — this is its second refused closing design (the 07-11 aim refit was the first); the registry discipline held both times: losers do not enter.** No deploy; code committed and gated dark.

### 2026-07-13 (night) · C-EV3-BACKTEST v1 (read-only trial): EV3 beats actual by +$5.35 on 264 held-out legs — but the CI straddles zero and n<300: THE PRE-AGREED VERDICT IS FLAT-AT-INSUFFICIENT-N — no cutover word this read; the leash stays; the shadow keeps accruing (~90 graded legs/day → the bar within a day or two)
Design: TRAIN = M15 cells (built through Jul 10); TEST = every settled leg entered Jul 11–13 (never seen by the surface); 264 graded, 33 NO-OPINION excluded and counted, 9 no-tape, 11 unsettled; leash floor (−3¢) applied to every frame; hold realizes the leg's real outcome, flatten realizes −1¢/share (the live flattens' measured shape). **Dollars (5-share legs): actual +$2.35 · two-term +$5.75 (53 flattens) · EV3 +$7.70 (48 flattens) · EV3+posterior-proxy +$6.60 (1 flatten).** Per-day EV3-minus-actual: +1.35 / +6.10 / −2.10. **The cutover number: EV3-minus-actual = +$5.35, 95% CI [−20.20, +29.10] — positive point estimate, statistically FLAT, and n=264 < the pre-agreed 300.** Per the pre-agreement: the two-term leash STAYS; the frame does not go back to the shop as a failure (it graded no worse and dominates two-term by +$1.95 with fewer flattens) — it waits for n. **Part 2 answered: the posterior variant (proxy = runmid/100; the real composer posterior needs tick replays — proxy stated) flattens almost NOTHING (1/264 — the EV≈0 identity made flesh) and grades BELOW cell-only EV3 (+$6.60 vs +$7.70): live posterior wiring does NOT earn its own build on these numbers.** Both judgment frames beat actual behavior on point estimate — the leash floor itself is doing real work. Re-read at accrued n≥300 via the nightly's dual-frame grading; the needle question has its real answer schedule: days, not months.

### 2026-07-13 (evening) · C-COMPOSER-EV3 v1 (`07cf29a2`, gated deploy, SHADOW): the three-term frame ships — and the acid test is reported as it fell: THE BAR IS NOT MET (winners flip 9/20, saves retained 6/16); the separator the bar demands is mostly the price itself; the nightly is the judge
**The frame:** EV_hold = p_exit_fill×band + p_win_ride×(100−basis) − p_loss_ride×basis — every probability an M15 cell share (cashed/win/loss per cell; the 3-way partition was in the building, the two-term frame never consumed win/n); posterior blend hook K_POST=8 (None live for now); NO-OPINION on thin cells. Shadow stream only: every completion_shadow line carries the ev3 sub-record; **the leashed two-term actor is unchanged — no live behavior change in this deploy.** **The acid test (walk-forward by construction — M15 predates the graded legs): 9/20 win-side giveaways flip to hold/defer; 6/16 true saves still flatten; 11 residual giveaways cited with numbers (all high-basis leaders: at basis ≥88 the cell ride-loss share prices −basis beyond any historical win share).** The structural finding, plainly: a market-posterior ride is EV≈0 by identity — ex-ante, the acid set's winners/losers separate mostly by PRICE, so no EV threshold on these inputs meets the 20-and-16 bar; the frame's worth is decidable only on forward tape, which is what the shadow + nightly grading now measure. **Standing corollary on the same tape: the flatten leash's 3¢ floor nets +$11.00 (blocks $24.20 of shallow-EV giveaways against $13.20 forgone saves) — yesterday's bound validated by today's grade.** Cutover = positive nightly grade + the operator's word, per the migration doctrine. Proof: `.claude/proof_20260713/PROOF_COMPOSER_EV3.md`.

### 2026-07-13 (late afternoon) · C-ADJUDICATION-READ v1 (`b7abdc5b`, gated deploy): the flatten branch graded on REAL settled outcomes — NEGATIVE (N=36, −$0.264/leg, 16/36 beat the ride) — and the operator's pre-authorized leash applied same hour
**The number decided, not the bleed anxiety: flatten-vs-ride = −$9.50 across 36 settled counterfactuals.** The structure named: all 20 giveaways were legs whose kept side WON (dogs at 12–16¢ flattened flat that settled 100 = −$4.40 each); the 16 saves were real ride-to-zero legs (+$0.35…+$3.25). **Root for the composer's next fit: the two-term EV frame excludes the win-ride residual by design — winners' upside never enters EV_hold, so high-probability legs systematically read negative and get flattened.** The leash (operator word embedded in the dispatch): `flatten_daily_action_cap=8` + `flatten_ev_margin_cents=3.0` (the noise band where graded deltas straddle zero), named defer/cap lines monitor-rendered, counts boot-persistent (today's 32 already exceed the cap → the branch is capped from this boot). Sunset = positive calibration on the nightly. **Band-collapse decomposed by path (today):** maker bands +7.9…+15.5¢ vs engine flattens −3.1…+2.0¢ per cat — the 4.8¢ blended band explained; the nightly GOVERNOR SPLIT now prints avg band per governor so tomorrow reads one table. Replay 8/8 at the SHA (both leash cases named, zero orders). Legacy exit path untouched. Proof: `.claude/proof_20260713/PROOF_FLATTEN_LEASH.md`.

### 2026-07-13 (afternoon) · C-PNL-TRUTH (read-only): the four-day exchange-truth ledger — bot-only +5.41 / +2.31 / +0.14 / −8.48 = −$0.62 cumulative; the June geometry is alive at 5-share scale (+0.5–0.9 avg winner vs −2.1–2.4 avg rider); the screen ties to the cent
Method verified on known legs (CORBRU +0.80, PODMAR +0.93): every fill = purchase of side at side price (Kalshi books sell-yes as buy-no); settlement pays result-side count minus fee_cost. **Per-day bot-only net (entry-day attribution, settled legs): 07-10 +$5.41 · 07-11 +$2.31 · 07-12 +$0.14 · 07-13-so-far −$8.48 → cumulative −$0.62.** Bands hold ~10–13¢ on 10–12; **07-13's bands COLLAPSED (ATP_CHALL +1.8¢, ITF_W +1.7¢ avg)** — the completion engine's 32 flatten-at-bid exits crush the cashed-band average by design. **Asymmetry, per day (winners avg / losers avg): +0.94/−2.41 · +0.54/−2.12 · +0.60/−2.08 · +0.50/−0.77** — the June −53.7¢ geometry re-measured: band wins ~+11¢/leg vs full-basis riders ~−42¢/leg; a day survives only when winners outnumber riders ≳4:1; 07-13 ran 39W/36L. **Governor split (07-12/13 settled): per_leg_policy n=33 realized −$6.48 (−19.6¢/leg) vs maker_exit n=42 −$2.00 (−4.8¢/leg) — naive read: the new brain underperformed; honest read: selection bias by construction (the policy inherits the DOOMED legs — its counterfactual is the −$2.08 avg ride-to-zero, against which −19.6¢ flattens SAVE ~$1.9/leg IF the EV verdicts are calibrated; tonight's LIVE-vs-SHADOW grades exactly that).** MANUAL bucket excluded from bot basis: +$31.85 (Jul-9/10 operator trades, ARSCOL/MOCJAS/STRKIR/IMANAK/SHENON/SHOKRO/TUPMAK families). **Screen tie-out: cash $791.02 = API $791.0258 EXACT; positions $16.05 vs API $16.12 (mid drift); total $807.07 ≈ $807.15; 24h change −$8.49 vs bot-only 07-13 −$8.48 — the day's bleed is entirely bot trading, delta 1¢ = drift/taker-fee residual (settlement fees $2.09 today already inside net; IOC taker trade fees not itemized by the fills API — absorbed in the ≤7¢ residual, named).** 8 unsettled legs carry the open $16.12.

### 2026-07-13 (midday) · C-BACKHALF-NOW (read-only + one verdict correction): the back half is HEALTHY — 81 fills → 64 cashed / 13 settled / 4 exit-resting / ZERO naked; the engine evaluated ALL 71 singles (32 flattened, 2 taker-crossed, cap held); the 6:10's OPEN was UNSOUND — corrected to REFUSED and the prefilter that swallowed the honesty brands fixed
No exit silence: 91 exits posted against 91 entry bookings — the "no exit lines" read was a RENDER GAP (LIVE_STATUS has no exits section at all; a build for a future dispatch, filed). Engine's day: 656 evaluations, never-evaluated = 0; verdict totals: ACTED flatten_kept 32 · ACTED taker_complete 2 · hold 17 · NO-OPINION 20; taker cap 3/day held with 29 named deferral lines. **The gate contradiction rooted with the code line: the scorecard scan's string PREFILTER (gun_scorecard.py scan loop) never admitted `chase_cap_*`/`gun_buy_refused`/`self_fill_unfrozen` lines to parsing — the honesty collectors ran on zero rows and the gate printed OPEN against two suppressed windows. Also swallowed since their births: MULTI-SOURCE (gun_source_confirm), HALT-MIN and UNBOOKED-FILLS-BOOKED (conception_halt_*/fill_booked_reconcile) — those footer zeros were DEAD numbers, now live.** The rendered 07-13 scorecard's verdict corrected in place to REFUSED (insufficient clean tape, reason on the card); prefilter fixed in code — tonight's 6:10 consumes the brands by construction; the deletion word rides tonight's genuinely clean tape.

### 2026-07-13 (~3:30 am, emergency #2) · C-BELL-SCOPE v1 (`0a6ab782`, gated deploy): the self-fill bell scoped to unsanctioned behavior — OVER-BROAD LOCK instance 2, one night apart; 24 false freezes lift at boot; two suppressed windows force the deletion gate REFUSED
**The instance:** the bell's 4¢/30-min threshold was calibrated against the pre-arming 4¢-walk world; the −0j arming (ITF honest allowances 14–20¢) invalidated it the SAME DAY — the C-CAP-SCOPE rescope freed the re-aims at 1:30 am and every sanctioned walk ≥4¢/30min rang the bell: **24 events frozen 1:30–3:09 am (the dispatch's eleven kept growing), 1,780 cumulative frozen-minutes, 42 entries refused.** **The correction:** the bell fires only when the rise is EVIDENCE, not sanctioned behavior — (a) `leg_fill` (post-fill pursuit, the CORBRU shape) · (b) `exceeds_sanctioned_walk` (rise > the ARMED walk allowance, read from the LIVE table via `_sanctioned_walk_cents_cat` so re-ratifications can never strand the threshold again) · (c) `tape_corroborated` (≥5 non-self prints in-window, DECREED). Every fire logs its condition. **The unfreeze (Part 2):** the gun rebuild captures rise + confirms; boot audit pops self_fill-only freezes with no fills, no confirms, rise within allowance — `self_fill_unfrozen` with the true state; genuinely-live events re-fire via the other six sources in minutes. **Replay, three lanes PASS at the SHA on the box:** CORBRU still freezes at the 51 via (c) (its tape printed through the ladder — RAIZHU's condition proven by the same lane); tonight's sanctioned shapes run NO-FIRE end to end; 23¢>20¢ fires (b); post-fill +6¢ fires (a); post-arm cap discipline intact. **The honesty branding (Part 4):** the 6:10 scorecard carries BOTH suppressed windows (cap 9pm–1:30am · bell 1:30am–deploy), and **two windows force DELETION GATE: REFUSED — insufficient clean tape, whatever the four proofs say. A clean night means clean.** The class lesson extends into code: a lock's threshold must scope against currently-sanctioned behavior DYNAMICALLY, never against constants other deploys change. Proof: `.claude/proof_20260713/PROOF_BELL_SCOPE.md`.

### 2026-07-13 (~1:30 am, emergency) · C-CAP-SCOPE v1 (`22039f30`, gated deploy): the pursuit cap rescoped to the operator's actual words — OVER-BROAD LOCK founded; 931 refusals / 7 stranded bids counted; CORBRU still freezes
**The correction:** the chase-kill closing build over-rotated — the DECREED constant was a CYCLE cap and the build made it a PLACEMENT cap from the first buy ("cycles became placements"). Rescoped: **pre-fill, pre-bell upward re-aims are normal market-making — unlimited in count, each bounded per placement by the armed honest-anchor walk cap; the cap (2) begins counting at the leg's FIRST FILL or the event's FIRST BELL, whichever first — exactly where chasing becomes possible.** Down/equal re-places pass always. `_pursuit_armed` gates counting and both enforcement sites; boot rebuild recomputes under armed semantics. **Three replay lanes PASS at the SHA on the box:** (i) CORBRU's tape still freezes at the 51 — the self-fill bell is and was the chase-killer; the rescope removes the strangler, not the kill; (ii) tonight's WONBOW/BONFAB/SUNYUN refusal shapes re-run with ZERO chase_cap refusals; post-arm discipline intact (3rd upward re-aim after a fill refuses); (iii) stranded bids re-aim first cycle post-deploy (prices in the dispatch report). **The damage, counted and filed to OVER-BROAD LOCK: 931 refused re-aims / 20 legs / 15 events (4:23 pm → 1:19 am), 7 bids stranded stale at count time (WONBOW +36¢ below mid, SARBOV +40¢, SARANG ×2 +34¢, BONFAB +23¢, HASZAG +21¢, DUHGAT +13¢), 539 sidelined leg-minutes, 215 refusals on one leg in 45 min.** The 6:10 scorecard prints the refusal-window honesty footer by construction (the 12:20 cron pre-dated the fix; the regrade read carries the caveat). Class lesson, standing: a lock's scope is part of the decree — tightening beyond the operator's words is a defect, not prudence. Proof: `.claude/proof_20260713/PROOF_CAP_SCOPE.md`.

### 2026-07-12 (pre-6:10 build) · C-DELETION-GATE v1 (`9b57bcc4`, gated deploy): the governor stamp separates the two brains; percat priority defined and checkable; the self-fill bell's accident becomes a design with its tension named; the taker cap DECREED; tomorrow's scorecard carries Plex's four proofs and refuses the deletion word itself if any is missing
**Part 0:** `migration_day.html` + `PLEX_MIGRATION_FINDINGS.md` committed as RESERVED-VERBATIM slots — the body did not arrive with the dispatch (paste-drop, occurrence six; the relay's standing debt); the verbatim wins on landing; the four demanded proofs are BUILT regardless. **Part 1 — the governor stamp:** every acting decision on the completion path stamps `governed_by` at the single emitter (`per_leg_policy` | `pair97_bound` | `maker_exit` | `match_live_cancel`); the nightly gains GOVERNOR SPLIT (actions per brain + exit ¢ attributed to the last exit-posting governor per ticker) — no more hand-reading logs to know whose hand moved. **Part 2, vaulted as ordered — percat priority in one paragraph:** all seven sources (te_scoreboard · schedule_live · tape_latch · price_divergence · fallback_bell · self_fill · percat_fitted) feed the SINGLE fused stamp `_gun_stamp`; first-fire-wins is the code law — the existing-entry short-circuit at the top of `_gun_stamp` (`if self._gun_state.get(et) is not None → gun_source_confirm, return False`) means a later source can never re-stamp, re-time, or re-source a fired event; precision on "dedup silently": later fires leave a `gun_source_confirm` disagreement record with the inter-source delta — evidence, not leak; the scorecard footer now prints MULTI-SOURCE events=N nightly so the claim is checkable. **Part 3, vaulted — activity-firing is the DESIGN:** the self-fill bell feeds on placements AND fills by intent — our own placements are the one tape that cannot be someone else's noise (CORBRU had zero fills below 65; a fills-only bell could never have frozen the ladder); the false-positive mode stated honestly: an armed ITF honest-anchor walk (≤20¢) CAN rise ≥4¢/30min premarket and fire the bell on a not-yet-live match — tape shows zero instances (RAIZHU, the only fire, was genuinely live); the mode gets a nightly meter (SELF-FILL-UNCONFIRMED in the footer), not a redesign. **Part 4 — DECREED (operator, C-DELETION-GATE Part 4): `taker_daily_action_cap=3` until shadow+live taker verdicts reach n≥30 graded** — the 4th+ cross defers with a NAMED line (`completion_taker_capped`, monitor-rendered), boot-persistent count; not struck — 3/day bounds a miscalibration at ≤3×spread×qty while the nightly grades. **Part 5 — the deletion gate:** the 6:10 scorecard renders (i) clean-regrade numbers explicitly superseding the inadmissible 71, (ii) percat-vs-legacy priority reconciliation on tonight's real events, (iii) MAINS-OFF excluded from the denominator asserted in the footer (both ratios printed), (iv) the percat-vs-self-fill same-event table — **verdict line OPEN only with all four present, REFUSED otherwise with the missing proofs named: the gate enforces the migration doctrine itself.** Replay: taker-cap case added to `replay_completion_live.py` (4th cross defers, named, zero orders). Live note at ship time: the chase locks are refusing real ladders tonight (monitor rendering chase_cap violations + FORENSIC_chase_cap.md blocks). Proof: `.claude/proof_20260712/PROOF_DELETION_GATE.md`.

### 2026-07-12 (night) · THREE OPERATOR WORDS, one gated deploy (`9b26fc17`): percat bell ARMED (source seven) · −0j ARMED (the window-open void dies) · THE TAKER WORD — the completion policy goes LIVE
**The words, verbatim:** (1) *"Percat bell: arm the per-category fitted trigger as an ADDITIONAL bell source under the fused freeze, effective immediately. Full cutover decision (legacy trigger deletion) still rides tomorrow's 6:10 regrade scorecard."* (2) *"−0j: arm the honest-anchor walk-cap fallback, gated flip — the window-open void dies."* (3) *"Taker word: operator_taker_word = true. The completion policy goes live per its shadow-graded design: per-leg economics, taker-completion only when its own math clears, never-hold-naked verdicts enforced, pair-97 arithmetic consulted nowhere. Its first live day is graded against its own shadow record in tonight's adjudication — any live-versus-shadow divergence is a named violation."* **Execution:** `percat_gun_armed` fires `_gun_stamp(et, "percat_fitted")` inside the existing fitted-threshold block (additional source; legacy stays until the scorecard word); `walk_cap_honest_anchor` armed (Plex-ratified ITF_W 20/ITF_M 14/CHALL 2/MAIN 1 on the honest first-target — the CORBRU 24¢-walk class bounded); `completion_live_enabled` + the taker word: the shadow's verdicts ACT via `_completion_execute` — taker_complete cancels the sibling maker bid and CROSSES the sibling at ask (deliberate IOC taker; **one-shot fused-gun exemption** — the policy's cross is the single legitimate buy on a live match), flatten_kept cancels sibling bid + kept exit and sells the kept leg at the bid (never-hold-naked ends at the verdict), hold/NO-OPINION act nothing, **once per event per boot** (a flipped verdict after action is the adjudication's business). Nightly gains **COMPLETION LIVE-vs-SHADOW** (action-vs-verdict mismatch AND verdict-without-action = named violations). **Replay 5/5 PASS at the SHA on the box** (real `_completion_execute`, real gun guard: the belled-event cross passes exactly once; word=false holds; flatten cancels both and sells the bid). Boot resolves the two live bids (MIYKUZ-KUZ 32¢, CALRAD-CAL 57¢) by the engine's verdict — outcomes with prices in the deploy report. Proof: `.claude/proof_20260712/PROOF_THREE_WORDS.md`.

### 2026-07-12 (late evening) · C-SLATE-NOW (read-only): the day's book is ~77 singles / near-zero completed pairs; all 8 open positions are singles ($20.60 at basis); 2 pair-97 completion bids rest on LIVE tape (CALRAD 57¢, RAIZHU 54¢ — the latter the self-fill bell's own trigger bid); the June mechanism did NOT bite today — kept legs exited ABOVE basis in ~46/50 cashed singles (the in-play-fill pattern, not the starved-winner pattern). Operator's 2–3 pm example = MIYKUZ: leg1 65¢ partial 2:15 pm → complete 3:03 pm, sibling KUZ re-aimed then re-placed 32¢ post-deploy — the bid survives because NO bell ever fired on MIYKUZ (fused sources silent, state PRE); combined bound 97 held. Detail in the dispatch reply; no build, no restart.

### 2026-07-12 (evening) · C-CHASE-KILL v1 (`8a857ecf`, gated deploy): the in-play chase ladder dies — three locks; the cap-void class gets its closing build; the fitted trigger would have killed CORBRU before rung one
**Part 0 first, priced: the whole cohort is FLAT — zero exposure, no flatten words needed.** All six chased legs (CORBRU 65/32, TOMKAR 54, ROJPER 56, DURDOU 72, MILARZ 27, LEEGAO 46) exited maker hours before triage, all positive, **+415¢ total** — the ladder WON today, which is the trap and not the excuse (A49: the chase buys the sucker's anchor; today's winners are the roulette paying for the CORBRU that loses 65 straight down). **Part 4 root:** the 14 rungs (41→65, 90 min, live match) were `v4_move_repost` walk-steps with `premarket_walk_cap`'s `_window_open` anchor ABSENT — the cap never engaged (the void = the missing anchor, not the constant), and the honest-anchor fallback built for the hole sits staged OFF: **−0j escalates from ruling to defect.** Not cap-void re-entries (zero completed cycles). **The three locks (armed, one gated deploy):** (A) conception stamp RETROACTIVE to the event's first order; (B) the pursuit cap — upward buy placements per leg cap at `reentry_cycle_cap` (2, DECREED cycle ruling 07-08, constraint #11) from the FIRST buy, early window included, while neither leg has fills (completion after leg1 EXEMPT by construction — no manufactured starved singles; down/equal re-places pass so nothing strands above a falling tape); organ gate refuses BEFORE the cancel (the resting bid survives, ICHOCH) + chokepoint backstop; (C) the self-fill bell, gun source six — own buy activity rising ≥4¢/30 min IS match tape → standard gun stamp → the fused-gun guard freezes every later entry buy; the triggering placement passes as the evidence. 5th boot lineage rebuild (pursuit counters + activity series + conception registry). **Replay (harness law), real chokepoint, tape clock: fail-before 17/17 accepted (= the live tape); cap-only refuses BRU rungs 3–14 and COR's 3rd; both locks freeze the event at the 51 — 15 refusals incl. the whole COR side, 3+ hours of chase refused.** Delta named: exchange truth shows NO 51¢ fill (2 buy fills only, 65/32) — the "fills" were our bid ladder; the bell feeds on own buy ACTIVITY (placements + fills), else it could never have frozen CORBRU. **Part 3 cutover evidence (the cutover file `.claude/cutover/CUTOVER_EVIDENCE.md`):** chase cohort 6/6 would-fire — CORBRU's fitted crossing at 12:31 am, **29 min BEFORE rung one**; today's 55 bell_missing (cited 11/day — today 5×, dominated by the ATP_MAIN 6:59 am lying-schedule block): 12 timely / 3 early / 17 no-tape / 23 mains-off. Evidence favors percat as an ADDITIONAL bell source (coverage only adds; a premature fire costs queue position, not money, under the fused freeze). Gate still opens on tonight's clean regrade; tomorrow 6:10 the word is the operator's. Lane 2 stated without varnish in `.claude/proof_20260712/PROOF_CHASE_KILL.md`: the locks refuse trades that won today; the operator decreed the kill knowing the ledger.

### 2026-07-12 (afternoon) · −0k SHIPPED (`476b4326`): the truth instrument cured on its own tape; the four "divergences" were the comparator, not the math; the 97-constant's two blind spots named; the fitted trigger's cutover evidence cuts both ways — and disk-full #4 struck mid-forensic
**Flow-step onset (TRUTH-JOIN DRIBBLE-ONSET closing build):** `tape_onset` now demands trailing-15 prints ≥ max(8, 3× the window's FIRST-hour baseline — not the prior hour; gradual dribble makes every ramp stage a "step" over the last) + 30-min forward sustain (kills burst clumps, keeps 0.5/min thin ITF joins) + a NEW symmetric `onset_lags_fresh_fire` quarantine (>20 min). Same-tape validation: **fail-before reproduces all 16 SUSPECT deltas exactly; pass-after: 10/16 → clean joins within ±12 min of the tape-proven fires, all 6 heavy-premarket ambiguities QUARANTINE instead of averaging** (SAGYOD's −68.7 would have poisoned the meter without the new bound), good rows 11/12 unchanged (MILARS → quarantined, the refusing direction). Deliberately not tuned past that — premarket-hotter-than-match tapes don't separate on tape alone. **(b) The 4 live-vs-replay divergences, rooted: all four were the COMPARISON instrument** — the replay re-minted trade IDs in fill order while live mints at placement (4/4 violation labels named the WRONG trades: live T-0039 = TALPAP, replay's #39 = BOSKAR), and the checker compared a live shadow line up to 5m56s STALE (300s/site dedup) against the replay at the FILL tick. Both fixed; rerun on the same tape: **divergences 4 → 1, labels correct; the survivor is real and rooted** — live n_eff counts only WS-seen prints since boot (2.0) vs the replay's full CSV+mids; review item: composer-state boot seed (C-TAPE-SEED pattern), own dispatch. **(a) Percat fitted trigger vs the 12 bell_missing events of 07-11 (dispatch said eleven; tape says twelve): 7/12 would-fire — 3 timely (+12…+14 min), 4 on premarket dribble (−22…−87 min), 3 nothing-to-catch (lying-schedule anchors, no tape), 2 MAINS-OFF by design.** The naked vol30 threshold needs the bell's gauge-plus-start composition or it recreates FERCER — on the cutover table with these rows as evidence. **(c) WALBAD/MIRMAL combined-99 breaches → COMBINED-CAP BLIND SPOTS class founded:** WALBAD = the race second (correct reaim cancel lost `cancel_fill_race` by ~1 s; exit-sell-before-sibling-cancel ordering gives away race window); MIRMAL = the conception-void ledger (pre-conception 11–12¢ fills invisible to pair accounting → sibling rested/filled at 87 uncapped, machinery re-aimed the already-filled leg). Remedies named, operator's word owed, no build. **Disk-full #4 mid-forensic (100%, 0 bytes):** bot rode it out (C-LOG-ENOSPC held); emergency = archive-sync's own step-(e) prune at 2-day retention (12,667 checksum-verified twins, 1.9G) + 186 CSVs gzipped (ticks 5.3→2.3G); now 97%, still tight; **named eater: ws_depth_recorder 7.2G (~1.5G/day since Jun 23) was NEVER in archive_sync's scope — board item to extend the sync; tennis.db 16G remains the standing hog. TONIGHT IS THE CLEAN REGRADE NIGHT — both crons ride the fixed instruments; the gun-cutover gate reopens on those numbers.** Report: `.claude/triage_20260711/ZEROK_REPORT.md`.

### 2026-07-11 (midday) · C-MORNING-TRIAGE-0711: bot never blinked; the monitor was blind 8h (MONITOR-BLIND founded); the sixteen SUSPECT rows acquit the bell and convict the schedule AND the grading rule (TRUTH-JOIN DRIBBLE-ONSET founded); shadow-gun grades ruled INADMISSIBLE for cutover
**Part 0:** bot ALIVE throughout (PID 3164947 since the 12:38 am composer deploy; 11:20 am steady-cadence audit PASS — 16 positions / 57 resting orders / 0 flags; no restart). The monitor stopped rendering at 4:09 am because its `bell_missing` violation item shipped without `cls` — the first live bell_missing (04:10, STATOM) crashed `forensic_check` and the per-cycle catch swallowed it for ~7h52m; the failing-then-passing replay exposed two more assumed-key sites (`detail`) in the same family; all three fixed (`d81a0709`, `c55f576c`), monitor re-rendering 12:01 pm. **MONITOR-BLIND class founded** (nothing watches the watcher) with its structural kill shipped: 3 consecutive crashed cycles → ntfy. **Part 1 (16 rows, one verdict — the dispatch said fourteen, the table carries sixteen, all ruled):** the Kalshi schedule lied 1–6 h LATE across the slate (timezone-shaped: ITF −6h CEST↔ET, ATP −1…−3h); te_honest carried the REAL starts; every suspect fire cites `start_passed=True, anchor_src=te_honest` (2 escape fires: MONHER rise 4¢ 16 min early on a ramping tape, KALTIK rise 6¢ where te_honest was absent); the hourly histograms show match-grade flow igniting within minutes of each fire and the tape ENDING hours before "scheduled start" (SHIROB settled before its 9:30 first ball; POLMIY ended 7:45 pm vs sched midnight). The scorecard's "truth" was premarket dribble: `tape_onset`'s ≥5-of-15-any-size rule passes ~1 print/min noise 75–115 min before onset — **TRUTH-JOIN DRIBBLE-ONSET class founded; the SUSPECT quarantine worked but starves the meter (within-±3 n=2/6, 3/6). Ruling: last night's shadow-gun grades (71 percat would-fires) are NOT admissible for a cutover decision** — onset rule needs a flow-step requirement + one clean regrade night, then the operator's word. **Part 2:** the mass fires (six at 3:30:18, five at 4:00:22) are venue slates sharing te_honest half-hour starts — gauge-plus-start on every cluster fire, zero escape fires in the clusters; the escape's two overnight fires were both tape-confirmed reality; no threshold review item. **Said plainly: the clock was the liar and the bell caught reality — the only instrument that worked last night; its one cost was indirect (its first zero-tolerance line felled the unprepared monitor).** Read-only except the monitor fix; §0A untouched; onset-rule fix FILED not built. Report: `.claude/triage_20260711/TRIAGE_REPORT.md`.

### 2026-07-11 (late night) · C-SYSTEM-PAGE v1: the viewer's third page — the knob canon rendered live, classification a COMPUTED field, the 52-NAKED population permanently visible (UNTRACED LIVE KNOB's standing exhibit)
Code `3062d639`, viewer restarted (tmux `live_viewer`, loopback 8787; live_v4 untouched). **Part 0 named plainly: `.claude/render/SYSTEM_PAGE_SPEC.md` and `system_map.html` are still reserved-verbatim slots (Plex's bodies dropped in paste) — v1 is built from the dispatch's binding scope; THE SPEC WINS ON LANDING and every delta gets named here; node-for-node Plex-map equivalence is deferred to the verbatim, with `.claude/render/proof/proof_system.html` committed as the diff target.** **The data contract:** `knob_census_check.py --emit` writes the build-time census artifact (regenerated per deploy — the line is in deploy_live_v4.sh; CODE-DRIFT hash included, and the page's DRIFT line is currently HONEST: pin 1f0f2c16 vs running 05b03dce, Plex's canon predates the composer deploy); `deploy_v5_live.json` polled at 30 s is the one live input; endpoints /system · /api/census · /api/config · /api/results (the four; a named choice pending the spec). **Classification is a computed field from `knob_citations.json` — a person's citation is the ONLY path out of NAKED; the emitter has no text-similarity classifier (red line honored structurally). Proven, not asserted: a DECREED→FITTED flip in the citations file alone recolored the row with zero frontend change.** Today's honest split: FITTED 3 / DECREED 18 / **NAKED 101** of 122 mechanical knobs — my citations cover 22; the gap to the 124-row canon reconciles when Plex's verbatim census lands (uncited = NAKED, full stop). **The panels:** anatomy spine L1–L9 with knobs attached per step (the L5 crowding renders as-is — never merged, the crowding IS the finding); classification strip + knob-vs-decision line (levers vs the step-level migration meter from the latest slate review); organ-ledger strip with the "procedural, no knobs" caption; dual staleness stamps (census 24 h vs config 90 min — different thresholds, as specced); **DOCTRINE CONFLICT pin overlay, toggle DEFAULT OFF, drawing its four lines to the live pair-97 knobs (combined_goal · completion_combined_ceiling · completion_all_cells · completion_reprice)** — verified 4/4 in the VPS-rendered DOM. Read-only end to end; renders decreed exposure, consults nothing; $0. Proof: `.claude/proof_20260710/PROOF_SYSTEM_PAGE.md`.

### 2026-07-11 (night) · C-COMPOSER-G1 v1: the composer goes LIVE-SIDE — its opinion at the moment of every decision, O(1) by the math's own construction; live-vs-replay agreement is now a nightly law; G1's distance is a number
Migration infrastructure (organ: blind aim table → composer conviction; the replacement now runs beside the incumbent in-process). **The shadow:** at every _os_shadow decision site (v4_place, sibling_repost, walk, move_repost, hold_review), `conviction_shadow` logs the composer's opinion — discovery prior + incremental posterior + M15 range prior + the M16 margin chip on ITF_M — **at the moment of the live decision, not hours later.** The compute budget is structural: the posterior's final value = f(prior, n_eff, latest print) — each evaluation is O(1), no tape re-scan; the 300 s/site dedup rides on top; kill switch `composer_live_shadow_enabled`; purity ASSERTED at the lint gate (composer = json/pathlib only, beside the oslayer boundary). **Part 3 (harness law): 200 synthetic series — live incremental ≡ offline full form, 0 mismatches** (caveat named: live n_eff counts prints; offline adds 0.25-weight mids; the agreement tolerance covers it). **Part 2 (same-instrument law, live edition): the nightly adjudication now checks every trade's live verdict against the replay's on the same tick — divergences are NAMED violations** (a shadow that grades differently live than in replay is measuring nothing). **Part 4 (G1 honest): no mains model invented — mains lines print NO-OPINION with G1 named; the distance to fitting G1: 371 settled mains legs graded (183 ATP + 188 WTA) and 20,332 mains tape samples banked.** NO-OPINION discipline verbatim throughout; nothing decreed added; trades nothing.


### 2026-07-11 (night) · C-BOOK-REPLAY v2: the whole book back-adjudicated (2,953 settled legs, composer-graded at their recorded ticks) — and the held-out gate did its job: ONE refit survived, three were REFUSED entry by their own numbers
Part 1: every settled leg Jun 26→Jul 10, retroactive ids per era, composer evaluated at each recorded fill tick (range layer included; the corpus is live-era by construction), settlements joined — **the full trading history as graded decisions with outcomes** (`.claude/book_replay/BOOK_REPLAY_V2.json`; walk-forward split: train 1,653 < Jul 7 ≤ held-out 1,300). **Part 3, the discipline that matters: fitted-vs-decreed in dollars on HELD-OUT data, and surfaces that lost were refused entry, said plainly:** (1) **AIM-SURFACE REFIT: FAILED** — held-out legs in the train-fitted best bucket ran −6.5¢/leg vs −5.5¢ baseline; it does not enter the registry. (2) **REFUSE MARGINS: ITF_M ALONE SURVIVED** — fitted 8¢ margin saved **+1,170¢ held-out vs the decreed 2¢'s −85¢** → registered M16, SHADOW, with the caveat NAMED (train-window sign flip = small-sample caution); **ATP_CHALL's fit lost outright; WTA_CHALL tied (not a beat → refused); and ITF_W's evidence FAVORS the decreed 2¢ (fitted-0 saved 1,310 < 2¢'s 1,562)** — the constant WON one, on the record, per category law. (3) Completion-params + participation tables ride as evidence feeds (into leg_econ + the unlock file), not decision surfaces. **Part 4:** the M16 margin runs SHADOW in the nightly (REFUSE-MARGIN line prints both margins' would-have savings for ITF_M every night); cutover on the operator's word, organ by organ. The migration meter's FITTED share rises by REFIT, not by grading — and tonight it rose by exactly one honest surface.


### 2026-07-11 · C-COMPLETION-POLICY v1 (SHADOW deploy): the worst class's closing design — per-leg economics live beside every one-sided pair; 44/44 strands verdict+priced; the taker branch computes but cannot act
The fix queue's #1 (ADVERSE-SELECTION STRAND: June 93/−$50.65, July-10 55/$6.25) gets RULING_PAIR_ECONOMICS as code: **`oslayer/leg_econ.py`** (pure, boundary-asserted) prices each leg per tick — **EV_hold = P(exit fills before collapse)×band − P(ride to zero)×basis** — from the M15 range layer's cell splits (win-ride residual EXCLUDED, conservative, named); **pair-97 consulted nowhere**. **Both policy branches built, both shadow:** (a) never-hold-naked — when the sibling can't pass its own math, the shadow records `flatten_kept` with the price it would have gotten; (b) `taker_complete` — cross the winning sibling's spread when the per-leg math prices it — **GATED behind `operator_taker_word: false`: computes and logs nightly, cannot act.** The nightly adjudication gains the **COMPLETION-SHADOW section** (verdict mix + kept-leg EV sums per cat) — the cutover case builds itself on paper; cutover on the operator's word; then pair-97 is DELETED. **Replay-harness law: today's 44 strands through the engine — 44/44 produced a verdict AND a price** (hold 21 · flatten_kept 18 · taker_complete 1 · NO-OPINION 4 thin-cells named). No live behavior changed anywhere in this deploy.


### 2026-07-11 · C-KNOB-CANON v1 (doc-only + two read-only forensics): the knob census is canon (124: 52 NAKED / 42 DECREED / 28 FITTED / 2 FITTED-UNARMED); UNTRACED LIVE KNOB founded; the WALK-CAP INVERSION named with today's cents
⚠ The paste channel dropped Plex's three files (KNOB_CENSUS.md, system_map.html, SYSTEM_PAGE_SPEC.md — third instance of the dropped-relay class); slots reserved verbatim in `.claude/render/`, the census's binding numbers recorded as relayed, the /system spec binds the viewer's third page when its turn comes. **Forensic (a), `marketable_stale_pin_exempt`, one paragraph, line-cited:** born `aba83afa` (07-04, C-CHURN-FIX b, armed same commit), read at live_v4.py:1713 (config true) and :8731 (the exemption site): a bid PINNED at the reshuffle bound (goal−sibling-basis) is exempt from the `bid_marketable_stale` kill — the LEG-49 exhibit (bound held 10 straight cycles, then murdered) is its birth story; degenerate/T51/T52/T-15 kills still govern. **The finding with teeth: the exemption fires SILENTLY — no log line exists when it holds a bid, so its July-10 firing count is unobservable from the tape** (the pin population it protects ran 5,373 reshuffle lines today). That unobservability is exactly why CLASS — UNTRACED LIVE KNOB was founded (instance 1 = this knob; population = the 52 NAKED; kill bar = study, ruling, or deletion — no fourth option; count prints mechanically: `knob_census_check.py`, 119 keys today). **Forensic (b), the walk-cap tables:** live walks are governed by the UNRATIFIED code-default (`_walk_cap_cents` MAIN 2 / CHALL 3 / ITF 4, anchored on the CONCEPTION CELL; config override absent) while the Plex-RATIFIED table (ITF_W 20 / ITF_M 14 / CHALL 2 / MAIN 1, anchored on the HONEST first-target) sits staged-off — **today's tape: 3 capped walks (ATP_CHALL ×2, WTA_CHALL ×1, cap 3 — each 1¢ LOOSER than ratified) and ITF's live cap 4 runs 10–16¢ TIGHTER than the ratified sizes; the anchors differ too, not just the cents.** BOARD −0j filed; the arming ruling is the operator's. **DOCTRINE CONFLICT gains the census evidence:** four pair-97 knobs live in production (combined_goal, completion_combined_ceiling, completion_all_cells, completion_reprice) — recorded WITHIN the interim clause, now BOUNDED by C-COMPLETION-POLICY v1 shipping next. **Part 4:** the census is pinned to its source (`census_source_hash.json`, sha 1f0f2c16…) — regeneration + CODE-DRIFT flag mechanical; the map cannot silently rot.


### 2026-07-11 (early) · C-PERCAT-GUN v1 SHADOW deployed (resumed on the operator's word): one trigger shape per market, FITTED — the July-8 replay shows the silent ITF cohort dying (14%→100%, 7%→98%); shadow night tonight, cutover on the graded word
The seventh-time class (GUN FIRE-STARVATION, ledger entry with the seven-fix lineage) gets its class-kill candidate: the volume ledger's own OPEN thresholds as the trigger — **ITF ≥6 prints/30m · CHALL ≥16/30m · MAINS OFF (their fitted line: fill flat ~1% at every volume state)** — one source of truth with the flow gauge, registry-cited (M3/M13), nothing decreed (#11). **Replay-harness law satisfied on the July-8 post-gun forensic tapes: coverage ITF_M 14%→100% (n=73) · ITF_W 7%→98% (56) · ATP_CHALL 24%→96% (25) · WTA_CHALL 40%→100% (10) · mains 0%→0% by design.** Deployed SHADOW ONLY (`percat_gun_shadow` would-fire lines beside the live gun; zero behavior); the shadow night starts tonight; the scorecard grades it; **cutover arms only on the operator's word, then the legacy burst constant is DELETED (migration doctrine, no exceptions for urgent nights).** Queue restored per the same order: **C-KNOB-CANON v1 and C-COMPLETION-POLICY v1 are the next two slots.**


### 2026-07-11 (early) · THE RANGE LAYER (Part 2b REVISED): the composer's historical anchor is the June framework era-stamped — WINDOW_MAP_3WAY's exact axes over 2,783 era-admissible legs; gate 4/4; G2 FILLED
Nothing invented (the operator's standing framework, not alongside it): **the axes are WINDOW_MAP_3WAY's verbatim** — fill−runmid buckets (deep_disc/disc/at_mid/over/deep_over) × price cells (≤25/26–50/51–75/≥75) × windows (W1 fill→scheduled · CORRIDOR scheduled→gun · W2 gun→settle), runmid = the trailing-30-min traded mean at the fill instant (observable, never retrospective) — extended per category per side from the June settled set (486 legs) to the **full era-admissible book Jun 26→today: 2,783 settled legs → 204 cells (112 at n≥5; per cat: ITF_M 27 · ITF_W 25 · ATP_CHALL 23 · WTA_MAIN 14 · WTA_CHALL 12 · ATP_MAIN 11)**, with the settlement vocabulary carried (win/cashed/loss/knife split — richer than June's ride-only win%). C45 lineage cited in the builder's header (WINDOW_MAP_3WAY · WINDOW_MAP · THREEPRICE_FAVORITES · MIDPOINT_FAVORITES · the fill-grid two-regime verdict · the census granularity law: ITF live-era only — this table IS live-era by construction; **archive-CHALL ±30m coarse addendum pending as its own detached pass, ranges only, never timing**). **Composer wiring:** `range_prior(cat, side, fill, runmid)` → the cell's resolved outcomes as the fitted prior, revised per tick; empty/thin cells → NO-OPINION with the cell NAMED. **Gate 3a grew test (iv) and passed 4/4** — one honest stumble on the way: the test's "absurd corner" probe (deep-discount cheap ITF dog) turned out to be a POPULATED cell in the 5.7× book — the composer was right, the test corner was wrong; the probe now finds a guaranteed-empty cell. THREEPRICE distortion-vs-discount rides as the fill-quality dimension (the bucket IS that grade, stamped per trade in the nightly instrument). Registry: **M15 added; gap G2 FILLED** (residuals named: thin cells stay NO-OPINION; archive addendum owed).


### 2026-07-10 (night) · C-LIVE-VIEWER v1: the operator's eyes on the meter — localhost command picture + per-trade viewer, read-only end to end, truth = the nightly files by definition
Migration-doctrine infrastructure (organ: human memory as process; no new class). **Part 0:** `.claude/render/LIVE_BUILD_SPEC.md` — ⚠ the paste channel dropped Plex's spec verbatim AGAIN (not in the dispatch, not in git); slot reserved word-for-word per the standing discipline; **v1 built from the dispatch's binding scope; the spec WINS on landing and any delta gets named here**. Colors PROVISIONAL pending the spec's key. **Parts 1–2:** `analysis/live_viewer.py` — stdlib, loopback-only (127.0.0.1:8787), zero write paths, tails exactly the files the monitor/adjudication write (spec §0: if a viewer number disagrees with the nightly ledger's own footer, THE VIEWER IS THE BUG). `/command`: migration bars per cat + step-level meter, ranked fix queue, no-fill taxonomy, class-ledger badges, exchange-truth verdict — every panel stamped with source mtime, STALE badge past 90 min (non-negotiable, shipped). `/trade/:id`: Exhibit-1 triptych grammar (tape polyline + prints dots + decision markers from the day's jsonl) + the L1–L9 step strip parsed from the slate review's OWN rows (the L7 chip from row data, never hardcoded — the spec's explicit warning honored). **Part 3, resolved honestly: YES — the live log writes incrementally** (jsonl append-per-event, ticks/trades CSVs append live) → open trades poll at 5 s (page refresh; grades compute on request); no demotion needed. **Part 4:** loopback only; the operator opens it with ONE line: `ssh -N -L 8787:127.0.0.1:8787 root@104.131.191.95` then http://localhost:8787/command. **Part 5 proof: BLIZAN = /trade/T-20260710-0059 renders the triptych (12 decision markers) + all 9 step cells (L7 from row data) + verdict banner; /command's migration bars sum 116/125 — matching the adjudication footer's AGREE 116/125 (92.8%) NUMBER FOR NUMBER.** No in-app browser exists in this session — the rendered HTML pages are committed as artifacts (`.claude/render/proof/`) in lieu of screenshots, said plainly; Plex can diff its static Exhibit-1 render against the committed HTML.


### 2026-07-10 (night) · C-DAYLIGHT-ROOTS v1: all 17 exchange-truth violations ROOTED — 0 double-count (the suspect is innocent) / 6 report error (my arithmetic, fixed at its line) / 11 unbooked-fill history (healed forward); VANKOI reconciles 5.0-vs-5.0 clean; the open book ties tonight
The buckets, mechanisms cited: **(1) LOG DOUBLE-COUNT: ZERO** — `entry_filled` carries cumulative `qty` AND incremental `new_fills`; the increments are correct and no fill ever booked twice; no ingestion dedupe was built because the defect does not exist (fixing a phantom would have been its own defect). **(2) REPORT ERROR: 6** (VANKOI/LAGROS/JONBAR/HOSVAN/ALFLOO/HARKIR) — the reconciliation column summed cumulative `qty` instead of `new_fills` (VANKOI: 2-then-5 cumulative = 7 vs exchange 5; increments 2+3 = 5 ✓). Fixed at its line in conviction_replay.py; **replay-harness law: fail-before = the original 7-vs-5 row; pass-after = the rerun clears all six (violations 17 → 11), VANKOI absent.** **(3) EXCHANGE TRUTH — our records missing fills: 11** (the six under-logged + five wholly missing) — the fingerprint of CLASS — UNBOOKED FILL: the list IS the no_exit halt roster (NAKYAM/ADDCRA/ROLWEI/SOUBOH/GHASPI/DYUSAG/TUPMAK×2/SHOKRO/SHENON) plus BLIZAN's cancel-raced cycle-2. Healed FORWARD by C-BOOK-THE-FILL, and healer bookings (`fill_booked_reconcile`) now count as log truth in the reconciliation so healed fills never re-flag; the July-10 rows stand in the report as history, correctly. **Part 3 — position truth sweep: DONE, 0 daylight** (1 unsettled held position; exit-qty law holding live; foreign excluded per bot-only basis) — the books tie tonight, not eventually. **Part 4:** the 12:20 am cron re-counts nightly; EXCHANGE-TRUTH DAYLIGHT's KILLED claim = zero violations nightly or it reopens itself. Ops lessons banked: the pgrep-self-match trap re-confirmed; a raced old-code rerun was killed and relaunched on synced code.


### 2026-07-10 (evening) · C-FULL-SLATE-REVIEW (read-only): the logic graded, not just the fills — 1,125 step-decisions, the no-fill cohort graded for the first time, exchange truth reconciled three ways, the fix queue ranked by measured cost
The operator's correction accepted on the record: the nightly instrument graded fill-vs-conviction — a third of the standard. Now shipped, and the 12:20 am cron INHERITS all of it (same code path; same-instrument proof: **123/123 base rows match**, full run 125). **Part 1 — per-step grades: 125 trades × L1–L9 = 1,125 rows**, each with the rule that fired (cited), FITTED/DECREED/NAKED classification, the composer's verdict at that tick, disagreement dollars where priceable. **STEP-LEVEL MIGRATION METER: FITTED 37% · DECREED 59% · NAKED 3.8%** (the 43 NAKED are L4 adopted/booked-path legs with no v4_place lineage — named, not hidden). Exits graded only (§0A). **Part 2 — the no-fill cohort, graded for the first time: 70 legs placed and never filled**; taxonomy per leg (aim-below-achievable / late-vs-dip / queue-starved = the adverse-selection signature / gate-blocked / anchor-refused), forgone edge_p50 per the fitted surfaces; **44 strands**; the June winner-test verified where settlement rows determine it (1 determinable — the verification method's own coverage is named, not smoothed). **Part 3 — exchange truth (the operator's cross-reference, audited REST path): 633 buy-fill shares across 123 tickers reconciled three ways — 17 NAMED VIOLATIONS** (first: VANKOI-KOI log 7.0 vs exchange 5.0 shares) → CLASS — EXCHANGE-TRUTH DAYLIGHT founded; the browser exchange-eye stays queued behind #8, this column is its forerunner. **Part 4 — the fix queue, ranked by measured cost: ADVERSE-SELECTION STRAND 55 instances/$6.25 · DOCTRINE CONFLICT (exit-band vs conviction) 10/$3.51 · AIM/TIMING MISS 11/$2.60 · conviction-gap placements 7/$1.05** — reconciliation feeding execution, discoveries applied not narrated. One process defect self-caught and fixed in-pass: the wiring heredoc printed WIRED without verifying its replace applied (silent no-op) — re-applied via deterministic edit; lesson stands with the artifact-committing-audit-in-tests lesson from C-BOOK-THE-FILL.


### 2026-07-10 (~6:15 pm) · C-NIGHTLY-ADJUDICATION (gated deploy): live trade identifiers + the migration meter prints itself nightly — no dispatch needed
Loop infrastructure per the MIGRATION DOCTRINE (organ: human memory as process; no new class). **Part 1:** every trade carries `T-YYYYMMDD-NNNN` — minted at PLACEMENT at the single _log emitter (new cycle → new id), inherited by entry_filled/exit_filled/scalp_filled/settled and every artifact; sequence rebuilt at boot from the jsonl (**the fingerprint pattern's 4th application**, `trade_ids_rebuilt` beside orders/gun/cycles) with the retroactive-continuity seed — July 10's replay identifiers (T-20260710-0001…0123) remain valid and the live series continues from the day's fill count. **Part 2:** the conviction replay is a 12:20 am cron (`/root/adjudication_nightly.sh`): the 3a gate self-checks the composer every night (a failure writes GATE FAILED and refuses the replay — constraint #1 in perpetuity), then grades the closed day (AGREE / WOULD-REFUSE cited / NO-OPINION named) into `ADJUDICATION_YYYYMMDD.md` + RESULTS json committed beside the ledger, with the **MIGRATION METER** line in NIGHTLY_PASS (fitted share vs legacy constants, per category — complete at zero legacy). **Part 3 (replay-harness law): tonight's build re-run on the July 10 slate matched the one-shot ROW FOR ROW — 123/123 identical** (the re-run appends the 2 fills that landed after the one-shot). Constraints: #3 nothing else rode · #2 §0A untouched (grading only) · #11 pair-97 graded as legacy, never anchor.


### 2026-07-10 (~5:30 pm) · C-BOOK-THE-FILL (gated deploy): CLASS — UNBOOKED FILL's closing design — the day's dominant cost (276.8 min of halts) dies; both replays fail-before/pass-after per the harness law
Class cited first (CLASS_LEDGER.md): a fill arrives that nothing books — 13 tickers July 10, 5 halts, 276.8 min conception downtime; parents TUPMAK 07-09 (cancel success=false → replacement placed anyway) + GHASPI recurrences. **Part 1 — the healer:** an audit `no_exit` leg OUR OWN ORDER LINEAGE explains is BOOKED, never halted on — routed through the proven `_v4_reconcile_naked` path (books the entry via `_book_v4_entry_fill`, posts the native band exit — existing policy, §0A unchanged), stamped `fill_booked_reconcile {lineage}` + flag `unbooked_fill_booked`; the halt REMAINS for genuinely unexplained holdings (no lineage → failure stands, proven in replay 1b). **Part 2 — the race dead at the source:** `_cancel_entry_and_resolve` returning "cancelled" on a FAILED cancel with the order still live was the hole (TUPMAK: poll saw fill 0, the fill landed 6 s later, the replacement became `conception_on_owned`); now → "unresolved" + `cancel_failed_hold` = held-inventory-until-reconciled; the caller never places. **Part 3 — replay-harness law satisfied: 4/4** (GHASPI booked-no-halt · no-lineage halt preserved · TUPMAK replacement aborted · confirmed-cancel byte-identical); fail-before = the recorded tape itself plus one accidental old-code harness run that reproduced the exact FAIL (and exposed that the audit commits artifacts even under test — three junk artifacts purged; harness runs pre-halted now). **Part 4 — the watch:** the scorecard footer prints HALT-MIN + UNBOOKED-FILLS-BOOKED nightly beside BELLS-MISSING — the KILLED claim is those at zero, or the class reopens itself. BOARD −0f and −0c fold into the class entry and strike.


### 2026-07-10 (evening) · RULING_PAIR_ECONOMICS + THE MIGRATION DOCTRINE recorded (doc-only): pair-97 RETIRED from all logic — the contradiction is on the record
Operator verbatim in `.claude/rulings/RULING_PAIR_ECONOMICS.md` (three rulings): pair-97 is NOT guaranteed profit in an exit-first machine (the winner's exit fills on the way up, the pair dissolves, the loser rides to zero — Exhibit 1 realized it: +$0.25 winner band, −$3.70 loser ride); it is not even a catastrophe floor in a binary market; and the legacy bot migrates to the OS **organ by organ** (fitted spec → build → shadow → nightly replay grade → cutover on the operator's word → legacy organ DELETED). **Pair-97: retired as logic; interim role = dumb safety bound until the per-leg replacement proves itself in shadow, then deleted, not disabled. Footprint at retirement: 34.1% of the July 10 slate.** The June evidence indexed by name (registry): COMPLETION_FUNNEL (406 pairable / 72% completed / 93 strands / −$50.65) · ADVERSE_SELECTION_STRANDED (kept leg loses 65%; cross to complete or never hold the kept leg naked) · HELD_IF_NOT_CANCELLED (43% win, −$653 counterfactual; pre-start cancel stays). Replacement frame (build QUEUED, shadow-first): each leg an independent scalp priced per tick — P(exit fills before collapse)×band vs P(ride to zero)×basis; sibling completes on its own passing math or as priced insurance; TAKE-the-winner needs an operator ruling (breaks maker-only). **Executed in the same push: constraints file #11 (settlement-hold arithmetic banned from specs; gate-enforced) · CLASS_LEDGER.md founded (DOCTRINE CONFLICT — instance 1 = settlement math inside an exit-first machine · ADVERSE-SELECTION STRAND — June 93 priced, July-10 45 observed, open until the completion policy ships) · the ORGAN LEDGER opened in LIFECYCLE.md (six organs, opening states).**


### 2026-07-10 (afternoon) · C-CONVICTION-REPLAY: the brain built and acceptance-tested BEFORE the rerun (constraint #1 honored); 123 trades graded at their own ticks; pair-97's footprint counted (34%); the fire drop attributed to SLATE SIZE with numbers
**Part 0 triage:** the 1:31:59 pm halt CLEARED at 1:32:55 pm (0.9 min) — GHASPI-SPI `no_exit`, the −0f class RECURRING. Day downtime: **5 episodes, 276.8 min** (1:38–3:53 am 135.4 + 4:56–7:04 am 127.1 + three small) — all −0f-class causes; −0f is the day's dominant operational cost. **Canon shipped:** `docs/OPERATOR_CONSTRAINTS.md` (10 numbered verbatim standing orders, now on the deploy gate's grep surface — gate step [5/5] refuses if a heading vanishes) · `docs/MODEL_REGISTRY.md` (M1–M14 with lifecycle step, artifact, categories, validation, census-era admissibility; **five NAMED gaps G1–G5** — including the honest one: we own NO fitted match-winner model; the direction prior is market-implied and cited as such) · `docs/LIFECYCLE.md` (nine steps with checkpoint stamps + conviction anchors + trust status; goal rail; **pair-97 filed under LEGACY MECHANICS PENDING ADJUDICATION**; the REPLAY-HARNESS LAW recorded: every future defect fix replays its original tape failing-then-passing or the gate refuses it; Plex Exhibit-1 findings slot RESERVED for the relay's verbatim). **Part 3 composer** (`analysis/conviction_composer.py`): discovery prior per leg (direction, confidence, W1 shape from M1/M2/M3, citations on every number) + tick posterior that alters confidence on EVERY print and tight-book mid + NO-OPINION discipline (missing model named, never a guess, never a constant in the vacuum). **Part 3a GATE 3/3 PASS** — honesty (mains → NO-OPINION G1) · posterior (Dellien 0.63 → 0.24 over 1,191 obs — the collapse demonstrated on the SAIDEL tape) · era (ITF refuses an offered archive anchor) — and only then: **Part 4 replay, 123 trades (T-20260710-0001…0123): AGREE 114 (92.7%) · WOULD-REFUSE 7 (5.7%, rule cited per row) · NO-OPINION 2 (both ATP_MAIN, G1). pair-97 touched 42/123 (34.1%) of placement arithmetic — the largest un-fitted influence on the book.** Honest dollars (n=7, luck-flagged): all seven refusals were 2.0–4.3¢ maker-join gaps and ALL CASHED (+$5.30 forgone if refused) while priced AGREEs ran −$9.68 — the composer is honest, not yet a trader; the 2¢ refuse margin is named MIS-CALIBRATED-TIGHT, calibration rides the next replay. **−0e ATTRIBUTED WITH NUMBERS: tracked events 398→201 (slate halved) vs fires 169→104 — fires-per-event ROSE 0.42→0.52 → slate size; starvation refuted; deploy side-effect = the −0f halts (participation, not fires).** Bell coverage: 10 bell_missing since the 1:13 pm bell deploy — dead-tape matches past start (the never-wake class; a volume-floor question wearing a bell costume). Report: `.claude/conviction_20260710/CONVICTION_REPLAY_20260710.md` + REPLAY_RESULTS.json. §0A held: graded, changed nothing.


### 2026-07-10 (afternoon) · C-REALITY-BELL (gated deploy): the operator's test FAILED — recorded verbatim first — and the bot now knows a match is live without the gun, and compares its book to the MARKET, never to itself
**The failed test, verbatim as dispatched:** *"the operator's test failed — the fair-value sweep did not surface Sanchez Izquierdo–Dellien (entry 62, market 24, bought in-play, graded premarket, resting bid live into the second set until the operator cancelled it by hand). Co-fault named: the sweep specification compared the model's two fair values to each other; no comparison to market price existed anywhere in the system."* The anatomy, cited: SAIDEL-DEL walked UP into an in-play ascent (premarket_walk_capped 63→62 at 11:46:59, current 64) and filled 62 at 11:47:27 am — Kalshi clock lied 2.5h EARLY (sched 2:00 pm, match live ~11:30), TE never banked the match (no observed_starts row, not even ambiguous), the tape never hit the burst latch (13 prints/41min), and price_divergence was suppressed by its own not-yet-started clock gate. Four sources, four blind spots. **Part 1 — FALLBACK BELL (gun source 5):** the bot's own prints/min gauge — trailing-10-min rate ≥0.4/min AND (honest anchored start passed OR the clock-liar escape: a leg RISEN ≥4¢ from its window-open ref on sustained tape IS a match whatever the clock says; premarket dips FALL, the FERCER class stays excluded). Fires through `_gun_stamp` → grace, in-play buy refusal, resting cancels, persistence, and the nightly scorecard grades every fallback fire (the constants are self-auditing). Fills on belled events stamp `in_play + bell_source`. **Lane-1 A: SAIDEL replay — bell fires 11:46:40 am (rate 1.0/min, rise +4), the 11:47:08 buy REFUSED, 28s margin.** **Part 2 — REALITY INVARIANT:** every audit cycle, every held position and resting bid: basis (or bid) vs current market MID; divergence past 25¢ (config) logs `reality_divergence` → MORNING REVIEW. Bell-coverage: any exposed event past its anchored start >10 min without a bell logs `bell_missing` → zero-tolerance VIOLATION + nightly count on the scorecard footer (BELLS-MISSING). **Lane-1 B: Dellien 62 vs mid 23.5 at 12:30 pm → divergence 38.5 → FLAGS.** **Part 3 — AIM SENTINEL, root named in one line:** `_reshuffle_leg2_target`'s `return max(1, min(aim_level, goal_level))` MANUFACTURED a 1¢ "aim" whenever the fitted inputs degenerated (goal_level ≤0 when leg1_basis ≥ goal = SENTINEL LEAK; collapsed faller_anchor = DIP-HUNT DEGENERACY — both flavors real). Now: no fitted level → None → `aim_unresolved_refused` NAMED, never a placement; the ≥50 branch's twin `max(1, ...)` fixed identically. **Part 4 — CONCEPTION AT UNLOCK:** `conception_stamp` logged per leg at unlock-qualification; the monitor grades early-window buys against it (window_open never overwrites an earlier stamp); the "ungradeable" pre_conception_buy lines end. Config-explicit: fallback_bell_enabled/rate 0.4/disp 4/sustain 1 · reality_divergence_cents 25. Lane 2: $0.


### 2026-07-10 (~12:45 pm) · CASE-FILE EXHIBIT 1 (BLIZAN, both legs) + LIVE FV SWEEP + two contained no_exit halts + MORNING REVIEW standing change
**Exhibit 1 delivered for Plex** (`.claude/casefile_20260710/`: EXHIBIT1_BLIZAN.md + per-leg JSONs w/ tape slices, decision points, rule citations, render-spec). The four answers, evidence-cited: (1) the 8:15:24 "dual-FV disagreement" was the CLOCK lying, not a price feed — Kalshi start +140 min vs TE honest 8:00 am (`clock_liar`), every book-derived FV inherited a book 15 min behind the live match; (2) **the exit machinery consults FV NOWHERE** — `exit_rule_for` = (category, entry-cell) band lookup, no FV parameter exists; the 28¢ exit was cell-23+band-5, sold a leg the burst later priced 50.5 (counterfactual +$3.85 vs +$0.25 — FUCKUP-3 boundary); **FV-blind exits are the DESIGN → operator-ruling item, not a bug**; (3) BLI was pair-naked 8:18→8:28 (exit resting, pair one-sided); the 8:18:38 rebuy = sibling-completion machinery (leg1 74, goal 97→aim 23), cycle-2 INTENDED and stamped, fill raced the match-live cancel (`entry_cancel_partial`, the −0c class on its benign side); (4) settlement-vocabulary accounting: BLI c1 rode-to-0 −$3.70 (F-bucket) · ZAN c1 cashed +$0.25 · ZAN c2 rode-to-WIN +$3.85 · pair +$0.40 at combined 97=goal — net-positive only because the cycle-2 accident recaptured what the FV-blind exit gave away. **Part 2b sweep** (FV_SWEEP.json): 21 positions + 36 resting buys vs burst-FV; 2 flags >10¢ (both ALFLOO: held 78 vs FV 12.5; resting 18 vs FV 86.5 — the BLI pattern live). **INCIDENTS contained same hour: the unbooked-cycle-2-fill class — 13 distinct no_exit tickers today** (NAKYAM ×89 audits, ADDCRA ×88, TUPMAK ×72 — the bot spent much of the morning conception-halted); GHASPI-SPI/SOUBOH-SOU/ROLWEI-ROL contained by direct-API band exits (7aaba84c/6a80b5ae/cf0f3a1c), halts cleared 12:29:52 pm; **class BOARDED (-0f) with the number**. **Standing change: LIVE_STATUS gains MORNING REVIEW at the top** — overnight watch fires (deep_neg_fv class + drain violations, 12am–9am) are the first read of the AM checkpoint, never line 396.


### 2026-07-10 (midday) · CLOCK CROSS-CALIBRATION CENSUS (BOARD −1b1, read-only): no global re-stamp — a scoped exclusion; ITF's ONLY clock is the live era; self-contamination REFUTED
Full findings `.claude/census_20260710/CLOCK_CROSS_CALIBRATION.md` (+ census_out.json + script; Plex-consumable). The archive window (03-20→04-10, 2,714 matches) shares zero matches with live sources, so the DETECTOR was cross-calibrated: mid-jump re-run on live premarket_ticks vs 257 gun fires vs 33 TE joins. **Findings: (1) the archive has NO ITF rows at all, and the mid-jump detector is structurally unfit for thin ITF books (vs gun: ITF_M med −119 min, p10 −812 — it fires on premarket repricing hours early); the OS historical layer's only admissible ITF clock is the live era. (2) ATP_CHALL jump-method agrees with the gun (med −0.6 min, 68% ±30) — trust at ±30m tolerance for coarse T-relative ranges, never timing triggers (GRANULARITY LAW). (3) The weak slices get EXCLUDED, not re-stamped: fallback 76 + no_bbo_data 444 (10–25% per cat, clustering in thin books, med vol 119k vs 274k). (4) Mains stay convicted (CLOCK_AUDIT; n=2 here). (5) SELF-CONTAMINATION REFUTED: clean books agree no better (ATP_CHALL clean WORSE: ±5 15% vs ours 44%) — the morning suspect wall is structural premarket churn, pointing the CERT-NIGHT-3 forensic at slate/source starvation, not our fills.** Part 0 recorded: fire count 50→25 night-over-night, cause unattributed (BOARD −0e); the scorecard now prints a nightly FIRES-vs-SLATE footer so a starving gun is visible the morning it starves. Caveats named in the findings (tick-coverage era difference, 30s-grid reimplementation, TE n tiny).


### 2026-07-10 (~12:40 am) · HAND-RESTORE ADOPTION: the 8 restored orders were floating as manual-class (every healer + the match-live cancel skip them) — adopted into bot ownership via the fingerprint lineage before the 3:00 am wave
The operator's question answered with a citation: NO — all 8 hand-restored orders logged `manual_bid_observed` (12:17:12 am; ORPHAN-CLASS doctrine: manual = visibility only, no lifecycle, no match-live cancel, adopted-class basis on fill). Action: `order_placed` lineage lines appended to the live jsonl for each restore order (O_APPEND-safe, marked `lineage: hand_restore_adoption_20260710`) → gated restart → the boot's own `_load_order_fingerprints` + reconcile orphan pass re-adopt them BOT-OWNED (the same rebuild path every boot uses; no new machinery). **Root BOARDED (-0d): hand-restored orders must enter bot ownership AT RESTORE TIME — the restore tool writes the lineage itself, never floats an order.**

### 2026-07-10 (~12:20 am) · C-DRAIN-REPLAY aftermath: the new watch caught NINE violations on its first judged window — the transitional boot's own manifest-less drain — all restored or accounted within 15 minutes
The deploy that shipped the fix drained on the OLD code (no manifest), and the new monitor section's first judgment (12:14 am) named all nine silently-lost legs: CATDEL-DEL, MIRTAL-TAL, ROBDEC-DEC, FRISOL-SOL, NATTOM-TOM, PAVKAR-KAR, PAVKAR-PAV, PLOERC-ERC, SUPPOP-SUP. **Restoration pass (direct API, each at its exact drained price, post-only, gates checked): 7 RESTORED (CATDEL 80¢ · MIRTAL 59¢ · FRISOL 37¢ · NATTOM 85¢ · PAVKAR-KAR 57¢ · PAVKAR-PAV 41¢ · PLOERC 89¢, 5 shares each) · 2 already covered by the router (ROBDEC-DEC, SUPPOP-SUP).** The nine violations stand in LIVE_STATUS as the transitional boot's honest record until the next boot; every future drain writes the manifest and replays through the chokepoint. The watch's first catch was its own deploy — working as built.

### 2026-07-10 (~12:00 am) · C-DRAIN-REPLAY (gated deploy): the deploy drain destroyed a ruling-mandated entry bid and NOTHING noticed — restored, accounted, class closed (restart-amnesia #6), and the monitor that said "zero violations" now has the missing watch
**Owning the miss plainly: the C-STALE-ANCHOR deploy's own close-out verified PAP's resting bid at 11:00:49 pm — and that deploy's 11:14:54 pm drain then cancelled it (`order_cancelled, label: shutdown_cancel`, order `45f12259`) and the boot never re-placed it. The verification was INCOMPLETE — it read the bid before the restart that killed it. The monitor watched and reported zero violations. Both defects, not one.** THE ARC, cited: placed 11:00:49 (staircase no-trade path) → drain-cancelled 11:14:54 → post-boot, JER re-placed 11:15:54 and PAWHRU 11:15:59 (fresh evaluation reached them) but PAP fell silent — no v4_place, no skip, nothing; then a live print 11:36:55 (55¢, delta 0 vs seed) made it fresh-anchor-eligible and the router STILL never returned. Compounding: a **conception halt 11:33–11:43 pm** (TUPMAK-MAK `conception_on_owned`: the 11:24:12 move_repost cancel returned `success: false` mid-fill and the code placed the replacement anyway → held 5 + resting 5; contained by direct-API cancel of orphan `45dfd41b`, halt cleared 11:43:05) was blocking every conception meanwhile — the cancel-race-replace defect is BOARDED (-0c), not fixed tonight. **RESTORE (Part 1, 11:48:43 pm): PAP run through every gate by hand — anchor 708s ADMIT (live print, no allowance needed) · book 53/55 cell intact · combined bound 97−44=53, joined AT it · dedup/position clean → buy 5 sh @53¢ RESTING, order `91c5024d`. Pair whole again 3h11m before the 3:00 am start.** **THE CLASS FIX: `_shutdown_drain` writes a drain manifest BEFORE cancelling; boot replays every drained entry through the FULL place_order chokepoint — each ends re-placed (oid logged) / refusal NAMED (the chokepoint's own reason) / already-covered; halt-refused replays retry at halt-clear (tonight's halt would have eaten them); stale manifests (>2h) discard LOUDLY. Silent loss is structurally impossible.** **THE WATCH: the monitor gains the zero-tolerance DRAIN-REPLAY section — any order alive pre-drain that is neither filled, re-placed, nor refusal-named within 10 min post-boot renders as VIOLATION in LIVE_STATUS.** Restart-amnesia family: orders → gun → cycles → unlock-vol → tape → THE DRAIN ITSELF.

### 2026-07-10 (early) · FABLE SESSION FINDINGS recorded (doc-only; C45-cites the operator's dispatch of this date): the full-information archive is named, it is self-clocking, and the program now runs THREE start-derivations that have never been cross-calibrated
1. **The full-information historical corpus exists and is named: the G9 archive** (`data/scripts/build_g9_archive.py` → `data/historical_pull/` on the VPS): ~14,700 tennis markets / 10 months; per market: metadata JSON + sub-second trades CSV with taker side + 1-min bid/ask candlesticks; parqueted by `build_g9_parquets.py`. Companion durables per `MANIFEST.md`: `bbo_log_v4` (515M BBO rows), 1,678 tick bins, 7,489 server fills, `match_facts_v3`.
2. **The archive is self-clocking:** `build_match_facts_v3.py` derives match starts from the TAPE (mid-jump ≥3¢/30s sustained detector on BBO), NOT from Kalshi metadata — consistent with CLOCK_AUDIT's finding that Kalshi carries no per-match start (card markers only). **Fallback-tail caveat, named:** matches where the detector fell back to 80%-of-lifetime carry weak clocks; that subset is uncounted.
3. **Three coexisting start-derivations now span the program:** match_facts_v3 mid-jump (archive era) · gun volume-onset (live era; certification pending tonight's 6:10 am scorecard) · TE/ESPN honest schedule (Jul 6+). **They have never been cross-calibrated.** Queued: CLOCK CROSS-CALIBRATION CENSUS (BOARD, analysis queue) — overlap-window deltas per category, the 80%-fallback tail counted, verdict on whether archive-era T-relative price ranges need re-stamping before the OS's historical layer trusts them. Feeds the PLEX_REANCHOR path (body still owed by the relay) + the eventual aim-surface refit. Sibling queued: PHANTOM-SKIP CENSUS (amnesia participation damage sizing — what the −1.0 skip class cost in entries before C-TAPE-SEED killed it).

### 2026-07-10 (early) · C-STALE-ANCHOR-ALLOWANCE (operator THRESHOLD ruling, config-scope gated deploy): unlock-qualified ITF books anchor on prints up to 7,200s old; everything else keeps 1,800s
The ruling C-TAPE-SEED's close-out named, ruled (`.claude/rulings/RULING_STALE_ANCHOR_ALLOWANCE.md`): **events with an OPEN early-unlock (realized ≥2,500, ITF only) get an anchor freshness allowance of 7,200s; all non-unlock tickers keep 1,800s UNCHANGED; never-traded books still skip; NO BBO-mid fallback** — the discipline stands, only the allowance widens where volume already qualified the book. Build: per-ticker threshold resolution at the anchor gate (`early_unlock_anchor_age_sec: 7200` config-explicit; the 1,800s constant untouched); every buy stamps `anchor_age_sec` beside `tape_basis`; daily_ledger splits tape-basis cohorts by anchor-age band (fresh ≤1,800 vs stale-allowance) so the stale-anchor cohort's fill rate + Δaim grade against fresh before anyone trusts it. Watches: confirm-delta distribution vs anchor age · stale vs fresh cohort fill rates. Lane 2: $0 — the cohort split exists so the allowance's P&L is measured before claimed.

### 2026-07-10 (early) · C-TAPE-SEED (gated deploy `2f26cc00` + completion `e7b4e8f0`): last-trade memory boot-seeded from REST — the restart-amnesia class's FIFTH instance dies; the discipline stands, only the amnesia dies
**Completion, live-caught on the first boot:** the seed took (151/228 tickers, 10:43 pm) and read −1.0 again by 10:44 — `apply_snapshot` builds a FRESH Book on every ws (re)connect and was wiping last-trade memory (the amnesia's SECOND mechanism, pre-existing: 186 reconnects today wiped LIVE prints the same way; PAWHRU-PAW's confirm line showed `seed_price: 0`). Snapshot now replaces the BOOK, never the TAPE — last-trade fields carry across rebuilds.
The C-EARLY-UNLOCK exhibit exposed it: PAPJER unlocked at 2,895 realized shares but both legs skipped `skip_no_trade, last_trade_age_sec: -1.0` — the exchange knew PAP printed 55¢ at 10:04 pm; only the bot's since-boot memory was empty (orders → gun → cycles → unlock-vol → THE TAPE, the same lesson five times). Build: every discovery pass seeds tickers with no last-trade memory from REST `/markets/trades?limit=1` at the print's ACTUAL timestamp — `last_trade_age_sec` reads honest age, never −1.0/unknown; **`V4_LAST_TRADE_MAX_AGE_SEC = 1800` gates UNCHANGED against the honest age; NO BBO-mid fallback introduced** (a >30-min print still skips — that becomes an operator threshold ruling, never a code fix). Live prints overwrite seeds immediately (basis `rest_seeded` → `ws_live`; one-time `tape_seed_live_confirm` grades seed-vs-live agreement). Scope: anchor resolution ONLY — volume/gun detectors never fed a seeded print. Cohorts: buys stamp `tape_basis`, fills inherit, daily_ledger renders rest_seeded vs ws_live separately. No-trade tickers (the JER class) seed nothing and keep skipping honestly. Watches: tape_seeded counts/pass · confirm-delta distribution · cohort fill rates · honest ages on every skip line. Lane 2: $0.

### 2026-07-09 (late night) · C-EARLY-UNLOCK (operator ruling, gated deploy): realized volume ≥ the staged floor unlocks the FULL T−8h window — ITF only, self-measuring cohort
Operator, verbatim (`.claude/rulings/RULING_EARLY_UNLOCK.md`, on the PAPJER exhibit — 2,866 realized shares untradeable in the silent T−8h→T−4h corridor): *"with this particular game, it clearly crosses the volume floor we had set no? therefore, its tradeable. we are taking advantage of the full 8 hour window to scheduled start if the volume meets standards."* Build: at the v4 event gate (the silent T−240 return), ITF_M/ITF_W events whose **realized LIFETIME contract volume ≥ 2,500** (Kalshi REST `volume_fp` summed per event each discovery cycle = `kalshi_rest_lifetime`; `ws_contracts_since_boot` the NAMED fallback, never silent) get the placement edge lifted to the conception horizon (T−8h honest) — never past it; the horizon chokepoint still owns beyond-8h. **REALIZED only, never projected — P1b stands** (volume that already happened is not a projection; the unlock and the projectability ban are compatible by construction). Mains/CHALL byte-identical. Everything downstream unchanged (FV, cells, plays, sizing, all protections — the unlock moves ONE comparison bound). **Self-measuring:** `early_unlock_open` once/event/boot; buys placed under the unlock stamp `early_unlock + unlock_vol + unlock_basis` at the single _log emitter, fills inherit, standard-window entries never stamped (mark clears at T−240); daily_ledger renders the cohort separately so the entry-table refit grades early vs standard honestly. Named limitation: fill-stamp memory is in-session (placement lines keep the stamp; offline joins recover exactly). Config-explicit: `early_unlock_enabled: true, early_unlock_floor: 2500`. Watches: early_unlock entries/night · fill rate + Δaim vs standard · basis distribution · any mains/CHALL unlock = defect. Lane 2: $0 — the cohort exists so early-entry P&L is graded before it is ever claimed.

### 2026-07-09 (late night) · Plex's verbatim T3/T4 ruling body LANDED — the reserved slot in `PLEX_T3_T4_RATIFICATION.md` filled word-for-word (doc-only); the relay's debt cleared; recorded content confirmed accurate against it.

### 2026-07-09 (late night) · C-T4-DUAL-FLAG (gated deploy, shadow/logging only): Plex's binding condition instrumented before the ITF wave — the floor reading was structurally DEAD until tonight
Pre-instrument truth, named: the live `hold_review` lines carried both flag FIELDS but the caller passed `expected_share_by_now=None` — `floor_miss_flag` was a constant False (a silently dead reading; divergence invisible, exactly what Plex's condition forbids) and the quiet baseline was a running median of ALL samples, not the leg's own T−8h→T−4h span. **Instrumented (zero order-path/decision-core behavior; os_active untouched):** (1) per-event realized **contract** volume accumulated from ws trades — the floor reading now unit-matches the staged 2.5k floor, basis named `ws_contracts_since_boot` on every line; (2) the quiet baseline banked ONLY from the leg's own T−8h→T−4h samples (a running median would let recent quiet drag the baseline toward itself), value logged on every line; (3) shadow-default per-cat expected-share curve (coarse, sourced to the 07-07 HOURLY_APPENDIX shape, refit at the threshold pass — never a ruling) so the floor projection evaluates; (4) `floor_qual` state on every line — `qualified_now / on_pace / below_pace / unevaluable`, never silent; (5) **the fence:** lines without `t4: dual_flag_v1` are `pre_instrument` — counted visibly in the rollup, EXCLUDED from Plex's threshold dataset (300 such lines existed today pre-deploy). Plex's stated reason lives in `oslayer/holdgate.py`'s comment: divergence between the two readings must be VISIBLE IN THE DATA, not hidden inside one number. Unit checks 4/4 including the diverging case (quiet=True ∧ floor=on_pace, both named separately). **The T4 accumulation clock starts at the first dual-flag line after this deploy** (stated in the close-out). Lane 2: $0.

### 2026-07-09 (night) · PLEX RATIFICATION RECORDED — T3 supersession + T4 hold-gate clause (verbatim body still owed by the relay)
`.claude/rulings/PLEX_T3_T4_RATIFICATION.md`, recorded BEFORE the code per the dispatch's sequencing. As relayed: **T3 supersession RATIFIED** (the riser is RETIRED; lineage stated in CLIMBSIDE_SPEC.md); **T4 hold-gate clause RATIFIED, threshold NUMBER reserved** for Plex on shadow data; dispatch cleared with the binding sequencing note. **The upgrade, stated as LAW: the hold-gate carries two jobs — anti-selection defense (the original T4 ask) and the volume floor's only enforcement point (P1b's finding).** ⚠ Flagged loudly per the contract: the paste channel DROPPED Plex's verbatim ruling text (not in the dispatch, not in git) — the ratification file reserves the word-for-word slot; the relay owes it. Board hygiene same push: −0a struck (delivered + ruled), EXTERNAL riser bounce resolved (retired, not re-armed; fv_observe continues as climb-side evidence), arm prerequisites unchanged (four bars · joint-shadow n≥30 · shadow-first).

### 2026-07-09 (night) · C-INFLIGHT-LOCK (`330266c7`, gated deploy, alone): the GORSTE same-tick race class closed at the chokepoint — os_active precondition #2 of 5 satisfied
The 11:41:42 am GORSTE mechanism, named precisely: the exchange-truth buy guard (C-BUY-POSITION-GUARD, the outer wall) `await`s two per-ticker API reads before the POST — a same-tick sibling coroutine passes the same guard on the same stale truth, and two identical buys reach the exchange 104.6 ms apart (log epochs .8943/.9989; TANCHE lineage). **The fix: ONE per-(ticker,direction) in-flight lock at the single placement chokepoint** — `place_order` acquires SYNCHRONOUSLY (no await between check and set: same-tick siblings structurally cannot both pass), the original body becomes `_place_order_unlocked` (every existing guard byte-identical) entered only inside try/finally, so every placement path (conception, DCA, repost/walk, completion, re-entry cycle-2 — the per-leg-patchwork lesson applies to locks) and every return releases by construction. **Crash-safe:** time-bound to the exchange-ack timeout (20s = 15s aiohttp + margin); a stale lock force-releases LOUDLY (`inflight_lock_forced_release`) and can never orphan a ticker. Lane 1 (VPS harness on the real wrapper, 4/4): GORSTE replay → second buy refused in-process at held_ms=105.3, named; synthetic same-tick ×2 → exactly one places; stale-lock → loud release + clean map; non-interference (sell/other-ticker unaffected). Lane 2: $0 claimed. Watches: `inflight_lock_refused` per night (rare and named), `inflight_lock_forced_release` (must stay ZERO), buy_stack stays 0. The exchange-truth guard remains the outer wall; the lock closes the 103 ms it cannot see. Proof `.claude/proof_20260709/PROOF_INFLIGHT_LOCK.md`.

### 2026-07-09 (evening) · C-BOT-ONLY-BASIS (gated deploy): the operator's basis ruling recorded + operationalized; FRAMAR was HIS manual — the shake-up worked
Operator, verbatim (full text `.claude/rulings/RULING_BOT_ONLY_BASIS.md`): *"FRAMAR is my manual. its to shake you up. just pay attention to bot only. the money that is traded on the bot is our basis.. every day at midnight its reset. thats how we gage true pnl without confusion."* Shipped in one gated deploy: **(1) the ruling file** — bot-traded money = THE basis; manual/foreign flagged and named, never blended into any P&L/grading/status read; basis resets 12:00 am ET nightly (day-boundary check: the scorecard's 6-pm grading cut is a grading window, not a P&L basis — no collision found). **(2) C47 audit fix (−2a)** — a no-cell-config ticker is `foreign_position` FLAG, never FAIL, never a conception halt, never a self-clear deadlock; the 4:07 pm FRAMAR sequence replays under the fix as flag-and-PASS (Lane-1 proof = the live boot audit over the still-held FRAMAR position); every naked-leg/exit assertion untouched for BOT legs. **(3) Containment sell `7cf55a8c` CANCELLED** direct-API, logged manual-class — the FRAMAR book is the operator's to manage; exit_checker exempt line added so the out-of-process checker doesn't page on his book. **(4) `analysis/daily_ledger.py`** — the canonical bot-only day renderer: midnight-ET anchor, per-cat rows (category law), cashed-via-exit ∥ rode-to-settlement columns (settlement vocabulary), manual/foreign a named side section when present, fee-blind caveat standing until −1a000. **(5) −1a000 spec amended** — account-snapshot Δ = bot-only P&L + manual book Δ + fees, to cents, bot-only headline; the +$17.90 anchored line is the cross-check, not the answer. Lane 2: $0 claimed (the cancel touches the operator's manual book — noted, not claimed).

### 2026-07-09 (~5:15 pm) · C-TRUTH-JOIN (`572d678a`, gated): the gun scorecard's truth-join debugged — certification night 3 unblocked; the ±3-min bar UNTOUCHED
BOARD −2 shipped, analysis-side only (live_v4/order path/oslayer byte-untouched). The four named defects, closed: **(1) truth is now FIRE-SOURCE-INDEPENDENT** — every fire, whatever fired it, grades against its match's certification-grade tape onset (trades-CSV 5-of-15 convention, unchanged), obs_starts bank fallback; the old convention gave tape truth only to te_scoreboard fires (46/50 overnight got nothing). **(2) No silent half-joins** — `tape_onset()` returns a named reason; every row grades or prints `UNJOINABLE:reason` (VONZID, an old silent half, now grades 0.5 min via the rebuilt both-leg obs_starts matcher — 8/17 bank rows join vs ~0 before). **(3) The MOCTAN day-boundary class** — the onset window now centers on the anchor hierarchy's honest datemiss-aware clock (`clock_liar.te_honest_start` / `pm_clock_shadow.honest_start` > sched > fire; census 165/173 tape@honest); a truth predating its own FRESH fire >15 min (or any join >120 min out) is **SUSPECT — named per cat, excluded from bar AND median, never averaged** (old med|Δ| 168.4m was MOCTAN's poison; clean-FRESH ITF_W now 1.7m). Suspects cannot eat bar-passers (≥15m > ±3m by construction); the 15-min threshold is operator-tunable. **(4) Miss path proven live** — synthetic drop-one-fire names the miss; tonight's `misses=[]` is TRUE (all 8 joined events fired). Overnight-50 disposition coverage: **3/50 → 50/50** (7 clean / 42 suspect named / 1 unjoinable named). Honest residual: the suspect population mixes premarket-churn joins with genuinely LATE fires — named per row for manual splitting; GRANULARITY LAW held (the 349 training-grade bells never read). A/B `.claude/gun_scorecard/TRUTH_JOIN_AB_20260709.md`, proof `.claude/proof_20260709/PROOF_TRUTH_JOIN.md`. The 6:10 am cron runs the fixed join for certification night 3.

### 2026-07-09 (4:07→4:47 pm) · FOREIGN-POSITION INCIDENT: an external taker buy halted conceptions 40 min; contained without touching the bot
A 250-share taker BUY of **KXWCGAME-26JUL09FRAMAR-MAR** (World Cup FRA–MAR, MAR yes @14¢ ≈ $35, order `a500c5e9`, 4:07:09 pm) appeared on the shared Kalshi account — **zero bot trace** (no order_placed, no fingerprint; the reconciler correctly refused adoption: `reconcile_orphan_no_cell`). C47's `no_exit` assertion then did its job too well: a held leg with no exit → conceptions HALTED — but the bot has no cell config for the market, so the halt could never self-clear. **Containment (direct API, live_v4 untouched): resting sell 250@99¢ posted (order `7cf55a8c`, cancellable) → `no_exit` and `exit_qty` pass → audit PASS, halt cleared 4:47:34 pm.** ntfy sent. Open on BOARD (−2a): operator word on whose trade this is (if not the operator's → key-security incident), and a small gated fix making no-cell-config tickers a `foreign_position` FLAG instead of a FAIL. Lesson: the audit's universe is the EXCHANGE book; any human touching the shared account halts the bot until the audit learns the foreign class.

### 2026-07-09 (~4:30 pm) · HANDOFF_FABLE.md established (session-zero read) + C50 checklist extension
`.claude/HANDOFF_FABLE.md` = the Fable seat's session-zero read (disk map / contract / live-state paragraph / first action), operator-dictated verbatim. BOARD header now points to it. **C50 extension: any close-out that changes live state, the ruling stack, or the queue updates the handoff's State paragraph in the same push.** Doc-only.

### 2026-07-09 (evening) · C-CYCLE-CAP (`bbd8cada`, gated deploy): the re-entry ruling executed — ALLOWED, capped at 2, persistent, cycle-stamped
The operator ruled on the DAALU evidence: **premarket re-entry after a cashed cycle is ALLOWED, capped at 2 cycles per leg per premarket** — the accident becomes doctrine, bounded. Built per the forensic's allowed-path shape: **per-leg cash-cycle history rebuilt at boot from the jsonl fill lineage (the fingerprint pattern's THIRD application, same boot slot as orders + gun state — all three rebuild lines assert at every boot)**; consulted at BOTH gates — the repost sweep (the 07-07 blanket session-set skip AMENDED to a count gate: cycle-2 legal, cap binds) and the place_order chokepoint (`cycle_cap_refused` — covers the router path the old guard never touched, the make-it-stick lesson); the live counter increments on COMPLETED cash only (partials exempt by construction; adopted-class excluded from the lineage); **every fill/exit/scalp row now carries `cycle: N` stamped at the single _log emitter — ledger grades never blend cycles**; the audit assertion inherits the persistent set (`post_exit_rebuy` → `cycle_cap_breach`, fails only at/beyond cap, survives restarts); `reentry_cycle_cap=2` explicit in config (no law lives as a code default); the monitor's `open_cycle2` counter stays as the standing watch. Lane-1 replay: DAALUX cycle-2 conceives LEGALLY under this build and cycle-3 dies at both gates across restarts; LIXSUN-class same-session router re-conceptions now capped too. Proof `.claude/proof_20260709/PROOF_CYCLE_CAP.md`. **DEPLOYED + VERIFIED (3:50 pm, `3c811cf` PID 2205164, gate 4/4 incl. os-import-boundary): ALL THREE lineage rebuilds asserted at the same boot — `order_fingerprints_loaded n=2738` · `gun_state_rebuilt n=148` · `cycle_history_rebuilt n=231 legs, 10 AT CAP` — ten legs already at the cap are now cycle-3-proof across restarts; boot audit PASS with the amended assertion; cycle stamps flow on the next fills; `cycle_cap_refused` 0 (nothing has attempted a third cycle yet).**

### 2026-07-09 (~3:50 pm) · OPEN-BOOK RE-ENTRY SCAN: the live book is CLEAN — zero open cycle-2 legs; the 2:54 boot minted exactly the two DAALUX legs, both already cashed; the monitor now counts the class every cycle
Exchange truth + jsonl lineage, right now (`.claude/master_20260709/DAALU_REENTRY.md` companion scan). **(1) Every open leg checked against its premarket cash history: 0 open cycle-2+ positions — nothing to hold/cap/kill; the operator's ruling space is unpressured.** (2) The prime-suspect sweep (everything conceived in the 2:54:30–3:04:30 boot window vs pre-restart history): exactly TWO cycle-2 conceptions — DAALUX-DAA @63 (2:55:33) and DAALUX-LUX @34 (2:55:51) — **both CASHED** (+$1.10 combined); no other leg in the boot window carried prior cash history. (3) **Forward-armed: the live-monitor cycle line now carries `open_cycle2=N`** (count-only; git-commit tag `[OPEN-CYCLE2 N]` when nonzero) until the re-entry ruling ships its fix. (4) Nothing cancelled — as ordered; the class is 96% winners historically and the book held none of it at scan time.

### 2026-07-09 (~3:30 pm) · DAALU RE-ENTRY FORENSIC: the no-rebuy guard never covered the router and dies at restarts — and the defect class is PROFITABLE (+$15.84/week, 22/23 legs positive); the re-entry ruling is the operator's
Evidence package `.claude/master_20260709/DAALU_REENTRY.md`. **The trace: DAALUX ran two full both-legs cycles in one premarket (cycle-1 cashed +$1.15 by 1:49 pm; cycle-2 conceived ELEVEN SECONDS after the 2:54 pm OS-deploy restart — `window_open_set` re-fired and the boot router treated the pair as fresh canvas; cashed again +$1.10).** The guard's scope, named precisely: `_session_exited` gates ONLY the sibling-repost sweep (:4321) + the audit assertion (:9427) — **the router's fresh-conception path was NEVER wired to it, and the set is in-memory so restarts wipe it (the fingerprint lesson's THIRD instance: orders → gun state → cashed history).** The audit's post_exit_rebuy fired zero times on the whole class. **The week sweep: 23 legs, 46 completed cycles, 22/23 premarket cycle-2s, TOTAL +$15.84 (fee-blind convention flagged), 22/23 legs NET POSITIVE — both mechanisms live (restart-wipe: DAALUX/PLADIG; same-session router re-conception hours apart, no restart needed: LIXSUN/MATKOM/DAMARN — the BOUHAR day's engine ran its winning swing twice).** Reported as ordered: doctrine violation ≠ loss — **the bot discovered premarket re-scalping by accident.** Fix shapes filed for BOTH rulings (forbidden → persistent cash-history at repost+router gates, fingerprint pattern; allowed → the same persistence as a cycle counter with ledger cycle-stamps). NOTHING ships; the operator rules on whether accident becomes doctrine.

### 2026-07-09 (evening) · THE OS BUILD (`1a187f77`, gated deploy): the consumption layer ships — shadow-first, order-path-pure by gate assertion, unable to trade twice over
The lifecycle's steps 2–5 as code (`oslayer/decision_core.py`, PURE, every rule citing its ruling: grid cell → regime → staged qualification → climb-side T−90 fitted aim-then-HOLD / decay-side join-at-close flow-gated / mains join-at-touch / stand-down-never-chase / never-marketable / ask-hold conditioning) + the Plex T4 hold-gate (`oslayer/holdgate.py`: TWO SEPARATE readings per review — quiet-flag symmetric to the ramp definition + floor-pace flag, never one merged boolean, divergence visible by construction; the threshold NUMBER reserved for Plex on shadow data). **SAME-PR non-negotiable honored: the gate's lint stage gains the `os-import-boundary` AST assertion — no oslayer module imports live_v4/network or references any order-path name, hard-fail at every future deploy (it ran in its own deploy's gate).** Shadow operation: `os_shadow` jsonl beside every real placement/repost/walk/hold decision with the FULL input vector per line (#12 satisfied by construction; dedup 300s); nightly rollup renders agreement/divergence/hold-flag-divergence; **cap-sensitivity DEFERRED until joint-shadow n≥30 (operator's order)**. **Arm path DORMANT: `os_active` absent (default False), gated on coverage ruling + in-flight dedup lock + certification + four bars + operator word — the OS ships unable to trade twice over.** Proof `.claude/proof_20260709/PROOF_OS_BUILD.md` (Lane 2 $0 — no order path exists to claim through). **DEPLOYED + VERIFIED (2:54 pm, PID 2177560, `24670f98`; gate printed `os-import-boundary OK` in its own run; boot audit PASS 0/0): the os_shadow stream flows — and its FIRST LINE is already the product working: FEAKOZ-FEA placed by the bot at T−135 while the OS ruled `defer` (climb-side, timing=early — the spec says the aim rests from T−90, not T−135), full input vector on the line. The divergence dataset the arm decision will be judged on has begun accumulating. Nightly rollup wired to the 6:10 am cron.**

### 2026-07-09 (~4:30 pm) · STEP 1: the lifecycle pinned · the floor roster named (139 matches) · projectability verdict STAGED (the floor lives at the HOLD gate) · climb-side spec committed with the riser-disarm accounting · execution scoreboard live
The Plex resubmission package (`.claude/master_20260709/STEP1.md` + step1.json; rulings GAME_LIFECYCLE.md + CLIMBSIDE_SPEC.md). **P0: the 8-step lifecycle pinned to the front page, every step citing its fitted source; climb-side/decay-side DEFINED, replacing fav/dog.** **P1: the sub-2.5k roster BY NAME — 139 ITF matches this week (volumes down to 57 contracts); projectability regression: early density is a WEAK ITF classifier (bal-acc 0.56–0.65, false-flag ~50% — ITF wakes late by nature) → VERDICT: the floor is NOT callable at conception; it is STAGED — placement-light, hardening with realized volume: the volume floor and the Plex T4 hold-gate are ONE mechanism.** **P2: the execution scoreboard renders (per cat/day: gap-to-low med, dip-catch %, reprice share, below-OPEN fills) and rides the nightly cron.** **P3: CLIMBSIDE_SPEC committed — T−90 resting posture at fitted levels, aim-then-HOLD (upward revision does not exist), stand-down-never-chase at onset+15, walk caps per Plex T2, joint-shadow n≥30 before any cap read (operator), fv_observe lineage stated (staged 491/43–76 vs current series, never blended); EXPLICIT supersession clause over the riser package — the disarm stands until Plex re-rules on THIS document, with the Jul-6 walks-ate-60% erosion mapped mechanism-by-mechanism against the new posture.** **P4: the hold-gate clause folded into the early-window doctrine — placement gate ≠ hold gate; quiet-threshold symmetric to the ramp definition; sustained fall below own baseline → hold-review, not silent rest; shadow logs both branches from the OS build; the number is Plex's once shadow shows where dumps cluster.** BOARD: OS build queues behind this close-out WITH the import-boundary CI assertion (Plex T1, accepted).

### 2026-07-09 (~3:20 pm) · THE RECONCILIATION: +$502.59 RETRACTED as accounting · the settlements `revenue` field is DEAD and prior realized lines parsed ghosts · the one solid line is +$17.90 anchored · the floor ruling survives on parse-robust structure
Full doc `.claude/master_20260709/RECONCILIATION.md` + reconcile.json. **The anchors, stated first: NO clean pre-Jul-8 anchor exists (first clean snapshot since Feb = Jul-8 16:01Z, $912.61) — the week-scale identity is unreconcilable BY CONSTRUCTION.** The solid line: **$912.61 → $930.51 = +$17.90 total portfolio over the anchored ~25h** (snapshot vs live read, no convention needed). **Convictions: (1) the settlements API `revenue` field is DEAD (0 always) and costs live in `*_dollars` keys — every prior "+$3.87 realized" line (red-morning realized, the vocabulary entry's residue column) parsed ghosts and is RETRACTED as dollars (the red-morning inventory CONCLUSION survives on snapshot evidence alone); (2) the sell/no cash-booking convention is underdetermined — three parsings bracket residuals +$406/−$1,388/−$138, none closes to $1: NAMED, not smoothed; QUEUED: empirical convention derivation against the hourly snapshots (exists since Jul-8 — every future window then closes to cents); (3) fees are REAL (~$144/week on fills) and jsonl pnl is fee-blind by documented design (P0 #5) — cent-claims on it are structurally invalid; (4) the +$502.59 exit/F table stands ONLY as the relative shape (exits carry, rides burn), never as dollars.** **The never-wake floor ruling: DIRECTIONALLY SURVIVES — parse-independent counts stand (never-wake third, 16-vs-5 unsweepable, 30%-vs-19% rides) and the margin gradient is robust to parsing (6k+ earns 1.7–1.9× sub-floor per ticker under BOTH conventions); the "$77/week" figure is retracted; the ruling basis is restated on the robust structure and need not wait for the convention pass.** Manual book $0.00 in-window; deploy footnote: logging-only, not a driver.

### 2026-07-09 (~2:45 pm) · NEVER-WAKE BLEED + EXIT RE-ATTRIBUTION (week scale): a +$1.4k/week exit engine fighting a −$0.9k/week F-bucket — and the ITF sub-2.5k floor is worth ≈ +$77/week plus the unsweepable class
Full doc `.claude/master_20260709/NEVERWAKE.md` + neverwake.json (Jul-2 → cut 1:06 pm; mold fenced per leg, ~50% of week legs mold-era, flagged). **(1) The vocabulary law at week scale: CASHED-VIA-EXIT +$1,400.71 (ITF_M +810 = 58% of all exit cash; ITF 77%) ∥ F-BUCKET −$898.12 — net +$502.59. The lever is the F-bucket's SIZE (rides), not exit efficiency.** **(2) The never-wake verdict in dollars: sub-2.5k ITF = 385 legs, realized NET −$76.66 (exits +$100 vs F-bucket −$177); the gradient is monotone (−$77 → −$45 → +$42 across bands — profitability begins where the book wakes); rode-share 30% sub-floor vs 19% at 6k+; ITF_W sub-floor carries 16 band-touched-NO-BUYER events (the tape reached our band and nobody swept — the dead book can't pay you even when you're right) vs 5 at 6k+ on 2.2× the legs. Anti-selection (~20% only-print fills) is band-UNIFORM — the aim program's problem, not the filter's.** **Filter proposal PRICED for the ruling: ITF gate ≥2,500 projected W1 contracts + gauge-OPEN confirm — saves ~$77/week realized + retires 42 open sub-floor legs + kills the unsweepable door; forfeits nothing that nets positive; the 2.5–6k band (−$45/week as traded) is the operator's judgment row; ATP_CHALL sub-floor ≈ $0 → any CHALL floor is its own ruling (category law).**

### 2026-07-09 (~2:15 pm) · SETTLEMENT VOCABULARY (operator ruling → FOUNDATIONS) — and the EXTENSIVE Part 1 corrected by it in real time: the exits DID pay us back (+$163.41), settlements measured the failure residue (+$3.87)
The vocabulary law lands (front page + ruling verbatim): settling = the game ending (meaningless); a SETTLED POSITION = the F-bucket, an exit that never filled; the doctrine lifecycle is flat-before-resolution; **cashed-via-exit ∥ rode-to-settlement as separate columns in every realized table, forever.** **Applied immediately as the correction it was written to force: the EXTENSIVE's respawn→cut recovery attribution re-read with the exit column — CASHED VIA EXIT +$163.41 across 323 exit fills (ITF_M +69.85 / ITF_W +63.10 / ATP_CHALL +20.77 / WTA_CHALL +8.54 / WTA_MAIN +1.15) vs RODE-TO-SETTLEMENT residue +$3.87. The recovery = exit cash + mark recovery; "nobody paid us back" is RETRACTED — the bot's own ITF exit machine paid us back, which is exactly what the doctrine says health looks like.** RETIRED row filed for settled-as-neutral. The Part-1 hourly table stands as the F-bucket attribution (its correct meaning under the new vocabulary); the two-column re-render rides tonight's ledger.

### 2026-07-09 (~1:45 pm) · THE EXTENSIVE: nobody paid us back (the climb was marks, proven on settlements) · the ITF volume floor exists (~2.5k: a third never wake) · the heavy fav's bottom is a 3-MINUTE event that IS the volume onset
Full doc `.claude/master_20260709/EXTENSIVE.md` + extensive.json (cut 12:37 pm; mold fenced per leg, gross+clean). **(1) P&L attribution, respawn→cut on exchange settlements: total realized +$3.87 in three rows; bot mains $0.00, MANUAL book $0.00 in-window — the recovery climb was OPEN-MARK + inventory cycling, no settlement engine anywhere; the mains hypothesis answered: the account didn't ride mains, the book marked back.** **(2) ITF tradability: below ~2,500 W1 contracts, 36-37% of ITF matches NEVER wake and produce ZERO dip-fill events (structurally untradeable — anti-selection is their entire fill mechanism); ≥6k volume: 92-97% wake. Filter proposal: per-cell volume gate at ~2.5k hard floor + gauge-OPEN micro-confirm; excluded-population would-have P&L lands at tonight's bell refresh (join was empty — universe lag, named).** **(3) Favorite timing, the climb anatomy (n=535 heavy legs): the ITF heavy leg's bottom DWELLS ~3 MINUTES at median (p75 28-36); the dip STARTS 11-15 min before volume onset (the wake IS the dip); the ascent owns the final ~45 min; missing the window costs med 17-21¢ (p75 32-33) — the whole edge plus the climb, which a dip fill instead carries as W2 cushion + the chain's +6-9pts of band-reach. THE SPEC: heavy-cell aims RESTING from T−90 (the bid must pre-exist the 3-minute window — placing on the signal is late); onset ±15 min = the hold; no fill by onset+15 → stand down to join-at-close, never chase the ascent. ATP_CHALL differs (dwell p50 160 min — a soggy bottom, workable patiently): category law.** Consumption postures now fully specified corpus-side; arm gates unchanged; everything awaits rulings.

### 2026-07-09 (~1:10 pm) · CHAIN PROOF: a third of "exit never came" is the entry gap · the +121min claim KILLED by its own decomposition (a feed-defect signature) · the 0/500 counter was BROKEN and is fixed
Full doc `.claude/master_20260709/CHAIN_PROOF.md` + chain_proof.json. **(A) The entry→exit chain, asymmetric-achievable convention (depth cells at corpus-median dip bounded by their own tape; JOIN cells at close; band offsets untouched — 0A): band-reach rises +6–9 points at achievable entries (ITF_M 73.5→82.1%, ITF_W 79.4→85.0%); 12/34 of the band-never-printed class CONVERTS on the entry side alone (the other 22 are the pair-selection frontier — unreachable even from perfect fills); the rot curve: median band-touch 26–36 min after fill, ~half by 1h, plateau by 4h — an entry unpaid within its first hour has spent most of its odds.** **(B) The +121min "ITF schedules lie" headline does NOT survive the category-law decomposition: the lag is ~CONSTANT (120.5–125 p50) at EVERY hour of day in both ITF cats — a timezone/source-offset signature in the schedule feed's ITF lane, not court queuing (CHALL's +13–63 heterogeneous = plausibly real); 124/145 events liar-flagged = the same systemic disagreement; tournament/certified joins unavailable (0/33 observed_starts join schedule rows — named pipeline gap). Queued: ITF schedule-source tz audit + certified-join fix. No blanket claim stands.** **(C) The 0/500 coverage counter traced end-to-end: collected ✓ belled ✓ binned ✓ counted ✗ — `bell_src` was COMPUTED but NEVER SERIALIZED (all 64,644 samples '<none>' → n_honest ≡ 0, structural at every prior weight). Fixed `739ec32d` (collector one-liner, VPS synced, cron 4:45 am); re-forecast: the bottleneck moves to a RULING — which bell_src counts as honest at which prior weight; three-weight coverage table to the operator after 2–3 stamped nights.**

### 2026-07-09 (~12:15 pm) · MASTER PLAN: the GORSTE same-tick race contained by surgery (13 min halted) · the anchored era's first clean cohort (1.4% W2 contamination) · ITF schedules lie by ~2 HOURS · the exit machinery is airtight ($0.36 total counterfactual on 288 legs) · the red morning is INVENTORY, not bleed
Full doc `.claude/master_20260709/MASTER_20260709.md` + RAW_TABLES.md (288-leg cohort, per-cat, category law enforced) + cohort.json. **Triage: the 11:42 halt was a REAL buy stack — GORSTE-GOR ×2 @62, placed 103 ms apart (same-tick race, TANCHE lineage — the exchange-truth guard cannot see in-flight orders); direct-API surgery 11:51:35 → re-audit PASS 11:54:46, halt cleared; queued: in-flight dedup lock at the chokepoint. The 8:12/10:22 halts were no_exit transients, healed as designed.** **Entry grading (clean cohort since 07-08 midnight): W1/corridor/W2 = 209/75/4 — in-play contamination 1.4%, the anchored clock's first clean read; gaps to own W1 lows med ITF_M 5 / ITF_W 8 / CHALL 4¢ (corridor lows sit ON our fills: 0–4¢); reprice still ~2:1 over divot in ITF; THE CERTIFICATION DATA: ITF matches go live a median +121 MINUTES after their scheduled start (n=115 dual-stamped legs; CHALL +23, WTA_CHALL +5) — the scheduled start is an optimistic fiction in ITF, the gun-actual is the only honest boundary.** **Exit reachability: across 288 legs exactly ONE reachable exit failed to take (+$0.36 counterfactual) — actual ≈ could-have; the 38 band-never-printed legs are the entry problem in an exit costume (0A held); 68–80% of exits FILLED, skewing W1/corridor.** **Red morning DECOMPOSED, not assumed: realized today +$3.87 across 231 settlements — the red-at-wake is ~$68 of cash→INVENTORY conversion into today's own morning-wave conceptions (22 legs/$57 vs overnight-Euro 1 leg/$2) — mark timing, confirmed; the climb-back is the slate settling.** Fold-ins: MACRO RAMP still 0/500 BLOCKED-ON-DATA; **gun certification NOT achieved night 2 — 50 fires but only 1 truth-graded FRESH at a suspect |Δ|=102 min: the scorecard's truth-join needs a debug pass (queued), the bar rolls forward**; four-depth gauge re-proposal awaits the operator (no config deploy).

### 2026-07-09 (~3:25 am) · FILL REDO: the operator's correction vindicated both ways — mains is a JOIN market (40–50% at the touch at volume), the mains DRIFT-PATH thesis is refuted (the touch moves 1¢ in 8 hours), and the CATEGORY LAW + DEPTH-RELATIVE AMENDMENT go on FOUNDATIONS
Spread-relative redo of the fill grid + the traverse, 2,448 pairs (`.claude/fillredo_20260709/FILL_REDO.md` + GRID_TABLES.md raw + aggregate + script). **(1) The corrected grid: mains JOIN at V3 = 40–50% fill within 30 min (ttf ~15m) — the previous ~1% was DISCOUNT SCARCITY (a 3¢ ruler in a 1¢ world), exactly as the operator called it; but mains CLIFFS below the touch (any depth → ≤8%) while ITF's ladder degrades gracefully (67→55→52→42% at V3 down to 2×spread) — a JOIN market vs a DEPTH market, structural. Dead books stay dead at every depth incl. the touch (V0 JOIN 3–7%).** **(2) The traverse: the mains touch-path is 1¢ range / 0 net / 2¢ path-vol over the WHOLE 8h window — the '1¢ × 10k × 8h drift' thesis fails at the drift factor (no path exists); the strict patient-join ratchet (touch−1, downward-only) captures ~nothing in ANY cat (fills med and p75 = 0 — it undercuts falling books and is pierced only by knives); ITF's fat-divot-at-depth confirmed side-by-side (heavy-zone path 45¢ range/+29 net — the June winner-up shape at scale — with depth fills 33–52% inside volume windows).** **(3) Verdicts: TWO REGIMES exist — mains join-at-touch thin-edge high-frequency (depth forbidden) vs ITF/CHALL fat-divot-at-depth (cell-edge-scaled); postures per the category law (macro says which regime per cell, tape says when). Gauge re-fit RE-STATED: mains-OFF REVERSED-as-suspended — the gauge is meaningful in mains at the JOIN depth (5–7→12–15→40–50% across states), join-posture-only if ever ruled; ITF/CHALL shape unchanged. Open question flagged not answered: mains join FILL probability ≠ PROFITABILITY (adverse selection at 1¢ is the whole game; exit-side, 0A-sealed).** FOUNDATIONS gains the DEPTH-RELATIVE AMENDMENT (fifth costume, dated lineage) + THE CATEGORY LAW (six flattening kills, dated; cross-cat aggregates print with decomposition or fail review). Nothing ships; the gauge config deploy waits on the operator's read of the corrected grid.

### 2026-07-09 (early, ~12:15 am) · C-VOL-LEDGER forward field + CONFIG HYGIENE (`d8d0c62f`, gated deploy) — and the volume corpus job dispatched
The operator's VOLUME LEDGER + FLOW CALIBRATION build, part 1 of 2. **Deploy: `gun_fired` gains `vol_prints_30m`** (trailing-30min prints across both legs from the in-process deques — the honest in-process number; the cumulative W1 ledger lives collector-side from the trades CSVs, which capture live forward by construction); scorecard gains the vol30@fire column. **Config hygiene rides the same restart (the drift list's own prescription): DRIFT-1 `exit_band_resolution` removed · DRIFT-2 `conception_horizon_hours=8` EXPLICIT (no law lives as a code default) · DRIFT-6 `per_match_clock_shadow` retired + two dead keys removed. 83→81 keys; every delta logging-only or value-identical (proof).** **Part 2 running detached (`/root/volume_ledger.py`): per-match W1 volume ledger (corpus backfill) + the two operator hypotheses — fill-rate|volume (does a dead book EVER pay a patient bid; per cat × cell-zone × trailing-30m print band) and spread stubbornness (lattice convergence vs participation) — plus recut_cells volume augmentation (thin-tape flags). Deliverable: QUIET/WAKING/OPEN re-fitted as FILL-PROBABILITY states per cat, findings only — the gauge threshold change ships as its own gated config deploy after operator read (granularity law: macro fits, micro reads).** **FINDINGS LANDED (~1:05 am, `.claude/volume_20260709/VOLUME_LEDGER.md` + aggregate + recut_cells_volume.json): (2a) a dead book NEVER pays a patient bid — at 0 prints/30m there is no fillable reference at all; fill probability ~DOUBLES per volume band in ITF (<50 zone: 16→31→50%), CHALL inflects only at ≥16 prints/30m, and MAINS are FLAT ~1% at EVERY volume state — the par-lock is participation-independent (the pass's cleanest kill). (2b) the dead ITF lattice is a 6¢ OPINION tightening only 20%/15min; convergence is monotone (6→4→3→2¢ with participation); CHALL pre-converged at 2¢; mains never had anything to converge. Volume ledger: per-match W1 volume med ~6k ITF/CHALL, 9-11k mains (p10-p90 1k-47k — a per-match variable, never a cat constant); 538 recut cells volume-stamped, only 2 thin-tape. THE RE-FIT (proposed, ships ONLY on operator read): ITF QUIET=0 prints/30m / WAKING=1-5 / OPEN=≥6 (≥0.2/min, fill 20-50%); CHALL quiet extends through V1, OPEN=≥16/30m; mains gauge OFF (confirmed on fill probability itself). The provisional WAKING line (0.2/min) is actually the ITF OPEN line — fitted, not decreed.**

### 2026-07-08 (late, ~11:25 pm) · WAL PASS (board #19) — and the honest discovery: tennis.db was ALREADY WAL; tonight's starvation was a HUNG WRITE TRANSACTION, a class WAL cannot fix
Ops pass, no bot restart (pre-flight CONFIRMED zero live_v4 handles on tennis.db — the no-restart claim proven, not assumed). **The discovery: `PRAGMA journal_mode` read 'wal' BEFORE any flip — the db has been WAL all along; the #19 "rollback-journal contention" theory is STRUCK.** What tonight's 3h feed starvation actually was: **tennis_odds holding a hung WRITE transaction — writers serialize in WAL too; no journal mode fixes a writer that never commits.** The cure stack, verified live: `synchronous=NORMAL` set (new) · busy_timeout=30s (the belt, shipped earlier) · **verify replay: reader-vs-writer 0.05s (never blocks), writer-vs-writer 4.05s clean wait via busy_timeout** — and the hang class is owned by the watches (te_live keepalive+notify, exit-checker feed-staleness, both live) not by PRAGMA. **wal_watch cron armed (hourly: sidecar >2G → checkpoint TRUNCATE + channel notify).** ENOSPC lesson honored: first run self-ABORTED at 6.1G free; freed to 10G (smoke_env 2.1G dropped — the gate rebuilds it fresh every deploy; 254 idle tick CSVs + the 144MB yesterday-jsonl gzipped; disk 88%→79%); 16GB full-copy backup REFUSED per the lesson — small tables banked instead (observed_starts 20 / live_scores 19,755 / players 612 / bookmaker_odds 120,453 rows → /root/tennis_small_tables_backup_20260708.db); quick_check ok. Collectors quiesced and resumed (tennis_odds needed a second start — caught and fixed; all holders re-attached; banking verified post-flip). **#19 re-framed on BOARD: the real residual is the writer-hang class → per-collector db split remains the structural cure if it recurs; the watches are the tripwire.**

### 2026-07-08 (night) · THE GRANULARITY LAW (operator ruling → FOUNDATIONS): macro assumes, micro confirms — either alone is a failed entry
Front page gains the law (verbatim in `.claude/rulings/RULING_GRANULARITY_LAW.md`), completing §0A's two-element frame: the per-minute historics are the MACRO instrument (cell edges, floors, timing distributions — the assumptions; they can never time an entry); the sub-second live tape is the MICRO instrument (three observables, non-self chain, ask-hold/divot conditioning, flow state — the validation and the trigger). Every entry = macro × micro; HUIAHN (micro ignored) and the reprice-chase (macro ignored) are the two named failure poles. Binding corollaries: no per-minute-derived number is ever a timing trigger (bell-recovery's training/certification split = the law applied, queued #18); no live-tick pattern overrides a fitted cell edge without the refitting study. Companion to THE COMBINED-PRICE CLAUSE: the clause fixes WHAT to grade against, the law fixes WHICH INSTRUMENT answers which question.

### 2026-07-08 (night) · THE COMBINED-PRICE CLAUSE (operator ruling → FOUNDATIONS): no fixed number defines a good entry — the floor is fitted per pair, the combined is the receipt
Front page gains the clause (ruling verbatim in `.claude/rulings/RULING_COMBINED_PRICE_CLAUSE.md`); supersedes EVERY prior 97-framing. Dynamic floor = Σ(own W1-close − edge_p50(cat,cell)) per pair; 97 retires to exactly two roles (interim goal-law bound — a countdown, never an achievement — then emergency ceiling); 100/par law unchanged; progress = combined-vs-floor gap + per-leg dip-catch. **CONTAMINATION RULE armed: no fixed-line rate headlines anywhere without the dynamic-floor gap beside it; renderers inherit (position_grade live, DRIFT-5 adopts, future surfaces refused at review).** RETIRED row filed for fixed-line combined grading — the fourth and final demotion of the 97 number (goal → S-line → per-leg → receipt).

### 2026-07-08 (conformance, ~10:00 pm) · FULL AUDIT pulled forward: 8 deploys' tape columns re-run, every new law live-proven, ONE archaic-ARMED flag, six drifts queued
Board #4 executed a week early (`.claude/CONFORMANCE_20260708.md`; the volume justified it: 8 gated deploys, 3 rulings, 2 new surfaces since 07-07). **Config-vs-code: walk-cap staged values = the Plex ruling EXACTLY; 2 orphaned keys; `conception_horizon_hours` found living as a CODE DEFAULT (a law without a config line — DRIFT-2); dead-flag verdicts filed (per_match_clock_shadow = the sweep's one archaic-ARMED find, retire; fv_anchor/round5 remove; scoot tombstone; riser awaits Plex).** Code-vs-tables: recut_cells has NO bot consumer BY DESIGN (the ruling armed the key, not the ramp); **the 97-basis census CURRENT: 93/99 of today's sibling placements (94%, up from the era's 77%) price at exactly combined_goal−basis — it is the GOAL LAW on every branch, not drift; under the provenance clause it is the S-blocker by construction, and it re-prints nightly until the cell-aim ramp arms (DRIFT-3).** Rulings-vs-renderers: dynamic-S verbatim in the day-close renderer ✓, provenance enforced live (EBEDUP demoted) ✓, anchor_source on scalps only (entry/exit stamps owed — DRIFT-4), old SLATE cross-tab not yet GRID-aware (DRIFT-5). Tape columns: all 23 prior rows + 7 new-law rows fire on today's jsonl; **`never_marketable_clamped` FIRED (PLADIG 8:00:59 pm) — a standing UNPROVEN-LIVE row closes**; tripwire file checked ABSENT (the 07-07 gap closes); 410s zero; C50 strict on all 6 deploys. Six drifts = the follow-up queue; one config-hygiene deploy takes three of them.

### 2026-07-08 (day close, ~9:35 pm) · POSITION GRADE — first full cut on everything ratified today: S=0 printed honestly, and the day banks TWO BOUHAR pairs
Full grading `.claude/live_20260705/POSITION_GRADE_20260708.md` + raw json; SLATE_LEDGER carries the day-close CUT. **The headline: DAMARN (ATP_CHALL, ARN@62+DAM@35=97) and PLADIG (ITF_W, PLA@6+DIG@89=95) are genuine BOUHAR pairs — both legs W1-filled AND W1-cashed at ≤97, independent provenance, clean anchors — against an era baseline of 2/357. One day matched the era's whole count (n=1-day, luck-flagged; the SHAPE is what the mandate multiplies).** S=0 under dynamic-S (refs 57–93 vs combineds 95–97: the surface demands the heavy leg's dip and today's fills paid near close); B 8; EKSLUX's would-be A STRUCK (retro-stamp — fake-W1). **Heavy-zone dip-catch, the ratified surface's first live day: 2 catches (SYDBER 4¢ miss/dyn-S +8; PLADIG-DIG 1¢ miss — the tape offered 8¢ where the corpus median promised 23) / 3 misses (GONBEN's promised 19¢ dip PRINTED at 65 and we paid 79)** — the aim gap, now measurable per leg per day. Held book: 12 legs ALL covered, by name, marks + anchored windows + liar flags per row; corridor crossings flagged TAKJAS/OCHSAM/DERMIL; YAMSHI (the watch) cycled out, not held; 0 naked. **First anchored scalp stamp printed live (ZIVMIK +4¢, anchor_source=te_honest_datemiss, liar-flagged) — HOLDING-FRESH begins exercising.** Day totals: 251 bookings = 60 clean + 191 mechanical; scalps 139 med 8¢; settled 42 named; cash $819.06 + 12 held + PM $80 banked as the day-close anchor; vs Jul-7 flagged NOT comparable (7 boots, blackout, 2 deploys).

### 2026-07-08 (night, +1) · C-GUN-PERSIST (`1a8a266e`, gated deploy): the scorecard's only PARTIAL closes — a restart can no longer forget its fired guns
Board #20, found and quantified by the fix scorecard hours earlier (7 post-fire buys on 3 LIVE events in the 7:55 pm boot minute, gun amnesia until poll re-fire). **The fingerprint pattern applied to the gun: at every boot, BEFORE the first conception/repost pass, `_gun_state` rebuilds from the jsonl `gun_fired` lineage (two most-recent files, ≤12h fires) — any previously-fired match boots FIRED, post-fire buy block + grace posture in force with no re-fire wait.** C47 gains `buy_placed_post_fire`: a resting buy whose fingerprint placement-ts postdates its event's fire FAILS the audit (pre-fire bids surviving grace stay grace's domain — no false positives on the graced-pending class). Fail-soft; no flag needed (the rebuild only adds what the poll would re-derive minutes later). Proof `.claude/proof_20260708/PROOF_GUN_PERSIST.md` (Lane 1: the 7:55 pm boot replayed dead; Lane 2 $0). **VERIFIED on the deploy's own restart (8:59 pm, `fdba24e` PID 1656151): `gun_state_rebuilt n=18` = the day's fire log EXACTLY (events named in the log line); 0 post-fire placements in the boot window (33 buys, none on fired matches); boot audit PASS with `buy_placed_post_fire` armed. FIX_SCORECARD #9 flipped PARTIAL → HOLDING in place — the night ends 9 HOLDING / 1 HOLDING-FRESH / 0 PARTIAL / 0 VIOLATED.**

### 2026-07-08 (night grading pass, ~9:00 pm) · THE FIX SCORECARD: every deployed fix vs today's tape — 8 HOLDING · 1 HOLDING-FRESH · 1 PARTIAL · 0 VIOLATED
Every fix graded against exchange truth + jsonl, claim-by-claim (`.claude/live_20260705/FIX_SCORECARD_20260708.md` + raw json). Day shape: 7 boots, 251 fill-bookings (60 clean / 191 adopted-class), taker share **0.0%** on 506 exchange buy fills. **HOLDING:** fused gun (17 events, fallbacks carried the TE-blind wave; bar open on coverage) · orphan fingerprint (23 re-adoptions, manual class 0, audit assertions 0 across 58 audits) · conception horizon (342 defers, beyond-horizon 0 post-anchor) · band clamps (59+7 refusals — vs 4 yesterday; 0 out-of-band resting NOW) · exit machinery (**10/10 held legs covered by name**, 8 ITM takes, 1 transient no_exit healed in 60s — the halt/heal cycle working) · walk guard (**free-the-leg live-proven ×8 with confirmed re-conceptions** — JOHMAL/PLADIG re-filled same evening) · C47+checker (58 audits, 57 PASS; the checker's first run caught the feed starvation) · anchor (17 liars conservative; **HOLDING-FRESH** — anchored scalp stamps not yet exercised live). **PARTIAL: gun-buy-refused — in-process airtight (0 attempts even reached the chokepoint) BUT the 7:55 pm boot minted 7 post-fire buy placements on 3 LIVE events (ISOIMA/YAMSHI/MILMIS): gun state is in-memory, a restart forgets fires until the poll re-fires (self-healed ≤ minutes by re-fire + grace-cancel) — #20 quantified, queued (the fingerprint pattern, applied to the gun).** Rubric cohort: 33/60 clean fills in the ratified JOIN zone (cell <50), 9 in the deep-aim zone (75–94, cell edges 4–23¢ quoted per row — dynamic-S grade rides the nightly renderer); EKSLUX's SECOND scalp pass (5:33/5:36 pm, "1.4h before commence" on the lying clock, pre-anchor) added to the struck set — 4 lying-clock scalp rows total. Yesterday-vs-today clean columns flagged not smoothed (build day, 7 boots — not a comparable entry night).

### 2026-07-08 (evening cut, ~8:20 pm) · SEND-ORDER #6: the wave caught by fallbacks as designed; the ±3-min bar UNEVALUABLE (feed coverage + a 3h lock starvation, both named, one fixed); repost watch CLEAN
Full cut `.claude/live_20260705/EVENING_CUT_20260708.md`. **Gun proof table (Part 3): 37 fires, 17 graded events — 13 FRESH / 4 CATCH-UP; the evening US ITF wave was caught 100% by tape_latch/price_divergence (the priority order's fallbacks doing their job) — but feed-covered FRESH = 0, so the pre-registered ±3-min bar cannot pass or fail tonight.** Causes named, not smoothed: (a) **TE /live/ does not carry the US-evening ITF wave** (6 matches listed at 8:04 pm, all the Asian afternoon wave + tour) — a coverage bound; (b) **the observed_starts bank starved on a stuck tennis_odds write transaction for ~3h (5:19–8:04 pm)** — queued #19 biting live; contained same hour (tennis_odds bounced, bank verified working 8:05 pm, te_live busy_timeout=30s shipped `226a4d13`). **The fused-gun close-out stays OPEN on Part 3a — certification expected on the Euro-morning wave (TE's strong zone) via the 6:10 am scorecard.** **Repost watch (7–9 pm): CLEAN** — JANFUN/BEKPAN/JUHKLO/VANSEL entered the window quiet (defer 1 each, 0 refusals, no churn; steady audit PASS 0/0, conception_beyond_horizon 0 conservative); **MILMIS superseded by a REAL gun fire** (tape latch FRESH 5:41 pm — its "Jul-9" schedule was wrong too; grace armed 5:41:34, completion bid cancelled GRACED 5:46:44 — the machinery working as designed on the morning drama's exact leg). Retro-stamp consumed (EKSLUX struck from W1 metrics). Cash $829.67; book 28 unsettled / 69 resting, PASS 0/0; full mark-to-market recon rides the morning dossier. **New gap queued #20: gun state is in-memory — a restart forgets fired guns until re-fire (MILMIS 7:56 pm exhibit); the fingerprint lesson, applied to the gun.**

### 2026-07-08 (late night) · C-ANCHOR (`1eeebc7b`, gated deploy): the lying-clock fix — the anchor class closes (board #14)
Two live exhibits demanded it: **GILOBR lied SHORT** (horizon counting down to a knife) · **EKSLUX lied LONG** (7:00 pm claimed vs true 3:50 pm; in-play knife scalps ledgered as W1) · tonight's **5 sibling-repost completions re-placed beyond T-8h through a blind chokepoint**. **(1) ANCHOR HIERARCHY, explicit + stamped (`anchor_source`): gun_truth > te_honest > kalshi_schedule LAST — never alone when TE coverage exists.** (2) **The pm-honest-matcher MISS class fixed:** `_date_ok`'s >12h rejection was discarding the ONLY true row on lying-ticker-date events; nothing-clean-matched now returns the nearest date-rejected row within **36h** (the C-KALSHI-OCC guard bound), marked `date_mismatch`, method `datemiss_36h` — the 5 reposts' exact hole. (3) **CLOCK_LIAR detector:** kalshi-vs-te disagreement ≥60 min, or a gun fire while a schedule clock claims premarket >60 min → `clock_liar` logged once/event; **every tts gate on a flagged match uses the CONSERVATIVE side** (horizon: the NEARER start — refusing a held leg's completion on a lying-far clock re-starves the pair, tonight's own lesson; a fired gun = live NOW, tts≤0, the gun buy-refusal owns the block). (4) **Anchored window stamps:** scalp classification runs on the hierarchy (a fired gun is never a scalp) and carries `anchor_source` + `clock_liar` — **EKSLUX-class fake-W1 lifecycles can no longer grade blind.** Lane-1 replay: the 5 reposts re-anchor to their true Jul-9 rows as datemiss_36h + liar-flagged + conservatively legal-and-resting (the no-sweep ruling, now principled); EKSLUX's exits classify NOT-scalp under gun_truth. Lane 2 $0. Proof `.claude/proof_20260708/PROOF_ANCHOR.md`. Config `anchor_hierarchy_enabled=true` (OFF → byte-identical). **DEPLOYED + VERIFIED same night (`0d555fb`, PID 1624563, gate 4/4, boot audit PASS 0/0): `clock_liar` named 17 events in the first 5 minutes (330–360 min disagreements — the class finally measurable); 7 datemiss_36h recoveries at first resolve (JANFUN/BEKPAN/JUHKLO + sweep-class GILOBR/TORMEL/ZIVMIK to their true Jul-9 rows; BARDON, a 26JUL09 ticker already COMPLETED this morning, recovered as completed); VANSEL/MILMIS pending their TE rows at cadence (conservative-legal either way); conception_beyond_horizon = 0 on the conservative anchors; fingerprint re-adoption held across the restart (manual class 0). RETRO-STAMP filed (`.claude/anchor_20260708/RETRO_STAMP_20260708.md`): EKSLUX's two "W1 scalps" struck to W2/IN-PLAY + mechanical-flagged lying-clock; the pair excluded from BOUHAR/W1 metrics; the 5 reposts had no W1 rows to regrade.**

### 2026-07-08 (late night) · CONSOLIDATED: DYNAMIC S + CELL-AIM RATIFIED · bell recovery measured · out-of-process exit checker LIVE · BOT_DOWN channel LIVE (ntfy)
**Ratifications committed** (`.claude/rulings/RULING_DYNAMIC_S_CELL_AIM.md`, front page updated): dynamic S = Σ(own W1-close − edge_p50(cat,cell)), constants 79/79/92 retired to footnotes — honest-era regrade S = 0 unchanged (provenance binds before price); cell-keyed AIM (cat,cell): <50 JOIN / 50–74 shallow / 75–94 deep, recut json = the prior, arm gates unchanged. **BELL RECOVERY (SEND-ORDER #3, read-only, `.claude/bellrecovery_20260708/`):** the latch-equivalent taker signature (burst ≥10/60s, two-stage confirm 60–300s) run standalone over 3,301 two-leg trades events — overlap validation vs 2,353 corpus bells: **delta med +1.3 min but only 33.9% within ±5 min** (p10 −45/p90 +33 — the standalone detector lacks the live latch's premarket suppression, so the tails are FERCER-class contaminated; 95 corpus bells it misses outright are thin tapes). **No-corpus-bell class: 853 events, 349 recovered (40.9%)** — ITF_M 152/415, ITF_W 136/293, CHALL 55/105, mains 6/40. **AIM_V2 cell-coverage delta: +694 legs across 271 cells** (gains concentrated in the extreme cells — the thin tiers). **Verdict, honest: recovered bells are TRAINING-GRADE (cell population counts), NOT certification-grade — no time-sensitive ingestion until a residual-refinement pass adds the premarket suppression** (queued as the follow-on; the fused gun covers forward-looking bells, this was the backfill). **OUT-OF-PROCESS EXIT CHECKER LIVE (SEND-ORDER #5):** `/root/exit_checker.py`, own cron every 15 min, outside the bot process — paginated exchange truth, asserts the v4 band contract (every held leg ≥1sh carries full-qty resting exits; hold-legs exempt file), violation → artifact + channel, deduped w/ 30-min re-alert, heal notice on clear; survives the bot dying (the 07-08 blackout is its reason). First live run: 14 held / 0 violations — **and it immediately caught the gun-feed staleness watch it also carries (observed_starts 115 min stale; te_live alive, transient sqlite lock contention self-healed 7:12 pm; staleness likely benign set-once quiet, watch stays).** **BOT_DOWN CHANNEL LIVE (channel-agnostic, config-keyed `/root/notify_channel.conf`):** one sender `/root/notify.sh`; default = **ntfy.sh push, topic `omi-livev4-omqs-x7k3q9v2`** (operator subscribes on phone: https://ntfy.sh/omi-livev4-omqs-x7k3q9v2); SMS/email slots stubbed for later word. **Wired: watchdog (BOT_DOWN/disk/log-stale, transition-deduped + 30-min reminder, heal notice), exit checker (naked legs, feed silence), te_live keepalive (feed death/revival).** Test fire delivered 7:13 pm. The 58k-silent-BOT_DOWN-lines era ends tonight.

### 2026-07-08 (night, re-cut) · THE GRID: sequential floor re-cut per (cat, cell) — fav/dog binary RETIRED; depth is a property of the CELL
Re-cut of the seqfloor json on the current grid — **90 cells/cat ([5,95) 1¢) × 6 cats, each leg keyed by its OWN W1-close cell, no side aggregates** (`.claude/seqfloor_20260708/SEQFLOOR_RECUT.md` + RECUT_TABLES.md, 538 populated cell rows + recut_cells.json). **The fav/dog split was a proxy: edge p50 = 0 below ~cell 50 in EVERY cat (a sub-50 leg has no W1 dip below its close, whoever it is; its floor arrives T−5..−23 min, dip-first 7–45%); the crossover is ~50–55 ITF / ~80 CHALL; the heavy zone (cells 75–94) carries the depth — ITF edge p50 6–19¢ per band, ≥5¢ on 55–75%, T−36..−71 min, dip-first 68–84%.** Mains 1–2¢ flat at every cell (par-lock confirmed cell-by-cell). **Heavy-fav focus: the crater class's cells are also the grid's largest ENTRY discount — the W1-clip (med 9–19¢ below own close, ITF heavy cells) is measured to W1 close; no settlement claims (W1→W1 factor 2 untouched).** S-line restated cell-conditional: dynamic S reference = Σ legs (close − edge_p50(cat,cell)); the cat constants (79/79/92) = the corpus medians of that formula. AIM_V2 aim key = (cat, cell); aim depth = edge_p50(cat,cell): <50 → JOIN (a depth aim there aims at nothing), 50–74 shallow, 75–94 deep; the json loads directly as the aim-surface prior with per-cell timing priors. **Exit provenance VERIFIED + PINNED (closes queued #17): `ITF_EXIT_BORROW = {'ITF_M': 'ATP_CHALL', 'ITF_W': 'WTA_CHALL'}` (live_v4.py:288); every ITF row labeled borrowed.** FOUNDATIONS gains THE GRID with lineage (Apr 56-cell → VERSION B 41 → 90×6); quoting a retired grid = C45 failure. **Supersedes: the fav/dog frame in tonight's first seqfloor cut (its cat-level numbers stand; the frame does not).**

### 2026-07-08 (night) · SEQUENTIAL FLOOR: the doctrine's own number — ITF's achievable combined is 79, the edge lives on the FAV side, and the dog has no depth, only timing
2,435 detected-bell pairs, W1 T-8h→bell, prints-only convention, ENTRY ONLY (`.claude/seqfloor_20260708/SEQUENTIAL_FLOOR.md` + aggregate + script; SEND-ORDER #2). **Leg edge = W1-closing fillable − deepest fillable at ITS OWN time; sequential pair edge = fav+dog edges at independent times.** Headlines: **ITF sequential implied combined med 79 (p25 63–65) vs the simultaneous lens's 84–85 — the doctrine premium is med 4¢, 10¢+ on a quarter of ITF pairs**; pair edge ≥5¢ on 64–68% of ITF pairs (≥12¢ on 43%). **The edge is FAV-SIDE: dog edge is median 0 in every cat (the dog's cheapest moment ≈ its close, arriving med T−9..−17 min); the favorite carries the depth (med 4–6¢ ITF, p75 19–27), dipping mid-window (med T−31..−70) and dipping FIRST 2:1 — 82–86% in the 90+ fav buckets.** Richest cell: ITF_W fav-80-90 (med 15¢ fav dip, implied 77, 76% ≥5¢). The 45–65 min divot-gap doctrine number reproduces (med 41–62) and is now SIGNED: fav first. **Mains re-verdict: still par-locked (sequential opens ~1¢ at median; implied 98) — the extended liquid clock hides nothing; the 93 mains S-line is fiction on ANY lens.** ATP_CHALL softened bucket-conditionally (80-90/90+ buckets: 50% ≥5¢). **Proposed S-line refits (operator ratifies): ITF_M 79 / ITF_W 79 / ATP_CHALL 92 / WTA_CHALL hold 90 (n too small) / mains restate-or-keep-as-empty-filter.** AIM_V2 re-anchor note: the aim surface is **side-conditional** — fav-leg depth aims bucket-keyed (cat, fav-bucket, side); **a dog-side depth aim is aiming at nothing** — dog policy = join at close level, timed to the late window on flow-state; timing priors (fav T−30..−70, dog T−9..−17) ride the table. This is B3's half_timing at population scale, inverted into a recipe; the dog's floor lives exactly where the latch was blind — the fused gun + gauge are what make the late window workable. Entry-only caveats carried (nothing here claims the cash side; knife-contaminated p75 tails flagged). **Supersedes: the simultaneous joint floor as the S-line basis (PAIR-STORY's lens stays valid as the co-fillable measurement).**

### 2026-07-08 (night) · C-FUSED-GUN + C-ORPHAN-FINGERPRINT (`6d84f27e`, one build, one gated deploy): the gun becomes multi-source and the restart stops minting orphans
**Orphan fix ships first (BOARD #13 closed):** boot adoption re-claims by **FINGERPRINT, not memory** — `_load_order_fingerprints` reads the two most-recent jsonl files (the `order_placed` lineage every placement already writes at the chokepoint = the persistent registry) BEFORE the boot reconcile; the orphan pass re-adopts fingerprinted resting buys as bot-owned `entry_resting` Positions in full lifecycle (`orphan_readopted_fingerprint`), only true unknowns classify manual; **C47 gains `fingerprint_in_manual` + `fingerprint_untracked` assertions (FAIL → halt; the next reconcile pass heals by re-adoption)**. Boot close-out line: `fingerprint_readopt_summary` (adopted count vs banked snapshot). **The fused gun:** first credible signal wins, priority **te_scoreboard (observed_starts — C-RETENTION-2's first consumer) > schedule_live (the honest matcher's `status` row, carried in `_pm_honest` since C-PM-CLOCK, UNUSED until now — 42 live rows in the current schedule) > tape_latch (unchanged, stamps itself) > price_divergence (flag-grade, ONLY past the scheduled start ≤6h — FERCER/E113 honored)**. A fire adds the event to `_events_live` (grace/cancel/ride/route all key on it), stamps `gun_source`, logs disagreements (`gun_source_confirm`) and truth deltas (`gun_truth_delta`); an external-truth gun is exempt from the tape-quiet unlatch (the EKSLUX lying-clock lesson: the clock is the lying party). **NO new buy placement after ANY gun source fires** — `gun_buy_refused` at the place_order chokepoint (closes POST_GUN_FORENSIC's 80-buys-after-latch class; grace governs exits only, standing doctrine). **Feed liveness is a build component:** te_live.py found DEAD during build recon (last observed_starts row 07-07 7:20 pm — the C-RETENTION-2 feed down ~24h, silently); resurrected under tmux + keepalive cron in the same deploy. **Standing tripwire: `analysis/gun_scorecard.py`** — nightly GUN SCORECARD line (per-cat detection %, med |truth delta|, misses NAMED, never averaged away; independent-truth convention: te-fired guns grade vs tape onset, everything else grades vs the te row). Proof `.claude/proof_20260708/PROOF_FUSED_GUN_ORPHAN.md` (Lane 1: EKSLUX + the 3:20 pm restart replayed; Lane 2 $0 claimed). **Close-out INVALID without the live proof table (Part 3, pre-registered: ≥90% of feed-covered matches within ±3 min, every miss named) — lands tonight as this entry's amendment.**

### 2026-07-08 (night) · POST-GUN FORENSIC: the gun is blind in the S-line's habitat — detection 17–62%, median lag 31–46 min; EKSLUX = all four controls failing in one +$1.00 pair
Full doc `.claude/postgun_20260708/POST_GUN_FORENSIC.md` (script + raw banked; truth = tape-derived corpus bells, only 2 certified observed_starts joinable — the certified column thickens nightly). **(a) Coverage: ITF latch-silent on 73–83% of engaged truth-start matches (ITF_W detects 17.2%, ITF_M 26.7%; CHALL 55–62%), and a fired latch is median 31–46 min LATE; only ~10% of ATP_CHALL latches beat the bell.** (b) Boundary: post-grace fills 55 / **−$19.02** settled-attributed (paths: latch-silent 24, latch-too-late 15, outlived-latch 11 — MONCOU/LIXSUN ~+110 min, in-latch-grace 5); the ADOPTED dead-bot/orphan class (758 fills, −$167) flagged out per dossier and dwarfs the live leak ~9×. (c) The graced cancel path works as designed (latch→cancel p50 340s = grace+cadence); **every cancel_fill_race in the era (14/14) is the WALK path, zero latch races**; p90 latency tail 86 min unnamed; **80 buys placed >5 min after a fired latch** (in-play completion policy visible, named not convicted). **(d) EKSLUX (W50 Columbus): kalshi_primary lied LONG (7:00 pm vs true ~3:50 pm), honest_start NULL, latch +27 min (thin-shadow would-fire FALSE at the bell), grace never armed — EKS filled AT the gun (3:50:35 pm exchange-truth maker), LUX +10 min in-play; both exits cashed in-play yet ledgered "~2.9h before commence" — an in-play knife pair masquerading as a W1 lifecycle at 96.** Both legs were independently-priced joins (passes price provenance, fails the input read: fictional tts, no flow state) — the S-tier clause's living near-miss. +$1.00 LUCK-POLLUTED n=1, not claimed. **(e) Verdict: no cat's gun supports bell-proximate entries as-is — the T-20m→bell habitat sits inside the latch's median blind spot everywhere.** Findings only; bell-recovery/flow-keyed-grace are queue items. Same push: **S-TIER consolidated on the front page (provenance clause; honest-era regrade S = 0; 77% of era sibling placements are 97−basis arithmetic)** + BOARD SEND-ORDER + queued adds (sequential floor, corridor flag, exit provenance).

### 2026-07-08 (evening) · EARLY-CANVAS STUDY LANDED (CONCEPTION-HORIZON Part 2): the T-8h bound forfeits NOTHING measurable — and the floor arrives with the FLOW, lag zero
2,460 pairs, T-24h→bell, pair-story conventions, detached run 2:28→3:55 pm ET, 0 watcher restarts (`.claude/early_canvas_20260708/EARLY_CANVAS_PART2.md`; aggregate committed, raw jsonl on the VPS). **Coverage first: beyond T-16h the non-mains tape is ≤17% covered (collectors adopt games ~T-12); T-12→T-8 is well-covered (88–96%) and the verdicts there are real.** The canvas beyond T-8h: **joint-fillable ≤97 is 0.0–2.2% in EVERY T-24→T-9 bin, every cat** (ITF zero-print bins 80–91%, joint-ask lattice 104–186 — often not even two-sided; mains par-locked at 101 all 24 hours — the 07-07 refutation extends to the full day); early sell-flow prints run 28–69% underwater at the bell (coin flip before fees); ITF drift variance from T-12 is 14–17¢ with drift_corr strictly rising toward the bell — early prices are less informative, never more. **Verdict, all six cats: nothing before T-8h ever justifies a resting bid — the bound stands; evidence is compatible with tightening, nothing supports loosening.** The §5 doctrine fitted: **onset→floor97 lag p50 = 0–1 min (ITF/CHALL)** — fillability IS the flow transition; the "early-floor" pairs (6–12%) are flow-early, not clock-early (they too open at their own onset); mains onset is meaningless (liquid all day — gauge excludes them). **Provisional gauge thresholds fitted: WAKING ≥ ~0.2 prints/min (trailing-30m), OPEN ≥ ~0.4–0.6 with spread ≤4¢; the early-floor signature is fast tape + WIDE spread (p50 6–7¢).** Thresholds are same-corpus fits — ratify on forward tape before the gauge carries any decision; conception-keyed-on-flow-state is the eventual horizon replacement, on operator ruling only. floorS: the S canvas exists on ~half of ITF/CHALL pairs and is a last-hour object everywhere (ttb p50 −35 min).

### 2026-07-08 (evening close-out) · THE ORPHAN CLASS — one headstone, every costume
**Mechanism, one sentence: a restart makes the bot misread its OWN pre-restart orders as operator-manual (they are absent from the new process's own-order registry, so the C-COPILOT manual-order courtesy claims them) — and every control loop exempts manual orders, so the book's most dangerous bids are exactly the ones nothing watches.** Every sighting below is this one class in a different costume:
- **June lineage** — the C-COPILOT manual-classification courtesy is the design root; restart adoptions stamping orphans "manual" is why SLATE_LEDGER exclusion sets had to be ratification-timestamp-scoped (C48: naive refresh sweeps real legs out, naive rescue breaks the cuts).
- **F39, 07-04** — link-path invisibility / unbooked fills: exchange truth ruled the ONLY result-side source precisely because the bot's own ledger loses track of orders it no longer recognizes as its own.
- **07-06/07 dup-buy storm** — each restart re-conceived completion bids on top of orphaned resting ones it no longer counted (119 tickers, 902.8 surplus shares); the page-1 reconcile amplified it, but the re-buy fuel was own-orders-read-as-someone-else's.
- **07-08 blackout** — the dead bot's **316 resting orders** kept working the market for 12.6h (8 legs filled naked, ~4:37–11:18 am ET); the respawn adopted the survivors as manual-class legs, outside the control loops from birth.
- **07-08 conception-horizon sweep** — the in-code sweep deployed that afternoon could not fire on the 11 surviving beyond-horizon bids because they were manual-class restart orphans (on lying clocks besides); the operator's sweep had to be **direct v2-API surgery, 11/11**.
~~The fix is queued on BOARD (ORPHAN-CLASS FIX)~~ — **SHIPPED same day (`6d84f27e`, C-ORPHAN-FINGERPRINT, ledger entry above): boot re-adopts everything carrying our order fingerprint** (the jsonl order_placed lineage), C47 asserts zero fingerprinted orders in manual class. Companion class, separate headstone: LYING-CLOCK (BOARD; GILOBR exhibit) — the two compound (today's 11 were orphans ON lying clocks: doubly invisible; the clock class stays open).

### 2026-07-08 (afternoon) · CONCEPTION HORIZON: the unstudied early window gets a bound (T-8h, INTERIM SCAFFOLDING) — and then gets studied
**THE DECLARATION: the T-8h bound is INTERIM SCAFFOLDING — a map edge drawn while unstudied territory gets studied, NEVER a target (the cap-as-goal grave, three resurrections, is the named prior). It moves automatically on the early-canvas study's evidence + operator ruling.** Prior art: FOUNDATIONS front page (PRE-T-4H open debt; three observable prices); HOURLY_APPENDIX 07-07 (ITF prints/min 0.00 until T-3h — wide silent lattice quoting ~99; MAINS liquid-at-par every bin, early hypothesis refuted; CHALL intermediate — per-cat mechanisms differ, never flatten); PAIR-STORY ("early posting buys POSITION, not early fills"). **Part 1 shipped `6d9ebe31` (defect-class, full gate, one restart): no conception or resting entry bid earlier than T-8h before the honest-anchored start** (per-match clock supplies the anchor; unknown start → the bound never blocks — schedule_gap keeps its handling). Three sites sharing one `_horizon_state` resolver: **router gate** (`conception_horizon_defer`, once/event; re-conceives the moment the match enters the window) · **manage-pass sweep** (`conception_horizon_cancel` → `_untombstone_entry`; pre-deploy census 18:06Z: **12 resting buys 12.9–14.9h out** — GILOBR×2 the exhibit, TORMEL/BEKPAN/JUHKLO/ZIVMIK pairs, JANFUN-FUN, VANSEL-SEL) · **place_order chokepoint refusal** (`conception_horizon_refused` — the C-BAND-CLAMP make-the-cancel-stick lesson applied at build time: without it the sibling-repost scan re-posts the JANFUN-FUN class, held leg-1 with sibling >8h out, in a churn loop). Exits and taker paths untouched; a legally-placed bid can only age INTO the window. **C47 audit gains the regression flag `conception_beyond_horizon` — must stay 0.** What the bound forfeits, stated honestly: early queue position in the silent lattice + early completion on held-sibling pairs until T-8h — no observed fill value before T-8h anywhere in the studied corpus; the unstudied remainder is exactly what Part 2 measures while the bound holds. **Part 3 same push: live-monitor gains the per-game per-cat print-rate/flow-state gauge (QUIET/WAKING/OPEN, thresholds PROVISIONAL until the study fits them; flow_state jsonl transitions append-once) — and the monitor loop itself was found DOWN since the 02:52 disk crash (last cycle 02:54) and restarted with the gauge aboard.** §5 doctrine added to FOUNDATIONS (early quiet is volume-conditional; distributions over medians). Part 2 (early-canvas study T-8h→T-24h, read-only, detached+watcher) dispatched same session — findings land as their own ledger entry. Proof `.claude/proof_20260708/PROOF_CONCEPTION_HORIZON.md` (Lane 2 $0 claimed; the swept bids' counterfactual is the study's question, not a claim).
**DEPLOY VERIFICATION AMENDMENT (same day, flags not smoothing):** deployed `f41330f` PID 1456032, gate 4/4 PASS; boot audit FAIL was GEAZIN-ZIN — match already settling at boot (`v4_exit_fatal` on a closing market, leg SETTLED 12s after the audit read); halt cleared on the 14:25 re-audit PASS. **Router defer live-proven: 18 `conception_horizon_defer` events first hour (KOSNOS 19.6h, MUCGAU 18.1h).** But the in-code SWEEP never fired — the 11 surviving census bids were **(a) manual-classified after the restart** (pre-boot orders absent from the new process's own-order registry → C-COPILOT treats them as the operator's → untracked by every control loop) **on (b) LYING legacy clocks** (`kalshi_schedule_primary` date-coarse: GILOBR legacy start 14:00 TODAY vs true 2026-07-09 08:30Z, `honest_start: null` — the pm-honest matcher misses the event entirely), so tts read NEGATIVE and no gate/sweep/flag could see a violation. **Executed the operator's sweep directly: 11/11 cancelled via v2 API, named (GILOBR-GIL@5/-OBR@93, TORMEL×2, BEKPAN×2, ZIVMIK×2, JUHKLO-KLO, JANFUN-FUN, VANSEL-SEL), post-sweep census BEYOND T-8h: 0.** Residual classes NAMED, not fixed here: (1) the lying kalshi_primary clock + pm-honest matcher miss on ticker-date≠true-date events — the standing C-KALSHI-OCC / PM-CLOCK lane (BOARD queued #11 + this branch's own staged work), GILOBR now its sharpest exhibit; (2) restart orphan bids read as MANUAL and leave every control loop — adjacent to the BOT_DOWN/audit-twin lane; the horizon bound is only as good as its anchor and it says so.
**EVENING CENSUS AMENDMENT (close-out session, ~3:37 pm ET): `conception_beyond_horizon` = 0 in-code — and the direct-API census says 5.** Four of the morning's swept names (JANFUN-FUN@40, BEKPAN-PAN@72, JUHKLO-KLO@37, VANSEL-SEL@59) plus MILMIS-MIL@6 re-materialized 2:35–3:21 pm ET via `sibling_repost_placed` — **all five are completion bids for HELD leg-1s** (JUH basis 60 / BEK 21 / JAN 57 / VAN 38 / MIS 92), all `26JUL08` tickers whose true starts are Jul 9 — **the lying-clock class re-conceiving straight through the chokepoint refusal** (`conception_horizon_refused` = 0: on a lying clock the horizon state reads "no violation," so the guard built to stop exactly this churn never sees it). **Deliberately NOT re-swept:** cancelling a held leg's completion bid re-starves the pair (the exact MILMIS-MIL state the b0661bc2 addendum healed at 2:23 pm), the repost sweep re-places within ~a minute (TIKCHO lineage), and all five age legally into the window between 7:00 and 9:00 pm ET tonight. This is the LYING-CLOCK queue item's first live re-materialization proof: **sweeping without fixing the anchor is churn, not enforcement.** Router defers meanwhile healthy: 46 fires by 3:37 pm.

### 2026-07-08 (afternoon, addendum) · C-BAND-CLAMP-WALK aftermath completion (`b0661bc2`): a failed recovery now FREES the leg
First live full-recovery failure (14:23 ET, inside the GEAZIN boot-halt window): the conception halt blocked the walk re-place AND the recovery → 3 legs (REARAB-REA, MILMIS-MIL, LAUTOR-LAU) left tracked-with-oid="" — the exact starved state every healer skips, and MILMIS-MIL was the HELD MIS leg's missing completion bid. One-branch fix through the gate: failed recovery → `_untombstone_entry` (unfilled freed for router re-conception downstream of halt/horizon gates; partials keep their position) — byte-identical semantics to the function's other terminal cancels. The deploy restart heals the three starved legs. Addendum in PROOF_WALK_BAND.md.

### 2026-07-08 (midday, second pass) · C-BAND-CLAMP-WALK: the band law reaches the walk chokepoint — and the dossier's "bypass" is amended to what it really was: cancel-then-refuse STARVATION
BOARD item 2 executed; the conviction sharpened the class before the fix shipped. **Jsonl scan (both log files, `/root/walk_band_scan.py`): the dossier's 29 outside-band walk placements ALL land in the 23:12→23:57 window — before the clamp boot; HARMAI-HAR @4 was placed 23:53:09 and filled 23:55, two minutes pre-clamp. Since 23:57 the placement clamp is AIRTIGHT (zero out-of-band placements — every creation site flows through `place_order`).** The live post-clamp defect at the walk chokepoint is ORDERING: a walk that computes an out-of-band target cancels the good resting bid FIRST, then `band_refused` kills the re-place, and the leg is left **starved with `entry_order_id=""` (validate_resting_buys skips it — no healer exists) and a phantom `entry_price` stamp** — ICHOCH-OCH 00:43 (28¢ bid destroyed, refused @4, settled WIN with ledger basis 4/qty 0), PIAPIE-PIE 02:34 (8¢ bid, refused @2). **Fix `a5a64962` (defect-exempt, config untouched): (1) pre-cancel band guard on the projected landing price (`_projected_repost_price`, the C-CHURN-FIX parity primitive) — outside [5,95) → `walk_band_refused` (deduped), walk refused, bid keeps resting; (2) any failed re-place (band residue, guard api fail, order_error) now recovers at the just-cancelled price instead of stamping a phantom basis — `repost_place_failed` logs the outcome.** In-band walks byte-identical (pure pre-check). Proof `.claude/proof_20260708/PROOF_WALK_BAND.md` (Lane 1 deterministic replay; Lane 2 $0 claimed — ICHOCH's +$3.60 counterfactual flagged LUCK-POLLUTED n=1, not claimed). **Midday live-book verify on the pre-deploy boot (17:09Z): PASS — 45/45 held legs exit=qty exact, 0 buy stacks, 0 out-of-band resting buys (13 sells at 97/98 = the capped-exit class, band law is buys-only), 61 fills last 75min named + window-stamped (W1?/W2~ honest-flag, tape-latch anchors); raw `/root/midday_verify_20260708/`.** Supersedes: the dossier Part 1 side-answer (a)'s "second chokepoint the clamp does not cover" framing (the chokepoint was covered; the ORDERING was not).

### 2026-07-08 (midday) · THE MORNING DOSSIER: last night was a liveness incident wearing an entry ledger — the clean entry read is 0–1¢ off own lows and the gap is CLASS + HABITAT, not price
Full doc `.claude/dossier_20260708/MORNING_DOSSIER_20260708.md` (+ PART1_COHORT 293 rows, PART2_UNISON 174 pairs, PART3_CLEAN3DAY). **Footprint correction: the 8 naked open legs were survivors — the dead window produced 211 MECHANICAL legs of 293 fills (95 settled winners / 121 rode to 0), 77% of fills landing in-play because a dead bot can't cancel at match-live.** Collectors died with the disk (02:46–11:30) — Kalshi public prints bridged the hole (starred trades-only classes). **Entry findings (clean data only): median gap to own fillable W1 low +0¢ cohort-wide** (POST_FILL leading-edge finding reproduced at 10×); 3-day clean trend **ITF_M genuinely improving (6.0→1.0→0.5¢, band-reach 7→38%)**, ITF_W improving on price (10→3→1¢) but **still reprice-dominated** (divot ~18%); CHALL flat; mains no-read. **Unison counterfactual buys ~nothing (median delta 0–1¢/cat)** — the binding constraint is print scarcity (224/348 legs zero W1 sell-flow: the silent lattice live), and **unison-≤97 was claimable on only 14/174 pairs** (the 80 actual ≤97 pairs are mechanical in-play fills — not an entry achievement). **Doctrine finding routed to post-gun forensic: the S-line's habitat (T-20m→bell) is exactly what the premature latch clock refuses to trade — the achievable floor lives inside the latch-vs-bell gap.** Verdicts: ITF_W/CHALL = waiting-on-the-brain (checkpoint: honest coverage **0/500 cells, trigger=False** — AIM_V2 stays BLOCKED-ON-DATA; config-hold CONFIRMED; leak table rides tonight — last night is not a valid leak night). Shortlist (defects only): **(1) walk/repost band-clamp bypass — 29/639 post-boot buys outside [5,95) via the walk path, 1 filled @4¢ (HARMAI-HAR); the preflight clamp covers the placement chokepoint only → BOARD**; (2) BOT_DOWN channel (standing). LOUD-8: all filled, 7/8 cashed, 1 casualty (MIXKRU-MIX @39 rode to 0). Cross-ref flags (not smoothed): preflight band-clamp entry incomplete as book-wide guarantee; adopted-exit prices must be read from /fills not the bot ledger (LUENAT-LUE 65-ledger vs 85-exchange, A52/A54 family). P&L reconciled: Kalshi cash $757.26 + portfolio $77.10 + PM $80.00 = **$912.61** (cash_ledger's first clean snapshot since Feb 26).

### 2026-07-08 · NAKED-EXIT RECURRENCE = A DEAD BOT, NOT AN EXIT DEFECT: disk-full killed the logger, the logger killed the loop, and every watchdog was inside the blast radius or never alive
The overnight naked legs were not an exit-lifecycle failure. **Conviction (jsonl + exchange truth):** disk hit 100% (16G tennis.db, 11G durable, 9.0G *uncompressed* tick CSVs, 243MB console log) → 02:52:04 ET `_log("skipped")` flush raised `OSError ENOSPC` **uncaught** through `routing_tick` → `run()`'s catch-all died in its own `_log` → process exit 02:52:22. The `<90%`-gated respawn cron then correctly refused for 12.6h (absorbing state: nothing frees disk, nothing escalates). The dead bot's **316 resting orders kept filling: 8 legs naked 04:37–11:18 ET — every naked fill is post-crash; every pre-crash fill had its exit resting** (07-07 sweep-era machinery held). WS handlers had survived the same ENOSPC for 18s before the loop died (reconnect swallows) — the crash class was *which caller* hit it first. **The watchdog verdict, per audit surface:** in-process 15-min steady_cadence audit PASSed 02:50:37 (55 pos/316 orders) then ceased to exist — C47-CONTINUOUS shares the process's blast radius; external `position_audit.py` cron **has never run since the line was written** (dash `.` is PATH-only: `. .env` → `rc=2` before python ever launched — `audit_cron.log` never existed, `audit_log.json` froze Feb 28); nightwatch/watchdog.sh detected BOT_DOWN within a minute and screamed ~758 minutely alerts **into local files no one reads** (58k lifetime BOT_DOWN lines). NO audit ever PASSed while a leg was naked — the operator's "PASS all night" window ends 02:50. **Containment (freeze lifted for this):** disk 100→82% (2,205 idle tick CSVs gzipped lossless; crash log archived `logs/live_v4_crash_20260708.log.gz`), respawn fired 15:30:02, boot reconcile posted all 8 exits in 36s at standing bands (LUE 65→ITM taker-sold 5@85¢ basis 53; MIL 9→taker 5@13¢ basis 6 — C-ITM-EXIT-TAKE's first live outing on adopted legs), steady-state adopted the containment-window wave (DAMARN×2/PDARIB/MAXABA/ISHCRO ≤8min). **Sweep verified 15:44:59 UTC: 32/32 covered, 0 naked.** **Shipped through the full gate: `9a74b061` C-LOG-ENOSPC** — `_log`/`_log_tick`/`_log_trade` try/except OSError (drop the line, never the bot; `_log_write_errors` counter); proof `.claude/proof_20260708/PROOF_LOG_ENOSPC.md` (Lane 1 unchanged by construction; Lane 2 $0 claimed). Ops closures same pass: `position_audit` cron `. ./.env` fix (external audit alive for the first time), daily disk-hygiene cron (gzip idle ticks >4h, truncate console log at 500MB, gzip old jsonls). **Open (BOARD): BOT_DOWN needs a channel OUT (operator decision); the every-15min PASS log needs an out-of-process twin — both named, neither improvised tonight.** MECHANICAL flag: all 8 legs + the second wave carry `naked-class` in tonight's ledger so the entry read stays clean.

### 2026-07-08 · POST-FILL MOVE QUOTE: our fills mostly stand in front of REPRICES, not divots — the aim's conditioning variable named
Era W1-filled population (n=276, tick tape, strict classifier): decisive fills split **divot 17-26% / reprice 48-67%** per cat (ITF_W worst: two-thirds of decisive fills are the market moving through us). **REPRICE → band-unreachable 65-81%** (the no-bueno class costed); **DIVOT → band touched at 2-3× the reprice rate** (gold pattern confirmed; tonight's WEISUN-SUN dipped 1¢, snapped, cashed); ITF_W's strongest class is **NO_UNDERCUT (86% band touch)** — strength at the fill predicts the band. Undercuts arrive in 0.1-0.3 min — the fill IS the move's leading edge. **The aim build inherits: deepening the dip alone buys more reprices cheaper; the selector is ask-hold + sibling-chain state (the ex-self fields already log it every decision), not depth.** Tonight's 12 named in the doc (6R/2D/2N/2A). `.claude/sweep_20260707/POST_FILL_MOVE_20260708.md`.

### 2026-07-08 (preflight) · ENTRY ROLL + BAND CLAMP + OVERNIGHT FREEZE
Final-deploy cohort rolled per ticker (`.claude/sweep_20260707/ENTRY_ROLL_20260708_PREFLIGHT.txt`): 12 fills under the 23:12 boot, **every one exit qty=held** (the sweep-era machinery holding); 129 resting overnight entries — 8 LOUD (tape-latch-only liveness, no schedule anchor), 11 out-of-band bids (≤4¢ noise-goals + ≥95¢ par-adjacent, ADAIMA/CAVPLO/HARMAI/KUBSHK/MANKAV/VAJKAR/BOSBOY class) CANCELLED and **C-BAND-CLAMP deployed**: no maker buy rests outside [5,95) — the audit's flag class becomes enforcement. NASLEE-LEE the roll's lesson: +9 off aim, below_chain post, own low 1¢ printed after (in-play knife). **FREEZE: no deploys/restarts until the morning checkpoint; audits/shadows/accumulator run.**

### 2026-07-07 (late) · EMERGENCY SWEEP: 4 naked legs, two evasion classes named and closed; the audit goes CONTINUOUS; S goes PER-LEG
Operator called live naked legs against clean counters; the exchange decided: 4 naked (ISOTOM/KUSTAG/NASLEE/TIKCHO, all tonight's fills), 0 stacks — swept clean in one forced pass (exits at standing bands; NASLEE's band-34 filled at 45). **Class 1 — ITM-exit-death:** band exit 400s "post only cross" twice → v4_exit_fatal → naked past every healer (the CLAHER flag class was this, live) → **C-ITM-EXIT-TAKE**: the retry crosses as taker, realizing ≥ band (qty-mechanical; band levels untouched). **Class 2 — post-exit re-buy:** a cashed leg's qty=0 reads as "missing sibling" and the repost sweep re-conceives within a minute (TIKCHO 34s) → **C-NO-REBUY-AFTER-CASH**: `_session_exited` stamped at exit booking; repost sweep skips cashed pairs; audit gains the `post_exit_rebuy` assertion. **Structural:** the book audit was a boot gate (19:39 PASS, then 4 legs went naked unasserted) → **C47-CONTINUOUS**, every 15 min steady-state, same halt/clear. TANCHE 98 stamped into the race-class counter (untracked-sibling adoption outran the C42 reaim — the reaim can only move tracked orders). **S REDEFINED PER-LEG** (ruling): each leg within 4¢ of its OWN fillable W1 low (the gold-census N) + own W1/corridor cash; **combined demoted to descriptive scoreboard everywhere** — see RETIRED. **Deployed 3ec8cc0 (23:11 ET): boot audit PASS, first steady_cadence audit fired 23:13:45 PASS — C47-CONTINUOUS live-proven; itm_exit_taken/post_exit_rebuy detectors armed awaiting occurrences.** **The per-leg S regrade found what combined hid: S = 3** — BARSIM (misses 0.5/0.0¢ off own W1 lows, combined 97.5 — the old lens graded it C!), WALVAL (0.0/0.0¢, +$0.83), SCHZID (2/−4¢, +$2.40): perfect per-leg entries whose divots simply didn't co-occur, 0A's inverse relationship in three exhibits. Walk/repost model (Part 2, `.claude/sweep_20260707/WALK_REPOST_MODEL.md`): P(walked|below_chain)=86% vs join 44%; erosion median 17% PASSES the 25% bar, tail FAILS (31-42% of walked legs over; ITF_W p75 233%) — expression suppresses walk incidence, cap bounds magnitude; arm case = the shadow nights converting the tail. Artifacts: `.claude/sweep_20260707/`.

### 2026-07-07 · EVENING CUT: S/A rubric adopted — the honest era holds ZERO S pairs
S = full W1 lifecycle at combined ≤ per-cat S-line (PAIR_STORY §1: ITF 84 / ATP_CHALL 93 / WTA_CHALL 90 / mains 93); A = the shape at ≤97; reachable-not-cashed no longer earns A. Wired into the ledger renderer; **honest-era regrade across 557 settled events: S = 0, A = 7** — the floor the tape offers has never once been taken with a full W1 lifecycle; S measures exactly what the machine has not yet done. Tonight's 17 pairs completed med +13-14¢ off the ITF S-line (best 95); defect counters clean under the day's five deploys (boot audits PASS 0/0, halts 0, guard blocked 15, topups 2, CLAHER-class 2, post-gun fill n=1 VANMIL +48.7m, band-edge bid n=1 @2¢). Joint-shadow smoke: 19 constrained, all honest-anchored (false-flag 0), caps bite (0/19 would-fill under cap alone — starvation watch), ≤97 held 16/16. Ledger cuts A-D reproduce; cash $747.43 banked. (`.claude/live_20260705/EVENING_CUT_20260707.md`.)

### 2026-07-07 · FLOOR-BY-HOUR: the ITF floor is a last-2-hours phenomenon; the mains-early hypothesis is REFUTED
Hourly decomposition of the pair-story floor, two conventions side by side (`.claude/pair_story_20260707/HOURLY_APPENDIX.md`): **ITF prints/min are 0.00 until T-3h** — the early ITF book is a wide (4-5.5c) silent quote lattice at ~99 mid; the 84/86 floor exists only T-2h→bell (jf_any 5-13% early → 98% last hour, where prints beat quote-mid by 8-9c). **ATP_MAIN's quote-touch floor is 100.0 flat in EVERY bin T-8→T-1** (1c spreads, liquid all day, priced at par) — no convention rescues an early sub-97 mains pair: **mains NO-GO**, the wide-spread-no-prints regime hides nothing anywhere. Pre-T-4h spec feed: early posting buys POSITION (queue, presence), never early discounted fills; the "≤97 achievable pre-T-4h" thesis reads as *achievable by bids resting from pre-T-4h*. Coverage caveat quantified (early ITF bins: ~2/3 of pairs, ~15 quoted-min/hr). **Supersedes: the mains-early hypothesis; any early-window discount framing for ITF/CHALL.**

### 2026-07-07 · PLEX WALK-CAP RULING executed (shadow-first): sizes staged, JOINT SHADOW live
Ruling verbatim-as-relayed at `.claude/rulings/PLEX_WALK_CAP_SIZES.md` (lineage corrected: staging cite is `7def47e2`, the 07-06 census close-out — the relayed `e1505e92` was the AIM_V2 commit, noted honestly). Per-cat honest-anchor cap sizes STAGED (`walk_cap_honest_by_cat`: ITF_W 20 / ITF_M 14 / ATP_CHALL 2 / WTA_CHALL 2 / mains 1 — the census p75s); **`walk_cap_honest_anchor` stays OFF**. The arm condition is live from tonight: every placement/repost shadow line now carries walkcap_level / exself_level / joint_level / constrained (both counterfactuals + combined, log-only), with the nightly rollup (`analysis/joint_shadow_rollup.py`) reporting conversion/starvation of combined-vs-each-alone and ≤97 held on constrained pairs. Invariant = step law, cap = journey bound; both arm together on shadow evidence + the four-bar gate.

### 2026-07-07 · PAIR-STORY at corpus scale: the achievable floor per cat — 97 reverts to emergency ceiling; S-lines set
2,396 detected-bell pairs, tick grain, honest axis (`.claude/pair_story_20260707/`): the canvas TYPICALLY offers **ITF 84 / ATP_CHALL 93 / WTA_CHALL 90 / mains 99** as the best jointly-fillable combined (prints-only conservative convention); ITF offers ≤90 on two-thirds of pairs with ~30min dwell at ≤97. **Per-cat S-lines (operator ruling: S beats the wall meaningfully; A = 94-97): ITF_M ≤84 · ITF_W ≤84 · ATP_CHALL ≤93 · WTA_CHALL ≤90 (n=29 provisional) · mains ≤93.** The W1 opportunity number (drift-over-lowest-traded gap): ITF ~15-16c, CHALL 7-10c, mains 1c (par-locked — the canvas declines to pay). Divots sit ~50-65min apart with no reliable leg ordering (0A sequential doctrine confirmed at scale); seesaw is simultaneous at minute grain (lag-0 corr −0.68..−0.84 ITF/CHALL, −0.29 ATP_MAIN); the floor is LATE (med bestT −15/−17min, post-ramp 72-77% where a ramp exists at all — only 9-19% of pairs show one). Doctrine read: early POSITION, late FILL. **Supersedes: the vanished 07-06 pair-story run (no artifact ever landed); 97-as-target framing for ITF/CHALL (it is the mains' number).**

### 2026-07-07 · join_trial_mode RETIRED (config flip through the full gate) — the 06-18 trial's lineage closes
The join-the-bid 5c trial (staged 06-17, pre-registered abort spec LOCKED at 8cc9ff1) **died 2026-06-18: night-1 -$10.12, join-induced naked legs — the partner starved behind the MM wall and the abort was structurally blind to starvation** (join_trial_night1 diag; the RETIRED grave). The FLAG outlived the trial by 19 days: `join_trial_mode=True` kept stamping every joined leg `join_is_trial` (528 stamps on today's tape alone) and kept the dead abort path armed-dormant — the conformance audit's one archaic-ARMED finding (§4). Flipped False through the full gate (config-only, one restart, C47 boot audit, C50 two-file close-out — its bootstrap deploy). Placement behavior unchanged by construction: the flag gates only the trial-abort resolve (live_v4.py:5139) and the stamp (:7031). **Supersedes: the trial-era join_trial_abort_spec as an armed surface (stays LOCKED as history).** Adjacent dead-flag list on BOARD (listed, not flipped).

### 2026-07-07 · CONFORMANCE: the Vault marched against the live process — 23 laws LIVE-PROVEN on today's tape
Every actionable claim in this vault + LESSONS + rulings got claim|cite|live-proof, no fourth option (`.claude/CONFORMANCE.md`): 23 laws with same-day jsonl fires (57 buy-blocks, 13 top-ups, complete_cross admitted at basis exactly 100 with 30 bound-refusals, 22,389 reshuffle re-aims, 1,787 ex-self posture lines, the full halt arm→clear cycle). 6 UNPROVEN-LIVE stamped with detectors (never-marketable, hold-to-settle, C50 bootstrap, float-guard fractional, error-tripwire file, kalshi-occ observe). Every grave has a tripwire or a named gap (one real gap: sub-1 residues invisible to the audit tolerance). Archaic sweep: INTERIM-ARCHAIC has no surviving implementation; one archaic-ARMED flag found (`join_trial_mode`=True, trial dead since 06-18) → BOARD. Cadence: tape columns post-any-deploy, full audit weekly.

### 2026-07-07 · THE CONSOLIDATION (this system) — C50 lineage
The knowledge surface itself was the defect: ROADMAP (frozen 06-01) and SESSION_HANDOFF (frozen 06-05) fossilized on BOTH branches while the working truth migrated to a Vault on a branch the deploy tree can't see, two VAULT_PENDING docs never merged, and today's ex-self reversal proved a verdict can die without a grave. This file + `.claude/BOARD.md` are now the spine; the fork is tombstoned (deriv freeze 9aea01fe); **C50 enforcement LIVE: deploy_gate.sh [4/4] refuses any deploy push that doesn't touch BOARD.md + this file (last_deploy_sha window), and the C45 grep surface now names the LESSONS A-G ledgers + this file's RETIRED section explicitly.** Inventory: `.claude/INVENTORY.md`. **Supersedes: ROADMAP.md, SESSION_HANDOFF.md, JUNE_VAULT (as living surfaces).**

### 2026-07-07 · C47-ENFORCE: post-boot book audit, assert-and-halt (LIVE)
Every boot: paginated exchange pull, 5 assertions (buy stacks / exit qty==held / no naked held / no over-lot resting buys / [5,95] flag-only), diff vs the gate-banked snapshot; ANY fail → conception halt (exits keep working), artifact on arm transition, clears only on passing re-audit; jsonl-only verification. First cycle: caught real storm-era stacks (27 orphan orders cancelled), the guard's own int-floor (EVAGOW 0.72+5), and two of its own defects (bq>0; artifact spam). First PASS 12:59:36 ET. **C49.** Source: PROOF_AUDIT.md.

### 2026-07-07 · SLATE_LEDGER refresh: days read GROSS and NET-OF-MECHANICAL (⚑a/⚑b/⚑c)
07-06 = −24.56 gross / **+30.90 net**; 07-07 = −86.99 / **−6.00 net**. Un-flagged, both days grade as strategy failure; flagged, the defect family owns the loss. Exclusion sets are ratification-timestamp-scoped (restart adoptions stamp orphans "manual" — naive refresh sweeps real legs out, naive rescue breaks the cuts). All four continuity cuts reproduce. GOMOFN tape verdict recorded. **C48.** **Supersedes: any daily grade read without the mech flag.**

### 2026-07-07 · THE BLEED ATTRIBUTION: strategy was POSITIVE; the loss was mechanical
Era (07-05 boot→07-07): gross −$72.81 realized = strategy **+$81.30** + defect family **−$154.11** ((a) dup surplus −49.35 on 892.9 sh; (b) naked band-touch foregone −110.72/73 legs — 2.2× class (a); (c) fractionals +0.67). **No band-unreachability signal survives attribution.** Stated daily nets reconcile at the 2-day aggregate (Δ2.6%, boundary convention). Source: BLEED_ATTRIBUTION_20260707.md.

### 2026-07-07 · EX-SELF VERDICT REVERSED + the measurement law (C47's second half)
`bid_ex_self` fired from 02:34:33 ET (1,668 jsonl rows); every "ABSENT" read (02:30 verify, morning forensic §4c) grepped the console log, which truncates JSON at ~190 chars. **Key-presence checks run on the jsonl, never the console.** `expression_clamped` silent BY CONFIG (`expression_invariant` OFF). **Supersedes: VERIFY_20260707_0230.md's verdict (file stands as the exhibit).**

### 2026-07-07 · DUP-BUY STORM: partial exchange-truth reads are worse than none (C47's first half)
`reconcile()` read PAGE 1 ONLY of positions/orders (100/page vs 277 resting) → every restart stacked completion bids (119 tickers past the lot since 00:00; ZHAISH 3×5 one tick; VANBOO re-engaged in-play 7min post-boot); the buy guard read empty boot memory and never counted resting buys ("0 occurrences" while the storm ran); exits sized order-scoped with a downward-only clamp (SIMROU 10/5, ECHADD 15/5). Fix `ccf8fa8f`: cursor walks + exchange-truth committed-exposure guard (fail-closed, FLOAT qty) + EXIT QTY = POSITION QTY + link-path top-up. **Grep rule: any guard-feeding `portfolio` read without a cursor loop is a defect.** Sources: FORENSIC_20260707_MORNING.md, LESSONS C47.

### 2026-07-07 · EXPRESSION INVARIANT ruled (front page) + spread census
See front page. PLEX_EXPRESSION_INVARIANT.md is the law text; SPREAD_EXPRESSION_20260707.md the census. **Supersedes: all hard-offset expression framings (INTERIM-ARCHAIC).**

### 2026-07-06 · W1 GRADING LAW (Vault 0E) + B-subgrades
W1_CASHED / W1_REACHABLE / W2_ONLY per leg; BOUHAR headline; **A requires the W1 shape**; B1/B2/B3 permanent rubric (OPERATOR_RULING_B_SUBGRADES). Baseline: ZERO W1-cash across 257 pre-flip legs. **Supersedes: grade-by-combined-alone.**

### 2026-07-06 · AIM_V2 built + operationalized, GATED-OFF (Plex parameters)
LATCH-CAL canonical, w=0.25, dip ITF/CHALL ≥3¢ floor, mains NULL, fallback ⑦, honest-only triggers; gold-leg production + B3-conversion ratified as Lane-1 arm bars; ramp = dated schedule to honest n≥30. Sources: AIM_V2_BUILD_REPORT, AIM_V2_OPERATIONAL_REPORT, PLEX_AIM_V2_RULING, AIM_V2_SPEC (no silent neighbor interpolation). **Supersedes: TIME_AXIS aim table (DOES-NOT-SHIP) and every flat/hard offset.**

### 2026-07-06 · RISER DISARM (pre-registered rule executed)
riser_post_revision true→false through the gate on its own scoreboard's disarm rule — the first pre-registered disarm honored as written. The deploy (07-05, first full four-bar gate) and the disarm are BOTH the system working. Sources: SCOREBOARD_20260706, RISER_DISARM.

### 2026-07-06 · HONEST CLOCK FLIP (per_match_clock LIVE, Ratification #20) + post-flip autopsy + 5-fix bundle
Config-only flip; post-flip audit clean; the first honest-clock session's autopsy proved violation mechanisms (adoption mark-to-market fabrications, orphaned dup orders, conception-void walk cap, site-4 bound hole) → b8a73a55. AX2 per-leg-cheapest gauge STRUCK by operator order, reissued role-relative. Sources: PLEX_FLIP_RULING, POST_FLIP_AUDIT, AUTOPSY_20260706, ENTRY_CENSUS.

### 2026-07-06 · C-RETENTION: observed true starts banked (VAULT_PENDING absorbed)
te_live.py banks FIRST observed in-play row per match (`observed_starts`, set-once, ≤44MB/yr); zero live_v4 delta (grep-proofed); accumulator prefers observed bells, LATCH-CAL bar else, `bell_src` on every sample. Closes the START_TIME_JOIN hole (zero starts on disk). Source: VAULT_PENDING_retention_fix.md (merged here; file tombstoned).

### 2026-07-05 · THE GATE LAWS COMPLETE: C45 prior-art + C46 outcome-proof (two-lane)
Every doc opens with prior-art (grep LESSONS A–G + this vault + rulings + the gated-flags inventory; staged-never-armed builds count); NOTHING deploys without per-game outcome replay (Lane 1 mechanism convicts/acquits; Lane 2 settlement P&L luck-flagged below n≈30). Full gate = lint + smoke + prior-art + outcome-proof. Born of the clock audit re-derivation (0B: MEASURE-BEFORE-READ). Sources: PRIOR_ART_GATE.md, Vault 0B/0C.

### 2026-07-05 · Week standing order (07-05→07-12): config HOLDS, analysis attacks
No tuning; defects exempt via gate. Central question: the leak decomposition (night-1: half_timing dominates ~131¢). End-of-week: leak answer + riser Plex bounce + pre-T-4h spec. Source: WEEK_STANDING_ORDER.md (verbatim).

### 2026-07-05 · Re-anchor Part-1 staged (ce38ca8c, gated OFF) + PLEX_REANCHOR slot OPEN
C-PM-CLOCK/C-SCALE-GUN staged; source RATIFIED (PART1_SOURCE_RULING); the REANCHOR ruling body itself was dropped by the relay — slot reserved, tracked on BOARD.

### 2026-07-04 · THE GATE IS LAW (C39–C41) + reaim (C42) + fill-is-information (C43) + causal audit method (C44) + F39
Duplicate-def crash (8h) → deploy/deploy_live_v4.sh is the ONLY restart path (lint + smoke; later + prior-art + outcome-proof + book-snapshot bank). Re-aim on ANY sibling basis arrival, no bucket exempt (C42). Reaction gap = defect; seesaw = rest at bound, never chase (C43). EARNED/GIFT causal audit (C44). Exchange truth is the ONLY result-side source (F39 — link-path invisibility, unbooked fills). Sources: LESSONS C39–C44/F39, CAUSAL_AUDIT_20260704.

### 2026-07-03 · AIM-TABLE DOCTRINE (VAULT_PENDING absorbed): aim = the dip; FV = yardstick, never target
97% of tickers dip below anchor pre-gun; post at the size-backed dip, get PAID the discount. **FV-gate on A refuted by data: non-FV-positive pairs settled BETTER (+$5.95 vs +$3.35) — gating on FV discards winners.** Spread bounds fillability (tour shallow 2-3¢; ITF/CHALL wide but knife-contaminated → conservative caps). aim_table.json per-cat: MAIN ~2¢ / CHALL ~3¢ / ITF ~4¢. Dispatch ba08243 armed per_cat_depth + leg2_reshuffle(97) + walk_cap + grace_kill + latch_tape_override. Source: VAULT_PENDING_aim_table_doctrine.md (merged here; file tombstoned). **Supersedes: FV-anchored placement framings.**

### 2026-07-01/02 · THE JULY ARC OPENING (Vault §4E–4I, archived in place)
**4E — the completion-gap cancer** (blind-laying keeps the loser, strands the winner: THE central diagnosis) · **4F — the June consolidation map + meta-failure** · **4G — stranded-winner recovery measured** (β gun-cross NO-SHIP; ALWAYS-LAY-BOTH neutral not +EV; pair completion neutralizes t20m-cancel cost) · **4H — P3–P6 + Plex rulings** (incl. capped-walk replay, error ledger; the 0A invariants) · **4I — bound ruling + clock verdict** · 0A standing order dated (exits out of scope). C-KALSHI-OCC staged-never-armed (the Gen-2 whole design C45 exists to find). These sections remain readable in the FROZEN June Vault; their operative content lives above and in LESSONS.

### ≤2026-06-30 · THE JUNE RECORD (archived)
The full June arc — bleed theories 1–6, deploy ledger, T50/T52, staircase seals, completion ceiling, disk-full/410 incidents, entry-blend verdicts — lives in the FROZEN `JUNE_VAULT.md` + `JUNE_VAULT_APPENDIX.md` on `blend/agent-derivation` and in LESSONS A–G. Operative survivors are all re-stated above or in LESSONS; if you find yourself re-deriving June, you are in §0's recurring failure mode.

---

# RETIRED — the graves (view · what killed it · when)

Findable forever; never a new insight. Newest kills first.

| dead view | killed by | when |
|---|---|---|
| **"Settled" as neutral bookkeeping / synonym for concluded** | SETTLEMENT VOCABULARY law: a settled position is the F-bucket (an exit that never filled); cashed-via-exit and rode-to-settlement are separate columns forever — the same-day proof: +$3.87 "recovery" vs +$163.41 in the exit column | 07-09 |
| **Absolute-cent depth thresholds (the 3¢ fill line)** | DEPTH-RELATIVE AMENDMENT (operator-caught): one ruler across a 6:1 spread spectrum measured discount scarcity, not fill probability; depths are spread-relative (join/touch−1/1×/2× spread) — fifth costume of the fixed-line grave | 07-09 |
| **Fixed-line combined grading (any "≤97 rate" as a headline)** | THE COMBINED-PRICE CLAUSE (FOUNDATIONS, 07-08 night): the floor is fitted per pair (Σ close−edge_p50); a fixed line grades a 96 and an 85 identically; contamination rule bars fixed-line headlines without the dynamic-floor gap beside them | 07-08 |
| **Cat-constant S-lines (84/84 → 79/79/92 arc) as the grading bar** | DYNAMIC S ruling 07-08 night: S reference = Σ(own W1-close − edge_p50(cat,cell)); constants demoted to corpus-median footnotes | 07-08 |
| **Fav/dog binary as an analysis frame** | THE GRID (cell re-cut): depth is a property of the leg's own price cell (edge p50=0 below ~50 everywhere; heavy zone 75–94 carries it) — fav/dog was a proxy; frame retired, cell key canonical | 07-08 |
| **April 56-cell grid · VERSION B 41-cell grid** | THE GRID lineage (FOUNDATIONS): current = 90 cells/cat × 6 cats; older grids history-only — quoting one in analysis = C45 failure | 07-08 |
| **Per-leg S graded on price alone (the 07-07 redefinition's S=3)** | S-TIER PROVENANCE CLAUSE (front page, 07-08): all three exhibits were cap-arithmetic legs (BARSIM 75=97−22, WALVAL goal:97, SCHZID 27=97−70) — price-only grading certifies luck, not the decision; honest era regrades S=0 | 07-08 |
| **Combined-as-target (third resurrection, via the morning S-tier)** | S redefined PER-LEG (4¢-to-own-W1-low + own cash); combined is a descriptive scoreboard, never a grading threshold — the pair-level number keeps sneaking back in as a target (97 goal → S-line → per-leg) and keeps being demoted; 0A always said it is the MEASUREMENT of entry success, the target is each leg's own divot | 07-07 |
| "ex-self re-fire never landed / fields ABSENT" | jsonl truth (1,668 rows from 02:34:33); console-truncation artifact — C47 | 07-07 |
| Daily grades read gross-only | C48 GROSS + NET-OF-MECHANICAL law; 07-07 read −86.99 gross / −6.00 net | 07-07 |
| "The band is unreachable / strategy is bleeding" (July era) | bleed attribution: strategy +$81.30, defects −$154.11 | 07-07 |
| Memory-scoped buy guard as protection | dup-storm forensic: 0 occurrences while 119 tickers multi-bought | 07-07 |
| int() on exchange quantities | EVAGOW 0.72+5=5.72 committed under int-floor; v1.2 floats | 07-07 |
| Hard entry offsets / INTERIM-ARCHAIC expression | aim build + expression-invariant ruling (join/improve-1 vs non-self chain) | 07-06/07 |
| Deeper pre-bell aims are free discount | GOMOFN live test: 16-17¢ never printed; the 21¢ fill was 2¢ off best | 07-07 |
| TIME_AXIS aim table | its own outcome proof: DOES-NOT-SHIP, −$20.31 | 07-05 |
| riser_post_revision (deployed 07-05) | its own pre-registered disarm rule fired | 07-06 |
| AX2 per-leg vs-own-cheapest timing gauge | operator strike (incoherent under the inverse relationship); reissued role-relative | 07-06 |
| FV as A-grade gate / FV as placement target | grade-vs-result: FV-gating discards winners; FV = yardstick only | 07-03 |
| Autopsy "settled" P&L convention (exit-realized counted as settled) | SLATE_LEDGER canonical settlement-realized rule (CUT A divergence named) | 07-06 |
| TRADE_ROLL as the book | SLATE_LEDGER supersedes (its own header) | 07-06 |
| Scheduled start as the clock (kalshi_schedule_primary era) | honest clock flip (Ratification #20) + LATCH-CAL canonical | 07-06 |
| pre_deploy_bug_checklist as the deploy gate | deploy_gate.sh law (C40 → C45 → C46 → C50) | 07-04→07 |
| freeze_at_gun | SHELVED at the aim dispatch | 07-03 |
| "one fill per pair is structural" | single-leg synthesis: differentiator is per-leg sell-flow | 06-19 |
| Directional entry edge (entry-blend) | coin flip on replay; dip-capture survives, direction doesn't | 06-15 |
| Join-the-bid 5¢ trial | night-1 abort: join-induced naked legs, starvation-blind abort | 06-18 |
| "T50 is backwards" hypothesis | forensic re-check: 1,440 events, zero blocked ≤cap — working correctly | 06-03 |
| A43 "pre-June exits veto live changes" | live monotonic cut +$205/wk — pre-live lessons never veto live results (A43 OVERTURNED; A46 RETIRED same family) | 06-under |
| pair_governor_scoot | duplicate-buy; lineage closed into reaim-on-arrival | 07-03 |
| "always-lay-both = post both simultaneously" | 0A: always be WORKING both legs, each at its own divot | 07-01 |
| "exit looks negative on live trades" as exit evidence | 0A standing order: symptom of broken entries; P6's E2 reading struck | 07-02 |
| Both-legs-≥100 taker-cross cohort | T50 cap + T52 taker-spread | 06-02 |
| entry_mode label as fill truth | A52: 87% is_taker while labeled resting_maker — exchange /fills only | 06-03 |
| pos_map avg_price as cost basis | A54: it is market value; fills-VWAP only | 06-03→07-06 (C-TRUE-BASIS) |
| ROADMAP.md / SESSION_HANDOFF.md as live surfaces | fossilized 06-01/06-05; this consolidation (BOARD + LIVING_VAULT) | 07-07 |
| JUNE_VAULT-on-another-branch as the current-truth home | this file; the June Vault FREEZES as the June archive | 07-07 |
| Naive-accumulated WS orderbook | needs snapshot anchor; premarket_ticks CSV + raw trades authoritative | standing (F-class) |
| Constructed mids as analysis anchors | 0A: only bid/ask/last exist | standing |

*(Analysis-artifact graves live in `arb-executor/analysis/dead_ends/README.md` — same law, code-side.)*
