# THE BOARD — standing queue (est. 2026-07-07; the first read of every session)

> **SESSION ZERO: read `.claude/HANDOFF_FABLE.md` first** (the seat handoff: disk map, contract, live state, first action).

> One queue, five states. Close-outs touch THIS file + `docs/LIVING_VAULT.md` in the same push (C50 — the gate enforces it). **C50 checklist extension (07-09): any close-out that changes live state, the ruling stack, or the queue ALSO updates `HANDOFF_FABLE.md`'s State paragraph in the same push.** An item leaves the board only by shipping (→ LIVING_VAULT entry), dying (→ LIVING_VAULT RETIRED), or being explicitly parked.

## SEND-ORDER (operator, 07-08 — the dispatch sequence; nothing jumps the line)

1. ~~Fused gun + orphan fix~~ ✓ **DEPLOYED 07-08 night** (`6d84f27e`; live proof table still owed tonight — see IN-FLIGHT)
2. ~~Sequential floor recompute~~ ✓ **DONE + RE-CUT + RATIFIED 07-08 night** (`SEQFLOOR_RECUT.md`; **DYNAMIC S + CELL-KEYED AIM ratified — `.claude/rulings/RULING_DYNAMIC_S_CELL_AIM.md`**; constants retired to footnotes; honest-era regrade S=0 unchanged, provenance binds first; arm gates unchanged)
3. ~~Bell recovery~~ ✓ **MEASURED 07-08 late night** (`.claude/bellrecovery_20260708/`: 349/853 no-bell events recovered, +694 legs/271 cells for AIM_V2 — but overlap validation only 33.9% within ±5 min (no premarket suppression standalone): **TRAINING-GRADE only; residual-refinement pass queued #18 before any time-sensitive ingestion**)
4. ~~BOT_DOWN channel~~ ✓ **LIVE 07-08 7:13 pm** (channel-agnostic `/root/notify.sh`, config-keyed; default ntfy push — **operator subscribe: https://ntfy.sh/omi-livev4-omqs-x7k3q9v2**; SMS/email stubbed for later word; watchdog + exit checker + te_live keepalive all wired, transition-deduped)
5. ~~Out-of-process exit checker~~ ✓ **LIVE 07-08** (`/root/exit_checker.py`, cron */15, exchange-truth v4 band contract, survives bot death; first run 14 held/0 naked + caught the gun-feed staleness watch)
6. ~~Evening cut~~ ✓ **DONE 07-08 ~8:20 pm** (`.claude/live_20260705/EVENING_CUT_20260708.md`): repost watch CLEAN (4 entered window quiet; MILMIS superseded by a real gun fire — grace-cancel worked as designed); retro-stamp consumed; **gun proof table delivered with the bar UNEVALUABLE (0 feed-covered FRESH: TE /live/ coverage bound on the US-evening wave + a 3h lock starvation, both named; lock fixed) — fused-gun close-out stays OPEN on 3a, certifies on the Euro-morning wave via the 6:10 am scorecard**; cash $829.67, book 28/69 PASS 0/0.

(~~Post-gun forensic~~ ✓ DONE 07-08 night — `.claude/postgun_20260708/POST_GUN_FORENSIC.md`.)

## IN-FLIGHT (armed or accumulating now)

- **C-REALITY-BELL (gated deploy 07-10 afternoon; the operator's failed-test verdict recorded verbatim in the vault)** — fallback bell = gun source 5 (prints/min gauge + honest-start-or-4¢-rise clock-liar escape; SAIDEL replay: fires 11:46:40, refuses the 11:47:08 buy, 28s margin); reality invariant (basis vs market MID, 25¢ bound → MORNING REVIEW; Dellien 62-vs-23.5 flags); bell-coverage zero-tolerance (`bell_missing` + nightly BELLS-MISSING scorecard count); aim sentinel (max(1,…) root fixed, `aim_unresolved_refused`); conception stamp at unlock. **Watches: fallback_bell fires graded nightly by the scorecard (premature-fire = the FERCER regression signal; tune constants, never disable) · bell_missing must stay 0 · reality_divergence lines reviewed at the AM checkpoint · aim_unresolved_refused count (each = a degenerate fitted level named) · pre_conception_buy "ungradeable" lines must stop.**

- **C-DRAIN-REPLAY (gated deploy 07-10 ~midnight; restart-amnesia #6 — the PAPJER-PAP class)** — drain manifest written before shutdown_cancel; boot replays every drained entry through the full chokepoint (re-placed / refusal NAMED / already-covered; halt-refusals retry at halt-clear; stale manifest discards loudly). Monitor gains the zero-tolerance DRAIN-REPLAY section (violation = pre-drain order neither filled, re-placed, nor refusal-named within 10 min post-boot). **Watches: drain_replay outcomes at every deploy · DRAIN-REPLAY section stays 0 · drain_manifest_error stays absent.**

- **C-EARLY-UNLOCK (operator ruling, gated deploy 07-09 late night)** — realized lifetime volume ≥ 2,500 unlocks the full T−8h placement window, ITF only (`RULING_EARLY_UNLOCK.md` verbatim; PAPJER the exhibit). Realized never projected (P1b stands); basis named (`kalshi_rest_lifetime` / ws fallback); downstream untouched. **Watches: early_unlock entries/night + fill rate + Δaim vs standard cohort (daily_ledger EARLY-UNLOCK section) · unlock_basis distribution · any mains/CHALL `early_unlock_open` = defect · conception_beyond_horizon stays 0.**
- **C-TAPE-SEED (gated deploy 07-10 early, `2f26cc00`+`e7b4e8f0`)** — restart-amnesia 5th instance dead (BOTH mechanisms: boot amnesia + the pre-existing ws-snapshot wipe that killed even live prints at each of today's 186 reconnects): last-trade memory boot-seeded from REST at the print's actual timestamp (honest ages, never −1.0); 1800s freshness threshold + last-trade discipline UNCHANGED (no BBO-mid fallback); live prints overwrite seeds; anchor-resolution scope only. **Watches: tape_seeded counts/pass · tape_seed_live_confirm delta distribution · rest_seeded vs ws_live cohort fill rates (daily_ledger TAPE-BASIS section).** ~~Threshold ruling owed~~ -> **RULED 07-10 early (C-STALE-ANCHOR-ALLOWANCE, RULING_STALE_ANCHOR_ALLOWANCE.md): unlock-open ITF events anchor on prints <=7,200s; all else 1,800s unchanged; never-traded still skip; no BBO-mid. Buys stamp anchor_age_sec; daily_ledger splits stale vs fresh anchor cohorts. Watch: stale-cohort fill rate + dAim vs fresh before trusting the allowance.**

- **C-FUSED-GUN + C-ORPHAN-FINGERPRINT (`6d84f27e`, gated deploy 07-08 night)** — gun = first credible signal (te_scoreboard > schedule_live > tape_latch > price_divergence flag-grade); fire → `_events_live` + `gun_source` stamp + 5-min grace; **NO new buy after ANY gun fire** (`gun_buy_refused`, closes the 80-buys class); boot re-adopts by jsonl fingerprint (`orphan_readopted_fingerprint`), C47 asserts `fingerprint_in_manual`+`fingerprint_untracked`=0. **te_live feed resurrected same deploy (was DEAD since 07-07 7:20 pm — zero observed_starts today until the build recon restarted banking) + keepalive cron.** Watches: nightly GUN SCORECARD (analysis/gun_scorecard.py — detection %, med |Δ|, misses named), `gun_feed_error`/`gun_feed_ambiguous`, readopt count at every restart. **PROOF TABLE delivered 07-08 evening cut: 13 FRESH / 4 CATCH-UP fires, wave caught by fallback sources as designed — but 0 feed-covered FRESH (TE coverage bound on the US-evening wave + the 3h lock starvation, fixed) → the ±3-min bar is UNEVALUABLE and the close-out STAYS OPEN on 3a; certification expected on the Euro-morning wave (6:10 am scorecard).**

- **C-CONCEPTION-HORIZON (T-8h early bound, INTERIM SCAFFOLDING — `6d9ebe31`, deployed 07-08 afternoon, `f41330f` PID 1456032)** — no conception/resting entry bid beyond T-8h honest-anchored; router-defer (18 live fires hour 1) + manage-sweep + chokepoint-refusal; census swept **11/11 by direct API** (GILOBR exhibit — survivors were manual-class restart orphans on LYING kalshi_primary clocks, invisible to the in-code sweep; see vault amendment). **The bound is a map edge, NEVER a target; it moves on the early-canvas study + operator ruling.** Watches: `conception_beyond_horizon` audit flag (must stay 0 — caveat: blind where the clock lies short), `conception_horizon_refused`, swept pairs re-conceiving at T-8h. **Aftermath addendum `b0661bc2`: failed walk-recovery now frees the leg (3 starved legs 14:23 healed at the deploy restart).** **Residuals routed: lying-clock/pm-honest-matcher miss → queued #14 (GILOBR the exhibit); restart-orphans-read-as-manual → queued #13 + LIVING_VAULT "THE ORPHAN CLASS".** **Evening census (3:37 pm ET): in-code flag 0, direct census 5 — swept names re-materialized as sibling-repost completions for HELD legs on lying `26JUL08` clocks (chokepoint refusal blind); NOT re-swept (would re-starve held legs; all age into window by ~9 pm ET) — see vault amendment.** **WATCH tonight 7–9 pm ET (evening cut, SEND-ORDER #6): the five age legally into the window — confirm on the flow gauge / defer counters that they enter clean (no re-defer churn, no fresh beyond-horizon placements behind them).**
- ~~EARLY-CANVAS STUDY (Part 2)~~ — **LANDED 3:55 pm ET 07-08** (`.claude/early_canvas_20260708/EARLY_CANVAS_PART2.md`, 2,460 pairs, 0 watcher restarts). **Verdict all six cats: nothing before T-8h ever justifies a resting bid** (jf ≤97 = 0.0–2.2% every T-24→T-9 bin; the bound forfeits nothing measurable; tightening compatible, loosening unsupported). **Flow doctrine fitted: onset→floor97 lag p50 = 0–1 min** — fillability IS the flow transition; provisional gauge thresholds WAKING ≥~0.2 prints/min, OPEN ≥~0.4–0.6 + spread ≤4¢ (ITF/CHALL only, mains excluded); ratify on forward flow_state tape before any decision keys on them. Horizon moves on operator ruling only — see vault entry.
- **FLOW-STATE gauge LIVE in the live-monitor** (per-game per-cat prints/min + spread, QUIET/WAKING/OPEN, thresholds PROVISIONAL until the study fits them). Monitor loop restarted (was DOWN since the 02:52 crash — BOT_DOWN-channel adjacent finding).

- ~~OVERNIGHT FREEZE (07-08 00:xx ET → morning checkpoint)~~ — **BROKEN BY DISK-FULL CRASH 02:52 ET, not by hands** (bot down 12.6h; 8 legs filled naked on orphaned resting bids; the freeze's audits died with the process). Freeze lifted at the morning checkpoint for containment: swept 0-naked 15:44 UTC, **C-LOG-ENOSPC `9a74b061` deployed through the full gate**. See LIVING_VAULT 07-08 top entry + `.claude/proof_20260708/PROOF_LOG_ENOSPC.md`.

- **BOT_DOWN ESCALATION CHANNEL (new, from the 07-08 conviction)** — nightwatch/watchdog detected the death within 60s and told no one (58k lifetime BOT_DOWN lines into local files; no MTA, no push). Needs an operator decision on channel (push/SMS/dashboard-red). Companion: an OUT-OF-PROCESS 15-min naked-leg audit twin — `position_audit.py --cron` now actually runs (the `. ./.env` dash fix, first cron run since Feb) but asserts against trades.json lineage, not the v4 band contract; decide whether to extend it or write a thin exchange-truth checker. Until then C47-CONTINUOUS remains in-process-only (blast radius = the process).

- ~~SWEEP 07-07 containment~~ — **DEPLOYED 3ec8cc0 23:11 ET** (ITM-exit-take + no-rebuy-after-cash + continuous 15-min audit; first steady_cadence PASS 23:13:45). Book swept clean (4 naked → 0). TANCHE stamped race-class. **S per-leg LIVE: honest era S=3** (BARSIM/WALVAL/SCHZID — per-leg-perfect, combined-lens-invisible). Walk/repost model seeded (median-pass/tail-fail vs 25% bar); refit at checkpoint.

- ~~FLOOR-BY-HOUR decomposition~~ — **LANDED 2026-07-07 (.claude/pair_story_20260707/HOURLY_APPENDIX.md)**. Verdicts: ITF floor is entirely T-2h→bell (prints/min 0.00 until T-3h; early book = wide silent lattice quoting ~99); MAINS-EARLY HYPOTHESIS REFUTED (ATP_MAIN quote-touch floor 100.0 flat every bin — NO-GO); pre-T-4h spec feed: early posting buys POSITION, not early fills.

- ~~PAIR-STORY re-dispatch~~ — **LANDED 2026-07-07 (.claude/pair_story_20260707/PAIR_STORY_20260707.md, 2,396 pairs)**. Headline: ITF med achievable floor 84 (97 = emergency ceiling), S-lines set per cat (ITF 84/84, CHALL 93/90, mains 93); divots ~50-65min apart (sequential doctrine at scale); seesaw lag-0 corr −0.7 ITF/CHALL vs −0.29 ATP_MAIN; floor lands post-ramp, last ~20min. Follow-on (queued naturally, not built): bucket-refined S-lines at AIM_V2 arm; re-run as observed_starts coverage grows.

- **C50 two-file close-out law** — ENFORCED at deploy_gate.sh [4/4] as of 2026-07-07 (bootstrap warn-pass on the first post-law deploy, strict after).

- **Week standing order 07-05→07-12** — config HOLDS (defects exempt via gate); nightly leak decomposition; end-of-week: leak answer + riser Plex bounce at full n + pre-T-4h posting-window spec. `WEEK_STANDING_ORDER.md`.
- **C47-ENFORCE audit** — live (v1.2); watch: halt/clear behavior across tonight's boots, `exit_unpostable_itm` flag population, artifact cadence.
- **fv_observe riser accumulation** — toward ~100 FV-graded riser legs for the Plex bounce (18/100 at week open).
- **aim_shadow + ex-self posture lines** — accumulating for the expression rule's Plex package (below-chain 54%@9.1% is the standing finding).
- **JOINT SHADOW (walk-cap × expression, log-only)** — LIVE; first partial-evening smoke: 123 decisions/19 constrained, ALL honest-anchored (false-flag counter 0), caps bite (walkcap-alone would-fill 0/19 vs actual 2/19 — starvation watch), ≤97 held 16/16. Nights decide; rollup JOINT_SHADOW_<date>.md nightly.
- **S/A rubric LIVE in the ledger (evening cut)** — S-lines ITF 84/84, CHALL 93/90, mains 93; **S-TIER CONSOLIDATED 07-08 (vault front page): provenance clause — sibling-accounting-priced legs S-INELIGIBLE regardless of outcome; honest-era regrade S = 0** (the 07-07 S=3 all die on provenance; 77% of era sibling placements are 97−basis arithmetic). S-per-night = the arm's truest scoreboard. **Ledger renderer must inherit the clause + the EKSLUX window-stamp caveat (lying clock fakes W1 lifecycles).** **First full cut on the ratified rubric (POSITION_GRADE_20260708, day close): S 0 · A 2 — BOTH BOUHAR (DAMARN 97 / PLADIG 95; era baseline was 2/357) · B 8 · EKSLUX struck; heavy-zone dip-catch 2/5 (GONBEN's promised 19¢ dip printed and was missed — the aim gap measurable per leg per day).** **COMBINED-PRICE CLAUSE (07-08 night, FOUNDATIONS): no fixed-line rate headlines without the dynamic-floor gap beside it; renderers inherit.**
- **POST-FILL classifier feeds the aim build** — divot share only 17-26% of decisive fills (ITF_W 67% REPRICE: fading real information); REPRICE→band-unreachable 65-81%; DIVOT confirms gold (69% touch ITF_M); NO_UNDERCUT is the strongest class (ITF_W 86% touch). Aim conditioning variable = ask-hold/sibling-chain state, not depth alone. `.claude/sweep_20260707/POST_FILL_MOVE_20260708.md`.
- **AIM_V2 ramp** — gated OFF; dated schedule to honest n≥30 per tier; arm judged against the LIVING_VAULT front page (gold-leg production + B3-conversion bars).

## QUEUED (next builds, in rough order)

> **C-MORNING-TRIAGE v1 (07-15 midday, executed — full arc in LIVING_VAULT top entry):** Part 0 JONURG band-0 CLEAR (exit a1a3f307 resting, audits PASS since 11:32; **AUDIT-RACE class founded: fill-race band-0 ×3 + settlement-race ×1, all self-healed; healer silent-skip remedy PRICED**) · Part 1 RINTAB 3/3 exchange-reconciled + 40-mains = 17 tickers, 8/8 filled legs' exits confirmed, zero naked · Part 2 **C-DISCOVERY-FLOOR v1 ARMED via full gate** (corridor/stale-dated conceptions need ≥1,500 discovered; replay 4 refuse/+300¢, KOAYAZ forgone named; WATCH: `below_discovery_floor_refused` nightly with counterfactuals; fresh-W1 refusal = defect) · Part 3 DELXIL over-par fork: **TAKER-COMPLETE HAS NO COMBINED-PRICE CLAUSE — census intake, remedy shapes await the word** · Part 5 THE MORNING GRADE delivered (fills W2 75/W1 24/C 5 of 104; EARLY 79/TIGHT 25; exit-same-second 104/104; billing TAIL −280¢ vs PATH settles −3,159¢ vs exits +2,509¢; census = guard chatter except the rooted halts; **GUN SCORECARD: DELETION GATE OPEN, four proofs present — the 6:10 word is the operator's**). **OPERATOR WINDOW WORD VERBATIM in the vault; W1-PREFERENCE rule PRICED (mains/CHALL corridor refused; ITF corridor = fallback: ≥1,500 + volume-quiet + dossier-named) — AWAITS THE WORD after the grade is read.** New intakes: taker-over-par clause word · healer silent-skip fix · scalar-retirement settles (3 today, exits can't govern them) · T−9h+ evening mains placements vs the horizon (lying evening clocks).

-0l. ~~DEPLOY-STOP-WINDOW vs DRAIN-BUDGET mismatch~~ — **FIXED + DEPLOYED same night (operator word; `c6163580` via the full gate): bot drain budget sized to the book (5s + 0.25s/resting entry bid, floor 10s, cap 180s, printed with the count at signal time); script stop window 200s > cap, waits for actual death, prints measured stop time; only reachable failure = old-bot-alive → BOT_DOWN alert, never a dead book. Proof `.claude/proof_20260715/PROOF_STOP_WINDOW.md` (incident arithmetic + this deploy's own live stop as lane 2). Watch: `stopped in Ns` per deploy · shutdown_drain_begin/done BOTH present on every graceful stop.**

-0j. **WALK-CAP INVERSION (C-KNOB-CANON forensic 3b, 07-11)** — the Plex-RATIFIED table (PLEX_WALK_CAP_SIZES: ITF_W 20 / ITF_M 14 / CHALL 2 / MAIN 1, honest-anchor) sits STAGED-OFF while the unratified code-default (`_walk_cap_cents`: MAIN 2 / CHALL 3 / ITF 4, conception-cell anchor) governs live walks. **Today's tape: 3 capped walks (ATP_CHALL ×2, WTA_CHALL ×1, all at cap 3 — each 1¢ LOOSER than ratified); ITF live cap 4 = 10–16¢ TIGHTER than ratified.** AND the anchors differ (conception cell vs honest first-target) — not just sizes. Needs the operator's arming ruling: flip `walk_cap_honest_anchor` on, or re-ratify the default.

> **C-FLOW-REST-SEED v1 (07-15 night, operator R1 GO word, DEPLOYED via full gate):** the dossier flow gauge REST-seeded — consultation-local trades cache (`p30 = max(ws, rest)`, provenance stamps `gauge_src`/`flow_bucket_ws`, `flow_rest_seed` flip events); **the gun's fitted WS counter untouched**; flag `flow_gauge_rest_seed` DECREED+cited. Outcome replay on tonight's slate: 19,141 consultations / 228 tickers, **171 flips (~0.9%) concentrated at onsets** (KOA certified quiet→open 0.000→1.0; FOMCHA ws=0 vs 179-print tape); behavior isolation 201/201 aims page-priced regardless of bucket. **Honesty catches filed: exhibit YAZ row amended (honest bucket = warm/0.010 — the 30m bucket's ONSET-LAG is a second named limitation, NOT cured by R1, reach-refit territory); the replay's own first run silently truncated the densest tape (no-silent-caps applied to the instrument).** WATCHES: `flow_rest_seed`/night · `gauge_src` split (ws_only spikes = warm-hook starvation) · 429 pressure · onset-lag evidence accrual.
>
> **C-KOAY-EXHIBIT v1 (07-15 ~1:30 AM, operator live catch, executed):** the Koay/Yazdani autopsy = **REACH-RECAL EXHIBIT #1 — the queued recalibration read EXECUTED** (`.claude/reachrecal_20260715/KOAY_EXHIBIT.md`). Safety: YAZ exit same-second, resting, never naked; KOA bid cancelled by match_live (correct — tape never returned below 41). **The read: the reach law answered p_fill 0.000 on a bid that filled 75s later — the error's home is the FLOW-BUCKET INPUT (stale prints_30m=1 vs ~15 exchange prints/71s; the −0k WS-vs-full-tape class at the reach consultation site), NOT the page (p50 printed at the fill to the cent), NOT the rates (honest bucket prices 0.449/0.346).** REMEDY PRICED awaiting the word: **R1 REST-seed the flow gauge at consultation** (C-TAPE-SEED pattern; no decision-path change). NEW CENSUS INTAKE: **pair-law stamp fill-instant blind spot** — cancel-made one-sided pairs invisible to the stamp; the queued pair-law violation census must count them. Record hygiene done same push: nightly ADJUDICATION_20260714 pushed (origin → `738ced4c`); **40-mains-legs HAS a disk record** (§EARLY-CHALL DEFECT line) — adjudication still queued; te_live tripwire = source-lull, no restart.
>
> **OPERATOR QUEUE (07-15, standing recital):** ~~C-CONTENTION-LAW v1~~ EXECUTED end-to-end (contention fitted on 27/28 pages -- **the gradient IS the understanding: ITF_W dogs 87% vs ATP_CHALL leaders ge75 = -3.2%, the untouchable class named by number**; selector SHADOW live in trendpath_shadow; nightly SELECTOR price tag; **week as the selector's slate: 22.6%/23.0% yield vs the 8% bar, EV3 bleeders DROPPED 24/26, DROP bleed -$17.95 named, flatten-census victims all on DROP** -- doors on the letter: N=176<300, CI touches zero, **THIRD consecutive STRONGLY-POSITIVE-BUT-UNDERPOWERED; path+reach+selector ride ONE n>=300 clock -> one consolidated ruling packet, three words in one sitting**). **NEXT WORDS/READS: 6:10 deletion word - EV3 re-read n>=300 - THE CONSOLIDATED PACKET (path/reach/selector at n>=300, days away) - COMBINED-CAP remedy - -0g.**
>
> **UNFREEZE VERIFIED (07-15 12:21 AM):** boot 9de81e29, rebuild n=108 all-sourced (phantoms dead), audit PASS 0/0, 13 conceptions resumed pre-Euro; EC2 CHALL window ARMED same boot (first open pending the T-8h band). QUEUED: EC2 Part 2 fence re-validation read; EC2 Part 3 recording-scope verification (itf_ticks dir restored, collector healthy-check owed).
>
> **ONE-RECORD (07-15, operator mandate, executed):** the record IS the operation -- mandate vaulted verbatim; front-page retirement stamps applied; PAIR-97 GRAVE with the operator verbatim + mechanism; DOCTRINE CONFLICT 2-4 filed; UNSTAMPED-FRAME defect class founded (swept every C50); doctrine_registry.json machine-built (rebuild = every C50); receipts law in HANDOFF. EC2 Part 1 resumes at its own gate post-unfreeze.
>
> **HALT TRIAGE 23:10 (defect, fixed):** boot-fire sub-class 2 = STAMP/MATCHER COLLISION (window-stamp gun_fired bool vs rebuild substring grep -> phantom rebuilt fires); halt held correctly; four-organ fix (field-bound matcher, sourceless=UNKNOWN never a fire, audit flag, chokepoint skip, explicit-source evidence) deploying to clear the re-audit before the Euro slate. EARLY-CANVAS-2 Part 1 knob = its own candidate 7b7a9503, gates after. itf_collector recording dir restored (itf_ticks pruned by disk hygiene -- recording gap named).
>
> **TAPE GATE + TIMING RECUT (07-14 night, operator words, executed):** flatten tape gate ARMED (ship-condition met: 25 skips, 23 band-cashed, +$15.50 REAL -- honest vs the +$20.06 headline); nightly self-grading or revoked; timing recut live in the builder (timing_gun + lawful_share per page; LAWFUL_HARVEST_MAP; caveat lifts only on dual stamps). Reach refit deployed earlier (869de031). Remaining words: sizing; orientation clock; number-law renderer inheritance; GAP-B/D graders; unbooked-fill root; M15 intake #1.
>
> **READ-THE-TAPE 1-6 + CONVICTED-INSTRUMENTS (07-14 night, closed):** #1 leak = tape-blind flatten (+$20.06; 43/47 fired against the tape) -> TAPE-GATE PROPOSAL priced, AWAITING THE WORD; frozen aim = +$1.30 = nothing -> aim lanes STAY SHADOW (ruling + receipt); packet lawful-axis: PATH & SELECTOR STAND, REACH SHRINKS (-0.4%); reach refit DEPLOYED (bound=evidence gun); EV3 AT-THE-LETTER, NEGATIVE (-$17.20) -> NO CUTOVER, lane stays shadow; MIS-ANCHORED confirmed n=1563 (lawful share 14%) -> timing recut still priced; UNACHIEVABLE stands full-canvas. Queued: tape-gate word; timing-recut word; number-law renderer inheritance; GAP-B/D graders; unbooked-fill root; M15 intake #1; sizing word.
>
> **READ-THE-TAPE Part 0 (07-14 ~9pm): HALT TRIAGE CLOSED** -- boot race adjudicated by timestamp (drain replay vs landing stamps; fire_src null = the tell); 7 buys, 0 filled, all swept; halt held ~10 min and cleared 8:50:44 PM; ordering fix deployed (replay waits for gun state; refused_gun_fired belt; auditor names null writers). **Parts 1-6 in flight: expression audit priced by replay, number law, honest-axis recut, packet re-grade.**
>
> **W1 LIBRARY (07-14 late, executed):** census verdict UNACHIEVABLE (0/19 pairs <=97 in lawful span; books hugged par) -> SLATE, with numbers; cohort library live as dossier surface #14 (SHADOW, nightly-calibrated); **ATLAS CHALLENGE ADJUDICATED: MIS-ANCHORED (-0k onset lags the gun 56-201 min on ITF; 'pre-onset' bottoms are in-play) -- timing recut priced as follow-on; clock x-cal BOARD item STRUCK (executed).** Queued: timing recut word; presence word (repriced); unbooked-fill root; OPEN-transition event; M15 intake #1; sizing word.
>
> **WINDOW LAW (07-14 night, executed):** W1/CORRIDOR/W2 stamped on every decision (evidence-gun closes the corridor, bursts branded); consumer census -> CORRIDOR-BLIND = one family (latch+grace+latch-freeze); acceptance CONVICTS (3 W1/C cancels, no evidence gun) AND amends the reach verdict (6/8 touches were W2 -- not lawful fills under the W1 thesis; presence proposal REPRICED); **ATLAS-TIMING CHALLENGE filed (bottoms landed post-gun, atlas says pre-onset)**; WINDOW LEDGER nightly; gun-feed keyhole root REPAIRED (dedicated observed_starts.db + 30-min tripwires). Queued preserved: unbooked-fill root, OPEN-transition event, reach-count recalibration read, M15 intake #1.
>
> **REACH VERDICT (07-14 night): PRESENCE.** 8/36 pairs tilted; tape crossed our aims 9x; fills 0 -- every touch found NO BID (premature match-live latches + post-latch freeze + lawful clamp floor). Aims RIGHT, bids GONE. FERCER class at slate scale. **PROPOSALS PRICED, awaiting the word: (1) re-place on unlatch; (2) the vaulted ESPN-status gate for match_live_cancel.** Oriented lane no better (2/8 pairs) -- n to its clock. 317 below-floor refusals = the nickel floor binds the ITF dog side. COXHAM-COX ruled lawful (band move).
>
> **TRIAL EVE (07-14 pre-wave):** PACVED RULED (a) -- filled 11:14 AM pre-cutover, sweep clean, definitive line HOLDS; real finding = UNBOOKED FILL 4h17m (ledger instance, root study queued). LAJSVA bell root = dead te_live feed + 55-min schedule lie; fallback fired +12.3min correctly; NOT tuned. Arming audit: dossier p_fill ARMED; 3 gaps NAMED (nightly E-vs-A table, dedicated OPEN-transition event, zero-fill conviction line) -> census intake. Tail 9/24 remain; account $749.36 read to the cent.
>
> **LEG-RANGE CLAMP (07-14 urgent, executed):** path aims bind 5-95 (07-05 adjudication); below 5 = named refusal; replay 16 entries -> BARREA refused, 15 unchanged; OPERATOR ADJUDICATIONS now a census surface; **the consultation law self-caught its first violation on day one.**
>
> **TAIL RULING (operator word, 07-14): HOLD-AND-DRAIN the 24 positions** -- exits + settlements drain them; leash-bounded completion policy the only acting hand; INCUMBENT-COST bills nightly until the last leg drains.
>
> **TAIL SWEEP (07-14 URGENT, executed):** 22 pre-cutover buys cancelled (0 fail), 2 path-stamped buys + 26 exits kept, 24 tail positions awaiting the operator's hold-or-liquidate word. **Definitive line: no dollar enters except path-cited.**
>
> **OPERATOR QUEUE (07-14 vault-wired, standing recital):** ~~C-VAULT-WIRED-ENTRY v1~~ EXECUTED: every placement/refusal carries the ten-surface dossier (reach-law p_fill now consulted at every aim); nightly CONSULTATION CENSUS with AUTO-GAPS -> board; render shows dossiers; replay exhibit committed (7/7 post-cutover decisions; **first intake item: M15 ENTRY-band mapping unfitted**); vault-alive law (additions via GAP intake, challenges by tape, walk-forward re-validation) VERBATIM in the vault. SHADOW aim variants (range-shape, timing) accrue toward the standing bar. **NEXT: first-hour report (owed); sizing word; orientation clock; M15 entry-band study (intake #1); 6:10 deletion word; COMBINED-CAP; -0g.**
>
> **OPERATOR QUEUE (07-14 cutover, standing recital):** **THE CUTOVER IS EXECUTED (operator word, deadline waived): PATH-MODE IS THE LAW** -- entries at fitted path aims (NO-CALL posture, pair-cap 97), no page = no entry named; join-walking DELETED; completion priced at the sibling's path aim (C-BOUND ceiling retained); the trendpath_live flag REMOVED (deleted, not disabled); auto_cutover stood down (word outran the deadline). **THE STANDING LAW vaulted verbatim: yield-on-wagered forever first vs the 8% bar; below-bar nights named, diagnosed, never explained away.** Sizing DARK pending the operator's separate word; orientation DARK behind its own n>=300 bar. **NEXT: first-hour path entries cited (owed in the cutover report); the sizing word; orientation's clock; 6:10 deletion word; COMBINED-CAP; -0g.**
>
> **OPERATOR QUEUE (07-15 latest, standing recital):** ~~C-INCUMBENT-SUNSET v1~~ EXECUTED: **the incumbent (static aims / join-the-market / 97-remainder pricing) is CONVICTED, FROZEN, and billed nightly (INCUMBENT-COST); the cutover is DEFAULT-GO -- packet fires -> ntfy with numbers + 12h deadline -> silence flips trendpath_live through the FULL gate (auto_cutover.sh, 30-min cron); your STOP word halts it (relay -> STOP file)**; cutover lands NO-CALL posture (both legs at p50 path aims, pair-capped); orientation + sizing layer behind their own bars; SUNSET LEDGER prints every organ's death date nightly. **NEXT: the packet (24-48h) -> default-GO or your STOP; 6:10 deletion word; COMBINED-CAP remedy; -0g; ws_depth_recorder archive extension (overdue).**
>
> **OPERATOR QUEUE (07-15 late, standing recital):** ~~C-GOLD-NOW + C-PAIR-LAW + AMENDMENT~~ EXECUTED, one gated deploy: **pair-law LIVE** (DROP-AS-PAIR = no orientation composes <=97; seesaw lift at every re-aim, cancel-and-free, dynamic; one-sided fills = named violations) -- **the volume bracket measured: blanket -72% vs amended -0%; the DROP bleed (-$17.95/-1.3%) lives between the brackets**; oriented tells SHADOW (week: 8% coverage, 62% correct, n=8/300; ROVIEM: direction right, fitted NO-CALL on 3 prints); **DARK flags armed: trendpath_live, sizing_live** (flag-flip cutovers); **the consolidated clock runs itself** (2h cron -> first n>=300 crossing pushes RULING_PACKET to the phone); nightly gains REALIZED-BY-SUBTRACTION. **NEXT: the packet fires itself (24-48h) -> THREE WORDS in one sitting; 6:10 deletion word; EV3 re-read rides the packet; COMBINED-CAP remedy; -0g.**
>
> **FLATTEN CENSUS (07-15 read query):** 25 below-basis sells Jul 12–13, −$4.01, **one mechanism (completion_flatten 25/25)**, post-leash executions **0** (14 deferred + 21 capped — guard CLEAN, no defect named); KOPPRI = the frame dumping a 100-settler on a two-term EV that excludes the win-ride BY DESIGN; common cause = **the frame, not a lying input** → the EV3 three-term cutover is the cure (already on the n≥300 clock). Full vault entry 07-15.
>
> **OPERATOR QUEUE (07-15 early, standing recital):** ~~C-TREND-PATH v1~~ EXECUTED end-to-end (atlas 30 pages; **SHIMIC Lane-1 PASS — the page says 10 on a 16 discovery, the operator's watched drift to the cent**; week at path×reach **+$29.65 over actual, 9.0% yield both ways, 74/76 pairs ≤97 at path** — doors shut on the letter: n=285<300, CI kisses −1.11 → **second consecutive STRONGLY-POSITIVE-BUT-UNDERPOWERED**; trendpath_shadow DEPLOYED, logging from tonight; disk incident #5 mid-sequence, pruned +4.5G, **ws_depth_recorder 8.2G = the twice-flagged eater, archive-sync extension now overdue infra**). **NEXT WORDS/READS: 6:10 deletion word (honest gate) · EV3 re-read n≥300 · reach-law + path re-runs at n≥300 (days — settlements landing) · COMBINED-CAP remedy · −0g.**
>
> (prior recital:) **OPERATOR QUEUE (07-14 ~9:30 pm, standing recital):** ~~C-TAKER-REACH v1~~ LANDED — **the arc's strongest positive evidence: E$+39.61 vs actual +$11.20, 15.4–16.0% yield vs the 8% bar EVEN PESSIMISTIC — doors shut on their letter (n=244<300, CI kisses zero at −3.05): no bar-drop, no refusal five; forward clock stands; record = STRONGLY-POSITIVE-BUT-UNDERPOWERED (re-run clears n≥300 in days as settlements land).** THE HARVESTABLE MAP: the harvest lives ENTIRELY in OPEN flow (ITF_W|open 68 five-cent sweeps/hr; quiet ≈ 0) — the mirror census undercounted 100×. **NEXT: the loaded sequence from `7338fef7` (handoff-executable, HANDOFF_FABLE State): atlas build detached → SHIMIC Lane-1 → week re-grade at path prices under the reach law → gated deploy of trendpath_shadow → C50 w/ the operator ruling verbatim.** Then: 6:10 deletion word · EV3 re-read · COMBINED-CAP remedy · −0g.
>
> (prior recital:) **OPERATOR QUEUE (07-14 late night, standing recital):** ~~C-FULLWINDOW-REPLAY~~ DONE — **the third-door verdict: THE SLATE, not the engine** (N=765: engine −$2.80 vs actual, CI wide — no pass, no bar-drop; deep-fill partition **10/1**, selection vindicated; **THE MOMENT CENSUS: the harvestable discount on our tape = 0–1¢ for ~2 min median** — moment-timing cannot carry the entry edge on this slate; the engine stays in shadow, its ceiling is the census's). **Standing: 6:10 deletion word → EV3 re-read n≥300 → COMBINED-CAP remedy → −0g → LIVE-AIM forward accrual (unchanged bar).**
>
> (prior recital:) **OPERATOR QUEUE (07-14 night, standing recital):** ~~C-LIVEAIM-BACKTEST~~ DONE (741 era-clean rows: engine −$1.70 vs actual, CI [−34,+34] — **acceleration REFUSED, forward clock STANDS**; failure axis = the test's, not the design's: AIM_DEEP fired once because the moment-forming mechanism lives at RE-AIM time, invisible at discovery instants; the live shadow — which sees every re-aim — is the honest instrument, already accruing). **Standing: 6:10 deletion word → EV3 re-read n≥300 → COMBINED-CAP remedy → −0g → LIVE-AIM forward accrual (n≥300, CI clear of zero).**
>
> (prior recital:) **OPERATOR QUEUE (07-14 pre-6:10, standing recital):** ~~C-LIVE-AIM v1~~ SHADOW-DEPLOYED `da3deec7` (fourth aim design: per-market live conditioning — prior page × flow/book/spread/print state; chase-guard structural; lib_conf honesty; nightly dual line from tonight; **cutover bar pre-agreed: beat live aims at n≥300, CI clear of zero, then your word**). **6:10 today: deletion word (honest gate). Then: EV3 re-read n≥300 → COMBINED-CAP remedy → −0g → LIVE-AIM accrual watch.**
>
> (prior recital:) **OPERATOR QUEUE (07-14 early, standing recital):** ~~C-GUIDEBOOK-V2 v1~~ THIRD REFUSAL, named (timing-conditioned recut on G9: partition check PASSED — 8 win/4 loss deep fills, the WHO is fixed — but held-out dollars −$10.70 vs −$9.30 actual: nothing ships; findings that survive: NO era-admissible ITF depth corpus exists + tour dip windows are diffuse — v3 = flow-signature conditioning, gated behind ITF tape accrual). AIM/TIMING MISSES open, three refusals deep, discipline held. **6:10 today: deletion word (honest gate). Then: EV3 re-read n≥300 → COMBINED-CAP remedy → −0g.**
>
> (prior recital:) **OPERATOR QUEUE (07-13 late night, standing recital):** ~~C-GUIDEBOOK-AIM v1~~ BUILT + REFUSED BY ITS OWN TRIAL (week replay held-out: guidebook −$9.90 on $226 staked vs actual −$0.62 — static depth without flow conditioning = concentrated adverse selection; **nothing shipped, shadow gated OFF per the pre-agreement**; refusal machinery itself worked: 420 thin + 295 yield-refused; v2 = depth conditioned on dip TIMING + shape, own dispatch). AIM/TIMING MISSES stays open on evidence (second refused closing design). **Tomorrow 6:10: deletion word (honest gate). Then: EV3 re-read at n≥300 → COMBINED-CAP remedy → −0g → guidebook v2 recut.**
>
> (prior recital:) **OPERATOR QUEUE (07-13 night, standing recital):** ~~C-EV3-BACKTEST v1~~ DONE (264 held-out legs: EV3 +$7.70 vs actual +$2.35 vs two-term +$5.75; EV3-minus-actual +$5.35 with CI [−20,+29] — **VERDICT: FLAT-AT-INSUFFICIENT-N (264<300); leash stays; no cutover word this read; posterior variant grades BELOW cell-only → no posterior build**). **The nightly accrues ~90 graded legs/day — the n≥300 re-read lands in 1–2 days. Tomorrow 6:10: deletion word (honest gate, clean tape pending).** Then: EV3 re-read at n≥300 → COMBINED-CAP remedy → −0g.
>
> (prior recital:) **OPERATOR QUEUE (07-13 evening, standing recital):** ~~C-COMPOSER-EV3 v1~~ SHADOW-DEPLOYED `07cf29a2` (three-term frame in every completion_shadow line; acid test HONEST: bar NOT met — 9/20 winner-flips, 6/16 saves retained, 11 residuals cited; structural finding: the ex-ante separator is mostly price — forward tape decides; leash floor VALIDATED +$11.00 on the graded tape). **Tonight's nightly grades BOTH frames on settled outcomes. Tomorrow 6:10: deletion word (honest gate, clean tape pending).** Then: EV3 cutover word (yours, on positive nightly grades) → COMBINED-CAP remedy → −0g.
>
> (prior recital:) **OPERATOR QUEUE (07-13 late afternoon, standing recital):** ~~C-ADJUDICATION-READ v1~~ DONE + leash DEPLOYED `b7abdc5b` (flatten graded −$0.264/leg vs ride on N=36 REAL outcomes — giveaways were WINNERS, the EV frame's win-ride-residual exclusion named as the root; pre-authorized bound applied: cap 8/day + 3¢ EV floor, named lines; band collapse decomposed: maker +10–15¢ vs flatten −3…+2¢). **Tonight's nightly grades the leashed branch + GOVERNOR SPLIT prints band-by-path. Tomorrow 6:10: deletion word on the honest gate (prefilter fixed, clean tape pending).** Then: composer EV-frame refit (win-ride residual — the named bias, own dispatch) → COMBINED-CAP remedy → −0g.
>
> (prior recital:) **OPERATOR QUEUE (07-13 ~3:30 am, standing recital):** ~~C-BELL-SCOPE v1~~ DEPLOYED `0a6ab782` (emergency #2, one night apart: self-fill bell scoped to unsanctioned behavior — fires only on post-fill / exceeds-ARMED-allowance (live table) / tape-corroborated; 24 false freezes lift at boot with true state logged; OVER-BROAD LOCK instance 2 filed w/ 1,780 frozen-min + 42 refusals; replay 3 lanes PASS incl. CORBRU-still-freezes). **6:10 reads with BOTH honesty windows branded — two suppressed windows force DELETION GATE: REFUSED for insufficient clean tape; the deletion word waits for a clean night.** Then: COMBINED-CAP remedy word → −0g.
>
> (prior recital:) **OPERATOR QUEUE (07-13 ~1:30 am, standing recital):** ~~C-CAP-SCOPE v1~~ DEPLOYED `22039f30` (EMERGENCY rescope: pursuit cap counts from first fill/first bell — pre-arm re-aims unlimited under the walk cap; OVER-BROAD LOCK founded w/ the 931-refusal/7-stranded damage table; CORBRU still freezes — replay 3 lanes PASS; 6:10 honesty footer carries the refusal-suppressed window). **6:10 am: deletion word on the gate's verdict, read WITH the honesty footer.** Then: COMBINED-CAP remedy word → −0g.
>
> (prior recital:) **OPERATOR QUEUE (07-12 pre-6:10, standing recital):** ~~C-DELETION-GATE v1~~ DEPLOYED `9b57bcc4` (governor stamp live — GOVERNOR SPLIT in tonight's adjudication; percat first-fire-wins cited + MULTI-SOURCE checkable; self-fill activity-firing = design, tension named w/ SELF-FILL-UNCONFIRMED meter; **taker cap DECREED 3/day until n≥30, named lines**; **the 6:10 scorecard now carries Plex's four proofs and REFUSES the deletion word itself if any is missing**; Plex verbatims = reserved slots, relay debt, occurrence six). **TOMORROW 6:10: the deletion word rides the gate's own verdict.** Then: COMBINED-CAP remedy word → −0g. Live note: chase locks refusing real ladders tonight (FORENSIC_chase_cap blocks rendering).
>
> (prior recital:) ~~THREE OPERATOR WORDS~~ EXECUTED, one gated deploy `9b26fc17`: **percat bell ARMED** (source seven, additional; legacy deletion rides tomorrow's 6:10 scorecard) · **−0j ARMED** (honest-anchor walk cap live — the window-open void dies) · **THE TAKER WORD GIVEN** (completion policy LIVE: verdicts act, never-hold-naked enforced, pair-97 consulted nowhere; replay 5/5; first live day graded vs its own shadow tonight — divergences named). Boot resolved the two live bids (MIYKUZ-KUZ 32¢ / CALRAD-CAL 57¢) by the engine's verdict — outcomes in the deploy report. **REMAINING WORDS: legacy-trigger deletion (tomorrow 6:10, on the regrade scorecard) → COMBINED-CAP remedy → −0g FV-aware exits (evidence accruing).**
>
> (prior recital:) ~~C-CHASE-KILL v1~~ DEPLOYED `8a857ecf` (three locks armed: retroactive conception stamp · pursuit cap 2/leg from first buy · self-fill bell = gun source six; CORBRU replay frozen at the 51, fail-before = the tape; cohort triage: FLAT, +415¢, no flatten words needed; CAP-VOID class closing build shipped, KILLED bar = 7 nights zero ladders + refusal lines present; **−0j ESCALATED ruling→defect: the walk cap's `_window_open` anchor was ABSENT on CORBRU — the ratified honest-anchor table idles staged-OFF while live legs walk 24¢ through the void**). **TONIGHT: clean regrade night (−0k instruments) + first night of the locks (watch chase_cap/self_fill_bell lines). TOMORROW 6:10 am: the gun-cutover word is the operator's — the cutover file `.claude/cutover/CUTOVER_EVIDENCE.md` now carries the chase cohort (6/6 would-fire; CORBRU crossing 29 min BEFORE rung one) + today's 55 bell_missing census (12 timely/3 early/17 no-tape/23 mains-off); evidence favors percat as an ADDITIONAL source.** Then the words: → **−0j walk-cap arming (escalated — the void is live)** → **COMBINED-CAP remedy word** → **taker word**.
>
> (prior recital:) ~~−0k~~ SHIPPED `476b4326` (flow-step onset: 10/16 suspects → clean joins, 6 quarantine honestly; divergences 4→1 with correct labels — the 4 were the comparator (ID re-mint + tick misalignment), the survivor = live n_eff undercount, rooted; percat would-fire vs the 12 bell_missing: 3 timely / 4 dribble-early / 3 no-tape / 2 mains-off — the naked threshold needs gauge-plus-start composition; WALBAD/MIRMAL = COMBINED-CAP BLIND SPOTS founded, remedies await your word). **TONIGHT = THE CLEAN REGRADE NIGHT (both crons ride the fixed instruments) — the gun-cutover gate reopens on its numbers.** Then the words: → **−0j walk-cap arming ruling (operator's)** → **per-cat gun cutover (operator's, on the regrade-night scorecard + the (a) composition evidence)** → **COMBINED-CAP remedy word (operator's — race-order shave / resting-sum clamp / pre-conception basis)** → **taker word (operator's)**. New infra items: **ws_depth_recorder into archive_sync scope (7.2G, ~1.5G/day, disk at 97% after incident #4)** · composer-state boot seed (kills the last real divergence flavor).
>
> (prior recital:) ~~C-MORNING-TRIAGE-0711~~ DONE (bot never blinked; monitor 8h MONITOR-BLIND outage rooted+fixed+tripwired; the 16 SUSPECT rows = schedule lied 1–6h, bell caught reality, TRUTH-JOIN DRIBBLE-ONSET founded; shadow grades ruled INADMISSIBLE until −0k).
>
> (prior recital:) ~~C-SYSTEM-PAGE v1~~ SHIPPED (`3062d639`; /system live on the viewer — classification COMPUTED from citations, uncited = NAKED (honest split 3/18/101 of 122 until Plex's verbatim census lands), pair-97 pin toggle 4/4, spec slot still reserved and WINS ON LANDING).
>
> (prior recital:) ~~C-BOOK-REPLAY v2~~ DONE (2,953-leg corpus; held-out gate refused 3/4 refits — aim refit FAILED, ITF_W's 2¢ constant WON; M16 = ITF_M refuse margin 8¢ SHADOW) → ~~C-COMPOSER-G1 v1~~ DEPLOYED (live-side conviction_shadow; live-vs-replay agreement nightly; G1 distance = 371 mains legs + 20,332 samples).
>
> (prior recital:) **OPERATOR QUEUE (07-11, standing recital):** ~~C-KNOB-CANON v1~~ DONE → **C-COMPLETION-POLICY v1 (NEXT — restores the interim bound)** → **C-BOOK-REPLAY v1 w/ revised Part 2b** → **composer shadow wiring (G1 remains)** → **/system page build** (spec slot reserved) → **per-cat gun cutover on the operator's word after tonight's graded shadow**. Open words riding: taker · −0g · walk-cap arming ruling (−0j, new).

> **FIX QUEUE (ranked by measured cost — FULL_SLATE_REVIEW 07-10; the reconciliation side feeding this section nightly):** 1. ADVERSE-SELECTION STRAND (55 inst/$6.25 — the completion policy, −0h carries it) · 2. DOCTRINE CONFLICT exit-vs-conviction (10/$3.51 — the −0g FV-aware exit ruling's evidence) · 3. AIM/TIMING no-fill misses (11/$2.60 — feeds the aim-surface refit) · 4. conviction-gap placements (7/$1.05 — composer calibration) · 5. EXCHANGE-TRUTH DAYLIGHT (17 violations — root each before KILLED).

-0i. ~~EXCHANGE-TRUTH DAYLIGHT ROOTS~~ — **ROOTED 07-10 night (C-DAYLIGHT-ROOTS v1): 0 double-count (suspect innocent) / 6 report-error (fixed, VANKOI clean on rerun, 17→11) / 11 unbooked-fill history (healed forward, healer bookings = log truth). Open-book sweep: 0 daylight. Watch: nightly re-count at zero or the class reopens.**

-0h. ~~PER-LEG EXIT ECONOMICS + PRICED COMPLETION~~ — **SHADOW SHIPPED 07-11 (C-COMPLETION-POLICY v1 `490affa6`): leg_econ engine + one-sided-pair verdicts + gated taker branch + nightly COMPLETION-SHADOW; 44/44 strands verdict+priced. Cutover on the operator's word → pair-97 DELETED.** (was: **PER-LEG EXIT ECONOMICS + PRICED COMPLETION (RULING_PAIR_ECONOMICS, 07-10; shadow-first)** — each leg an independent scalp priced per tick from the fitted surfaces (P(exit-fills-before-collapse)×band vs P(ride-to-zero)×basis); leg-1 enters on conviction entry < achievable exit; sibling completes on its own math or as priced insurance; the adverse-selection verdict binds (TAKE-the-winner = operator ruling required, or never hold the kept leg naked). Pair-97 stays as a dumb safety bound until this proves itself in shadow — then DELETED. Migration meter = the nightly footer.

-0f. ~~UNBOOKED-CYCLE-2-FILL / no_exit CLASS~~ — **CLOSED into CLASS — UNBOOKED FILL (C-BOOK-THE-FILL `86c0a23d`, gated deploy 07-10 ~5:30 pm): audit no_exit legs with our lineage are BOOKED (never halted); replay 4/4. Watch: HALT-MIN + UNBOOKED-FILLS-BOOKED at zero nightly or the class reopens.**
-0e. ~~CERT-NIGHT-3 FORENSIC~~ — **ATTRIBUTED 07-10 (C-CONVICTION-REPLAY, numbers): tracked events 398→201 (slate HALVED) vs distinct fires 169→104 — fires-per-event ROSE 0.42→0.52. VERDICT: slate size; source starvation REFUTED (ratio improved); the deploys' side-effect was the −0f halt downtime (276.8 min on 07-10), which suppressed participation, not fires.** FIRES-vs-SLATE + BELLS-MISSING footers now watch nightly.
-0c. ~~CANCEL-RACE-REPLACE~~ — **CLOSED same deploy (C-BOOK-THE-FILL Part 2): failed cancel + live order → 'unresolved'/cancel_failed_hold, replacement never placed; TUPMAK replay aborts it. Watch: cancel_failed_hold per night (each = a caught race).**

-00a. **OPEN-BOOK RE-ENTRY SCAN (07-09 ~3:50 pm)** — live book CLEAN (0 open cycle-2 legs); boot-window sweep = the two DAALUX legs only, both cashed; monitor cycle line now carries `open_cycle2=N` until the ruling's fix ships. Nothing cancelled (as ordered).
-00. ~~RE-ENTRY RULING~~ — **RULED + EXECUTED 07-09 evening (C-CYCLE-CAP `bbd8cada`, gated deploy): ALLOWED, capped at 2 cycles/leg/premarket.** Persistent counts (fingerprint pattern #3, boot rebuild), both gates (repost sweep count-gate + chokepoint refusal covering the router), completed-cash-only counting, every ledger row cycle-stamped, audit `cycle_cap_breach` inherits persistence. Watches: cycle_history_rebuilt at every boot, cycle_cap_refused/reached, cycle_cap_breach=0, monitor open_cycle2 (standing).
(was: **RE-ENTRY RULING (operator's open decision, 07-09)** — evidence package DAALU_REENTRY.md: the class is PROFITABLE (+$15.84/wk, 22/23 legs, 46 cycles) and unintentional (router never wired to the cash guard + restart amnesia — fingerprint lesson #3). Fix shapes filed for BOTH rulings (forbid → persistent guard at router+repost; allow → cycle counter + ledger cycle-stamps). NOTHING ships until the word.

-0a. ~~PLEX RESUBMISSION PACKAGE~~ — **DELIVERED + RULED 07-09 night (PLEX_T3_T4_RATIFICATION.md): T3 supersession RATIFIED (riser retired, lineage in CLIMBSIDE_SPEC.md), T4 hold-gate clause RATIFIED (threshold reserved; hold-gate carries TWO jobs by ruling — anti-selection + the volume floor's only enforcement point), dispatch cleared with binding sequencing note.** Verbatim ruling body LANDED late 07-09 (slot filled word-for-word). Dual-flag shadow instrumented same night (C-T4-DUAL-FLAG); T4 accumulation clock starts at the first `dual_flag_v1` line. **Climb-side arm prerequisites UNCHANGED: four bars · joint-shadow n≥30 (currently 12+19) · shadow-first · os_active untouched.**
-0b. ~~OS BUILD~~ — **SHIPPED SHADOW-FIRST (`1a187f77`, gated deploy 07-09 evening)**: oslayer/ order-path-pure (gate-asserted every deploy), decision core = lifecycle 2–5, T4 dual-flag hold-gate wired before logging, os_shadow stream live, nightly rollup; `os_active` DORMANT behind five named conditions. IN-FLIGHT watches: os_shadow rate/sites, boundary line in every gate, hold-flag divergence (Plex's threshold dataset).

-1. ~~IN-FLIGHT DEDUP LOCK~~ — **SHIPPED 07-09 night (C-INFLIGHT-LOCK `330266c7`, gated deploy, deliberately alone).** One per-(ticker,direction) lock at the place_order chokepoint: acquired synchronously before any await, released in try/finally over the original body (every path — conception/DCA/repost/walk/completion/cycle-2 — every return). GORSTE 11:41:42 AM tick replayed under it: second buy REFUSED in-process at held_ms=105.3, named (`inflight_lock_refused`); synthetic same-tick, stale-forced-release (crash-safe 20s bound, LOUD), non-interference — 4/4 PASS. **os_active precondition #2 of 5 SATISFIED.** Watches: `inflight_lock_refused`/night (rare, named), `inflight_lock_forced_release` (must stay 0). Proof `.claude/proof_20260709/PROOF_INFLIGHT_LOCK.md`.
-1a00. **ITF VOLUME-FLOOR RULING — restated post-reconciliation (07-09)**: basis = the parse-robust structure (37% never-wake, 16-vs-5 unsweepable exits, 30%-vs-19% ride-rate, margin gradient 1.7–1.9× robust to accounting convention); ~~the $77/week figure~~ RETRACTED pending the convention pass (dollars re-price after it; the ruling need not wait). AWAITING OPERATOR WORD; ships as a gated config deploy.
-1a000. **CASH-CONVENTION DERIVATION (from the reconciliation, 07-09; spec AMENDED by C-BOT-ONLY-BASIS 07-09 evening)** — the sell/no booking + dead `revenue` field leave the cash identity underdetermined (residuals +$406/−$1,388/−$138 across parsings); derive the convention empirically against the hourly balance_snapshots (live since Jul-8 16:01Z). **The identity to close (per RULING_BOT_ONLY_BASIS): account-snapshot Δ = BOT-ONLY P&L + manual book Δ + fees, to cents, bot-only as the headline; anchored account-delta lines (the +$17.90/25h) are hereby the CROSS-CHECK, never the answer.** Prereq for ALL future dollar-precise claims; the reconciliation law's missing tool. Build stays queued.
-1a0. **SETTLEMENT VOCABULARY (ruled 07-09)** — every realized table: cashed-via-exit ∥ rode-to-settlement, separate columns, forever; renderers inherit (the nightly ledger + SLATE cross-tab refresh, with DRIFT-5). The EXTENSIVE Part-1 retraction is its proof-case (+$163.41 exit column vs +$3.87 residue).
-1a. **THE EXTENSIVE follow-ups (07-09)** — (i) ITF volume-floor filter (~2.5k W1 contracts + gauge-OPEN confirm) awaiting operator read WITH the excluded-population would-have P&L (computes at tonight's bell refresh — owed amendment); (ii) fav-entry timing spec filed (heavy aims resting from T−90; onset±15 hold; never chase the ascent) — feeds the cell-aim ramp's posture layer, arm gates unchanged.
-1b. **ITF SCHEDULE TZ AUDIT (from the chain proof, 07-09)** — the ITF lane's gun−sched lag is a ~+2h CONSTANT at every hour (feed tz/source-offset signature, not queuing); fix the lane + the certified-join gap (0/33 observed_starts join schedule rows). The +121min claim stays KILLED until this audit speaks.
-1b1. ~~CLOCK CROSS-CALIBRATION CENSUS~~ — **DONE 07-10 midday (read-only; `.claude/census_20260710/CLOCK_CROSS_CALIBRATION.md`). VERDICT: no global re-stamp — scoped exclusion. ITF: archive has NO clock (categories absent) + mid-jump structurally unfit on thin books (med −119m vs gun) → live era is ITF's ONLY admissible clock. CHALL jump-method: trust ±30m for coarse ranges (med −0.6m, 68% ±30). EXCLUDE fallback (76) + no_bbo (444) slices. Mains stay convicted. Self-contamination REFUTED (clean books agree no better).** Follow-on carried by the aim-surface refit + PLEX_REANCHOR (may be asked to ratify the exclusion list).
-1b2. **PHANTOM-SKIP CENSUS (analysis, read-only; sibling of -1b1)** — size the amnesia participation damage: entries lost to the `last_trade_age_sec: -1.0` skip class (boot amnesia + ws-snapshot wipe, both dead as of C-TAPE-SEED `2f26cc00`+`e7b4e8f0`) — how many event-cycles skipped on a book that HAD tradable prints, per cat, and the would-have participation under honest ages + the 07-10 allowance.
-1c. **AIM_V2 HONEST-WEIGHT RULING PREP** — bell_src stamps live from 07-10's 4:45 am run (`739ec32d`); after 2-3 stamped nights, run the derive and put the three-weight (1.0/0.5/0.25) coverage table in front of the operator: which bell_src qualifies as honest at which prior weight is now the ramp's actual bottleneck.
-2. ~~GUN SCORECARD truth-join debug~~ — **SHIPPED 07-09 ~5:15 pm (C-TRUTH-JOIN `572d678a`, gated; analysis-side only, bar unchanged)**: truth now fire-source-independent (every fire vs its match's tape onset, anchored on the datemiss-aware honest clock — 165/173 joins tape@honest; obs_starts both-leg fallback); no silent half-joins (UNJOINABLE named with reason); MOCTAN class → SUSPECT(named), never averaged (old med|Δ| 168.4m → clean-FRESH ITF_W 1.7m); miss path proven by synthetic drop-one-fire. Overnight-50 disposition coverage 3/50 → 50/50 (7 clean / 42 suspect named / 1 unjoinable). A/B: `.claude/gun_scorecard/TRUTH_JOIN_AB_20260709.md`. **Certification night 3 rides tonight's 6:10 am cron on the fixed join.** Residual watch: the 42-suspect population mixes bad tape joins (premarket churn) with genuinely late fires — the 15-min suspect threshold is operator-tunable and cannot touch the ±3 bar.
-2a. ~~FOREIGN-POSITION AUDIT SCOPE~~ — **RESOLVED + SHIPPED 07-09 evening (C-BOT-ONLY-BASIS, gated deploy).** Operator's word: FRAMAR is HIS manual ("its to shake you up") → key-security concern closed; **RULING_BOT_ONLY_BASIS.md recorded verbatim: bot-traded money = the basis, reset 12:00 am ET nightly, manual/foreign named never blended.** Audit fix live: no-cell-config tickers = `foreign_position` FLAG, never FAIL/halt (the 4:07 pm replay is the proof case); all bot-leg protections untouched. Containment sell `7cf55a8c` CANCELLED (his book, he manages it; logged manual-class; exit_checker exempt line added). Canonical bot-only renderer: `analysis/daily_ledger.py`. −1a000 spec amended (bot-only headline; account delta = cross-check).

0a. ~~FILL RATE REDO + TRAVERSE + CATEGORY LAW~~ — **LANDED 07-09 ~3:25 am** (`.claude/fillredo_20260709/FILL_REDO.md`): mains = JOIN market (40–50% at touch at V3; cliff below), ITF = DEPTH market (ladder alive to 2×spread); mains drift-path thesis REFUTED (1¢ range/8h); patient-join ratchet captures 0 everywhere; TWO REGIMES → per-cell postures (macro picks regime, tape picks moment). **Gauge re-fit restated: mains ON join-only; AWAITING OPERATOR READ before any config deploy.** DEPTH-RELATIVE AMENDMENT + CATEGORY LAW on FOUNDATIONS.
0b. ~~VOLUME LEDGER + FLOW CALIBRATION~~ — **LANDED 07-09 ~1:05 am** (`.claude/volume_20260709/VOLUME_LEDGER.md`): dead book NEVER pays (structural); ITF fill prob doubles per volume band (16→31→50%); MAINS flat ~1% at any volume (par-lock participation-independent); ITF dead lattice = 6¢ opinion @20% tighten. Re-fit proposed (ITF OPEN=≥6 prints/30m; CHALL ≥16; mains gauge OFF) — **AWAITING OPERATOR READ; the gauge config deploy ships only on your word.** Forward field live (`d8d0c62f`); 538 recut cells volume-stamped.

1. ~~Post-gun forensic~~ — **DONE 07-08 night** (`.claude/postgun_20260708/POST_GUN_FORENSIC.md`): ITF latch-silent 73–83%, median lag 31–46 min all cats, post-grace leak −$19 live-path (dead-bot class −$167 flagged out), EKSLUX all-four-controls exhibit; races 14/14 = walk path, zero latch races. Feeds SEND-ORDER #3 bell recovery.
2. ~~Price-band clamp — walk/repost bypass~~ — **CLOSED (C-BAND-CLAMP-WALK `a5a64962`, deployed via the full gate 07-08 midday).** Conviction AMENDED the dossier read: the 29 outside-band placements (incl. HARMAI-HAR @4 filled) all predate the 23:57 clamp boot — the placement clamp is airtight; the post-clamp walk class was **cancel-then-refuse STARVATION** (walk cancels the good bid, band_refused kills the re-place, leg left oid=""+phantom basis, invisible to validate_resting_buys — ICHOCH-OCH 00:43, PIAPIE-PIE 02:34). Fix: pre-cancel band guard on the projected landing price + recover-at-old-price on any failed re-place. See LIVING_VAULT 07-08 midday entry + `.claude/proof_20260708/PROOF_WALK_BAND.md`. Regression watch: `walk_band_refused` / `repost_place_failed{recovered:false}` counters.
3. ~~Post-only-cross exit hole~~ — **CLOSED 07-07 late (C-ITM-EXIT-TAKE in the sweep deploy)**; CLAHER/`exit_unpostable_itm` counter stays as the regression watch.
4. ~~Conformance audit~~ — **FULL PASS 07-08 ~10:00 pm (pulled forward from 07-14; `.claude/CONFORMANCE_20260708.md`)**: 8 deploys' tape columns re-run, all new laws live-proven, never_marketable UNPROVEN-LIVE row closed, ONE archaic-ARMED flag (per_match_clock_shadow → retire), 97-basis census 94% today (the goal law; S-blocker by construction until the ramp), SIX drifts queued (one config-hygiene deploy takes DRIFT-1/2/6). Next weekly 07-15.
5. **Walk/repost model** — same-price churn suppression follow-through (C-CHURN-FIX lineage) + walk-cap honest-anchor (Plex walk-cap ruling pending, see EXTERNAL).
6. **Fractional sizing** — int-floor class end-to-end (payload `str(int())`, exit sizing, guard now floats): WALVAL 0.87 residue exhibit; Kalshi accepts fractional counts (proven 201s).
7. **MALKOM timing** — the blocked-buy's `exchange_qty=0` while memory said 9 (settled-filter timing on the per-ticker positions read).
8. **PAT rotation** — origin URL still leaks a GitHub PAT (standing since 06-18). Security, not trading.
9. ~~join_trial_mode → False~~ — **SHIPPED 2026-07-07** (config-only flip through the gate; 528 same-day trial-stamps ended; LIVING_VAULT entry closes the 06-18 lineage). Adjacent dead-flag LIST (flip nothing, review): `per_match_clock_shadow`=True (shadow logging redundant since the flag armed 07-06 — retire candidate), `fv_anchor_scenarios_enabled`=False + `round5_detector_enabled`=False (dead legacy, already off — config-REMOVAL candidates), `pair_governor_scoot`=False (grave, keep as tombstone), `riser_post_revision`=False (deliberate disarm, re-arm pending Plex bounce — NOT dead).
10. **Sub-1 residue tripwire** — add residue count/Σ column to post_boot_audit (the audit's one blind spot: h<1.0 tolerance). [CONFORMANCE §3]
11. **kalshi_schedule_primary coexistence check** — one-line verify vs per_match_clock layering in the next clock pass. [CONFORMANCE §4]
12. **DECISION-INPUT CONFORMANCE pre-arm gate** — before ANY arm (AIM_V2 ramp, walk-cap honest-anchor, expression clamp), one real decision's FULL input vector pasted verbatim from the jsonl — prices / cat / time-bin / non-self chain / divot-conditioning / flow-state — any missing input BLOCKS the arm. (Operator, 07-08 close-out.)
13. ~~ORPHAN-CLASS FIX~~ — **SHIPPED 07-08 night (`6d84f27e` C-ORPHAN-FINGERPRINT, with the fused gun)**: boot re-adopts by jsonl order_placed lineage; C47 asserts zero fingerprinted orders in manual class. Regression watch: `fingerprint_in_manual` / `fingerprint_untracked` / `orphan_readopted_fingerprint` at every restart.
14. ~~LYING-CLOCK class~~ — **CLOSED 07-08 late night (C-ANCHOR `1eeebc7b`, gated deploy)**: anchor hierarchy (gun_truth > te_honest > kalshi LAST) + datemiss_36h matcher recovery (the miss class's exact hole) + clock_liar detector w/ conservative gates + anchored window stamps. Regression watches: `clock_liar`/night, `schedule_match{datemiss_36h}`, `conception_beyond_horizon` on true clocks, liar-stamped scalps. Absorbs #11's check (kalshi stays LAST in the hierarchy by construction); the staged C-KALSHI-OCC lane remains the occurrence-feed upgrade path.
15. ~~SEQUENTIAL FLOOR recompute~~ — **DONE 07-08 night** (`.claude/seqfloor_20260708/SEQUENTIAL_FLOOR.md`, 2,435 pairs): ITF implied 79 med (premium over simultaneous 4¢ med / 10¢+ on a quarter); edge is FAV-side (dog med 0 — timing not depth; dog floor T−9..−17m, fav divot T−31..−70m, fav-first 2:1); mains still par-locked. **Proposed S-lines ITF 79/79, ATP_CHALL 92 — AWAITING OPERATOR RATIFICATION**; AIM_V2 side-conditional re-anchor noted in the doc.
16. **CORRIDOR FLAG** — any lifecycle crossing W1 → corridor throws a loud board flag, weighted LOUDER for non-ITF (corridor crossings are rarer and more diagnostic there).
17. ~~EXIT PROVENANCE verification~~ — **VERIFIED + PINNED 07-08 night (with the grid re-cut)**: `ITF_EXIT_BORROW = {'ITF_M': 'ATP_CHALL', 'ITF_W': 'WTA_CHALL'}` (live_v4.py:288); durable exit dir = exactly four native parquets (atp/wta × chall/main), no ITF. Pinned in FOUNDATIONS "THE GRID"; borrowed bands labeled in every grid readout from now on.
18. **Bell-recovery residual refinement** — add the live latch's premarket suppression (tts-floor/E113 equivalents) to the standalone taker-signature detector; bar for time-sensitive ingestion of the 349 recovered bells (currently TRAINING-GRADE only: 33.9% within ±5 min on overlap). **The training/certification split is now LAW (GRANULARITY LAW, FOUNDATIONS 07-08: no per-minute-derived number is ever a timing trigger).** Also the 95 corpus bells the standalone detector misses (thin tapes) = the detector's own blind class, named.
19. ~~tennis.db WAL / lock hygiene~~ — **PASSED 07-08 ~11:25 pm, with an honest re-framing: the db was ALREADY WAL (the rollback-journal theory struck); tonight's 3h starvation was a HUNG WRITE TRANSACTION (tennis_odds) — writers serialize in WAL too.** Cure stack live: synchronous=NORMAL set · busy_timeout belt · keepalive/notify + exit-checker feed-staleness watches own the hang class · wal_watch cron (sidecar >2G → checkpoint+notify) · disk 88%→79%. **Residual (stays open, LOW): per-collector db split if the writer-hang recurs — the watches are the tripwire.** Vault entry has the verify replay (reader-vs-writer 0.05s / writer-vs-writer 4.05s clean).
20. ~~GUN-STATE PERSISTENCE~~ — **CLOSED same night (C-GUN-PERSIST `1a8a266e`, gated deploy): _gun_state rebuilds from the jsonl gun_fired lineage at every boot, before the first conception pass; C47 asserts `buy_placed_post_fire`=0. The 7:55 pm class (7 post-fire buys, 3 live events) is dead by construction — verified on the deploy's own restart.** Watches: gun_state_rebuilt{n} vs the day's fire log, buy_placed_post_fire.

## EXTERNAL (owed by others; slots reserved)

- ~~Plex walk-cap ruling~~ — **RECEIVED + EXECUTED 2026-07-07** (`.claude/rulings/PLEX_WALK_CAP_SIZES.md`; sizes staged ITF_W 20/ITF_M 14/CHALL 2/MAIN 1, flag stays OFF; JOINT SHADOW live from tonight — arm gate = shadow nights + four bars; nightly rollup in NIGHTLY_PASS).
- **PLEX_REANCHOR ruling body** — relay dropped it; slot open at `.claude/rulings/PLEX_REANCHOR_RULING.md` (slot 0a17ce03).
- ~~Riser Plex bounce~~ — **RESOLVED 07-09 night by T3 ratification: the riser is RETIRED (supersession ratified; lineage stated in CLIMBSIDE_SPEC.md — climb-side/decay-side is the frame).** fv_observe accumulation continues as climb-side evidence, no longer a riser re-arm path; `riser_post_revision` stays OFF as a tombstone of the retired frame.

## CRON / SCHEDULED

- **NIGHTLY ADJUDICATION (permanent, from 07-10)** — `/root/adjudication_nightly.sh` @ 12:20 am ET: 3a gate self-check → conviction replay over the closed day → `ADJUDICATION_YYYYMMDD.md` + MIGRATION METER into NIGHTLY_PASS + git push. Absence by morning = cron failure, visible. Proven same-instrument (123/123 row match vs the one-shot).

- **GUN SCORECARD (nightly, permanent)** — `analysis/gun_scorecard.py --nightly` (cron ~6:10 am ET): per-cat detection %, med |truth delta|, misses NAMED into NIGHTLY_PASS.md + dated table under `.claude/gun_scorecard/`. Self-grading forever — degradation flags itself. Companion: te_live keepalive cron (the feed is the gun's primary source; silence = scorecard shows it).
- **OUT-OF-PROCESS EXIT CHECKER** — `/root/exit_checker_cron.sh` every 15 min (outside the bot): v4 band contract from exchange truth; naked legs → artifact + channel; gun-feed staleness watch aboard; hold-legs exempt via `/root/exit_checker_exempt.txt`.
- **BOT_DOWN CHANNEL** — `/root/notify.sh` (config `/root/notify_channel.conf`, default ntfy topic `omi-livev4-omqs-x7k3q9v2`); senders: watchdog (1-min cron, transition-deduped + 30-min reminder), exit checker, te_live keepalive. SMS/email slots await operator word.
- **Conformance cadence** — `.claude/CONFORMANCE.md` §1 tape columns POST-ANY-DEPLOY; full audit incl. archaic sweep WEEKLY (next 2026-07-14). Watches: never_marketable + hold_to_settle natural occurrences, C50 bootstrap at next deploy, /tmp/live_v4_TRIPWIRE.json check.

- ~~Jul-8 checkpoint~~ — **RAN AT THE MORNING DOSSIER (07-08): config-hold CONFIRMED (all config commits since baseline = sanctioned gated flips; today's deploys code-only); ITF_W NOT CROSSED (honest coverage 0/500 cells at n≥30, trigger=False — AIM_V2 stays BLOCKED-ON-DATA; 14k-holdout harness read not yet triggered); leak cumulative table RIDES TONIGHT** (last night invalid as a leak night — bot dead 02:52→11:30; flagged, not smoothed).
- **Accumulator** — aim_v2 honest-sample accumulator (bell_src-stamped) rides nightly; feeds the ramp schedule.
- **Pair-story** — nightly pair-story/causal-chain pass (C44 EARNED/GIFT stamps via live_monitor + NIGHTLY_PASS.md).

## PARKED (deliberately not now)

- **Exits** — ⛔ 0A standing order: out of scope until the two entry problems are solved. (The post-only-cross item above is exit *mechanics*, not exit *strategy* — the band levels stay sealed.)
- **MULVILLE** — parked exhibit.
- **T-8h** — pre-T-4h expansion beyond the B3 §4 coverage look; folds into the week deliverable (c) spec, not before.
- **Serialization** — bot state serialization refactor; no build without its own dispatch.
- **live_scores retention beyond observed_starts** — PARKED per AIM_V2 ruling (own dispatch required); observed_starts shipped 07-06.


## AUTO-GAPS (consultation census intake)
- AUTO-GAP (consultation census 20260714): range_cell_m15 — entry-band mapping unfitted (completion-frame bands only) — vault intake item
- AUTO-GAP (consultation census 20260714): w1_cohort — cohort thin/absent (n<8) — intake
