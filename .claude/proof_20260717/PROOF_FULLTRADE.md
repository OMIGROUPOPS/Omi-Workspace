# PROOF_PASS — FULL-TRADE staged wire (P2 sweep + delta meters), candidate = HEAD

**Prior art:** P0v3 sched-floor (P2 completes it: the window ends at sched,
bell or no bell — bell_missing class-kill, 11 exhibits 07-18/19), the seal
wire (PROOF_SEAL, 0b329001), loops 5-7 (verdicts banked; nothing from them
arms — the delta/tier meters are REPORTING only).

## LANE 1 — MECHANISM
- P2 `sched_boundary_sweep` (flag on): cancels UNFILLED entry bids at
  honest-anchored start +120s when no gun fired; completion/exits
  untouched; a real bell still owns everything else. Replay: the 11
  bell_missing matches' resting bids die at sched+2m instead of resting
  10-15 min into live play — the W1-only law enforced by the clock itself.
  Liar-guard: fires only on non-liar anchors.
- Restart side-effect REQUIRED tonight: reopens the engine log (currently
  a deleted inode after the seat's own active-log gzip — defect named,
  record recovered 436k lines, fd-guard cron holding).
- Meters: reporting-only additions.
## LANE 2 — n/a (subtractive sweep + meters; nightly grades it).
- OUTCOME_PROOF cites HEAD at deploy; doc-only after.
- Candidate SHA cited: 6115aefa (code delta vs prior proof = the P2 sched_boundary_sweep block only).
