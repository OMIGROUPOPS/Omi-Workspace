# OUTCOME PROOF — C-DAILY-STANDARD v1 (07-15)

**PROVEN SHA: f5ef2864** (cited post-rebase per the gate's ancestry law).

## PRIOR ART (C45)
- C-TAPE-BELL v1 (07-15, this afternoon): the acceptance replay named the 17 grace-eaten fills and the grace-vs-zero-tolerance tension as a knob question. **Delta: Part 0 is the operator's word on that knob — per-source grace.**
- `match_live_grace_sec`=300 (C-GRACE-KILL): the uniform grace. Delta: source-consulted at the two sites (verdict + armed log), tape_flow→60s, everything else unchanged (FERCER shield intact).
- C-VAULT-WIRED-ENTRY dossier surfaces + AUTO-GAPS boarding (07-14): the census marker pattern. Delta: a second marker, "standard census intake."
- Timing recut (07-14): `w1_cohort.gun_axis.lawful_share` — the fitted share of cohort harvest landing pre-gun. **Delta: Layer 3's cash-window verdict reads it as P(band prints in W1/corridor); GOLD-PATH ≥0.5 DECREED.**
- Nightly adjudication (C-NIGHTLY-ADJUDICATION) + morning-grade machinery (C-MORNING-TRIAGE Part 5): the grading vocabulary Layer 1/2 inherit.

## PART 0 — PER-SOURCE GRACE, replayed on today's acceptance rows
On the 77 W2 fills (exchange tape, `.claude/tapebell_20260715/acceptance_rows.json`): full-chain catches rise **37 → 50 of 77** under a 60s tape_flow grace — **13 of the 17 grace-eaten fills convert to saves** (4 had sub-60s margins, named). No other source's grace changes; the premature-latch shield (burst sources, 300s) is untouched. The armed line now prints the per-source grace + gun_source; the nightly GRACE CENSUS grades the knob from night one.

## LAYER 3 — behavior isolation
`cash_window` + `w1_vitality` are dossier surfaces ONLY (emitted inside `_entry_dossier`, which "never raises; never places"). No aim, no exit, no completion reads them. Owed fits named INSIDE the surface (volume-path, wall-vs-theater = GAP) — the census tracks them; nothing guessed.

## LAYER 1/2 + CENSUS — self-run proof
`analysis/game_report.py` ran live on today's log: **397 game reports generated** (`.claude/game_reports/20260715/`), the LEOTSI report rendering the walked example exactly (clock-liar 330 min, `bell_missing` plotted 9:13, gun 9:26:59, W2 fill 9:27:08 — the timeline IS the vault's walkthrough, machine-made). The nightly adjudication now: builds the THREE-BUCKET table, invokes the game reports, prints the GRACE CENSUS, and runs THE STANDARD CENSUS — any layer that did not run = STANDARD DEFECT line + auto-board under "AUTO-GAPS (standard census intake)".

## HONEST LIMITS
1. Layer A's TIGHT/EARLY-vs-own-low and the refusal counterfactual tape columns are named to the nightly regrade (the report says so per row) — the generator is log-self-contained by design.
2. Layer 3's discount-bucket (vs trailing traded mean) rides the reach consultation's median internally; a dedicated bucket column is an owed fit, named in the standard's implementation-status table.
3. The three-bucket OPEN row cites the audit's exit truth rather than re-querying the exchange (the audit is the exchange-truth organ; a second query would be a slower copy).

## WATCHES
- GRACE CENSUS nightly: graced windows by source, fills-inside-grace, converts (the knob re-earns 60s or the operator re-rules).
- STANDARD CENSUS: four keys RAN every night; defect lines auto-board.
- `cash_window` verdict split (GOLD-PATH vs W2-DEPENDENT) per cat — the sizing word's future evidence.
