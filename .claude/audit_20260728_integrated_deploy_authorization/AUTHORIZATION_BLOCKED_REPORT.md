# INTEGRATED DEPLOYMENT AUTHORIZATION — RULING: BLOCKED (frozen-verifier structural defect; no authorization issued)

**BLOCKED. No effective authorization exists after this task, deliberately. The frozen verifier at package `1dd5787c` can NEVER accept a correct authorization report in the real repository: it requires the CASUKA repair commit `94be41137c0b64bfa448546c8bc3ee7c4ae32a60` to be an ancestor of `origin/audit/window1-independent` (verifier line 608), and that commit is reachable only from `origin/codex/casuka-live-safety-repair`. Every other check in the entire verification chain passes — proven by executing the real frozen verifier with fully correct bindings against a dangling sole-child probe commit, which cleared every control-value, parent, receipt, blob, hash, and branch-equality gate and stopped only at the ancestry loop. Issuing an authorization that the frozen verifier structurally rejects would recreate exactly the class of defect that made the prior CASUKA-only authorization (`8a142c86`) STRUCTURALLY_SUPERSEDED_AND_UNUSABLE. This lane fails closed instead.**

Date: 2026-07-28 · Branch: `audit/window1-independent` · Additions-only child of exactly `3e84c1c8f3f2c397b41255ef815a271d9364c7cd` · Author: independent audit lane
No deployment, restart, cron change, order/position action, or live mutation of any kind was performed. Dry-run verifier mode only.

## 1. The defect (deterministic, frozen)

- `DEPLOYMENT_CONTROL_V1.json` → `audit.casuka_repair = 94be41137c0b64bfa448546c8bc3ee7c4ae32a60` (the CASUKA D1–D3 repair implementation commit, parent `a4996dd0`).
- `integrated_live_safety_authorization_verifier_v1.py` line 608 places that commit in the ancestry loop that requires `git merge-base --is-ancestor <commit> origin/audit/window1-independent` to succeed for five commits: the authorization commit, the package-audit PASS (`3e84c1c8` ✓), the P0 v4 PASS (`cac1a144` ✓), the CASUKA PASS (`66136e62` ✓), and the CASUKA **repair** (`94be4113` ✗).
- Fact: `git merge-base --is-ancestor 94be4113 origin/audit/window1-independent` exits 1; `git branch -r --contains 94be4113` lists only `origin/codex/casuka-live-safety-repair`. The audit lane's PASS commits *reference* the repair; they do not *contain* it — the audit branch is additions-only and never carries implementation trees.
- No lawful bypass exists: the authorization commit must have **exactly one parent** (`_commit_parent` rejects merges), so ancestry cannot be manufactured with an ours-strategy merge; the control file cannot be corrected in place because the verifier requires a **clean worktree at exactly the package commit**, pinning the frozen control bytes; and merging the repair into the audit branch would inject a `live_v4.py` modification into the audit lineage, violating its additions-only law.

Consequence: the acceptance requirement of this task ("prove the correct report is ACCEPTED") is structurally unsatisfiable with the frozen package. The verifier's other 4/5 ancestry members and its entire preceding chain are correct — this is one wrong binding form, not a design failure.

## 2. What was proven with the real frozen verifier (dry-run, zero mutation)

A canonical authorization report was rendered with the frozen module itself (`render_authorization_report(expected_authorization_payload(control, 1dd5787c…, 3e84c1c8…, <canonical path>))` → 3,598 bytes, SHA-256 `f067e59ca1584b3c1a9473e9f32e664ab7c632cb16500bf41fb3519cce553fc5`, preserved byte-exactly in this directory as `EXECUTION_AUTHORIZATION_V2_CANONICAL_EVIDENCE.md` — deliberately **not** at the canonical consumption path). A dangling sole-child probe commit `204e0f0267007ce4e4922b6397e92759bd613da8` (parent = the PASS commit `3e84c1c8`, tree = PASS tree + the canonical report at the canonical path, attached to **no ref**) was used to exercise the verifier:

- **Correct bindings:** the run cleared control-value verification, all commit-existence and sole-parent checks (package sole child of 765083b9; authorization sole child of the PASS), the canonical `PACKAGE_AUDIT_PASS_RECEIPT.json` equality check read from `3e84c1c8`, integration/rollback single-file diffs and blob identities (f1857199 → d7d7cd1d → f1857199), candidate and preimage byte/hash checks against the object store, verifier/runner source hashes, the active outcome-proof contract (path, hash, status, frozen bindings), rollback artifact/script/command literals, and all three remote-branch equality checks — then failed at the ancestry loop's first member, `authorization commit not on audit ref` (expected: the probe is unpushed by design). The deterministic next failure after any push is `CASUKA repair commit not on audit ref` (§1). **`no_mutation_performed: true` in every run.**
- **Rejection matrix — 14/14 wrong inputs refused, each with its specific cause** (full JSON in `VERIFIER_EXERCISE_RECEIPT.json`): wrong package commit → `package commit is not the sole V1 child`; wrong audit-PASS commit → `authorization commit must be the sole child of the supplied PASS`; wrong/truncated candidate SHA-256, wrong candidate blob, wrong preimage SHA-256, wrong host, wrong target path, wrong deployment ID, wrong results directory, wrong deployment-command literal, wrong rollback-command literal (including a mutated cron hash inside it), wrong outcome-proof SHA, wrong integration commit → `runtime bindings differ from control: <field>`.
- Deploy mode was never entered; `--mode dry-run` throughout; remote probes are skipped in dry-run by the verifier's own law.

## 3. Bindings that WOULD govern the one ceremony (verified correct, held pending the fix)

All values below were verified against the frozen package, object store, and remote refs; they are recorded for the corrected re-issuance and bind nothing today:

- Package `1dd5787cfe00f8c69488fff1c9c93fbb7c3eb0a1` (parent 765083b9, additions-only, remote tip equal) · PASS `3e84c1c8f3f2c397b41255ef815a271d9364c7cd` with canonical receipt at `.claude/audit_20260728_integrated_live_safety_prerun/PACKAGE_AUDIT_PASS_RECEIPT.json` (verified byte-equal to the verifier's expected receipt).
- Integration `11e70454863e3508d5a7cbc8e83162232e3a4a09` (parent 995a8817, exactly one file) · candidate blob `d7d7cd1d6e9ca28863e97ed8593e0fbf4c87e223` / SHA-256 `62614501cb9708bb3c3c2b35823ba8431b2e95acdc027f659a4b37a66a777034` / 1,047,115 B · rollback commit `904a1993030c09c839a56ff78d5a7dc0dfd13b99` restoring the preimage blob.
- Preimage `f1857199164664037fef41b024e60f27fa373548` / `834b9e04…` / 997,352 B · true running parent `bb085ce06db5932049af85f927a7f9316ad76816`.
- Host `root@104.131.191.95` · target `/root/Omi-Workspace/arb-executor/live_v4.py` · service `live_v4` · inhibited crontab `0e2af22e…` (raw spool, verified live) · original backup `/root/root.crontab.pre_schedule_liar_stop_20260728_e7004235.raw` = `4c38967f…` (verified live) · deployment ID `integrated-p0v4-casuka-deploy-20260728-attempt1` · results dir `.claude/integrated_live_safety_results_integrated-p0v4-casuka-deploy-20260728-attempt1/` (absent on VPS and in repo, verified) · backup path absent (verified) · verifier `f0c66530…` · runner `3a3c552e…` · outcome contract `38c6daee…` ACTIVE · ceremony command literal exactly the frozen `ceremony_template`.
- One attempt; no retry after mutation; failure → rollback to exact f1857199 with engine stopped and cron inhibited; no self-referential authorization SHA (authorization commit supplied separately).

## 4. Required remediation (for the implementing lane)

Rebind `audit.casuka_repair` verification from audit-ref ancestry to the same form already used for the other codex objects: object existence + type, parent = `a4996dd0…` (computed), and remote-branch equality `origin/codex/casuka-live-safety-repair == 94be4113…` (tip equality verified true today). Concretely: remove `("CASUKA repair", audit["casuka_repair"])` from the verifier's audit-ref ancestry loop and add the explicit existence/parent/branch-equality checks; regenerate the package with the frozen deterministic builder (new package commit), delta re-audit, then re-issue this authorization bound to the corrected package. Every other binding in §3 is verified and reusable.

## 5. Containment affirmation

Engine, cron, orders, positions, and all live files remain unchanged: no VPS mutation occurred in this task (read-only checks earlier today stand: process count 0, keepalive inhibited, blob f1857199, zero entry buys, no backup/results artifacts). Local repository changes: this additions-only commit plus one dangling unreferenced probe commit (`204e0f02…`, on no branch, GC-collectable, documented here). The canonical authorization path `.claude/audit_20260728_integrated_live_safety_prerun/EXECUTION_AUTHORIZATION_DEPLOYMENT_V2.md` was intentionally left **absent** so nothing can be mistaken for, or consumed as, a live authorization.

## RULING

**BLOCKED — no authorization issued; nothing is deployable from this task.** The executing lane is NOT authorized. A corrected control package per §4, a delta re-audit, and a fresh authorization bound to the new package commit are required before any ceremony.
